---
title: 'Story 3.4: OIDC token-path simplification + relay refactoring round'
type: 'refactor'
created: '2026-08-27'
status: 'in-progress'
review_loop_iteration: 0
baseline_commit: cda2013948b9b2793fa0bb19a21e7f46fe664a79
context: []
---

<frozen-after-approval reason="human-owned intent — do not modify unless human renegotiates">

## Intent

**Problem, OIDC half (owner-directed 2026-08-27, ahead of the refactor):** the ROPC adapter carries two interop arms this deployment does not need. (1) An RFC 7662 opaque-token introspection fallback — a second wire arm, its discovery requirement, and its client-auth path — for a single operator-run Keycloak (pinned ≥26.7.0) whose token endpoint issues JWTs; the verdict is already decided by the token endpoint's own response before the token body is ever touched. (2) Local JWS signature verification (`verifyJwt` + `JwksCache` + its AD-28(2) refresh VT + the `oidc.jwks-cache-ttl` knob + the `jwks_uri` discovery requirement) re-proving a token the proxy itself just obtained over the AD-12-mandated HTTPS, client-authenticated provider link — the proxy is the token's only consumer and never forwards it.

**Problem, relay half:** the relay core still carries its 2.2-era shape under a 3.3-widened surface. One `RelayHandler` (345 lines) branches on `direction` at every pre-couple decision point — two direction-dictated state machines in one class (`RelayHandler.java:155,167,334`). The pair's state machine is distributed across ~10 decision sites that each re-derive state in-method and re-implement the AD-32 teardown ordering at six places. The pair-state vocabulary is six families deep — `couple`/`pair`/`leg`/`session`/`spliced`/`flip`. The 2026-08-16 owner-note cluster, re-scoped by the 2026-08-21 split, deferred exactly this round to Story 3.4, POST-widening (deferred-work.md §"Story 3.3 scoping decision").

**Approach:** Two owner-directed simplifications FIRST: (T1) JWT-only adjudication — the opaque/7662 arm, its discovery requirement, and its config surface are removed; a non-JWT token response fails closed (D6). (T2) the TLS channel to the IdP becomes the sole trust anchor — local signature verification, claim checks, `JwksCache`, its refresh thread, its knob, and the `jwks_uri` requirement are removed (D7); spine amendments AD-12/AD-8(c)/AD-28(2) + epics 4-path enumeration + register + TEA rows swept with dated markers + memlog entries. THEN the refactoring round, umbrella-first: decide the `ConnectionEntry` state-manager shape FIRST (owner 2026-08-16: "it is the umbrella"), then the couple-vocabulary unification, the `RelayHandler` direction-split (two per-leg classes over one shared post-couple splice component), and the state-manager extraction. Close with the full RED-on-neuter mutation re-run — a rename silently de-targets biters (2.2's N1 neuter site IS the `flipSpliced()` call site) — and one LAST additional analysis iteration that re-asks the cluster's own questions against the refactored shape (T9).

## Boundaries & Constraints

**Always:**
- Fail-closed everywhere (AD-11): a non-JWT token response denies `DenyIndeterminate` + WARN (D6 lean) — never an allow, never a silent skip; every removal leaves the surviving paths' guards RED-on-neuter-controlled (AI-1).
- The AD-12 port shape is untouched: `BindCredentialVerifier` / `Verdict` / `VerdictRequest` / `BindCredential` / `RequestContext` signatures and the AC2 verdict mapping (bare 401 / 400 `invalid_grant`|`invalid_client` → `DenyInvalid`; else `DenyIndeterminate`) are unchanged; zeroize protocol unchanged.
- Relay behavior-identical: wire bytes, teardown ordering, pinned trigger events, and fail-closed arms unchanged. All 352 tests at `cda2013` stay green except deliberately re-targeted names and deliberately retired rows — every delta enumerated in the File List. The 12 `RelayHandlerTest` + 11 `BindInterceptorTest` behaviors green **unchanged-in-intent**.
- AD-25 invariants stay structural: exactly one flipper (a data-plane handler, never the control plane); flip ONLY on decoded `bind_*_resp` ROK. The split must make single-flipper **structural-by-TYPE**.
- AD-8/AD-32 state contract: the registry entry's state (exists / tearing-down / coupled) IS the teardown predicate — no second boolean, no shadow state in the manager. Sync remove-and-mark BEFORE close, then cancel+wipe, then close — unchanged at every site.
- Thread-bounding first-class: all entry/registry transitions stay on the pair's single event loop (AD-2); the verdict continuation hop stays the ONE off-loop entry; T2 REMOVES an executor (the JWKS refresh VT) and none may be added (AD-28). The post-couple splice stays flag-read + forward.
- Contract-amendment discipline for both halves: dated in-place spine markers (AD-12, AD-8 clause (c), AD-28 arm (2), AD-25) + append-only `.memlog.md` entries + epics.md 4-path/register sweep + TEA row retirement + deliberate test re-pointing (`Relay025StatelessnessScanTest:109` resolves `ConnectionRegistry.java` by FILENAME).

**Ask First:**
- The Design Notes forks: **D1** vocabulary-sweep scope (incl. the `SpliceObserver` rename sub-fork), **D2** manager variant + mechanics (= the T3 checkpoint), **D3** split-half naming, **D4** F7 absorb-or-re-home, **D5** optional F9 key-hoisting absorb, **D6** non-JWT posture, **D7** what survives of the JWT body + the Nimbus dependency.
- Any wire-visible or timing change beyond T1/T2's enumerated removals; any package move crossing an AD-27 ownership line; any new dependency (none expected — T2 only shrinks).

**Never:**
- No new auth paths, no token caching, no verdict caching, no local password checks (AD-15 unchanged); no security-tier changes beyond T1/T2's enumerated removals.
- No behavior fixes riding along in the relay half: F1 (ingress-leg `bind_resp`), F10 (egress connect timeout), F14 (adjudication deadline), F16 (backpressure behavioral test) stay with the Epic-4 / RELAY-020/021 round; F9's CloseReason *semantics* stay Epic-4.
- No new features; no config-surface additions (T1/T2 only REMOVE keys — `jwks-cache-ttl` dies with the cache; a stale key must refuse startup under `ignoreUnknownFields=false`).
- No package moves across the five sub-packages; placement *within* `relay/` or within `security/` is free.
- Do not re-litigate 3.3 (TLS machinery, F13 cap, the executed role-split arms) or 3.2 beyond T1/T2's enumerated removals (verdict mapping, zeroize, Mode B ack — all unchanged).

## I/O & Edge-Case Matrix

| Scenario | Input / State | Expected Output / Behavior | Error Handling |
|----------|--------------|---------------------------|----------------|
| Opaque token response | token endpoint 200, token is NOT a three-segment JWS | `DenyIndeterminate` + WARN (JWT-only policy); NO introspection wire call (arm removed) | Fail-closed deny; never allow |
| JWT token response | token endpoint 200, three-segment JWS | `Allow` derived from the endpoint verdict alone — no signature verification, no JWKS fetch, no claim checks (per D7) | Structural JWT-ness gate fails → D6 arm |
| Discovery without `jwks_uri`/`introspection_endpoint` | provider discovery document omits either | ACCEPTED — the field-completeness requirement shrinks to issuer + token endpoint (both still HTTPS-fail-fast) | Absent `issuer`/`token_endpoint` still refuses startup |
| Stale `jwks-cache-ttl` key | config carries the removed knob | Refuses startup (`ignoreUnknownFields=false`) — retirement is loud | Fail-fast, key-named message |
| Vocabulary rename | `spliced()`/`flipSpliced()` → `couple`-variants across main + test | Wire-identical; every re-targeted test name enumerated; scan/shape/pipeline tests re-pointed deliberately | Missed re-target surfaces as build/scan failure, never silent green |
| Direction split | egress-leg decoded `bind_resp` ROK | Flip fires from the egress class ONLY; the ingress class cannot reference the flip by type | Non-ROK `bind_resp` → forward-then-teardown, unchanged |
| Ingress pre-couple violation | non-bind PDU pre-couple on ingress leg | Delegation to the interceptor's cancelHttp+zeroize seam (successor of `teardownForPreCoupleViolation`), ordering pinned | Bare-close fallback preserved |
| Manager extraction | teardown from any of the six sites | ONE centralized ordering (remove+mark → cancel+wipe → close); CAS-once + idempotence semantics unchanged | RELAY-005/006 + exactly-once pins stay RED-sensitive |
| Post-couple PDU | coupled pair, data PDU either direction | Flag-read + forward; zero manager/virtual dispatch on the splice path | Dead-peer + partial-frame guards unchanged (RELAY-009/010) |
| Verdict continuation | Allow/Deny settles off the VT executor | Single hop onto the pair's event loop; entry re-checked before acting | Absent/tearing-down entry → no-op (AD-25/AD-32 race-free rule) |
| Mutation re-run | each renamed/moved/removed guard neutered | Its biter RED at the NEW site (or the retired guard's row struck with evidence); zero `MUTATED` residue | backup → neuter → RED → restore, byte-identical |
| Final gates | `clean build :buildSrc:test` | BUILD SUCCESSFUL; `:proxy:test` XML count = 352 − retired + added, every delta enumerated; 0 failed / 0 skipped | Console counts lie — XML only |

</frozen-after-approval>

## Code Map

**OIDC half (T1/T2):**
- `proxy/.../security/RopcBindCredentialVerifier.java` — the structural dispatch `:342-361` (three-segment JWS → `verifyJwt :384-409`; else → `introspect :459`); the 7662 arm `introspect :452-464` / `mapIntrospection :476` / `introspectRequest :498-513` (AD-12 says: never cached, online-only); `verifyJwt`'s Nimbus path `SignedJWT.parse :392` → `typ` RFC 8725 gate `:396-397` → kid lookup `:406` → `RSASSAVerifier :409`; `EXPECTED_TYP :129-133`; `jwks` field `:143`, construction `:191-192`
- `proxy/.../security/JwksCache.java` — DELETE whole class (AD-28(2) refresh VT `:77`, TTL guard `:74`, fetch+parse `:112-119`, fail-closed retain-on-misbehavior `:122`)
- `proxy/.../security/OidcStartupDiscovery.java` — field-completeness `:145-187` shrinks (drop `introspection_endpoint :159,164,169-172` and `jwks_uri :165,169,173` requirements); `OidcProviderMetadata :205-219` loses both URI components; HTTPS fail-fast unchanged
- `proxy/.../config/ProxyCompanionProperties.java:282,305-307` — the `jwksCacheTtl` component + `@NotNull`/`@DurationMin` messages die; `application.yml:113-114,123,132` comments/templates swept
- `proxy/build.gradle.kts:28` — `nimbus-jose-jwt` survives ONLY for discovery-JSON parsing (`JSONObjectUtils`) unless D7 rules otherwise; the `NoRolledCryptoArchitectureTest` `com.nimbusds..` allowlist needs no rule change
- OIDC-half tests: `security/JwksCacheTest` (DELETE), `RpcBindCredentialVerifierTest` AC3/AC4 rows (verify + introspection retire; non-JWT deny row new), `OidcStartupDiscoveryTest` completeness rows, `RpcSliceFailClosedTest`, `AdjudicationLifecycleTest` (JWKS close ordering), `KeycloakFixture` + `OidcDiscoveryStandIn` (stand-in stops serving jwks/introspection), `RpcBindCredentialVerifierLiveTest` (paths 2/4 of the 4-path validation retire), `CompanionConfigMatrixTest` jwks-cache-ttl rows
- Spine/contract: `ARCHITECTURE-SPINE.md` AD-12 `:139-142` (JWKS-endpoint + introspection-fallback wording), AD-8 `:115-118` clause (c) (the JWKS cache + `AtomicReference<JwkSet>` sentence), AD-28 `:226-229` arm (2) (the JWKS refresh executor), epics.md `:378` 4-path enumeration (paths 2 and 4 die), the AD-12 accepted-risk register, `.memlog.md` L105 amendment precedent

**Relay half (T3–T10):**
- `proxy/.../relay/RelayHandler.java` (345) — the split subject. Fields `:98-100`; the single shared `channelRead` `:110-179`; **the AD-25 flip `:131`** (the only production `flipSpliced()` caller — the N1 neuter site); direction checks `:155` (EGRESS+nack conjunct) and `:167` (INGRESS delegation); `peerOf` `:333-335` (third direction reader); `teardownPair` `:240`; `channelInactive` `:263`; `CLOSE_REASON`/`CLOSE_FIRED` attribute keys `:83/:87`
- `proxy/.../relay/BindInterceptor.java` (648) — role map: wiring/role-resolution `:113-217` (pending handles `:149/:151`, `EgressConnector` seam `:215`); intake state machine `onRequest` `:238-280`; adjudication `:283-374`; deny/teardown/synth `:376-463` (`teardownForPreCoupleViolation` `:431`, `synthesizeBindFailure` `:450`); egress dial + couple `openEgressAndForward` `:477-534`; ingress teardown window `:536-583`; nested `EgressLeg` `:585-647` (`answered` `:607`). The four pre-existing owner-FIXMEs `:142,284,298,601` — not new, do not sweep silently
- `proxy/.../relay/ConnectionEntry.java` (93) — the four AD-8 fields `:42-53`; `attach` `:56`, `spliced()` `:61`, `flipSpliced()` `:72`, `tearingDown()` `:77`, `beginTearingDown()` `:90`; CAS-once contract javadoc `:26-31`
- `proxy/.../relay/ConnectionRegistry.java` (127) — AD-8's named home; map `:38`, `ENTRY` attr `:41`, `register` `:52`, `attachEgress` `:67`, `entryFor` `:82`, `beginTeardown` `:101-113` (the removal half of the state machine), `size` `:124`
- `proxy/.../relay/netty/RelayIngressInitializer.java:78`, `RelayEgressInitializer.java:99` — the only two `new RelayHandler(...)` sites (per-leg split = zero wiring cost)
- Tests that de-target on rename — re-point deliberately, never silently: `relay/Relay025StatelessnessScanTest` (filename resolution `:109` + reflection `:107-115` — the hardest), `relay/netty/RelayPipelineInitializersTest` (`.class` lookups `:135-151`), `RelayHandlerTest` (whole file; helpers `couple()` `:485`, `coupleAndFlip()` `:494`; `EgressConnector` impl `:505`), `BindInterceptorTest` (`coupledEgress()` `:489`; `EgressConnector` `:502`), `BindInterceptorForwardRoleTest` (`EgressConnector` `:195`), `ConnectionRegistryTest` (CAS-once pins `:116,:139,:194`), `observability/SpliceObserverShapeTest` (`PRE_COUPLE_NON_BIND_PDU` literal `:115`), `observability/CapturingSpliceObserver`, `testsupport/RelayTestFixtures`, `TlsModesLoopbackE2eTest`, `JsmppA1OracleTest`, `RelayA1SmokeTest` (assertion strings `:164,166,242,438`)
- Spine: AD-25 `:211-214` (names `RelayHandler`; "splice/flip" wording), AD-8 `:115-118` (names `ConnectionRegistry`), AD-2 `:72-75`, AD-27 `:221-224` (`SpliceObserver` 4-method shape + pinned triggers), AD-32 `:246-253`

## Tasks & Acceptance

**Execution:**
- [x] T1 — JWT-only adjudication: delete the RFC 7662 arm (`introspect`/`mapIntrospection`/`introspectRequest`, the discovery `introspection_endpoint` requirement, the metadata component, stand-in/fixture serving); non-JWT token response → the D6 fail-closed arm; sweep yml comments + matrix rows; register the policy in the discovery/probe docs surface. (AC1)
- [x] T2 — TLS-as-sole-trust-anchor: delete `verifyJwt`'s signature/kid/typ verification and `JwksCache` (class + refresh VT + `oidc.jwks-cache-ttl` knob + `jwks_uri` requirement); keep the structural JWT-ness gate per D7; spine amendments AD-12 / AD-8(c) / AD-28(2) + epics 4-path + register + TEA rows, dated markers + `.memlog.md` entries (rationale: owner 2026-08-27 — the proxy is the token's only consumer; the HTTPS client-authenticated provider link is the trust anchor). (AC2, AC3)
- [x] T3 — THE UMBRELLA DECISION, FIRST among the refactoring tasks (owner checkpoint): state-manager variant (single manager vs per-role managers — one fork with the interceptor role-split at manager altitude), mechanics (sealed decision returned to the caller vs executor-facing port), and state-absorption scope (`pendingVerdict`/`pendingPassword`? `EgressLeg.answered`? `CLOSE_*` attributes?). Recorded as a dated `.memlog.md` `(decision)` entry + Design Notes addendum BEFORE T5/T6 execute. (AC4)
- [x] T4 — Couple-vocabulary unification: code identifiers to `couple`-variants (`spliced()` → `coupled()`, `flipSpliced()` → successor named once with the T3 state names); relay prose + test names/assertion strings swept; spine-sweep scope per D1's resolution; full deliberate re-targeting list. (AC5)
- [x] T5 — `RelayHandler` direction-split: two per-leg classes over one shared post-couple splice component; flip structural-by-type; `CLOSE_*` key strategy per D5; `RelayPipelineInitializersTest` re-pointed; AD-25 dated spine marker + memlog entry. (AC6)
- [x] T6 — State-manager extraction per T3: policy OVER the registry's storage (no second state copy); the six teardown sites folded into one ordering; hot path untouched; `EgressLeg`/pending-handle placement per T3. (AC7, AC8)
- [x] T7 — Ledger hygiene: F13 RESOLVED marker (work landed 3.3 T6, ledger unmarked), the four 2026-08-27 story-text-pass markers (executed by `cda2013`), F7 disposition marker per D4. Marker form: a dated `**✅ RESOLVED …**` continuation under the entry (the two existing prose forms; no fielded entry has been resolved yet — this story sets that precedent, append the marker, don't restructure the fields). (AC11)
- [x] T8 — Token-path alignment of the test tier: JWT signature verification, opaque-token introspection, and `tokenRequest` mTLS client auth removed EVERYWHERE (owner 2026-08-29, extends T1/T2 + the 2026-08-19 mTLS removal past `src/main`). The slice loses its JWKS arm — `RopcSlice.verifyWithJwks` / `fetchJwks` / `jwksCache` / `asyncRefreshJwks`, the `adjudicate()` JWKS fork, `SliceConfig.jwksUri` + `issuer`; its RFC 7662 arm — `adjudicateByIntrospection` / `introspectRequest`, `SliceConfig.introspect` + `introspectionEndpoint` + the 7662 ctor guard, `KeycloakFixture.INTROSPECTION_ENDPOINT`; and its RFC 8705 branch — the `useMtlsClientAuth` no-secret path in `tokenRequest`, `SliceConfig.useMtlsClientAuth`, fixture mTLS-client surface used only by it (transport TLS stays — it is the trust anchor). Verdict collapses to the amended contract: ROPC grant with mandatory `client_secret` → token-endpoint response + structural three-segment gate (JWT → `Allow`; opaque → `DenyIndeterminate`); `cancelHttp` binds the token exchange alone; `SliceConfig` reduces to `tokenEndpoint, clientId, clientSecret`. Sweep: `RopcSliceUnitTest` kid-miss + claim-deny rows, `RopcSliceFailClosedTest` JWKS serving + introspection rows, `RopcSliceCancelTest` re-pointed, `RopcSliceLiveTest` paths re-pointed (7662 + mTLS rows retire), `NoRolledCryptoArchitectureTest` JOSE rows, `keycloak/README.md`, slice javadoc (four-path + STS fan-out narrative), all SliceConfig call sites. Gate: repo-wide `grep -rn "7662\|introspect\|jwks\|Jwks\|RSASSAVerifier\|SignedJWT\|JWKSet\|useMtlsClientAuth\|tls_client_auth" proxy/src` → survivors are dated historical prose only, enumerated in Completion Notes (the `src/main`-scoped T1/T2 gate now extends to `src/test`). Spine AD-12 marker extension + `.memlog.md` entry recording the disposition reversal (supersedes the 2026-08-27 "deliberately UNCHANGED" T1/T2 entries; the 2026-08-19 precedent no longer shields slice arms). (AC13)
- [x] T9 — Startup OIDC discovery removal (owner 2026-08-29, "simplify app": the probe's remaining value — startup-loud misconfiguration and the DAG warning — moves to documentation; the password-grant support prerequisite is an operator contract, not a runtime probe): `OidcStartupDiscovery` DELETED whole — the ctor probe, `discoveryUri`/`parseDocument`/`advertisesPasswordGrant`, the refusal family ("refusing to start (AD-12, fail-closed A-2)"), the `issuer == provider-url` equality, `DAG_WARNING`, `active()`/`metadata()`, the nested `OidcProviderMetadata` record (the discovered issuer was never runtime-consumed; the record's sole consumer was `metadata.tokenEndpoint()` at the token call). The token endpoint is DERIVED from the configured `provider-url` — the pinned-Keycloak realm path `protocol/openid-connect/token`, `URI.resolve` over the `Oidc` compact ctor's slash-stripped base — byte-identical to what the post-T8 slice injects directly (`KeycloakFixture.TOKEN_ENDPOINT`); derivation lives on `RopcBindCredentialVerifier` (constant + static, javadoc states the path-join contract), the ctor pair drops the `discovery` param (public 1-arg over the factory; package-private recording 2-arg), and `VerifierWiringConfig` drops the `OidcStartupDiscovery` injection. Config surface UNCHANGED — no key added or renamed; `provider-url` keeps https+host fail-fast at bind. ONE deliberate behavior addition (T1's D6-WARN precedent; log-only, verdict table untouched): a starred operator WARN on the token-call connection-error and non-mapped-non-200 arms naming the derived endpoint, `provider-url`, and remediation — the retired startup-refusal/DAG-warning posture's loud successor (a typo'd provider-url, dead provider, or DAG-off client denies fail-closed at FIRST BIND via the unchanged AC2 mapping, no longer silently). The DAG prerequisite becomes DOCUMENTATION (the T1/T2 operator-docs pattern): a dated `application.yml` policy block after the T2 block + the `Oidc` record's `providerUrl` javadoc mirror — provider-url MUST be the Keycloak realm base; Direct Access Grants is per-client and OFF since Keycloak 26.2, enable it or every bind denies fail-closed. Sweep: `OidcStartupDiscoveryTest` DELETED (−10 rows; the trailing-slash behavior re-points to a derivation pin; the DAG log-capture idiom re-homes for the new WARN pin), `RpcBindCredentialVerifierTest`/`AdjudicationLifecycleTest` provider-urls gain the realm segment (the derived endpoint must land on the served `TOKEN_PATH`; never-hit pins keep their registered ALLOWING handlers, rationale re-worded off "discovery-advertised"; the doc's `introspection_endpoint`/`jwks_uri` advertisement lines die — AC13 inventory re-enumerated in the same pass), +3 fresh pins (derivation unit pin incl. trailing-slash; never-hit well-known/flagship pin through the real TLS link; the new WARN pin), `VerifierWiringConfigTest` runner + javadoc, `RpcBindCredentialVerifierLiveTest` ctor + javadoc ("fully live: startup discovery →" clause re-points), `OidcDiscoveryStandIn` re-purposed NOT deleted (`url()` + `fixtureServerSslContext()` stay load-bearing; the well-known context becomes the never-hit startup-probe pin; dated javadoc note), `TestCompanionConfigs`/`RelayTestFixtures`/`ProxyCompanionProperties`/`CompanionConfigValidator`/`IdpSslContextFactory` javadoc re-points, spine AD-12 probe-clause supersession marker (the https fail-fast clause explicitly SURVIVES — it is the validator's, not the probe's) + AD-36 "discovery-JSON parser" dated wording note + module-map `security/` line note, epics operative provider-link row dated note (mirror bullets stay frozen, flagged), deferred-work "Evaluate an OIDC-discovery/parsing library" dated `**✅ RESOLVED … SUPERSEDED BY REMOVAL**` marker (T7 F13 form; the verbatim 2026-08-19 owner note stays as history). Gate: `grep -rn "OidcStartupDiscovery\|OidcProviderMetadata\|openid-configuration\|grant_types_supported\|well-known" proxy/src/main` → EMPTY; repo-wide over `proxy/src` → survivors are container readiness polling (fixture-internal), never-hit pin handlers, and dated historical prose, enumerated in Completion Notes. (AC14)
- [x] T10 — Full RED-on-neuter mutation re-run, POST-renames: fresh rows for every renamed/moved neuter site; struck-with-evidence rows for retired guards (7662 arm, verify path, cache TTL, + the slice's retired verify/introspection/mTLS rows, + the retired discovery guards — the refusal family, issuer-equality, the DAG probe); the 2.2 N-series relay biters re-proven at their new sites; fresh rows for T9's new pins (neuter the derivation static → derivation pin RED; re-introduce a startup GET → never-hit pin RED; neuter the WARN → WARN pin RED); final gates. (AC9, AC10)
- [x] T11 — ADDITIONAL ANALYSIS ITERATION for the refactoring (LAST, post-execution): re-ask the 2026-08-16 cluster's questions against the POST-refactor shape — direction-entanglement gone (no direction conditionals outside `peerOf`'s successor); teardown ordering genuinely single-sited; zero unaccounted `splice`/`flip` survivors; a FRESH scatter map drawn and diffed against the pre-refactor one (nothing merely moved); no new coupling introduced by the splits (circular manager↔handler references, shared-component misuse); PLUS an adversarial pass over the full story diff (the established finder pattern: blind-hunter / edge-case / verification-gap). Findings: fixed in-story or ledgered with owner disposition; structural findings → owner checkpoint. Any fix applied here re-runs the T10 gates. (AC12)

**Acceptance Criteria:**
- Given a provider whose token endpoint returns a non-three-segment (opaque) token, then the verdict is `DenyIndeterminate` with a WARN stating the JWT-only policy, no introspection wire call exists in the codebase, and a new pin proves the arm.
- Given a JWT-issuing provider, then `Allow` derives from the token endpoint's HTTPS-authenticated response alone — zero JWKS infrastructure remains (no `JwksCache`, no refresh thread, no `jwks-cache-ttl` knob, no `jwks_uri` requirement), the structural JWT-ness gate matches D7's resolution, and a stale `jwks-cache-ttl` key refuses startup.
- Given the spine at wrap, then AD-12, AD-8 clause (c), and AD-28 arm (2) carry dated in-place amendment markers, `.memlog.md` carries the entries with a Propagated list, epics.md's 4-path enumeration and the register are swept, and the retired TEA rows are marked.
- Given the T3 decision recorded in the memlog, when T5/T6 execute, then the split/manager shape matches it exactly — drift requires a new owner checkpoint.
- Given the vocabulary round, when it lands, then every surviving `splice`/`flip` occurrence in relay code or prose is either quoted spine contract text (exactly per D1's resolution) or swept — the survivor inventory lands in the Completion Notes.
- Given the split, when a decoded ROK `bind_resp` arrives, then the flip call site exists in exactly one class and the ingress class cannot express it (structural-by-type); `onBindAccept` still fires exactly at the flip.
- Given teardown from ANY site, then ordering is remove+mark → cancel+wipe → close, unchanged; RELAY-005/006 idempotence + no-orphan pins green; `onConnectionClosed` exactly-once per channel.
- Given the post-couple hot path, then a coupled PDU remains flag-read + forward — no manager consultation, no new virtual dispatch per PDU.
- Given each neutered/moved guard, then its biter goes RED at the NEW site, and each retired guard's ledger row is struck with evidence (restore byte-identical; zero `MUTATED` residue).
- Given `./gradlew clean build :buildSrc:test`, then BUILD SUCCESSFUL with `:proxy:test` count = 352 − retired + added (XML counts; every delta enumerated), 0 failed / 0 skipped.
- Given the deferred-work ledger at wrap, then F13 and the four story-text-pass entries carry dated RESOLVED markers and F7's disposition (absorbed-with-pin or re-homed) is recorded — no ledger marker owed by this story remains open.
- Given the refactored relay package, when the additional analysis iteration re-asks the cluster's questions against the new shape, then every finding is fixed in-story or ledgered with a disposition, the goals-met evidence per question (entanglement, ordering, vocabulary, scatter old-vs-new, new coupling) is recorded in the Completion Notes, and the final gates are green on the post-iteration tree.
- Given the full proxy module post-sweep, then no JWT signature-verification, opaque-token-introspection, or `tokenRequest` mTLS-client-auth primitive remains in `src/main` OR `src/test` (repo-wide grep survivors = dated historical prose only, enumerated), the slice's verdict derives from the ROPC token-endpoint response + structural three-segment gate alone with fresh D6/D7-aligned pins, `client_secret` is mandatory, and every count delta is enumerated on a GREEN `clean build :buildSrc:test`.
- Given a reverse cell at boot, then NO provider wire call is made at startup — `OidcStartupDiscovery`/`OidcProviderMetadata` gone from `src/main` (gate grep empty), the token endpoint derived at wiring from `provider-url` over `protocol/openid-connect/token` (provably: the adapter's token call lands on the derived URI; the well-known path records zero hits), the Direct-Access-Grants prerequisite carried by a dated `application.yml` policy block + the `Oidc` record javadoc, the config surface unchanged (no key added or renamed; `provider-url` https+host fail-fast intact), and a wrong or unreachable `provider-url` (or DAG-off client) denies fail-closed at first bind via the UNCHANGED AC2 mapping, now with a starred operator WARN naming the derived endpoint and `provider-url` — the retired startup-refusal posture recorded in the yml block and the Spec Change Log.

### Review Findings

_Chunk-A review (production code, `proxy/src/main`, `cda2013..fb68310`; 4 layers: blind-hunter / edge-case / verification-gap / acceptance-auditor; 2026-09-01). Chunks B (test tier) + C (planning docs) pending as follow-up runs. 13 raw findings dismissed after source-verification (unused-import ×2, RFC-3986 justification, the egress-close leak claim, jwks-ttl survivors, reflow artifacts, chunk-scope no-tests, final-asymmetry, the four already-dispositioned auditor deviations, and the provider-url shape-validation question — owner kept T9's uniform first-bind posture, 2026-09-01)._

- [x] [Review][Patch] `OPERATOR_WARNING` fires on the adapter's own shutdown aborts (close()-drain-timeout → `http.close()` aborts in-flight exchanges → IOException → "provider misconfiguration" banner on a routine restart; cancelHttp cancellations correctly stay silent). Owner decision 2026-09-01: suppress once close() begins — a volatile closed flag set at `close()` entry; the WARN skips when set (verdicts unchanged, still `DenyIndeterminate`). [`security/RopcBindCredentialVerifier.java:345-355, 700-718`] _(applied; no suppression pin — the closed state is reachable only through the defensive drain-timeout race, so it stays log-only hygiene; the T9 WARN pin on the live arms is unaffected and green)_
- [x] [Review][Patch] `Oidc` compact-ctor javadoc describes the join as `URI.resolve` — the superseded mechanism (impl is explicit append-after-last-segment; plain resolve drops the realm segment, T10-S3) plus muddled "replaced the former discovery-path join" history. [`config/ProxyCompanionProperties.java:314`]
- [x] [Review][Patch] `answered()` javadoc misstates the ordering invariant at both sites — claims both answer transitions run "before this forwarder observes the PDU"; the non-ROK arm forwards first (`fireChannelRead`) then tears down (`RelayEgressHandler:80-81`); the derivation actually holds because `answered()` is consulted at channelInactive/exceptionCaught after the closes — state that invariant. [`relay/ConnectionEntry.java:105-110`, `relay/BindInterceptor.java:559-564`]
- [x] [Review][Patch] Stale comment "Per-channel: holds the in-flight adjudication handles" — post-T6 the handles live on `ConnectionEntry` via `RelayStateManager`. [`relay/netty/RelayIngressInitializer.java:71`]
- [x] [Review][Patch] `CoupledRelayHandler` javadoc "the zeroize lives in the interceptor's teardown" — the wipe's owner is `RelayStateManager` since T6. [`relay/CoupledRelayHandler.java:68`]
- [x] [Review][Patch] `ConnectionRegistry` javadoc enumerates 4 entry fields ("peer-egress Channel, couple flag, session metadata, tearing-down mark") vs the post-T6 six-thing entry. [`relay/ConnectionRegistry.java:17`]
- [x] [Review][Patch] `Verdict.DenyInvalid` javadoc "(e.g. an IdP 4xx)" overbroad — only bare 401 + 400 `invalid_grant`/`invalid_client` map there post-T1/T2 (both occurrences). [`security/Verdict.java`]
- [x] [Review][Patch] `RelayStateManager` javadoc names the deleted `teardownForPreCoupleViolation` among "BindInterceptor's four arms … they now consume the decision" — reads as if four arms survive. [`relay/RelayStateManager.java:27-32`]
- [x] [Review][Patch] `TOKEN_ENDPOINT_PATH` javadoc cites "the post-T8 slice" — ambiguous cross-story task reference. [`security/RopcBindCredentialVerifier.java:132-133`]
- [x] [Review][Patch] `OPERATOR_WARNING` javadoc overclaims "exactly the arms a typo'd provider-url, a dead provider, or a DAG-off client lands on" — `unmappedStatus` also catches transient 3xx/403/404/429/5xx. [`security/RopcBindCredentialVerifier.java:159-161`]
- [x] [Review][Patch] `unmappedStatus` WARN discards the parsed 400 OAuth `error` code — `unauthorized_client` is the DAG-off smoking gun the adjacent comment names; include the code in the WARN line when present. [`security/RopcBindCredentialVerifier.java:386-411`]
- [x] [Review][Patch] Verifier ctor guards `maxInFlight`/`timeout` ("direct construction bypasses the annotation") but feeds `oidc.providerUrl()` to `deriveTokenEndpoint` unguarded — a programmatic null NPEs unnamed; add the matching `requireNonNull`. [`security/RopcBindCredentialVerifier.java:226-227`]
- [x] [Review][Defer] Unguarded `RelayObserver` callbacks (4 sites) — a throwing observer: `onBindReject` (worst: runs in an event-loop task with no exceptionCaught net → frame leak + no deny + client hangs to TCP timeout), `onBindAccept` (ROK leaked + bare close at the couple), `onFramedPdu` (per-PDU frame leak pre-write), `onConnectionClosed` (fireChannelInactive skipped); the interface documents no "must not throw" contract and the noop default can't throw — the risk materializes when Epic 4 swaps in the production observer. [`relay/BindInterceptor.java:370`, `relay/RelayEgressHandler.java:70`, `relay/CoupledRelayHandler.java:159,274`] — deferred, pre-existing (2.2-era seam posture; this diff moved/renamed the sites without changing guard behavior)
- [x] [Review][Defer] `OPERATOR_WARNING` per-bind flooding — no once-per-condition/rate bound; a dead/typo'd provider logs the full ~14-line starred banner per denied bind (up to max-in-flight concurrent). [`security/RopcBindCredentialVerifier.java:406-411`] — deferred (owner 2026-09-01): Epic 4 owns operator logging — decide with the production observer it lands
- [x] [Review][Defer] Non-JWT (opaque-token) WARN not starred and carrying no tokenEndpoint/providerUrl context, inconsistent with the `OPERATOR_WARNING` banner pattern. [`security/RopcBindCredentialVerifier.java:448-450`] — deferred (owner 2026-09-01): Epic 4 owns operator logging — decide with the production observer it lands

## Spec Change Log

- **2026-08-27 (pre-dev, owner-directed):** scope widened ahead of any implementation — two OIDC token-path simplifications inserted as T1/T2 (remove the RFC 7662 opaque-token arm, JWT-only; remove local JWT signature verification and with it `JwksCache`, its refresh VT, the `jwks-cache-ttl` knob, and the `jwks_uri` discovery requirement). Title, Intent, Boundaries, I/O matrix, Code Map, and ACs re-baselined; the refactoring round (former T1–T6) renumbered T3–T8 with the umbrella-first constraint preserved.
- **2026-08-27 (pre-dev, owner-directed):** T9 appended — a closing additional analysis iteration over the refactored relay shape (the cluster's questions re-asked with evidence + an adversarial diff pass), with re-run of the final gates if it applies fixes.
- **2026-08-29 (in-progress, owner-directed):** scope extension #2 — all three interop arms removed from the test-tier slice (JWT signature verification, RFC 7662 opaque-token introspection, RFC 8705 `tokenRequest` mTLS client auth), ending its historical-ratification exemption: the slice becomes the live ratification of the amended contract (ROPC + mandatory `client_secret` + structural three-segment gate). New T8 inserted; former T8/T9 renumbered T9/T10 (living cross-references swept; dated decision records and this log keep the numbering of their time). The 2026-08-27 "deliberately UNCHANGED" T1/T2 dispositions are reversed; the 2026-08-19 mTLS precedent no longer shields slice arms.
- **2026-08-29 (in-progress, owner-directed):** scope extension #3 — `OidcStartupDiscovery` removed whole ("simplify app"): the token endpoint is DERIVED from `provider-url` at wiring (the pinned-Keycloak realm path `protocol/openid-connect/token`); the Direct-Access-Grants/password-grant prerequisite moves to DOCUMENTATION (a dated `application.yml` policy block + the `Oidc` record javadoc); config surface unchanged — no key added or renamed. Deliberate posture change: provider misconfiguration (typo'd `provider-url`, dead provider, DAG-off client) surfaces at FIRST BIND — fail-closed deny via the unchanged AC2 mapping plus ONE new starred operator WARN (the retired `DAG_WARNING` form; log-only, verdict table untouched) — not at startup; the startup-refusal posture is retired. New T9 inserted; former T9/T10 renumbered T10/T11 (living cross-references swept; dated records and this log keep the numbering of their time). The frozen I/O-matrix discovery row (field-completeness = `issuer` + `token_endpoint`) is MOOTED — supersession recorded here, the frozen block stays untouched (the extension-#2 precedent). The open deferred-work entry "evaluate an OIDC-discovery/parsing library" is superseded by removal (nothing left to parse) — T9 marks it RESOLVED.

## Design Notes

**Rationale record (owner, 2026-08-27):** the proxy is the token's ONLY consumer — it never forwards, caches, nor shows the token to anyone; the verdict is the token endpoint's own response (AC2 mapping unchanged). Re-proving the provider's honesty locally (signature verification against fetched keys) guards against a threat that already owns the HTTPS channel the keys came over; the introspection arm exists for opaque-token provider fleets this single-pinned-Keycloak deployment is not. Simpler + fail-closed wins.

**D6 — non-JWT posture (Ask First):** token shape is observable only at bind time (a startup probe would need a real credential), so the arm is RUNTIME: `DenyIndeterminate` + a WARN naming the JWT-only policy and the operator remediation (configure the client/realm to issue JWT access tokens). Lean: that, verbatim.

**D7 — what survives of the JWT + Nimbus (Ask First):** lean = keep ONLY the structural three-segment gate (the "always returns JWT" requirement — cheap, no library: split on `.`); DROP `typ`/`iss`/`exp`/`kid` handling with the verification (unverified-claim checks are theater — the endpoint verdict already decided). `nimbus-jose-jwt` then survives only for discovery-JSON parsing (`JSONObjectUtils` — the proxy has no Jackson); keep the dep and its `com.nimbusds..` crypto-allowlist entry. Alternative (not lean): parse claims for expiry logging — rejected: adds parse surface for zero verdict effect.

**D1 — vocabulary-sweep scope (Ask First):** (a) code+prose only, vs (b) full unification including spine AD-25/AD-2 title + rule text ("Bind→splice transition state machine", "flip-flag") with dated in-place markers + memlog. The ledger warns (a) "re-creates the same code/prose split one level up" — lean (b). Sub-fork: does `SpliceObserver` rename? A ratified-contract change rippling AD-27, epics.md, the TEA docs, and the readiness report; memlog L105 is the amendment-path precedent. `onFramedPdu` is already couple-neutral; `PRE_COUPLE_NON_BIND_PDU` already speaks couple.

**D1 resolution (owner checkpoint, 2026-08-28 — full unification + `RelayObserver` + `relay` as the data-plane verb; governs T4; drift requires a new owner checkpoint):**
- **Scope — FULL unification (option b):** spine AD-25 + AD-2 (title + rule text) amended with dated in-place markers; the remaining spine splice/flip wording swept consistently (AD-1 "steady-state byte splice", AD-8 "flip-flag", AD-32 "splice flip", the AD summary-table + module-map rows); operative epics rows (FR-TRANSIT-1, SEC-2, REL-1, the Epic-2 goal/NFR lines) swept with dated notes; TEA live rows get wording notes. `[AD-N]` mirror bullets in epics stay frozen per the T2/8705 precedent (spine governs; the memlog flags them for re-distill); historical records (done-story files, `reviews/`, the deferred-work ledger, dated reports) are never retro-edited. One `.memlog.md` `(amendment)` entry carries the full Propagated list.
- **`SpliceObserver` → `RelayObserver`** (AD-27 amendment, the memlog L105 5→4-method precedent): `NoopSpliceObserver` → `NoopRelayObserver`, `CapturingSpliceObserver` → `CapturingRelayObserver` (+ its test), `SpliceObserverShapeTest` → `RelayObserverShapeTest`; the 4 method names unchanged (already couple-neutral); spine AD-27 amended (the interface name + "the splice metric" wording); epics/TEA per the scope rule above.
- **Vocabulary mapping (the T4 renames):** state — `spliced()` → `coupled()`, `flipSpliced()` → `couple()`, the `spliced` field/locals/params likewise (the 2026-08-16 ledger note's own `coupled()`/`couple()` pair); data-plane action — `RelayHandler.splice()` → `relayFramedPdu()`, "opaque splice" → "opaque relay", "one spliced PDU"/"spliced traffic" → "relayed PDU(s)", AD-1 "steady-state byte splice" → "steady-state byte relay", AD-2 title "…framed-ByteBuf splice" → "…framed-PDU relay"; "the flip"/"pre-flip"/"post-flip" → "the couple (transition)"/"pre-couple"/"post-couple" (already the dominant prose form). Incidental English "flip" (Netty writability flips, `AdjudicationLifecycle`/`VerifierWiringConfig` "pure flag flip") is NOT pair-state vocabulary — untouched, inventoried.
- **AC5 survivor policy:** after the sweep, zero `splice`/`flip` occurrences remain in `relay/` code or prose or the amended contract sites; deliberate survivors = frozen epics mirror bullets, historical records (done stories, ledger, `reviews/`, dated reports), and this story's own quoted-history text — each inventoried in the Completion Notes with its category. **"forward" is DISQUALIFIED as the data-plane verb** (collides with the proxy ROLE word + `openEgressAndForward`).

**D2 — manager variant + mechanics (the T3 fork):** (1) single manager covering both roles vs (2) per-role managers — the SAME fork as the interceptor role-split at a different altitude; treat as ONE. Mechanics: sealed decision returned to the caller (lean — avoids the circular manager↔handler dependency; record which is chosen). Thread-bounding: manager = event-loop-confined policy like the handlers; the verdict continuation hop is its single off-loop entry; VT-side adjudication kickoff stays in the interceptor's control-plane half.

**T3 decision (owner checkpoint, 2026-08-28 — RESOLVES D2 + D5; full record in `.memlog.md` same date; drift from this shape requires a NEW owner checkpoint):**
- **Variant — SINGLE manager:** one relay-internal state manager holding the transition policy OVER `ConnectionRegistry`'s storage (no second state copy) for BOTH role arms; the interceptor stays one class with its role branch (one fork, per D2). The pair lifecycle is role-agnostic — per-role would duplicate an identical state machine and double every invariant proof + T8 biter; the only divergence scenario (forward pooling/standing lanes) was rejected at the 2026-08-21 [B] decision.
- **Mechanics — SEALED decision returned to the caller:** manager transition methods return sealed outcomes (`beginTeardown` → `Won(entry) | Lost`); the manager performs the absorbed hygiene (`cancelHttp` + zeroize) BEFORE returning `Won` (the remove+mark → cancel+wipe half of the ordering is single-sited, structurally un-gettable-wrong); callers run only leg-specific tails (closes, AD-33 deny write, reason stash). No manager→handler references. The `channelInactive` teardown + exactly-once `onConnectionClosed` CAS stay handler-side (AD-27) — the executor-facing port was rejected because it could never own the inactive path.
- **Hard constraint — the AD-25 flip is NOT manager-routable:** manager surface = register / attachEgress / beginTeardown / lookups; the flip stays ENTRY state, its single-caller path made structural-by-TYPE in T5.
- **Absorption — ALL THREE** (dated AD-8 amendment lands with T6's code; entry goes 4→6 fields): (a) `pendingVerdict`/`pendingPassword` → entry (adjudicating becomes real entry state; the `RelayHandler:168` pipeline-lookup delegation DIES — the split handlers call the manager; zeroize stays caller-owned, the caller being the manager; the in-flight assert moves to registration); (b) `EgressLeg.answered` → entry as the `awaiting-bind_resp → answered` transition (candidate state set fully entry-resident — zero shadow bits; EgressLeg takes the entry ref at its construction site); (c) `CLOSE_REASON`/`CLOSE_FIRED` keys → hoisted to a shared relay-package home (D5 resolved; mechanics only — reason semantics stay Epic-4).
- **Unchanged:** manager = event-loop-confined policy; the verdict continuation hop is its ONE off-loop entry; VT-side adjudication kickoff stays in the interceptor (AD-28 — no executor added); every explicit state DERIVES from the registry entry on each read — never a shadow boolean (AD-32).
- **Surface ruling (owner, 2026-08-29):** T6's `beginAdjudication`/`settleAdjudication` are accepted as WITHIN the T3 surface constraint — the list named the absorption's anchor transitions, not an exhaustive API; the constraint's intent (the couple not manager-routable) holds as-built. No new checkpoint required.

**Candidate explicit state set (2026-08-16 T7 note + as-built):** `not-registered` → `registered/adjudicating` (RELAY-004 in-flight predicate) → `egress-attached/connecting` → `awaiting-bind_resp` (`EgressLeg.answered == false`) → `coupled` (the AD-25 flip) → `tearing-down` (CAS mark). AD-32 constraint: any explicit state DERIVES from the registry entry on every read — never a shadow boolean.

**D3 — split-half naming:** candidate `RelayIngressHandler`/`RelayEgressHandler` over a shared splice component (name it once with the T4 vocabulary). Spine AD-25 names `RelayHandler` — dated marker + memlog either way. `ConnectionRegistry`/`ConnectionEntry` type names: KEEP — AD-8 names the registry, RELAY-025 resolves it by filename, and the types were never the inconsistent words (all couple/pair vocabulary lives in identifiers and prose, not type names).

**D3 resolution (owner checkpoint, 2026-08-28 — `RelayIngressHandler`/`RelayEgressHandler` over abstract base `CoupledRelayHandler`; governs T5; drift requires a new owner checkpoint):**
- **Names:** `RelayIngressHandler` / `RelayEgressHandler` (mirroring `RelayIngressInitializer`/`RelayEgressInitializer`; the Relay-family prefix keeps pipeline listings self-describing). The shared post-couple component is the abstract base **`CoupledRelayHandler`** (extends `ChannelInboundHandlerAdapter`) — "AbstractRelayHandler" was rejected for near-colliding with the dying `RelayHandler` (grep/blame mud across the refactor boundary); composition was rejected for the per-callback delegation hop + re-passing direction into the component.
- **Shape mechanics:** the base owns the shared `channelRead` prelude (the AD-25 race-free re-check; `coupled()` → `relayFramedPdu`), the post-couple relay + writability re-arm, `teardownPair`/`channelInactive`/`exceptionCaught`, the exactly-once close telemetry, `classify`/`stash`/`releaseFrame`, and the `CLOSE_REASON`/`CLOSE_FIRED` keys (the D5 hoist home = the base class); each subclass implements the pre-couple read hook. `direction` is a base FIELD set via the protected ctor (NOT an abstract method — zero new virtual dispatch per relayed PDU, AC8); `peerOf` stays in the base with its field-compare ternary — the ONE residual conditional T10 sanctions. `GENERIC_NACK` + the `getInt(4)` read live in the egress class (the ingress path never reads the id).
- **Structural-by-type (AC6):** only `RelayEgressHandler` has a decoded-`bind_resp` arm — the couple (`entry.couple()`, the N1 site) is expressible in exactly one class; the ingress class has no bind_resp arm at all (a stray decoded `bind_resp` on the ingress leg is unreachable today — `BindInterceptor.channelRead0` releases client-sent bind-responses upstream — and post-split it fails CLOSED: bare-close via the delegation, never couples, never CCEs). Pin: an ArchUnit call-site rule (the `ObservabilityLayerRulesTest` FQN-scoped pattern — `class.getName()` on both sides so renames refactor both) asserting ONLY `RelayEgressHandler` among main-source classes calls `ConnectionEntry.couple()`; `ConnectionRegistryTest`'s direct CAS pins are test-source and unaffected.
- **Re-pointing:** the two `new RelayHandler(...)` sites (`RelayIngressInitializer` / `RelayEgressInitializer`); `RelayPipelineInitializersTest` (`.class` lookups, order assertions, DisplayNames — T4 deliberately deferred these to T5); `RelayHandlerTest` SPLITS along the same seam into `RelayIngressHandlerTest` / `RelayEgressHandlerTest` (tests partition by injection leg; helpers/prose re-pointed; every re-target enumerated); prose sweeps in `BindInterceptor` (incl. the `:530` `addLast` comment), `ConnectionEntry`/`ConnectionRegistry`/`RelayChannelOptions` javadocs, `RelayTestFixtures`, `CapturingRelayObserver`, `BindInterceptorTest`/`BindInterceptorForwardRoleTest`, `RelayA1SmokeTest`. The type `RelayHandler` and its file DIE (the `AttributeKey` namespace moves to the base — runtime-only attrs, no wire effect).
- **Contract:** spine — AD-25 dated amendment ("exactly one unit — the data-plane `RelayHandler`" → `RelayEgressHandler` over the shared base; every RelayHandler-naming site in the rule re-pointed, incl. the read-only-observer clause) + the other spine sites naming the type (`:44` paradigm bullet, `:51-52`/`:60` mermaid, `:104` AD-7, `:224` AD-27 "holds an injected reference", `:375` module map) swept under the same dated marker; epics — the two operative rows (module-map `:143`, AD-25 summary row `:272`) re-pointed with a dated note; the `[AD-N]` mirror bullets stay frozen (precedent). TEA — one dated note line appended to the T4 banner (rows naming `RelayHandler` as the couple unit read as the egress class post-split; live row text stays). One `.memlog.md` entry with the Propagated list.
- **T6 boundary:** the ingress class's delegation to `teardownForPreCoupleViolation` SURVIVES T5 unchanged (T6's manager replaces it); no state-manager work rides T5; the six teardown sites stay six until T6.

**D4 — F7 (Ask First):** verdict-future frame ownership — a cancelled, never-settling verdict leaks one pooled buffer (the `whenComplete` continuation is the only releaser of `req.originalFrame()`). Owner deferred to "Epic 3" (now done) → 3.4 must consciously **absorb** (small: one release arm + one pin — lean, fail-closed hygiene) or **re-home** to the Epic-4 RELAY-020/021 timeout round.

**D5 — optional mechanical absorb:** F9's `CLOSE_REASON` key hoisting (today `RelayHandler`-private, class-scoped) rides the direction-split naturally; the reason *semantics* stay Epic-4. Zero-cost if the split already moves the keys — decide with T3's state-absorption scope.

**Scatter map (T6 input):** pending handles `BindInterceptor:149,151` (settle/wipe protocol at `:332-333`, `:348,371-372`, `:408-419`); `EgressLeg.answered` `:607`; `CLOSE_REASON`/`CLOSE_FIRED` attrs `RelayHandler:83,87`; the registry's removal half `ConnectionRegistry:101-113`; teardown ordering re-implemented at six sites — `BindInterceptor:384,431,539,560` + `RelayHandler:240,263`.

**T11 — why an analysis iteration closes the round:** the cluster's deferral notes were written against the 2.2-era shape; executing them against the 3.3-widened surface can *relocate* problems rather than remove them (a scatter map that merely moves, vocabulary that re-splits at the new component seams, ordering that re-scatters across the split halves). The iteration is the refactor's own acceptance test: each original question re-answered with evidence from the new shape, plus the adversarial finder pass over the story diff. Stop criterion: zero unfixed findings — everything else is ledgered with a disposition or escalated to an owner checkpoint.

**Mutation-pass traps (inherited, 2.2 + 3.3):** neuter-site renames silently de-target biters (N1's site IS the flip call — T10 re-proves it at the successor site); a biter whose assertion can throw must release latches in `finally`; force `cleanTest` for source-scan tests (Gradle UP-TO-DATE skips them); EmbeddedChannel gotchas — `DefaultChannelId.newInstance()`, `runPendingTasks()` after completing held futures, pre-close delivery for exactly-once pins.

## Verification

**Commands:**
- `./gradlew clean build :buildSrc:test` — expected: BUILD SUCCESSFUL
- `./gradlew :proxy:test` post-`cleanTest` — expected: count = 352 − retired + added, every delta enumerated; 0 failed / 0 skipped, XML-verified (console counts lie)
- `grep -rn "FIXME\|@Disabled" <diff files>` — expected: zero new occurrences (the four owner-FIXMEs at `BindInterceptor:142,284,298,601` pre-exist)
- `grep -rn "MUTATED" proxy/src` — expected: empty (mutation-residue scan)
- `grep -rn "7662\|introspect\|jwks\|Jwks" proxy/src/main` — expected: empty (T1/T2 removal completeness)

## Dev Agent Record

### Agent Model Used

GLM (claude-code CLI), 2026-08-27 — T1 execution.
GLM (claude-code CLI), 2026-08-28 — T2 execution (main + test halves + the AD-5/8/10/12/28 spine markers landed in the 2026-08-27 tree; this session completed the residual spine sweep, the epics/TEA/register propagation, the memlog entry, the gates, and the bookkeeping).
GLM (claude-code CLI), 2026-08-28 — T4 execution (couple/relay vocabulary unification + `SpliceObserver` → `RelayObserver`, per the D1 owner resolution).
GLM (claude-code CLI), 2026-08-28 — T5 execution (the `RelayHandler` direction-split into `RelayIngressHandler`/`RelayEgressHandler` over `CoupledRelayHandler`, the AC6 ArchUnit couple-site pin, the D5 `CLOSE_*` key hoist, the test split + re-pointing, and the contract sweep, per the D3 owner resolution).
GLM (claude-code CLI), 2026-08-29 — T6 execution (the `RelayStateManager` extraction over `ConnectionRegistry`'s storage, the sealed `Won(entry) | Lost` teardown decision with the absorbed hygiene single-sited before the `Won` return, all three T3 absorptions landing on the `ConnectionEntry`, the `RelayIngressHandler`→interceptor delegation death, the AD-8 dated amendment + memlog entry, and the test re-pointing, per the T3 owner resolution).
GLM (claude-code CLI), 2026-08-29 — T8 execution (the test-tier slice's three interop arms removed — JWKS verify, RFC 7662 introspection, RFC 8705 `tokenRequest` mTLS client auth — `SliceConfig` reduced to `tokenEndpoint, clientId, clientSecret` with a mandatory secret, the structural three-segment gate + fresh D6/D7/mandatory-secret pins, the fixture/README/realm sweep, the spine AD-12 marker extension + register update, and the memlog disposition-reversal entry).
GLM (claude-code CLI), 2026-08-29 — T9 execution (`OidcStartupDiscovery` removed whole; the token endpoint derived on `RopcBindCredentialVerifier` over `protocol/openid-connect/token`; the starred operator WARN on the connection-error + non-mapped-non-200 arms; the yml/javadoc DAG documentation; `OidcStartupDiscoveryTest` deleted (−10) + 3 fresh pins; the spine AD-12/AD-36/module-map + epics SEC-3 + deferred-work RESOLVED markers; the memlog entry).
GLM (claude-code CLI), 2026-08-29 — T7 execution (ledger hygiene: the F13 + four story-text-pass RESOLVED markers and the F7/D4 re-home disposition in `deferred-work.md`; the TEA SEC-030 retirement note T2's notes routed here; every claim fact-audited at the source before any marker was written).
GLM (claude-code CLI), 2026-08-30 — T10 execution (the full POST-rename RED-on-neuter mutation re-run: 17 fresh rows at renamed/moved sites — 9 relay, 5 production-security, 3 slice — plus the struck retired-guard rows with evidence, and the final gates on the restored tree).
GLM (claude-code CLI), 2026-08-31 — T11 execution (the closing additional analysis iteration: the 2026-08-16 cluster's questions re-asked against the POST-refactor shape with grep/read evidence at every claim, the fresh scatter map drawn and diffed against the pre-refactor one, and the blind-hunter / edge-case / verification-gap finder pass over the full story diff `cda2013..e4ad0f3` — zero unfixed findings, zero structural findings, no owner checkpoint owed; the final gates re-proven green on the post-iteration tree; zero source changes).

### Debug Log References

- `./gradlew :proxy:cleanTest :proxy:test` → BUILD SUCCESSFUL (2m 14s), Docker UP.
- XML counts: **340 tests, 0 failed, 0 errors, 0 skipped** (49 suites) — baseline 352 − 12 retired + 0 added-retirement + 1 new D6 pin = 340. ✓
- `grep -rn "7662\|introspect" proxy/src/main` → empty (removal completeness, src/main half of the gate; the `jwks|Jwks` half remains for T2).
- New D6 row confirmed executed in `TEST-…RopcBindCredentialVerifierTest.xml`.

**T2 (2026-08-28):**

- `./gradlew clean build :buildSrc:test` → BUILD SUCCESSFUL (2m 23s), Docker daemon up (Testcontainers Keycloak).
- XML counts: **324 tests, 0 failed, 0 errors, 0 skipped** (48 suites) — 340 post-T1 − 16, every delta enumerated in the T2 Completion Notes. ✓
- `grep -rn "7662\|introspect\|jwks\|Jwks" proxy/src/main` → **empty** (the removal-completeness gate T1 left half-open is now fully green).
- Spine post-sweep grep: zero JWKS/introspection sites without an amendment marker (only intended markers + `RopcSlice`-family historical references remain).
- `git diff -- proxy/src | grep ^+ | grep -c "FIXME\|@Disabled"` → 0; `grep -rn "MUTATED" proxy/src` → empty.

**T4 (2026-08-28):**

- `./gradlew clean build :buildSrc:test` → BUILD SUCCESSFUL (2m 14s), Docker daemon up (Testcontainers Keycloak).
- XML counts: **324 tests, 0 failed, 0 errors, 0 skipped** (48 suites) — the post-T2 baseline unchanged: a rename-only round, zero rows added or retired. The renamed suites execute under their new names (`…observability.RelayObserverShapeTest`, `…observability.CapturingRelayObserverTest`). ✓
- AC5 survivor grep over `relay/` + `observability/` (main + test) + `testsupport/` → exactly the seven inventoried incidental-English lines (see Completion Notes); zero pair-state `splice`/`flip` survivors.
- `grep -rn "7662\|introspect\|jwks\|Jwks" proxy/src/main` → still **empty** (T1/T2 removal completeness re-proven on the T4 tree).
- `git diff -- proxy/src | grep ^+ | grep -c "FIXME\|@Disabled"` → 0 (the four owner-FIXMEs untouched); `grep -rn "MUTATED" proxy/src` → empty.
- Stale-identifier grep (`flipSpliced|.spliced()|coupleAndFlip|NoopSplice|CapturingSplice` over `proxy/src`) → empty — no de-targeted reference survives.

**T5 (2026-08-28):**

- `./gradlew clean build :buildSrc:test` → BUILD SUCCESSFUL (2m 11s, re-run 2m 13s after the final `Direction`-import prune — the gate is green on the FINAL tree), Docker daemon up.
- XML counts: **327 tests, 0 failed, 0 errors, 0 skipped** (50 suites) — post-T4 324 − 12 retired `RelayHandlerTest` rows + 13 split rows (6 `RelayEgressHandlerTest` + 7 `RelayIngressHandlerTest`, incl. the one NEW AC6 stray-bind_resp pin) + 2 `RelayCoupleSiteArchitectureTest` ArchUnit rules; suites 48 − 1 + 3. ✓
- AC6 evidence: `grep -rn "\.couple()" proxy/src/main` → exactly ONE hit — `RelayEgressHandler.java:69` (the N1 successor site); the ArchUnit forbid rule + positive control both green in XML.
- Dead-name grep: `grep -rnP '(?<![A-Za-z])RelayHandler\b' proxy/src` → **1 hit** = the new pin suite's own quoted-history javadoc ("the `RelayHandler` direction-split left exactly one…"); every other occurrence is inside the new type names (`RelayIngressHandler`/`RelayEgressHandler`/`CoupledRelayHandler`).
- `grep -rn "7662\|introspect\|jwks\|Jwks" proxy/src/main` → still **empty** (T1/T2 removal completeness re-proven on the T5 tree).
- `git diff -- proxy/src | grep ^+ | grep -c "FIXME\|@Disabled"` → 0 (the four owner-FIXMEs untouched); `grep -rn "MUTATED" proxy/src` → empty.
- `:proxy:compileJava` carries one PRE-EXISTING ErrorProne warning: `peerOf(…, self)` unused parameter — the D3-pinned two-arg field-compare shape, byte-identical to the pre-split code (the warning existed on the old class; the signature is kept so every call site moved verbatim).

**T6 (2026-08-29):**

- `./gradlew clean build :buildSrc:test` → BUILD SUCCESSFUL (2m 16s), Docker daemon up (Testcontainers Keycloak); re-run on the FINAL tree (two javadoc-comment edits landed mid-run) → BUILD SUCCESSFUL (2m 0s).
- XML counts: **332 tests, 0 failed, 0 errors, 0 skipped** (51 suites) — post-T5 327 + 4 new `RelayStateManagerTest` rows + 1 new `ConnectionRegistryTest` row (`answered()` derivation); zero rows retired. ✓
- AC8 hot-path check: the per-PDU path is the base's final prelude → `manager.entryFor` (a monomorphic delegation to the same cached-attribute read) → `coupled()` flag-read → `relayFramedPdu`; zero transition/policy methods on the relay path.
- AC6 preserved: `grep -rn "\.couple()" proxy/src/main` → exactly ONE hit, `RelayEgressHandler.java:69` (unchanged by T6); `RelayCoupleSiteArchitectureTest` forbid + positive control both green in XML — the manager has no couple surface at all.
- Re-point completeness: `grep teardownForPreCoupleViolation|cancelAndWipePending` over code → empty; `grep registry\.` outside `ConnectionRegistry`/`RelayStateManager` → empty; stale-ctor grep (`new RelayIngressHandler(registry`, `RelayEgressInitializer(registry`, …) → empty.
- `grep -rn "7662\|introspect\|jwks\|Jwks" proxy/src/main` → still **empty** (T1/T2 removal completeness re-proven on the T6 tree).
- `git diff -- proxy/src | grep ^+ | grep -c "FIXME\|@Disabled"` → 2, BOTH the pre-existing 2026-08-15 owner-FIXMEs (the adjudicate invariant comment rewritten around the moved assert; `EgressLeg`'s Lombok-ctor comment re-counted three→final-field for the entry-ref ctor) — zero NEW occurrences; `grep -rn "MUTATED" proxy/src` → empty.
- `:proxy:compileJava` still carries only the one PRE-EXISTING `peerOf(…, self)` ErrorProne warning (the D3-pinned shape; T6 added zero warnings).

**T8 (2026-08-29):**

- `./gradlew clean build :buildSrc:test` → BUILD SUCCESSFUL (2m 11s), Docker daemon up (Testcontainers Keycloak — both the slice's and the production suite's containers ran live; the slice's 3-row live suite proves the edited realm JSON imports and Client A works against the real container). Re-proven GREEN on the FINAL tree after the mutation spot-pass restore (2m 10s; the restore is byte-identical; zero `MUTATED` residue).
- XML counts: **322 tests, 0 failed, 0 errors, 0 skipped** (51 suites) — post-T6 332 − 9 retired `RopcSliceUnitTest` rows (kid-miss + the 8 claim-deny rows, retired with the `verifyWithJwks` seam they drove) − 2 retired `RopcSliceLiveTest` rows (path 2 = 7662, path 3 = mTLS) + 1 net new in `RopcSliceFailClosedTest` (−2 introspection rows, +3 fresh pins: D6 opaque-deny, D7 three-segment-garbage Allow, mandatory-client_secret). ✓
- AC13 gate grep, repo-wide: `grep -rn "7662\|introspect\|jwks\|Jwks\|RSASSAVerifier\|SignedJWT\|JWKSet\|useMtlsClientAuth\|tls_client_auth" proxy/src` → **37 survivors, every one dated historical prose or a load-bearing T1/T2 pin** (enumerated in the Completion Notes); the `src/main` half is still empty (T1/T2 re-proven).
- Spot RED-on-neuter pass on the fresh pins (backup → neuter → RED → restore, byte-identical): gate → always-`Allow` ⇒ the D6 row RED; gate → always-`DenyIndeterminate` ⇒ the D7 row RED; the `clientSecret` compact-ctor guard neutered ⇒ the mandatory-secret row RED; post-restore suite GREEN; `grep -rn "MUTATED" proxy/src` → empty.
- `git diff -- proxy/src | grep ^+ | grep -c "FIXME\|@Disabled"` → 0; `:proxy:compileTestJava` carries one PRE-EXISTING ErrorProne warning on the slice (`TokenOutcome`'s `char[]` record component — byte-identical to the pre-T8 shape); zero new warnings.

**T9 (2026-08-29):**

- `./gradlew clean build :buildSrc:test` → BUILD SUCCESSFUL (2m 9s first run; re-proven GREEN on the FINAL tree after the never-hit pin's provider-url fix + the mutation spot-pass restores, 2m 13s), Docker daemon up (the live suite ran against the real container — its 3 rows prove the DERIVED endpoint lands on the real Keycloak realm token path, the live-tier AC14 proof).
- XML counts: **315 tests, 0 failed, 0 errors, 0 skipped** (50 suites) — post-T8 322 − 10 retired `OidcStartupDiscoveryTest` rows (suite deleted with the probe) + 3 fresh pins in `RopcBindCredentialVerifierTest`; suites 51 − 1. ✓
- Gate: `grep -rn "OidcStartupDiscovery\|OidcProviderMetadata\|openid-configuration\|grant_types_supported\|well-known" proxy/src/main` → **EMPTY**; repo-wide over `proxy/src` → 5 survivors, all fixture-internal (enumerated in the Completion Notes).
- Spot RED-on-neuter pass on the 3 fresh pins (backup → neuter → RED → restore, byte-identical): `deriveTokenEndpoint` → plain `URI.resolve` ⇒ 14 RED incl. the derivation pin + the WARN pin; a re-introduced ctor GET of the provider-metadata path ⇒ the never-hit pin RED (after the pin's provider-url fix — see Completion Notes); both `log.warn(OPERATOR_WARNING, …)` call sites neutered ⇒ exactly the WARN pin RED (and NOTHING else — the verdict table is provably untouched by the WARN); `grep -rn "MUTATED" proxy/src` → empty post-restore.
- `git diff -- proxy/src | grep ^+ | grep -c "FIXME\|@Disabled"` → 0; `:proxy:compileJava` still carries only the pre-existing `peerOf`/`StringSplitter` warnings; zero new warnings.

**T10 (2026-08-30):**

- 17 mutations (T10-R1..R9 relay, T10-S1..S5 production security, T10-SL1..SL3 slice — see the ledger below), every one: unique `/tmp/t10-backups` master → neuter → `:proxy:cleanTest :proxy:test --tests '<pinning suites>'` (cleanTest forced — the UP-TO-DATE trap) → RED set extracted from the JUnit XML → restore → `cmp`-verified byte-identical. RED counts: R1=6, R2=1, R3=1, R4=2, R5=9, R6=3, R7=1, R8=2, R9=1, S1=1, S2=3, S3=14, S4=3, S5=1, SL1=1, SL2=1, SL3=1 — every named biter RED, every restore byte-identical.
- Final gates on the RESTORED tree: `./gradlew clean build :buildSrc:test` → **BUILD SUCCESSFUL** (2m 11s), Docker daemon up (both live suites ran); `:proxy:test` XML-verified **315 tests, 0 failed, 0 errors, 0 skipped (50 suites)** — the post-T9 baseline exactly; T10 adds/retires zero rows (352 − retired + added = 315 re-confirmed).
- `grep -rn "MUTATED" proxy/src` → **empty**; `git status --porcelain proxy/src` → **empty** (the mutation-only round leaves zero net code change).
- Removal gates re-proven on the T10 tree: `7662|introspect|jwks|Jwks` over `proxy/src/main` → empty; the T9 `src/main` gate pattern (`OidcStartupDiscovery|OidcProviderMetadata|openid-configuration|grant_types_supported|well-known`) → empty; the AC13 repo-wide pattern over `proxy/src` → 30 survivors (the T8/T9-inventoried dated historical prose + load-bearing pins — `RopcBindCredentialVerifierTest`'s `SignedJWT` builder, the never-hit pin handlers, the stale-key row's subject).
- FIXME/@Disabled: T10's only tree delta is this story file — zero code lines touched net, zero new occurrences.

**T11 (2026-08-31):**

- `./gradlew clean build :buildSrc:test` → **BUILD SUCCESSFUL** (2m 20s), Docker daemon up — BOTH live suites ran inside the build (`RopcBindCredentialVerifierLiveTest` 3/0/0, `RopcSliceLiveTest` 3/0/0, XML).
- XML counts: **315 tests, 0 failed, 0 errors, 0 skipped** (50 suites) — the post-T10 baseline exactly; T11 applies ZERO code changes (the round's only tree delta is this story file, so no T10 gate re-run was triggered — the final gates ran as the post-iteration green proof AC12 demands).
- Entanglement gate: `grep -rn "direction ==\|== direction" proxy/src/main` → exactly ONE hit, `CoupledRelayHandler.java:294` (`peerOf` — the D3-pinned, T9-sanctioned residual); the two pre-split branch sites (`RelayHandler:155/:167`) are gone with the class.
- Single-siting gate: raw `registry.{register, attachEgress, beginTeardown}` appear in `src/main` ONLY inside `RelayStateManager` itself (its own three delegations); all seven production teardown consumers call `manager.beginTeardown` and consume the sealed decision.
- Vocabulary gate (case-insensitive `splice|flip`): relay/ + observability/ (main + test) + testsupport/ → exactly the six T4-inventoried incidental-English lines (`RelayChannelOptions.java:27`; `RelayServerLifecycleTest.java:84/85/88/99/101`); the security-scope survivors (`VerifierWiringConfig.java:52`, `AdjudicationLifecycle.java:17`, `AdjudicationLifecycleTest.java:73/84`, `RpcBindCredentialVerifierTest.java:372`) are likewise all T4 inventory — line numbers shifted by later edits, categories unchanged. Zero pair-state survivors.
- Couple-site + removal gates re-proven on the iteration tree: `.couple()` over `src/main` → exactly ONE hit (`RelayEgressHandler.java:69`); `7662|introspect|jwks|Jwks` over `src/main` → empty; the T9 `src/main` gate pattern (`OidcStartupDiscovery|OidcProviderMetadata|openid-configuration|grant_types_supported|well-known`) → empty; `grep -rn "MUTATED" proxy/src` → empty; FIXME/@Disabled added lines in the story diff = 3, ALL re-anchored pre-existing owner-FIXMEs (the two T6 dispositioned + the manager-test's comment citing the FIXME) — zero new occurrences.
- Behavior-identity spot-audit: both split subjects diffed against their `cda2013` baselines (`RelayHandler.java` 345 lines, `BindInterceptor.java` 648 lines) — the single `channelRead` partitions verbatim into the final prelude + the two hooks; the six baseline teardown arms map one-to-one onto manager consumers running tails only; `ConnectionEntry`'s CAS mechanics are rename-only (plus the two absorbed volatiles and the derived predicate).

### Completion Notes List

**T1 — JWT-only adjudication (2026-08-27):**

- Main half landed in the prior session (uncommitted tree): `introspect`/`mapIntrospection`/`introspectRequest` deleted; structural dispatch `segments != 3` → D6 `DenyIndeterminate` + WARN (policy + operator remediation, runtime arm per the Ask-First lean); discovery `introspection_endpoint` requirement + metadata component removed; `OidcStartupDiscovery`/`ProxyCompanionProperties`/`IdpSslContextFactory`/`ConnectionRegistry` javadoc sweeps; `application.yml` T1 comment block. This session completed the test half + verification.
- **Test deltas (enumerated, AC10):** `RopcBindCredentialVerifierTest` 42 → 31 — retired the twelve 7662 rows (active:true/false, malformed bodies, non-200, timeout, F3 round-2 cancel ×2, F6 dead-JWKS, never-cached, request shape, budget clamp, path zeroization); added ONE D6 pin `opaqueTokenDeniesFailClosedWithoutAWireRound2` (verdict + WARN substrings + `introHits == 0` against a REGISTERED, ALLOWING endpoint still advertised by the stand-in discovery doc — the deny is provably the policy arm, not a wire failure). `RopcBindCredentialVerifierLiveTest` 4 → 3 — path 2 (live 7662 interop) retired with its stand-in IdP/control-client apparatus; class javadoc 3-path → 2-path with dated retirement note. Net: **−13 rows + 1 row = −12**.
- **Discovery completeness shrink pin:** the shared `OidcDiscoveryStandIn` and the `OidcStartupDiscoveryTest` doc template stopped serving `introspection_endpoint` — every happy-path discovery row now also proves omission-ACCEPTED (comment pinned at the assertion site); `metadata.introspectionEndpoint()` assertion deleted with the component. `missingEndpointFieldRefusesStartup` unchanged (still omits `token_endpoint`, still refuses).
- **Deliberately UNCHANGED (dispositions):** the `RopcSlice*` family + `KeycloakFixture.INTROSPECTION_ENDPOINT` — the slice is the historical-ratification artifact (the 2026-08-19 mTLS removal set the precedent: product-arm removals leave the slice; `RopcSliceLiveTest` path 2 keeps the 7662 ratification, its own container). `AdjudicationLifecycleTest`'s embedded discovery doc still serves `introspection_endpoint` (ignored field, zero behavioral effect) — its doc line carries `jwks_uri` too and is rewritten wholesale by T2; deferring avoids double-churn. The ad-hoc `standInIdP` fixture doc KEEPS advertising the endpoint deliberately (the D6 never-hit pin needs a reachable, allowing target).
- `jwks`/`Jwks` references intentionally survive everywhere (JwksCache, knob, `jwks_uri` requirement) — T2's removal, not T1's.

**T2 — TLS-as-sole-trust-anchor (2026-08-28; code halves dated 2026-08-27 in-tree):**

- **Main half (2026-08-27 tree):** `verifyJwt` deleted with its Nimbus JOSE/JWT imports, `CLAIM_SKEW`, `EXPECTED_TYP`, and the `jwks` field — the dispatch now returns `Allow` directly past the three-segment gate (D7: content past the segment count is never parsed, no library on the token path); `JwksCache` deleted whole (class + AD-28(2) refresh VT); `OidcStartupDiscovery` drops the `jwks_uri` requirement + metadata component (requirement = issuer + token endpoint, both still HTTPS-fail-fast); `ProxyCompanionProperties.Oidc` loses `jwksCacheTtl` + its `@NotNull`/`@DurationMin` messages (HV import pruned); `close()` loses the `jwks.close()` step — the AD-22 stop body is deny-in-flight → release client + zeroize secret; `application.yml` two-budget template + T2 policy block; `AdjudicationLifecycle`/`IdpSslContextFactory`/`Verdict` javadoc sweeps.
- **Stale-key AC (AC2):** new `ac8_staleKeyCacheTtlKeyRefuses` binds the formerly-valid `jwks-cache-ttl: 5m` and asserts refusal + the key named in the message (the `amendment_strayForwardOidcKeyRefuses` pattern) — retirement is loud, `ignoreUnknownFields=false`.
- **Test deltas (enumerated, AC10):** `JwksCacheTest` **−7 rows** (suite deleted with the class: initial-populate, retain-on-failed-refresh, whole-swap, empty-set-no-swap, periodic, close-stops-scheduler, non-positive-TTL-refusal); `RopcBindCredentialVerifierTest` 31 → 24 — retired nine verify-path rows (defense-in-depth pass; typ absent; typ unexpected; kid-miss + background refresh; wrong signer; claim failures; cold-cache; saturation-vs-refresh starvation; malformed-JWT dispatch), added **two D7 pins**: `jwtVerdictDerivesFromTheEndpointAlone` (a 3-segment token with a *hostile* signature and wrong claims must `Allow` — the verdict is provably the endpoint's, not a local check's) and `threeSegmentTokenIsNeverLocallyParsed` (3-segment garbage `Allow`s; the D6 deny fires only off the segment count); `CompanionConfigMatrixTest` — `ac8_absentOidcBudgetKeyRefuses` ValueSource 3→2 (−1 execution), `ac8_oidcJwksCacheTtlNonPositiveRefuses` retired (−2 executions), `ac8_staleKeyCacheTtlKeyRefuses` added (+1). Live suite: path 1 re-pointed to the structural gate (no `awaitCachePopulated`), stays 3 rows. Net: **340 − 7 − 7 − 2 = 324**.
- **Contract sweep (this session):** spine — six marker sites landed 2026-08-27 (AD-5 single-fork note, AD-8(c) retirement, AD-10 holdings, AD-12 rule + ratification, AD-28(2) retirement) + twelve residual sites 2026-08-28 (module map, AD-4 blocking-call list, AD-11 OIDC enumeration re-worded — note the malformed-JWT deny arm is *gone*, three-segment garbage now `Allow`s per D7, AD-22 drain steps 4–5 retired, AD-33 deny-cause list, AD-36 Nimbus-role amendment, register 3.1-verdict dated update + outage-row re-word, conventions state row, deployment graph, package map, deferred-tuning row); epics.md — seven sites (AD-12 bullet ×2 + summary row, two register rows, tuning row, the 4-path production-scope note at the Epic-3 risk gate); TEA — §4.2 + §4.3 RETIRED banners + per-row statuses (SEC-011/012/013..019/093/029/OBS-018 retired; SEC-005 expectation INVERTED with successor pins named; wording notes on SEC-001/002/028/047/094/OBS-016); `.memlog.md` — one T1+T2 `(amendment)` entry with the full Propagated list (T1 wrote none; both halves amend the same AD-12 cluster, so one record covers the pair).
- **Deliberately UNCHANGED (dispositions):** `RopcSlice*` + `KeycloakFixture` keep all four AC8 paths green as the historical ratification (the 2026-08-19 precedent); epics' AD-summary mirrors for AD-5/8/10/11/22/28 + its module-map line stay the frozen planning snapshot (the 8705 precedent — spine markers govern; flagged in the memlog for the next full re-distill); `nimbus-jose-jwt` dependency + the `com.nimbusds..` crypto-allowlist entry stand (discovery-JSON parsing only, per D7).
- **Observed, out of T2 scope (feeds T7 ledger hygiene):** TEA `SEC-030` (the mTLS/8705 live row) carries no retirement marker from the 2026-08-19 Story 3.2 removal — a pre-existing TEA gap, not touched by this story's diff discipline (no silent rides); T7 should mark it when sweeping the ledger.

**T4 — Couple-vocabulary unification + `RelayObserver` (2026-08-28; per the D1 full-unification resolution, owner checkpoint same date):**

- **Code renames (behavior-identical — wire bytes, teardown ordering, pinned triggers, fail-closed arms untouched):** state — `ConnectionEntry.spliced()` → `coupled()`, `flipSpliced()` → `couple()` (successor named with the T3 state set's own verb: the CAS transition INTO `coupled`; it stays ENTRY state with its single production caller, now `RelayHandler`'s `entry.couple()` at the N1 site), the `spliced` field/local/param likewise (RelayHandler `channelInactive`/`fireClosedExactlyOnce`); data-plane action — `RelayHandler.splice()` → `relayFramedPdu()`; seam — `SpliceObserver` → `RelayObserver`, `NoopSpliceObserver` → `NoopRelayObserver`, `CapturingSpliceObserver` → `CapturingRelayObserver`, `SpliceObserverShapeTest` → `RelayObserverShapeTest` (+ `CapturingSpliceObserverTest` → `CapturingRelayObserverTest`), the 4 method names unchanged (already couple-neutral). Prose swept across `relay/`, `observability/` (CloseReason/Direction javadocs), `security/Verdict`, the netty initializers/config/options, and 15 test files ("opaque splice" → "opaque relay", "one spliced PDU"/"spliced traffic" → "relayed PDU(s)", "the flip"/"pre-flip"/"post-flip" → "the couple"/"pre-couple"/"post-couple", "flipper" → "couple unit").
- **Deliberate re-targeting list (every renamed test identifier; missed re-targets surfaced as compile failures — none did):** `RelayHandlerTest` — helpers `coupleAndFlip()` → `couple()` and `couple()` → `awaitingBindResp()` (the wiring helper now names the T3 state it lands in — the old `couple()` did NOT fire the couple and would have collided with the new vocabulary; both call-site sets re-pointed), methods `flipsOnlyOnDecodedRokBindRespThenSplicesOpaquely` → `couplesOnlyOnDecodedRokBindRespThenRelaysOpaquely`, `nonRokBindRespDoesNotFlip…` → `…DoesNotCouple…`, `lateBindRespAfterTeardownDoesNotFlip` → `…DoesNotCouple`, `splicedPduRacingCloseIsWholeOrNothing` → `relayedPduRacingCloseIsWholeOrNothing`, `peerRstMidSpliceTearsDownBothLegsAndIsObserved` → `peerRstMidRelay…`; `ConnectionRegistryTest` — `flipSplicedIsCasOnce` → `coupleIsCasOnce` (the CAS-once pins themselves re-pointed to `couple()`/`coupled()`); `RelayObserverShapeTest` — `spliceObserverIsFourMethodInterface` → `relayObserverIsFourMethodInterface`; `ObservabilityLayerRulesTest` — the FQN-scoped rule follows `class.getName()` (rename refactors both sides, per its own javadoc). **Deliberately UN-re-targeted:** `Relay025StatelessnessScanTest` (resolves `ConnectionRegistry.java` by FILENAME — `ConnectionRegistry`/`ConnectionEntry` type names KEPT per D3) and `RelayPipelineInitializersTest`'s `.class` lookups (`RelayHandler`/`BindInterceptor` unchanged — T5 owns the split re-point).
- **Contract sweep (spine):** amendment-form dated markers on AD-25 (title "Bind→splice…" → "Bind→couple transition state machine" + rule swept; single-flipper → single couple unit), AD-2 (title "…framed-ByteBuf splice" → "…framed-PDU relay"), AD-27 (`SpliceObserver` → `RelayObserver` rename + "the splice metric" → "the relay metric" + "opaque-spliced" → "opaque-relayed"); dated wording notes on AD-1 ("steady-state byte splice" → "steady-state byte relay"), AD-8 ("flip-flag" → "couple flag"), AD-32 ("the AD-25 splice flip" → "the AD-25 couple", "no splice flip" → "no couple", "flipped to coupled" → "coupled"); plain sweeps at the Design-Paradigm preamble + data-plane bullet + mermaid edge label, AD-3 corollary, AD-5, AD-7 (×3), AD-9, AD-19 (×2), AD-22, AD-23, the register outage row ("ongoing splices" → "ongoing relayed sessions") + preview-API row, the module-map observability line, the Deferred "DLR-splice byte mechanics" → "DLR relay mechanics" row.
- **Contract sweep (epics.md):** operative rows FR-TRANSIT-1 / SEC-2 / REL-1 with dated notes (the PRD's original "spliced" reads through the mapping — the FR statement is no longer PRD-verbatim, the note says so); the Language/Runtime NFR row; the AD summary table (rows AD-1/2/9/22/25/27/32 swept, one dated note under the table intro); the FR-coverage line; the Epic-2 goal + NFR parenthetical + packages-owned line; the Epic-4 packages-owned line; the register outage row; the Deferred DLR row. **Two dispositions inside this sweep:** (1) the module-map row had ONLY the `SpliceObserver` token swept — the rest of that row stays the frozen planning snapshot per the T2 disposition (it still says "OIDC/JWKS via Nimbus"; spine governs); (2) the AD-22 summary-table row still enumerated "stop JWKS refresh → close JWKS cache" — a **T2 sweep miss** (T2 amended the AD-22 rule text but not the table row); closed HERE with attribution to the T2 spine amendment, not silently.
- **Contract sweep (TEA):** one dated vocabulary-note banner at the doc head carrying the full mapping (splice→relay, flip/spliced→couple/coupled, SpliceObserver→RelayObserver + impl renames); live row text deliberately NOT rewritten (D1: "TEA live rows get wording notes").
- **AC5 survivor inventory (categories per the D1 survivor policy):**
  1. *Incidental English "flip" — NOT pair-state vocabulary (D1 carve-out), in-code:* `RelayChannelOptions.java:27` ("writability flips back" — Netty writability, D1-named); `RelayServerLifecycleTest.java:84/85/88` (lifecycle running-flag flips); `RelayServerLifecycleTest.java:99/101` (the Story-3.3 "[B] pin flip" historical narration); `AdjudicationLifecycle.java:17` + `VerifierWiringConfig.java:50` ("pure flag flip" — D1-named); `AdjudicationLifecycleTest.java:74/85`; `RpcBindCredentialVerifierTest.java:360` ("any hit would flip this row's meaning" — verdict-row English).
  2. *Frozen epics `[AD-N]` mirror bullets* (the 8705/T2 precedent — spine governs, flagged for the next full re-distill): epics.md lines 154, 159, 167, 169, 170, 172, 174, 175, 176, 183, 219, 225, 226.
  3. *TEA live rows* — pre-T4 wording under the doc-head vocabulary banner (this round's wording-note form).
  4. *Historical records (never retro-edited):* done-story files; `docs/a-1-carrier-test-plan.md` (dated Story-2.2 T9 deliverable; still names `SpliceObserver` + "proxy splice" — flagged for the Epic-6 docs re-authoring, out of T4's D1-enumerated propagation surface); `reviews/`; the deferred-work ledger; dated reports (readiness report 2026-07-23, TEA working docs, `smpp-companions-handoff.md`); the PRD/brief/brainstorm docs; `walkthrough.md`.
  5. *Spine frontmatter* `scope: '…relay/splice core…'` (line 7) — the original charter string, mirrored verbatim in the append-only `.memlog.md` frontmatter; left unswept in both so the two do not drift (the memlog frontmatter cannot be retro-edited).
  6. *Spine register line 285* "dial-direction flip" — inside the dated 2026-08-21 [B] amendment marker; topology English, not pair-state.
  7. *This story's own quoted-history text* (Intent, Code Map line references, the D1/T3 quotes, prior tasks' Completion Notes).
- **Gates:** `clean build :buildSrc:test` GREEN; `:proxy:test` **324 / 0 failed / 0 skipped** (XML; = the post-T2 baseline — zero deltas to enumerate); survivor greps per the Debug Log; `7662|introspect|jwks|Jwks` over `proxy/src/main` still empty; FIXME/@Disabled added-lines 0; MUTATED residue 0. `.memlog.md` carries the T4 `(amendment)` entry with the full Propagated list.

**T5 — `RelayHandler` direction-split (2026-08-28; per the D3 resolution, owner checkpoint same date):**

- **Shape (exactly per D3):** `RelayHandler` (345 lines) and its FILE die; replaced by `public final class RelayIngressHandler` / `RelayEgressHandler` over `public abstract class CoupledRelayHandler extends ChannelInboundHandlerAdapter` — the base owns the shared `channelRead` prelude (made **`final`** — no subclass can bypass the AD-25 race-free re-check), the `coupled()` → `relayFramedPdu` branch, the post-couple relay + writability re-arm, `teardownPair`/`channelInactive`/`exceptionCaught`/`handlerAdded`, `fireClosedExactlyOnce`, `classify`/`stash`/`releaseFrame`, and the `CLOSE_REASON`/`CLOSE_FIRED` keys (**D5 resolved in-code: the hoist home is the base; the `AttributeKey` namespace moved from the dying class — runtime-only attrs, no wire effect, reason semantics untouched/Epic-4**). Each subclass implements the ONE hook `readPreCouple(ctx, entry, msg)` — pre-couple is the bind-handshake plane, so AC8 holds: zero virtual dispatch per relayed PDU (the per-PDU path is the base's final method + private `relayFramedPdu`). `direction` is a base FIELD via the protected ctor (never an abstract method); **`peerOf` stays in the base with its two-arg field-compare ternary — the ONE residual direction conditional (T9-sanctioned)**; `GENERIC_NACK` + the `getInt(4)` read live in the egress class only (the ingress path never reads the id). The base's `observer` field is `protected` (the one subclass trigger: `onBindAccept` at the couple); `registry`/`direction` stay private.
- **Structural-by-type (AC6):** the couple — `entry.couple()`, the N1 successor site — lives in exactly one class (`RelayEgressHandler.readPreCouple`); the INGRESS class has no bind arm AT ALL (a stray decoded `bind_resp` there is unreachable today — `BindInterceptor.channelRead0` releases client-sent bind-responses upstream — and post-split fails CLOSED: `releaseFrame` + `PRE_COUPLE_NON_BIND_PDU` stash + bare-close via the delegation; never couples, never a CCE). **Pin:** new `RelayCoupleSiteArchitectureTest` (the `ObservabilityLayerRulesTest`/`SecurityTlsSurfaceArchitectureTest` idiom) — a FORBID rule (`noClasses` in `smpp.companion.proxy..` without FQN `RelayEgressHandler` may `callMethod(ConnectionEntry, "couple")`) + a POSITIVE control (the egress handler REALLY calls it — a rename de-targets both rules, never silently greens the forbid); `DoNotIncludeTests` so `ConnectionRegistryTest`'s direct CAS pins stay the sanctioned test-source exception; `class.getName()` on both sides so renames refactor the rules with their subject.
- **Test deltas (enumerated, AC10):** `RelayHandlerTest` (12 rows) DELETED; **`RelayEgressHandlerTest`** — 6 rows moved unchanged-in-intent (couplesOnlyOnDecodedRokBindRespThenRelaysOpaquely, nonRokBindRespDoesNotCoupleAndTearsDownAfterVerbatimForward, lateBindRespAfterTeardownDoesNotCouple, egressPreCoupleNonBindPduIsNotLeaked, genericNackPreBindRespIsForwardedVerbatimThenTearsDown, headerOnlyBindRespTearsDownViaDecodeErrorNeverReachingIsOk — the couple unit + the egress-only arms); **`RelayIngressHandlerTest`** — 6 rows moved unchanged-in-intent (ingressPreCoupleNonBindPduBareClosesMidAdjudication, ingressPreCoupleViolationAfterEgressConnectBareClosesBothLegs, postCoupleHalfClosePropagatesAndDrainsWholePdus, relayedPduRacingCloseIsWholeOrNothing, peerRstMidRelayTearsDownBothLegsAndIsObserved, connectionClosedFiresExactlyOncePerChannel) + **ONE new row** `strayDecodedBindRespOnIngressLegFailsClosedNeverCouples` (the AC6 ingress half: drives the arm DIRECTLY on an interceptor-less leg — the real interceptor consumes bind-responses upstream so the arm is otherwise unreachable; asserts no couple, bare close observed `PRE_COUPLE_NON_BIND_PDU` exactly-once, frame released; the delegation's full ordering stays pinned by the RELAY-002 rows through the real pipeline; the recycled `@BeforeEach` channel's own exactly-once close is drained via `observer.clear()` between sub-scenarios). The shared pair fixture (channels/beans/`awaitingBindResp()`/`couple()`/builders/`FakeEgressConnector` — helper names UNCHANGED from their T4 forms) moved to package-private abstract **`CoupledPairHarness`** both suites extend (the `LatchedBindCredentialVerifier`/`RelayTestFixtures` precedent; `assemble`/`BodyWriter` widened to `protected` for the CODEC-021 row). Plus `RelayCoupleSiteArchitectureTest` +2 rules. Net: **324 − 12 + 13 + 2 = 327**.
- **Deliberate re-targeting list (T5's, beyond T4's):** `RelayPipelineInitializersTest` — prefix lookups `"RelayHandler"` → `"RelayIngressHandler"`/`"RelayEgressHandler"` per leg, method renames `ingressWiresFramerCodecInterceptorRelayHandler` → `…InterceptorIngressRelayHandler`, `egressWiresFramerCodecRelayHandler` → `…EgressRelayHandler`, `.class` lookups → per-leg classes, order/EXACTLY-n assertions + DisplayNames + javadoc pipelines re-pointed (T4 had deliberately deferred these to T5); `BindInterceptorTest` — 3× `new RelayHandler(registry, observer, Direction.INGRESS)` → `new RelayIngressHandler(registry, observer)` + pipeline/`RELAY-002c` prose; `BindInterceptorForwardRoleTest` — the `pipeline(...)` helper ctor + the now-unused `Direction` import pruned; `RelayTestFixtures`/`CapturingRelayObserver`/`RelayA1SmokeTest` — javadoc re-points; the two initializers — wiring + javadoc re-points + `Direction` imports dropped (direction is carried by TYPE now: `new RelayIngressHandler(registry, observer)` / `new RelayEgressHandler(registry, observer)`). **Missed re-targets surfaced as compile failures — none did.**
- **Contract sweep:** spine — AD-25 dated AMENDED marker (both rule-text naming sites re-pointed: "exactly one unit — the data-plane `RelayEgressHandler` … structural-by-TYPE" + the read-only-observer clause) whose marker text names the five sibling sites swept with it: the Design-Paradigm data-plane bullet, the mermaid leg labels + couple edge, AD-7's couple-trigger sentence (dated wording note), AD-27's "holds an injected reference" clause (re-pointed to the per-leg handlers over the base), the module-map `relay/` line (in-line dated note); epics — the module-map `relay/` parenthetical + the AD-25 summary-table row re-pointed with dated notes (the `[AD-N]` mirror bullets stay frozen per the 8705/T2/T4 precedent); TEA — one dated split note appended to the T4 banner. One `.memlog.md` `(amendment)` entry with the full Propagated list.
- **T6 boundary respected:** the ingress class's `teardownForPreCoupleViolation` delegation SURVIVES unchanged (byte-identical arm incl. the defensive no-interceptor bare close); NO state-manager work rode T5 — the six teardown sites stay six (`BindInterceptor` ×4 + base `teardownPair` + base `channelInactive`), no `EgressLeg`/pending-handle moves, no `ConnectionEntry` field changes (the 4→6 absorption is T6's, with its dated AD-8 amendment).
- **Behavior-identical:** wire bytes, teardown ordering, pinned trigger events, and fail-closed arms unchanged — the old single `channelRead` body partitions verbatim into the final prelude + the two hooks (the two old direction conditionals at the `EGRESS+nack` conjunct and the `INGRESS` delegation DIE; their behavior moved into the TYPES). The only pre-split arm with no reachable-behavior twin is the ingress stray-bind-family fail-close (documented above — it was unreachable before AND after; it fails closed now instead of propagating into the tail).

**T6 — State-manager extraction (2026-08-29; per the T3 resolution, owner checkpoint 2026-08-28):**

- **The manager (exactly per T3):** NEW `relay/RelayStateManager` — ONE bean (a `@Component` wrapping the `ConnectionRegistry` bean) holding the pair-lifecycle transition policy OVER the registry's storage for BOTH role arms: `register` (optimistic registration + the MOVED in-flight assert) / `beginAdjudication` / `settleAdjudication` (the absorbed handles' store and settle) / `attachEgress` / `beginTeardown` (the sealed decision) / `entryFor` (the hot-path flag-read). NO manager→handler references; the manager is stateless policy; the couple is absent from the surface (T3's hard constraint — `RelayCoupleSiteArchitectureTest` still green, no new caller).
- **The sealed decision + the single-sited ordering (AC7):** `beginTeardown` performs the registry's remove+mark, THEN the absorbed hygiene (`cancelHttp()` on a still-pending adjudication + zeroize of a still-pending password — "the caller being the manager", per T3), and only then returns `Won(entry)` / `Lost` (the `Verdict` nested-record idiom). All the pre-T6 teardown sites — the interceptor's `denyAndTeardown`/`teardownForPreCoupleViolation`/`channelInactive`/`exceptionCaught` (each pairing `beginTeardown` with its own `cancelAndWipePending`) and the base's `teardownPair`/`channelInactive`/`exceptionCaught` — now consume the decision and run only leg-specific tails (closes, the AD-33 deny write, the reason stash). `cancelAndWipePending` and the interceptor's two pending fields DIED. The interceptor's two explicit wipes of never-adjudicated secrets (synchronous-blow-up + null-return arms) stay interceptor-side — those secrets were never stored on the entry.
- **Absorption (a):** `pendingVerdict`/`pendingPassword` → `ConnectionEntry` (4→6 things, volatile, ingress-loop-written as today, `RelayStateManager`-routed package-private transitions `beginAdjudication`/`clearPendingVerdict`/`clearPendingPassword`); the in-flight assert moved to the REGISTRATION path — `manager.register` asserts no live pair on the channel (the surviving first-bind-arm guard; the pendings-clean half became structural: a fresh entry is clean by construction and the teardown wipe is single-sited). The `RelayIngressHandler`→`BindInterceptor` pipeline-lookup delegation DIED — the split handlers and the interceptor call the manager directly. The verdict continuation's settle (`manager.settleAdjudication`) is now its FIRST act: drops the cancellation handle + wipes the password on every completion path (the pre-T6 `finally` wipe moved from the tail to the head of the continuation — same class of timing ("when the adjudication settles"), idempotent, the never-corrupts-the-AD-14-forward property unchanged).
- **Absorption (b):** `EgressLeg.answered` → the DERIVED `ConnectionEntry.answered()` predicate (coupled ∨ tearing-down) — every bind answer already transitioned the entry in `RelayEgressHandler` upstream of the forwarder (the ROK couple on one arm, the non-ROK/nack teardown on the other), so the shadow bit AND its write site died; `EgressLeg` takes the entry ref at its construction site; its unanswered-death collapse reads entry state. **Semantic note:** the derived predicate classifies a torn-down-UNANSWERED pair as answered — wire-equivalent (the pre-T6 path called `denyAndTeardown`, lost the CAS, and no-oped; T6 skips the call), pinned by the unchanged RELAY-002/003/CODEC-021 rows.
- **Absorption (c):** `CLOSE_REASON`/`CLOSE_FIRED` — ALREADY landed in T5 (D5 resolved in-code to the `CoupledRelayHandler` base); nothing to do here, recorded for the three-checklist completeness.
- **Behavior deltas on UNREACHABLE paths only (dispositions):** (1) an interceptor-less ingress leg (a wiring bug) now runs the FULL manager ordering (remove+mark → cancel+wipe → close) instead of the pre-T6 defensive bare close WITHOUT teardown — strictly more fail-closed, same class as T5's stray-bind_resp note; (2) the `EgressLeg` unanswered-collapse on an already-torn-down pair skips the redundant Lost no-op call (above). Every reachable arm is byte-identical.
- **Deliberately UNCHANGED:** `ConnectionRegistry`'s public surface + `ConnectionRegistryTest`'s direct registry/CAS pins (the sanctioned test-source exception; the registry keeps its map, its `ChannelId` key, its CAS-once mechanics, and the RELAY-025 scan target — AD-8's named storage home); `registry.size()` stays the AD-22 drain/observation seam; the two relay-handler-suite raw `registry.beginTeardown(...)` race-window fabrications stay raw-registry (deliberate window fabrication, the pendings always already settled there); epics/TEA NOT swept — T6's mandated propagation surface is the spine AD-8 amendment + the memlog entry (the T3 decision's own list); the `[AD-N]` mirror bullets stay frozen per precedent.
- **Test deltas (enumerated, AC10):** NEW `RelayStateManagerTest` — 4 rows (`beginTeardownPerformsTheHygieneBeforeWonThenLoses`: Won carries THE entry with cancelHttp + zeroize already done, registry empty, attrs cleared, second call Lost with no re-cancel; `beginTeardownWithoutAPairIsLost`; `settleAdjudicationWipesWithoutCancelling` — settle never cancels, and a later teardown Won does not cancel either; `registerRefusesALivePairOnTheChannel` — the moved assert bites under `-ea`). `ConnectionRegistryTest` 11 → 12 (+`answeredDerivesFromCoupledOrTearingDown` — all three arms: awaiting/coupled/torn-down). Harness/fixture re-points only (zero assertion changes): `CoupledPairHarness`, `BindInterceptorTest` (3 ctor sites + the allow-path zeroize prose), `BindInterceptorForwardRoleTest` (2 ctor sites + the pipeline helper), `RelayIngressHandlerTest` (the interceptor-less stray-bind_resp leg registers through the manager + comment), `netty/RelayPipelineInitializersTest` (egress helper + the TLS 3-arg ctor), `testsupport/RelayTestFixtures` (the harness records carry the manager; `registry()` stays for size observations). Net: **327 + 5 = 332**.
- **Contract sweep:** spine — AD-8 dated AMENDED marker (the 4→6 state set, the manager-over-storage shape with its surface, the sealed decision + the single-sited ordering, the moved assert, the not-manager-routable couple, the answered derivation) + a dated note on the module-map `relay/` line (`RelayStateManager over ConnectionRegistry`); `.memlog.md` — one T6 `(amendment)` entry (append-only) with the full Propagated list + gates.
- **Gates:** `clean build :buildSrc:test` GREEN (re-proven on the final tree); `:proxy:test` **332 / 0 failed / 0 skipped** (XML, 51 suites); the couple-site grep + ArchUnit rules green; removal-completeness grep still empty; FIXME/@Disabled added-lines = the two pre-existing owner-FIXMEs re-anchored in rewritten comments (the `EgressLeg` Lombok-ctor comment re-counted three→final-field for the 4-field ctor — the FIXME itself stands); MUTATED residue 0; the one pre-existing `peerOf` ErrorProne warning unchanged, zero new warnings.

**T8 — Token-path alignment of the test tier (2026-08-29; owner-directed same date — extends T1/T2 + the 2026-08-19 mTLS removal past `src/main`, ending the slice's historical-ratification exemption):**

- **The slice (`RopcSlice`):** the three interop arms deleted — (1) the JWKS arm: `verifyWithJwks` / `fetchJwks` / `jwksCache` / `asyncRefreshJwks` + the `adjudicate()` forked JWKS subtask (the STS fan-out is ONE forked arm, the token exchange — the production adapter's post-T2 shape; JEP-505 single-join safe); (2) the 7662 arm: `adjudicateByIntrospection` / `introspectRequest`, `SliceConfig.introspectionEndpoint` + `introspect` + the 7662 ctor guard; (3) the 8705 branch: the `useMtlsClientAuth` no-secret path in `tokenRequest` (the secret is now ALWAYS sent), `SliceConfig.useMtlsClientAuth`. `SliceConfig` reduces to `tokenEndpoint, clientId, clientSecret` — `client_secret` MANDATORY (compact-ctor `requireNonNull`, the 2026-08-19-recorded shorthand non-200 mapping (4xx → `DenyInvalid`) standing unchanged); `cancelHttp()` binds the token exchange alone; the verdict = token-endpoint response + the STRUCTURAL three-segment gate (library-free, mirroring the production `adjudicateIssuedToken`: JWT → `Allow`, opaque → `DenyIndeterminate`); Nimbus in the slice survives only as `JSONObjectUtils` (the token-response JSON parse). Zeroize hygiene unchanged (password on every completion path; the token working copy after the gate).
- **Fixture surface used only by the retired arms:** `KeycloakFixture.INTROSPECTION_ENDPOINT` / `JWKS_URI` / `ISSUER` / `CLIENT_B_ID` deleted; the realm JSON's client-x509 Client B block deleted (Client A's name swept); `KeycloakContainer`'s two path-3 comments re-worded; `certs/generate.sh`'s Client-B/tls_client_auth comments re-worded to the transport-identity role; `keycloak/README.md` swept (amended-contract banner, 3-path live table, Client-B row removed, client-cert rows re-worded to transport identity; the dated Findings kept as history with a preamble note). **Transport TLS STAYS (the trust anchor):** the container keeps `KC_HTTPS_CLIENT_AUTH=required` and the slice keeps presenting the client cert on every provider call — only the OAuth-level cert-auth use died.
- **Test deltas (enumerated, AC10/AC13):** `RopcSliceUnitTest` 10 → 1 — retired the kid-miss row + the 8 Task-4 claim-deny rows with the whole Nimbus helper apparatus (the `verifyWithJwks` seam they drove no longer exists; compile-level absence is the guarantee — there is nothing behavioral left to bite); the unreachable-endpoint row re-pointed to the 3-arg config. `RopcSliceFailClosedTest` 7 → 8 — retired the two 7662 rows (`introspectionInactive`/`introspectionNon200`) + the JWKS serving (`/certs` context, `JWKS_BODY`, `introspectHandler` plumbing); ADDED three fresh pins through the real port against the in-process IdP: `opaqueToken_yieldsDenyIndeterminate` (D6), `threeSegmentGarbageToken_yieldsAllow` (D7 — a 3-segment garbage token `Allow`s; any reintroduced local check goes RED), `nullClientSecret_isRejectedAtConstruction` (the amended config guard). `RopcSliceCancelTest` re-pointed only (3-arg config; STS-teardown prose note — zero assertion changes). `RopcSliceLiveTest` 5 → 3 — paths 2 (7662) and 3 (mTLS) retired; path 1 re-pointed to the structural gate ("Allow on the endpoint verdict alone"); the `slice()` helper + class javadoc re-pointed. `NoRolledCryptoArchitectureTest` — the two JOSE rows + class javadoc re-worded (Nimbus = the provider-JSON parser in BOTH tiers; the JWT/JWKS verification role is gone everywhere; the rules themselves unchanged and still non-vacuous — both classes still depend on `com.nimbusds..` via `JSONObjectUtils`). Net: **332 − 9 − 2 + 1 = 322**.
- **Spot mutation pass (fresh pins bite, pre-T9):** gate → always-`Allow` ⇒ D6 row RED; gate → always-`DenyIndeterminate` ⇒ D7 row RED; the `clientSecret` guard neutered ⇒ the mandatory-secret row RED; restored byte-identical (zero `MUTATED` residue); the full fresh-pin suite GREEN post-restore. The retired guards' ledger rows (the slice's verify/introspection/mTLS biters) are T9's to strike with evidence.
- **Living cross-references swept (the 2026-08-29 change-log rule):** `RopcBindCredentialVerifierLiveTest` javadoc — the "path 3 keeps the historical ratification" + "suites stay green unchanged" clauses re-pointed; `TrustOnlyKeycloakContainer` javadoc — the "required because its client-x509 path" rationale re-worded to transport mTLS.
- **AC13 gate survivor inventory (repo-wide grep over `proxy/src`, 37 hits — dated historical prose or load-bearing pins ONLY):** (1) *T8's own dated notes* (the retirement record): `RopcSlice.java:35,71,120,163,213,293`; `RopcSliceUnitTest.java:22`; `RopcSliceFailClosedTest.java:37,38,147,148`; `KeycloakFixture.java:20`; `RopcSliceLiveTest.java:20`; `README.md:10,56`. (2) *T1/T2 dated retirement prose (pre-existing):* `AdjudicationLifecycleTest.java:153`; `OidcDiscoveryStandIn.java:111,112`; `OidcStartupDiscoveryTest.java:77,149,245`; `RopcBindCredentialVerifierLiveTest.java:30,31`. (3) *Load-bearing T1/T2 pins (pre-existing):* `CompanionConfigMatrixTest.java:421,428,433` (the stale-key refusal row — `jwks-cache-ttl` is its subject); `RopcBindCredentialVerifierTest.java:10,88,92,130,328,369,868,869,897,898,985` (the D6 never-hit pin's registered ALLOWING introspection handler + path strings, the discovery-doc omission-accepted template, and the D7 hostile-`SignedJWT` builder — the retired production arm must stay provably retired). Zero survivors name a living arm.
- **Deliberately UNCHANGED (dispositions):** the production-tier pins in (3) above (the slice gets no never-hit twin — its arm is gone at the compile level, so a behavioral twin could only ever test a phantom); the slice's shorthand non-200 mapping (the 2026-08-19 memlog record stands — T8's mandate is the three arms, not the slice's `mapNon200`); epics/TEA NOT swept — T8's mandated propagation surface is the spine AD-12 marker extension + the memlog entry (the T6 precedent; the epics `[AD-N]` mirror bullets stay the frozen planning snapshot, flagged for re-distill).
- **Contract sweep:** spine — the AD-12 ratification bullet's dated marker chain EXTENDED (2026-08-29, Story 3.4 T8: the 2026-08-27 "the test-tier slice keeps all four paths green unchanged" clause SUPERSEDED — the exemption ended, the arms removed from `src/test`, `SliceConfig` reduced with a mandatory secret, the slice the live ratification of the amended contract) + the register's ROPC row gains the matching dated update (same supersession). `.memlog.md` — one T8 `(amendment)` entry (append-only) recording the disposition reversal (supersedes the 2026-08-27 entries; the 2026-08-19 precedent no longer shields slice arms), the full removal/pin/fixture record, and the gates.
- **Gates:** `clean build :buildSrc:test` GREEN (2m 11s; re-proven post-restore); `:proxy:test` **322 / 0 failed / 0 skipped** (XML, 51 suites); the repo-wide AC13 grep → 37 survivors, all inventoried above; `src/main` half still empty; FIXME/@Disabled added-lines 0; MUTATED residue 0.

**T9 — Startup OIDC discovery removal (2026-08-29; owner-directed "simplify app"):**

- **Main half:** `OidcStartupDiscovery` + its nested `OidcProviderMetadata` DELETED whole (probe, discovery-URI builder, parse, refusal family, issuer equality, `DAG_WARNING`, accessors). The token endpoint is DERIVED on `RopcBindCredentialVerifier`: `TOKEN_ENDPOINT_PATH = "protocol/openid-connect/token"` + `static deriveTokenEndpoint(URI)`; the ctor pair drops the `discovery` param (public 1-arg over the factory; package-private recording 2-arg — `IdpSslContextFactory`, secret load, budgets, guards all unchanged); `VerifierWiringConfig` drops the probe injection. Config surface UNCHANGED (no key added/renamed; the `provider-url` https+host fail-fast stays the validator's — the spine marker says so explicitly). **The one behavior addition:** the starred `OPERATOR_WARNING` banner (the retired `DAG_WARNING`'s form) on the token-call connection-error arm (`adjudicate`'s catch, gated on an `IOException` in the cause chain — a CANCELLATION stays silent) and both non-mapped-non-200 arms (the 400-other-error path and the 3xx/4xx/5xx fall-through, now one `unmappedStatus` helper); it names the failure, the derived endpoint, `provider-url`, and the DAG/realm-base remediation — log-only, the AC2 verdicts byte-identical (proven: the WARN-neuter mutation reddened ONLY the WARN pin).
- **Derivation semantics — a deliberate deviation from the spec's mechanism shorthand, flagged for the owner:** the task text says "`URI.resolve` over the … slash-stripped base", but plain `resolve` with a relative reference REPLACES the base's last path segment per RFC 3986 §5.3 (`https://h/realms/x`.resolve("protocol/…") = `https://h/realms/protocol/…` — the realm name is LOST). The spec's own hard constraint (byte-identity with `KeycloakFixture.TOKEN_ENDPOINT`) overrides the mechanism: the implemented join APPENDS after the last segment (explicit separator over the slash-stripped base, never doubled; slash-tolerant either way). The fresh derivation pin caught the resolve shape RED on its very first run (14 rows) — exactly its job.
- **Never-hit pin blind spot, found and fixed during the mutation pass:** the pin's first form used the stand-in base + realm segment as provider-url, so a re-introduced startup GET landed on an UNREGISTERED realm-relative path and the pin stayed green. Fixed: the pin's provider-url is the stand-in BASE (no realm segment) — the retired probe's exact URL shape (`<issuer>/.well-known/openid-configuration`) now lands on the counted context; the mutation (a ctor GET of exactly that shape) turns the pin RED. The derived-endpoint wire proof rides the same row: the derived token path is unregistered on the stand-in, its 404 → the fail-closed verdict, with the counter proving the one wire call never went to the metadata path.
- **Test deltas (enumerated, AC10/AC14):** `OidcStartupDiscoveryTest` **DELETED (−10 rows, −1 suite)**; `RpcBindCredentialVerifierTest` 24 → 27 — **+3 fresh pins**: `tokenEndpointDerivesFromTheProviderUrlRealmBase` (derivation unit pin incl. trailing-slash-through-the-`Oidc`-ctor + raw-static slash tolerance + byte-identity with `KeycloakFixture.TOKEN_ENDPOINT`), `noStartupProviderCallEverFetchesTheMetadataDocument` (never-hit startup-probe pin over `OidcDiscoveryStandIn.discoveryHits()`, real TLS link, construction + full adjudication), `providerFailureArmsLogTheStarredOperatorWarning` (the new WARN pin over BOTH arms — connection error + 400 unauthorized_client, the DAG-off flagship; the retired DAG row's log-capture idiom re-homed). Fixture re-points, zero other assertion changes: provider-urls gain the realm segment (`realmBase(server)` helpers), `standInIdP` drops the discovery context + `tokenEndpointBase` param (its `introspection_endpoint`/`jwks_uri` advertisement lines die with it; the JWKS/introspection handler REGISTRATIONS stay for the never-hit pins), `unreachableTokenEndpoint…` + `constructorFailsFastOnBadConfig` now need NO server at all (construction makes no wire call), `AdjudicationLifecycleTest` ctor + realm base + its parked fixture's discovery context dies, `VerifierWiringConfigTest` runner drops the probe bean, live suite ctor re-pointed (its provider-url was already the realm base). Net: **322 − 10 + 3 = 315**.
- **AC13 gate inventory re-enumerated in the same pass (the doc's advertisement lines died):** DEAD vs the T8 list — `AdjudicationLifecycleTest:153` (its discovery doc's retired-field comment, gone with the context), `OidcStartupDiscoveryTest:77,149,245` (file deleted), `RpcBindCredentialVerifierTest`'s doc-builder advertisement lines (the stand-in doc no longer carries `introspection_endpoint`/`jwks_uri`). SURVIVORS (all dated prose or load-bearing pins): `CompanionConfigMatrixTest:421,428,433`; `KeycloakFixture:20`; `RpcBindCredentialVerifierLiveTest:30,31`; `RpcBindCredentialVerifierTest:10,88,92,142,340,381,992,1090` (the `SignedJWT` import backs the D7 hostile-token builder; `INTROSPECTION_PATH` + the registered handlers back the never-hit pins); `RopcSlice*` + `keycloak/README.md` unchanged (T8's own dated notes); `OidcDiscoveryStandIn:128,129` (shifted from 111,112 — the dated T1/T2 retirement prose). Zero survivors name a living arm.
- **T9 gate survivor inventory (repo-wide, 5 hits — the sanctioned categories):** never-hit pin handler — `OidcDiscoveryStandIn.java:55` (the pin's path constant), `:139` (the served doc body — never fetched), `RpcBindCredentialVerifierTest.java:480` (the pin's own quoted-URL rationale); container readiness polling (fixture-internal) — `KeycloakContainer.java:119`, `TrustOnlyKeycloakContainer.java:178`. The `src/main` half is EMPTY.
- **DAG documentation (the retired posture's successor):** `application.yml` gains the dated policy block after the T2 block (no provider wire call at startup; the derivation contract; provider-url MUST be the realm base; DAG per-client OFF since 26.2 — enable or every bind denies fail-closed at first bind with the starred WARN); `ProxyCompanionProperties.Oidc`'s `providerUrl` javadoc carries the mirror (realm-base MUST + DAG contract + the trailing-slash/token-path-join wording), and the compact-ctor javadoc's discovery-path join wording re-points to the T9 token-path join.
- **Deliberately UNCHANGED (dispositions):** `OidcDiscoveryStandIn` re-purposed NOT deleted — `url()` stays the reverse-cell default provider-url (now purely a valid https+hostful value; no boot makes a wire call), `fixtureServerSslContext()` stays load-bearing for every ad-hoc fixture server, the metadata context + `discoveryHits()` are the never-hit pin; its name keeps "Discovery" (a rename is churn T9 does not sanction — the dated javadoc note explains). `nimbus-jose-jwt` + the `com.nimbusds..` allowlist stand (the token-response JSON parse — AD-36's re-worded role). The WARN fires on EVERY non-mapped-non-200 (incl. 429/5xx) — sanctioned by the task text ("non-mapped-non-200 arms"); a rate-limit storm now logs one starred banner per denied bind, the price of the DAG-off case being loud.
- **Contract sweep:** spine — AD-12 probe-clause SUPERSEDED marker (the https fail-fast clause re-words to bind-time and explicitly SURVIVES as the validator's; the DAG-probe clause recorded as dead with the probe; the derivation + first-bind WARN + documentation posture recorded) + AD-36 dated wording note ("discovery-JSON parser" → the token-endpoint RESPONSE parser; no discovery document exists in either tier) + the module-map `security/` line's dated re-word; epics — the operative SEC-3 row's dated note (the `[AD-12]` mirror bullets stay frozen per precedent, flagged in the memlog for re-distill); `deferred-work.md` — "Evaluate an OIDC-discovery/parsing library" marked `**✅ RESOLVED … SUPERSEDED BY REMOVAL**` (the verbatim 2026-08-19 owner note stays as history); `.memlog.md` — one T9 `(amendment)` entry (append-only) with the full record + Propagated list + gates.
- **Gates:** `clean build :buildSrc:test` GREEN on the FINAL tree; `:proxy:test` **315 / 0 failed / 0 skipped** (XML, 50 suites); the src/main gate grep EMPTY; repo-wide survivors = the 5 inventoried fixture-internal lines; FIXME/@Disabled added-lines 0; MUTATED residue 0.

**T7 — Ledger hygiene (2026-08-29):**

- **Markers landed in `deferred-work.md` (the task's marker form; the fielded-entry precedent set by appending an indented `**✅ RESOLVED …**` continuation after `evidence:` — the fields untouched):** (1) the wildcard-listener/F13 entry — RESOLVED 2026-08-26 (Story 3.3 T6, commit `35fd64d`): `companion.bind.host` + `ConnectionCapHandler` at the acceptor, the cap reading `companion.memory.concurrent-pairs` directly (the AD-30 budget input — exactly the coupled-pair-residency accounting the 2026-08-17 refresh demanded; the `bind.max-connections` knob + cap≤budget guard retired per the memlog 2026-08-26), pinned by the loopback F13 row + mutation M6; the entry's idle-timeout clause explicitly NOT claimed resolved — no idle handler exists in `relay/`, and that clause re-homes to Epic-4 RELAY-020/021 with F10/F14. (2) The four 2026-08-27 story-text-pass entries — each RESOLVED by `cda2013` (2026-08-27): the story-contract re-key, the M14/M15 fresh mutation pass (M4/M10 struck, M3 superseded), the 352/0/0 Dev-Record re-baseline, the epic-3-context/sprint-status/ledger-line-511 sweep. The section's fifth entry (corroboration, "no new action") deliberately unmarked; the 2026-08-25 close-out entries it corroborates stay OPEN (real test gaps, not story-text passes). (3) The F7 entry — DISPOSITION per D4: **RE-HOMED to the Epic-4 RELAY-020/021 timeout round, not absorbed**: the production adapter already guarantees settlement on `cancelHttp()` (Story 3.2 AC5 — "resolves the relay's cancelled-verdict frame leak at the source"), T6's `RelayStateManager.beginTeardown` hygiene routes every teardown through exactly that call (so the continuation always runs and releases the frame on its losing-race no-op arm), the port-amendment arm was out of bounds (this story freezes the AD-12 port shape), and the relay-side absorb (release at teardown + a racer flag) was consciously not taken mid-refactor; the residue interlocks with F14 as F14's own entry anticipates.
- **TEA SEC-030 (the T2 Completion-Notes directive "T7 should mark it when sweeping the ledger"):** the mTLS/8705 live row (§4.4) carries the dated Status-RETIRED note in the established T1/T2 form — production arm removed 2026-08-19 (Story 3.2 amendment 5, user-directed), test-tier arm removed 2026-08-29 (T8); `client_secret` mandatory in both tiers (`nullClientSecret_isRejectedAtConstruction`); the client cert's surviving role is TRANSPORT mTLS (the T2 trust anchor); SEC-095's "(SEC-030 covers the mTLS path)" parenthetical flagged as history inside the note.
- **Fact audit BEFORE writing (the false-RESOLVED memory discipline — no marker claims unverified work):** `ConnectionCapHandler` wiring at `RelayServerLifecycle.java:88` + `bind.host` + its yml default/comment; `cda2013`'s commit message + its actual `deferred-work.md` diff (the line-511 re-key) + the M14/M15 rows with RED evidence in the 3.3 story; `RopcBindCredentialVerifier`'s settlement guarantee (class-javadoc AC5 + `RopcVerdictRequest.cancelHttp()` completing the pin `DenyIndeterminate`) + `RelayStateManager.beginTeardown`'s hygiene + `onVerdict`'s losing-race frame release — the full F7 chain; the spine's AD-12 wording for the 8705 removal (2026-08-19, amendment 5); zero idle-timeout handling anywhere in `proxy/src/main` (the NOT-claimed clause).
- **Scope notes:** the 2026-08-16 owner-note cluster + the 2026-08-21 scoping entry (the refactoring round's own subjects) carry NO markers — T7's enumeration names exactly F13 + the four + F7, and those entries' dispositions live in the T4/T5/T6 spine markers + memlog entries (wrap-time disposition is T11/story-wrap territory, not T7's). No `.memlog.md` entry: T7 amends no contract (the ledger + the TEA row ARE the records). Zero code files touched — the T9 build gates stand unchanged for the code; T7's verification is the fact audit + the diff review above.

**T10 — the full POST-rename RED-on-neuter re-run (2026-08-30):**

- **17 mutations, all RED on their named biters, all restores `cmp`-verified byte-identical** (the ledger above carries the per-row detail): 9 relay rows re-proving the 2.2 N-series at the sites T4/T5/T6 moved them to (the couple in `RelayEgressHandler`, the re-check/exactly-once-CAS/peer-write in `CoupledRelayHandler`, the folded ordering + settle wipe + in-flight assert in `RelayStateManager`, the renamed couple CAS in `ConnectionEntry`); 5 production-security rows (the structural gate both directions at its post-T1/T2 dispatch, the T9 derivation/never-hit/WARN trio); 3 slice rows re-proving T8's fresh pins on the final tree. AC9 closed: every renamed/moved guard's biter is RED at its NEW site.
- **Retired guards struck with evidence** (ledger section C): the 7662 arm, the verify path + cache TTL, the slice's three arms, and the discovery refusal family / issuer-equality / DAG probe — each row cites its retiring task, the removal grep re-proven EMPTY on the T10 tree, and its successor pin's fresh RED row.
- **Notable RED shapes (the mutation pass's own findings):** (1) T10-R1 reddens the `RelayCoupleSiteArchitectureTest` POSITIVE CONTROL alongside the behavior rows — a rename that de-targets the ArchUnit rule cannot silently green it; (2) T10-R5's immediate-`Lost` cascade (9 RED) is the sealed decision's no-op contract biting across every Won-caller tail, while `beginTeardownWithoutAPairIsLost` correctly stays green; (3) T10-S5 reddens EXACTLY the WARN pin — the log-only posture is proven, not asserted; (4) T10-S3 reproduces T9's 14-RED derivation failure count on the final tree; (5) T10-S4's ctor GET reddens the never-hit pin plus the two rows that construct adapters against dead targets — expected collateral of a construction-time wire call.
- **Deliberately NOT re-run** (dispositions): the unchanged-site guards of ledger section D (their 2.2/3.3/3.2 rows stand — T10's population is renamed/moved/removed guards per the frozen I/O row); the Docker-gated live suites were never mutation vehicles (they ran GREEN inside the final `clean build`); zero tests added, retired, or `@Disabled` — the count stays 315.
- **Gates:** `clean build :buildSrc:test` GREEN (2m 11s) on the restored tree; `:proxy:test` **315 / 0 / 0 / 0** (XML, 50 suites); `MUTATED` residue 0; `git status proxy/src` empty (the round's only lasting delta is this story file); the `src/main` removal greps + the T9 gate grep re-proven empty; FIXME/@Disabled delta 0 (no code diff at all).

**T11 — the closing additional analysis iteration (2026-08-31; the refactor's own acceptance test — the 2026-08-16 cluster's questions re-asked with evidence from the POST-refactor shape, plus the adversarial finder pass over the full story diff `cda2013..e4ad0f3`, 80 files):**

- **Q1 direction-entanglement — GONE.** Exactly ONE `direction` conditional survives in `src/main`: `CoupledRelayHandler.peerOf` (`:294`, the base field-compare ternary — the D3-pinned, T9-sanctioned residual; every other `Direction` use is a ctor argument, an observer payload, or javadoc). The two pre-split branch sites (`RelayHandler:155` EGRESS+nack conjunct, `:167` INGRESS delegation) died with the class — their behavior moved into the TYPES: the nack arm lives unconditionally in `RelayEgressHandler.readPreCouple` because the leg IS egress; the violation arm IS `RelayIngressHandler.readPreCouple`. The interceptor's `forwardRole`/`smsc` branch is the ROLE fork (control plane), kept one-class by the T3 resolution (D2 = ONE fork; the memlog 2026-08-28 record names the rejected per-role divergence scenario) — role branching, not data-plane direction entanglement.
- **Q2 teardown ordering — GENUINELY single-sited.** The AD-32 sequence (remove + mark BEFORE close → `cancelHttp` + zeroize) exists in exactly ONE method: `RelayStateManager.beginTeardown` (`:135-142`) over `ConnectionRegistry.beginTeardown`'s storage half (`:104-116` — unchanged home BY DESIGN: AD-8's named storage, RELAY-025's filename target). The seven production consumers (`RelayIngressHandler:53`; `BindInterceptor:387/:514/:537`; `CoupledRelayHandler:208/:229/:254`) consume the sealed `Won(entry) | Lost` and run only leg-specific tails; raw registry transitions appear in `src/main` only inside the manager. One behavioral nuance verified against the `cda2013` baseline: pre-T6 `denyAndTeardown` ran its wipe even on a LOST race (the handles were interceptor fields, so only IT could reach them); post-T6 the hygiene runs only in the CAS winner's call — sound because the handles are ENTRY state and every path to a teardown goes through the one method, so the winner always owns the hygiene (T10-R5's immediate-`Lost` cascade is the RED proof of exactly this dependence).
- **Q3 vocabulary — zero unaccounted survivors.** The T4 AC5 grep re-run on the final tree returns exactly the inventoried incidental-English lines (see the Debug Log) — no pair-state `splice`/`flip` survives anywhere in relay code or prose, and no post-T4 edit re-introduced one.
- **Q4 scatter map — FRESH map drawn, nothing merely moved.** Old→new, item by item: (1) the interceptor's pending handles + their five-site settle/wipe protocol (`BindInterceptor:149,151`; `:332-333`; `:348,371-372`; `:408-419`) → ABSORBED onto `ConnectionEntry` (`pendingVerdict`/`pendingPassword`, package-private manager-routed transitions) with the protocol COLLAPSED to two manager methods (`beginAdjudication`/`settleAdjudication`) + the one hygiene block — the state's home changed from one handler's private fields (invisible to the other teardown sites, which is WHY the ordering had to be re-implemented six times) to the shared single source of truth. (2) `EgressLeg.answered` (`:607` shadow bit + its write site) → DIED, replaced by the derived `ConnectionEntry.answered()` (coupled ∨ tearing-down; both flags are monotone false→true, so the non-atomic two-flag read cannot observe a regression). (3) `CLOSE_REASON`/`CLOSE_FIRED` (`RelayHandler:83,87` — one class's private namespace) → the `CoupledRelayHandler` base (`:76-81`), ONE home both legs share (the D5 hoist; the split made sharing structural, not duplicated). (4) The registry's removal half — unchanged home, now wrapped by the manager's policy (the T3-sanctioned shape; the registry stays directly testable). (5) The six ordering re-implementation sites (`BindInterceptor:384,431,539,560` + `RelayHandler:240,263`) → ONE ordering site; `teardownForPreCoupleViolation` and the `RelayIngressHandler`→interceptor pipeline-lookup delegation died outright. The consumer count is seven, but consumers pair the decision with TAILS only — everything that made the old map scatter (ordering knowledge, hygiene knowledge, shadow state, handler-private keys) is centralized, derived, or dead.
- **Q5 new coupling — NONE.** Import-level: `RelayStateManager`/`ConnectionRegistry`/`ConnectionEntry` reference nothing in the handler family — the arrow is strictly handlers/interceptor → manager → registry, and the sealed-decision mechanics are what avoided the circular manager↔handler reference (the T3 record's own rejection of the executor-facing port). Shared-component use is exactly the sanctioned surface: `manager`/`observer` are `protected` for the two NAMED subclass needs (the ingress violation teardown; `onBindAccept` at the couple), the `final channelRead` prelude no subclass can bypass (the AD-25 re-check), `stash`/`releaseFrame` static utilities; `RelayEgressHandler` makes no manager call of its own beyond the inherited base uses. `BindInterceptor`→`RelayEgressInitializer` construction (forward arm) is the pre-existing 3.3 shape, untouched by this story's splits.
- **Adversarial finder pass (blind-hunter / edge-case / verification-gap):** all 26 changed `src/main` files read in full or diff-verified (the nine relay files, `RopcBindCredentialVerifier`, `RopcSlice`, the initializers/wiring/config/yml; the remainder are doc/vocabulary-only — the filtered diff shows zero logic lines), both split subjects diffed against their `cda2013` baselines, and the new/rewritten suites reviewed (`RelayStateManagerTest`, `RelayCoupleSiteArchitectureTest` + its scope, `CoupledPairHarness`, the ingress stray-`bind_resp` pin, the slice's fresh D6/D7/secret pins — the latter T10-mutation-proven). Candidate findings, each traced to an existing disposition: (a) `deriveTokenEndpoint` on a host-root or query-bearing `provider-url` derives a wrong endpoint — lands on the non-mapped-non-200 arm: fail-closed deny + the starred WARN naming both URLs; the documented T9 posture (the yml policy block + the `Oidc` javadoc carry the realm-base MUST); (b) the WARN fires per denied bind under a 429/5xx storm — T9's own task-text-sanctioned price, dispositioned in its notes; (c) the slice's `execute()`-after-close permit leak and undrained `close()` — pre-existing ledger items (deferred-work §2.1 items 2/5), untouched by T8's mandate; (d) the ArchUnit couple pin scans `smpp.companion.proxy` only — complete for every REACHABLE caller (codec cannot compile against proxy — the codec-purity configuration gate), and it is the house idiom's scope (`ObservabilityLayerRulesTest`); (e) the settle-before-recheck timing in `onVerdict` and the `answered()` torn-down-unanswered classification — T6's own documented same-class notes, T10-R8-pinned / RELAY-002/003/CODEC-021-pinned. **Zero unfixed findings; zero structural findings — no owner checkpoint owed.** The T11 stop criterion (zero unfixed findings, everything else dispositioned or escalated) is met.
- **Gates:** no code fix was applied (the round's only tree delta is this story file), so the T10 mutation population and its 17-row ledger stand unchanged and no gate re-run was TRIGGERED — the final gates nonetheless re-ran green on the post-iteration tree as AC12's own evidence (see the Debug Log).

### RED-on-neuter mutation ledger (T10 / AI-1)

Population rule (the frozen I/O row): every guard whose neuter site this story RENAMED or MOVED gets a fresh row re-proven at the NEW site; every guard this story RETIRED gets a struck row with evidence; guards whose sites T1–T9 left untouched keep their 2.2/3.3/3.2 ledger rows (inventory in section D). Discipline, every mutation: unique `/tmp/t10-backups` master → neuter → `:proxy:cleanTest :proxy:test --tests '<pinning suites>'` → RED set extracted from the JUnit XML → restore → `cmp`-verified byte-identical. Zero `MUTATED` residue and an empty `git status proxy/src` after the last restore; the final gates ran on the restored tree.

**A. Relay half — the 2.2 N-series re-proven at their post-T4/T5/T6 sites (9 mutations):**

| # | Ancestor | Guard at its NEW site | Neuter | RED biter(s) |
|---|---|---|---|---|
| T10-R1 | 2.2 T8-N1 | the AD-25 couple — `RelayEgressHandler.readPreCouple`'s `if (entry.couple())` (the `flipSpliced()` call site's successor; the only production caller) | `if (false)` | **6 RED**: `RelayEgressHandlerTest` RELAY-001a/e (`couplesOnlyOnDecodedRokBindRespThenRelaysOpaquely`) + `RelayIngressHandlerTest` RELAY-008/009/010 + the AC5 exactly-once row + `RelayCoupleSiteArchitectureTest.egressLegHandlerPerformsTheCouple` — the ArchUnit POSITIVE CONTROL bites too: the couple's single-siting is doubly guarded (forbid rule + positive control + behavior) |
| T10-R2 | 2.2 T8-N3 | the AD-25 race-free re-check conjunct — the `CoupledRelayHandler.channelRead` final prelude (`entry == null \|\| entry.tearingDown()`) | drop the `tearingDown()` conjunct | **exactly 1**: `RelayEgressHandlerTest.lateBindRespAfterTeardownDoesNotCouple` — the fabricated race window (attrs still cached, entry marked) couples when the conjunct is gone |
| T10-R3 | 2.2 T8-N4 | the exactly-once close CAS — `CoupledRelayHandler.fireClosedExactlyOnce` | CAS gate dropped (always fire) | **exactly 1**: `RelayIngressHandlerTest.connectionClosedFiresExactlyOncePerChannel` |
| T10-R4 | 2.2 T8-N5 | the non-ROK forward-then-teardown — `RelayEgressHandler`'s non-ROK arm | teardown dropped (forward only) | **exactly 2**: `RelayEgressHandlerTest.nonRokBindRespDoesNotCoupleAndTearsDownAfterVerbatimForward` + `BindInterceptorTest`'s RELAY-002c row |
| T10-R5 | 2.2 T8-N2 | THE folded single-sited AD-32 ordering — `RelayStateManager.beginTeardown` (the successor of the six pre-T6 teardown sites; `teardownForPreCoupleViolation` is deleted) | immediate-return `Lost` | **9 RED**: the two N2 successors (`RelayIngressHandlerTest` RELAY-002 mid-adjudication + post-egress — no close, no cancelHttp, no zeroize) + `RelayStateManagerTest.beginTeardownPerformsTheHygieneBeforeWonThenLoses` + `settleAdjudicationWipesWithoutCancelling` + the RELAY-008/009/010/AC5/AC6-ingress-half cascade (every Won-caller tail is gated on the decision); `beginTeardownWithoutAPairIsLost` correctly stayed GREEN — universal `Lost` satisfies its expectation |
| T10-R6 | 2.2 T9/T10-N1 | the post-couple opaque relay coupling — `CoupledRelayHandler.relayFramedPdu`'s peer write (the `splice()` site's successor) | `peer.writeAndFlush` → `self` | **3 RED**: `RelayA1SmokeTest` RELAY-011 + REL-1 AND `JsmppA1OracleTest` OBS-038 (the bind stages correctly stayed GREEN — binds never traverse the relay) |
| T10-R7 | 2.2 T4-CAS (renamed by T4) | the CAS-once couple — `ConnectionEntry.couple()` (`flipSpliced()`'s rename; its biter re-pointed with it) | always-win (`set(true); return true`) | **exactly 1**: `ConnectionRegistryTest.coupleIsCasOnce` |
| T10-R8 | 2.2 T7-N2 (moved by T6) | the settled-adjudication wipe — `RelayStateManager.settleAdjudication` (the continuation's `finally`-wipe became its first act) | `wipePassword(entry)` dropped | **exactly 2**: `RelayStateManagerTest.settleAdjudicationWipesWithoutCancelling` + `BindInterceptorTest.allowForwardsTheOriginalFrameVerbatimToTheConfiguredEgress` (the ALLOW-path zeroize assert) |
| T10-R9 | 2.2 T7 in-flight assert (moved by T6) | `RelayStateManager.register`'s no-live-pair assert (from `BindInterceptor.adjudicate`) | assert deleted | **exactly 1**: `RelayStateManagerTest.registerRefusesALivePairOnTheChannel` (bites under the workers' `-ea`) |

**B. Security half — surviving guards at their post-T1/T2/T8/T9 sites (8 mutations):**

| # | Introduced by | Guard | Neuter | RED biter(s) |
|---|---|---|---|---|
| T10-S1 | T1/T2 (D6) | the production structural gate's deny arm — `RopcBindCredentialVerifier.adjudicateIssuedToken` | `if (segments != 3)` → `if (false)` | **exactly 1**: the D6 pin `opaqueTokenDeniesFailClosedWithoutAWireRound2` |
| T10-S2 | T1/T2 (D7) | the same gate's Allow arm | `return Allow` → `return DenyIndeterminate` | **3 RED**: both D7 pins (`jwtVerdictDerivesFromTheEndpointAlone`, `threeSegmentTokenIsNeverLocallyParsed`) + the AC6 allow-path zeroization row (its setup requires an Allow verdict — honest collateral of a verdict flip) |
| T10-S3 | T9 | the derivation — `deriveTokenEndpoint`'s append-after-last-segment join | body → plain `URI.resolve(TOKEN_ENDPOINT_PATH)` (the RFC 3986 §5.3 last-segment-replacement trap) | **14 RED** (T9's own spot-pass count, reproduced): incl. `tokenEndpointDerivesFromTheProviderUrlRealmBase` + the WARN pin — every real-wire row lands on the wrong path and denies fail-closed |
| T10-S4 | T9 | NO startup provider call — the ctor | a ctor GET of `<provider-url>/.well-known/openid-configuration` re-introduced (the retired probe's exact URL shape, over the injected TLS client) | the never-hit pin `noStartupProviderCallEverFetchesTheMetadataDocument` RED (+2 construct-an-adapter rows: the unreachable-endpoint row — construction now throws — and the WARN pin) |
| T10-S5 | T9 | the starred `OPERATOR_WARNING` — BOTH call sites (the connection-error arm + `unmappedStatus`) | both `log.warn` sites dropped | **exactly 1**: `providerFailureArmsLogTheStarredOperatorWarning` — nothing else reddened: the AC2 verdict table is provably untouched by the WARN |
| T10-SL1 | T8 | the slice's structural gate, deny arm (`RopcSlice.adjudicateIssuedToken`) | `if (false)` | **exactly 1**: `RopcSliceFailClosedTest.opaqueToken_yieldsDenyIndeterminate` |
| T10-SL2 | T8 | the slice's gate, Allow arm | `return Allow` → `DenyIndeterminate` | **exactly 1**: `threeSegmentGarbageToken_yieldsAllow` |
| T10-SL3 | T8 | the amended contract's mandatory secret — `SliceConfig`'s compact ctor | `requireNonNull(clientSecret, …)` dropped | **exactly 1**: `nullClientSecret_isRejectedAtConstruction` |

**C. Retired guards — struck with evidence:**

| Struck row | Guard | Retirement + evidence |
|---|---|---|
| ~~RFC 7662 introspection arm~~ | `introspect`/`mapIntrospection`/`introspectRequest` + the discovery `introspection_endpoint` requirement | T1 (2026-08-27): 12 `RopcBindCredentialVerifierTest` rows + the live path-2 row retired WITH the arm; `grep 7662\|introspect proxy/src/main` EMPTY (re-proven on the T10 tree); successor pin proven at T10-S1 |
| ~~local JWT verification + cache TTL~~ | `verifyJwt` (typ/kid/RSASSA), `JwksCache` + its AD-28(2) refresh VT, the `oidc.jwks-cache-ttl` knob, the `jwks_uri` discovery requirement | T2 (2026-08-28): `JwksCacheTest` deleted (−7), 9 verify rows + the matrix ttl rows retired; `grep jwks\|Jwks proxy/src/main` EMPTY (re-proven); the stale-key refusal pin `ac8_staleKeyCacheTtlKeyRefuses` still bites at its unchanged mechanism (`ignoreUnknownFields=false`); successor pins proven at T10-S2 |
| ~~the slice's verify/introspection/mTLS arms~~ | `verifyWithJwks`/`fetchJwks`/`jwksCache`/`asyncRefreshJwks`, `adjudicateByIntrospection`/`introspectRequest` + the 7662 ctor guard, the `useMtlsClientAuth` no-secret path | T8 (2026-08-29): retired at the COMPILE level (the T8 disposition — nothing behavioral left to bite); `RopcSliceUnitTest` 10→1, the 7662/JWKS fail-closed rows and live paths 2/3 retired; the AC13 repo-wide grep's 30 survivors are all dated prose or load-bearing pins (T8/T9 inventories; re-counted on the T10 tree); the surviving slice pins re-proven at T10-SL1..SL3 |
| ~~the discovery guards: the refusal family, issuer-equality, the DAG probe~~ | `OidcStartupDiscovery` whole — the ctor GET, `parseDocument`/`advertisesPasswordGrant`, the non-200/parse/missing-field refusals, `issuer == provider-url`, `DAG_WARNING` | T9 (2026-08-29): class + `OidcStartupDiscoveryTest` (−10 rows) deleted; the `src/main` gate grep EMPTY (re-proven on the T10 tree); the retired posture's loud successors proven at T10-S3/S4/S5 (derivation / never-hit / WARN); the DAG prerequisite is now the dated `application.yml` policy block + the `Oidc` javadoc |

**D. Rows that STAND (sites untouched by T1–T9 — their home ledgers remain authoritative):** 2.2 T7-N1 deny synthesis (`BindInterceptor.synthesizeBindFailure`, same method and site); T7-N3/N4 egress-bootstrap wiring (`applyToEgress` + `.handler(egressInitializer)`); T7-N5 verdict re-check (`onVerdict`'s `entryFor(channel) != entry || entry.tearingDown()` — same site and shape, only its internals re-pointed at the manager); T7-N6 adjudication-deadline positivity (validator, untouched); 2.2 T4's `beginTearingDown` CAS (`ConnectionEntry`, unrenamed — its couple twin was the rename, re-proven at T10-R7); the 3.3 M1–M15 TLS/config series (M4/M10 already struck 2026-08-26, M3 superseded by M14; 3.3 machinery is explicitly out of 3.4's scope); and the 3.2 named-guard controls outside the retired arms (the verdict-table rows, budget/timeout/secret-file/ctor guards — all at unchanged sites).

### File List

**T1 (2026-08-27):**
- `proxy/src/main/java/smpp/companion/proxy/security/RopcBindCredentialVerifier.java` — 7662 arm deleted; D6 WARN deny added at the dispatch; javadoc amendments (prior session, this tree)
- `proxy/src/main/java/smpp/companion/proxy/security/OidcStartupDiscovery.java` — `introspection_endpoint` requirement + `OidcProviderMetadata` component removed (prior session, this tree)
- `proxy/src/main/java/smpp/companion/proxy/config/ProxyCompanionProperties.java` — operator-guidance javadoc → JWT-only policy (prior session, this tree)
- `proxy/src/main/java/smpp/companion/proxy/security/IdpSslContextFactory.java`, `relay/ConnectionRegistry.java` — stale introspection prose swept (prior session, this tree)
- `proxy/src/main/resources/application.yml` — JWT-only policy comment block (prior session, this tree)
- `proxy/src/test/java/smpp/companion/proxy/security/RopcBindCredentialVerifierTest.java` — 12 rows retired, D6 pin added, AC4 javadoc amended, `@DisplayName`/fixture javadoc updated
- `proxy/src/test/java/smpp/companion/proxy/security/RopcBindCredentialVerifierLiveTest.java` — path 2 + stand-in/control apparatus retired, imports pruned, javadoc/`@DisplayName` amended
- `proxy/src/test/java/smpp/companion/proxy/security/OidcStartupDiscoveryTest.java` — metadata assertion retarget, doc template swept
- `proxy/src/test/java/smpp/companion/proxy/testsupport/OidcDiscoveryStandIn.java` — stopped serving `introspection_endpoint`

**T2 (2026-08-28; code halves dated 2026-08-27 in-tree):**
- `proxy/src/main/java/smpp/companion/proxy/security/JwksCache.java` — DELETED (class + AD-28(2) refresh VT + TTL guard)
- `proxy/src/main/java/smpp/companion/proxy/security/RopcBindCredentialVerifier.java` — `verifyJwt`/kid/typ/claim arms deleted; dispatch → `Allow` past the structural gate (D7); `jwksCache()` accessor + `jwks.close()` step removed; Nimbus imports pruned to `JSONObjectUtils`; dated javadoc amendments
- `proxy/src/main/java/smpp/companion/proxy/security/OidcStartupDiscovery.java` — `jwks_uri` requirement + metadata component removed; javadoc amendments
- `proxy/src/main/java/smpp/companion/proxy/config/ProxyCompanionProperties.java` — `jwksCacheTtl` component + `@NotNull`/`@DurationMin` messages + HV import removed; stale-key retirement javadoc
- `proxy/src/main/java/smpp/companion/proxy/security/AdjudicationLifecycle.java`, `IdpSslContextFactory.java`, `Verdict.java` — stop-body + trust-anchor + DenyIndeterminate-example javadoc sweeps
- `proxy/src/main/resources/application.yml` — two-budget template, `jwks-cache-ttl` template lines removed, T2 policy block
- `proxy/src/test/java/smpp/companion/proxy/security/JwksCacheTest.java` — DELETED (−7 rows)
- `proxy/src/test/java/smpp/companion/proxy/security/RopcBindCredentialVerifierTest.java` — 9 verify rows retired, 2 D7 pins added
- `proxy/src/test/java/smpp/companion/proxy/security/RopcBindCredentialVerifierLiveTest.java` — path 1 re-pointed, `awaitCachePopulated` deleted
- `proxy/src/test/java/smpp/companion/proxy/config/CompanionConfigMatrixTest.java` — ttl rows retired, stale-key refusal row added, javadoc dated note
- `proxy/src/test/java/smpp/companion/proxy/config/TestCompanionConfigs.java`, `CompanionRoleFailFastTest.java`, `security/AdjudicationLifecycleTest.java`, `security/IdpSslContextFactoryTest.java`, `security/OidcStartupDiscoveryTest.java`, `relay/netty/DirectMemoryBudgetStartupCheckTest.java`, `relay/netty/RelayServerLifecycleTest.java`, `testsupport/OidcDiscoveryStandIn.java`, `testsupport/RelayTestFixtures.java` — 6-arg `Oidc` ctor sweep, knob args dropped, discovery docs stop serving `jwks_uri` (omission-accepted pins at the assertion sites)
- `_bmad-output/planning-artifacts/architecture/…/ARCHITECTURE-SPINE.md` — 18 dated amendment sites (AD-4/5/8/10/11/12×2/22/28/33/36 + register ×2 + conventions + module/package maps + deployment graph + deferred row)
- `_bmad-output/planning-artifacts/architecture/…/.memlog.md` — one T1+T2 `(amendment)` entry (append-only)
- `_bmad-output/planning-artifacts/epics.md` — 7 dated sites (AD-12 ×3, register ×2, tuning row, 4-path note)
- `_bmad-output/test-artifacts/test-design/test-coverage-scenarios.md` — §4.2/§4.3 retirement banners + 22 row statuses/notes (incl. SEC-005 inversion, OBS-018 retirement)

**T4 (2026-08-28):**
- `proxy/src/main/java/smpp/companion/proxy/relay/ConnectionEntry.java` — `spliced`/`spliced()`/`flipSpliced()` → `coupled`/`coupled()`/`couple()`; javadoc swept to couple vocabulary
- `proxy/src/main/java/smpp/companion/proxy/relay/RelayHandler.java` — `splice()` → `relayFramedPdu()`; `entry.spliced()` ×4 → `coupled()`; `flipSpliced()` call → `couple()` (the N1 site); local/param `spliced` → `coupled`; full javadoc/comment sweep; `SpliceObserver` → `RelayObserver`
- `proxy/src/main/java/smpp/companion/proxy/relay/BindInterceptor.java` — `entry.spliced()` ×3 → `coupled()`; `SpliceObserver` → `RelayObserver` (import/field/2 ctors); post-flip/splice prose → post-couple/relay
- `proxy/src/main/java/smpp/companion/proxy/relay/ConnectionRegistry.java` — javadoc "AD-25 flip-flag" → "couple flag"
- `proxy/src/main/java/smpp/companion/proxy/relay/netty/RelayIngressInitializer.java`, `RelayEgressInitializer.java` — `SpliceObserver` → `RelayObserver` (imports/fields/ctors); flipper/flip/opaque-splice prose → couple unit/couple/opaque relay
- `proxy/src/main/java/smpp/companion/proxy/relay/netty/RelayNettyConfig.java`, `RelayChannelOptions.java` — "data-plane splice" → "data-plane relay"; "the splice re-arms" → "the relay re-arms"; "one spliced data plane" → "one coupled data plane" (the "writability flips" Netty-English line deliberately kept — inventoried)
- `proxy/src/main/java/smpp/companion/proxy/observability/SpliceObserver.java` → **`RelayObserver.java`** (git-mv) — interface renamed; "AD-25 ROK flip"/"splice flag flipped" javadoc → couple vocabulary; 4 method names unchanged
- `proxy/src/main/java/smpp/companion/proxy/observability/NoopSpliceObserver.java` → **`NoopRelayObserver.java`** (git-mv)
- `proxy/src/main/java/smpp/companion/proxy/observability/CloseReason.java`, `Direction.java` — seam-type javadoc refs renamed; "AD-25 flip" → "couple", "mid-splice" → "mid-relay", "no splice flips" → "the pair never couples"
- `proxy/src/main/java/smpp/companion/proxy/security/Verdict.java` — "may splice / relay" → "may couple / relay" javadoc
- `proxy/src/test/java/smpp/companion/proxy/observability/CapturingSpliceObserver.java` → **`CapturingRelayObserver.java`** (git-mv); `CapturingSpliceObserverTest.java` → **`CapturingRelayObserverTest.java`** (git-mv); `SpliceObserverShapeTest.java` → **`RelayObserverShapeTest.java`** (git-mv; + method rename `spliceObserverIsFourMethodInterface` → `relayObserverIsFourMethodInterface`)
- `proxy/src/test/java/smpp/companion/proxy/relay/RelayHandlerTest.java` — helper renames `coupleAndFlip()` → `couple()`, `couple()` → `awaitingBindResp()`; 5 test-method renames; DisplayNames/assertions/comments swept to couple/relay (see the Completion-Notes re-targeting list)
- `proxy/src/test/java/smpp/companion/proxy/relay/ConnectionRegistryTest.java` — `flipSplicedIsCasOnce` → `coupleIsCasOnce`; CAS pins re-pointed
- `proxy/src/test/java/smpp/companion/proxy/relay/BindInterceptorTest.java`, `BindInterceptorForwardRoleTest.java`, `JsmppA1OracleTest.java`, `RelayA1SmokeTest.java`, `TlsModesLoopbackE2eTest.java`, `MockSmsc.java`, `JsmppSmscServer.java` — `CapturingRelayObserver` refs + DisplayNames/assertions/prose swept
- `proxy/src/test/java/smpp/companion/proxy/relay/netty/RelayPipelineInitializersTest.java`, `RelayChannelOptionsTest.java`, `RelayServerLifecycleTest.java` — pipeline DisplayName/assertion sweeps ("post-flip plane" → "post-couple plane", "one spliced data plane" → "one coupled data plane", "data-plane splice" → "data-plane relay"); the lifecycle-test's incidental flip lines deliberately kept (inventoried)
- `proxy/src/test/java/smpp/companion/proxy/testsupport/RelayTestFixtures.java`, `proxy/src/test/java/smpp/companion/proxy/observability/ObservabilityLayerRulesTest.java`, `proxy/src/test/java/smpp/companion/proxy/bootstrap/PreviewFeatureCompileGateTest.java` — observer-type refs renamed; "egress-leg flipper" → "couple unit"; "data-plane splice" → "data-plane relay"
- `_bmad-output/planning-artifacts/architecture/…/ARCHITECTURE-SPINE.md` — AD-25 + AD-2 + AD-27 dated AMENDED markers (titles + rules); dated wording notes on AD-1/AD-8/AD-32; plain sweeps at the preamble/Design-Paradigm/mermaid + AD-3/5/7/9/19/22/23 + register ×2 + module map + Deferred row
- `_bmad-output/planning-artifacts/architecture/…/.memlog.md` — one T4 `(amendment)` entry (append-only) with the full Propagated list + survivor dispositions
- `_bmad-output/planning-artifacts/epics.md` — 12 dated sites (FR-TRANSIT-1, SEC-2, REL-1, Language/Runtime, module-map token, AD summary-table note + 7 rows, FR-coverage line, Epic-2 goal/NFR/packages, Epic-4 packages, register outage row, Deferred DLR row)
- `_bmad-output/test-artifacts/test-design/test-coverage-scenarios.md` — dated vocabulary-note banner at the doc head (rows read through it)

**T5 (2026-08-28):**
- `proxy/src/main/java/smpp/companion/proxy/relay/RelayHandler.java` — DELETED (the split subject; the `AttributeKey` namespace moved to the base)
- `proxy/src/main/java/smpp/companion/proxy/relay/CoupledRelayHandler.java` — NEW: the abstract base (final `channelRead` prelude, post-couple relay + writability re-arm, `teardownPair`/`channelInactive`/`exceptionCaught`, exactly-once close telemetry, `classify`/`stash`/`releaseFrame`, the `CLOSE_REASON`/`CLOSE_FIRED` keys (D5 hoist home), the `direction` field + protected ctor, `peerOf`, the `readPreCouple` hook)
- `proxy/src/main/java/smpp/companion/proxy/relay/RelayEgressHandler.java` — NEW: the couple unit (the N1 successor `entry.couple()` site, `onBindAccept` at the couple, the read-arm), non-ROK forward-then-teardown, the case-4 `GENERIC_NACK` + `getInt(4)` arm, bind-request propagation, the egress-leg bare close
- `proxy/src/main/java/smpp/companion/proxy/relay/RelayIngressHandler.java` — NEW: the no-bind-arm leg — uniform violation fail-close via the `teardownForPreCoupleViolation` delegation (unchanged, T6's seam) + the defensive bare close
- `proxy/src/main/java/smpp/companion/proxy/relay/netty/RelayIngressInitializer.java` — wiring `new RelayIngressHandler(registry, observer)`; javadoc re-point; `Direction` import dropped
- `proxy/src/main/java/smpp/companion/proxy/relay/netty/RelayEgressInitializer.java` — wiring `new RelayEgressHandler(registry, observer)`; javadoc re-point; `Direction` import dropped
- `proxy/src/main/java/smpp/companion/proxy/relay/BindInterceptor.java` — javadoc/comment re-points only (AD-25 forwarder-split bullet, `teardownForPreCoupleViolation` javadoc, the interceptor pass-through comment, the exceptionCaught propagation comment, `EgressLeg` javadoc)
- `proxy/src/main/java/smpp/companion/proxy/relay/ConnectionEntry.java` — javadoc re-points (the couple owner is `RelayEgressHandler`, structural-by-type)
- `proxy/src/main/java/smpp/companion/proxy/relay/ConnectionRegistry.java`, `relay/netty/RelayChannelOptions.java` — javadoc re-points (handler-name sweeps)
- `proxy/src/test/java/smpp/companion/proxy/relay/RelayHandlerTest.java` — DELETED (−12 rows; split along the injection-leg seam)
- `proxy/src/test/java/smpp/companion/proxy/relay/CoupledPairHarness.java` — NEW: the shared package-private pair fixture (channels/beans/`awaitingBindResp()`/`couple()`/builders/`FakeEgressConnector`; the old suite's EmbeddedChannel honesty notes)
- `proxy/src/test/java/smpp/companion/proxy/relay/RelayEgressHandlerTest.java` — NEW (6 rows, moved unchanged-in-intent)
- `proxy/src/test/java/smpp/companion/proxy/relay/RelayIngressHandlerTest.java` — NEW (6 rows moved + the new AC6 stray-bind_resp fail-closed pin)
- `proxy/src/test/java/smpp/companion/proxy/relay/RelayCoupleSiteArchitectureTest.java` — NEW: the AC6 ArchUnit call-site pin (forbid + positive control, `DoNotIncludeTests`, FQN-scoped both sides)
- `proxy/src/test/java/smpp/companion/proxy/relay/netty/RelayPipelineInitializersTest.java` — `.class` lookups, prefix searches, order assertions, DisplayNames, method names, javadoc pipelines re-pointed per leg
- `proxy/src/test/java/smpp/companion/proxy/relay/BindInterceptorTest.java` — 3 ctor sites re-pointed; pipeline/RELAY-002c prose swept
- `proxy/src/test/java/smpp/companion/proxy/relay/BindInterceptorForwardRoleTest.java` — `pipeline(...)` helper ctor re-pointed; unused `Direction` import pruned
- `proxy/src/test/java/smpp/companion/proxy/testsupport/RelayTestFixtures.java`, `observability/CapturingRelayObserver.java`, `relay/RelayA1SmokeTest.java` — javadoc/prose re-points (no assertion changes)
- `_bmad-output/planning-artifacts/architecture/…/ARCHITECTURE-SPINE.md` — AD-25 dated AMENDED marker (rule ×2 sites + structural-by-type) sweeping the Design-Paradigm bullet, mermaid ×3, AD-7 (dated note), AD-27, module map
- `_bmad-output/planning-artifacts/architecture/…/.memlog.md` — one T5 `(amendment)` entry (append-only) with the full Propagated list
- `_bmad-output/planning-artifacts/epics.md` — 2 dated re-points (module-map `relay/` parenthetical, AD-25 summary row; mirror bullets stay frozen)
- `_bmad-output/test-artifacts/test-design/test-coverage-scenarios.md` — dated split note appended to the T4 vocabulary banner

**T6 (2026-08-29):**
- `proxy/src/main/java/smpp/companion/proxy/relay/RelayStateManager.java` — NEW: the single pair-lifecycle state manager over the registry's storage (T3 shape): register (+the moved in-flight assert) / beginAdjudication / settleAdjudication / attachEgress / beginTeardown (the sealed `Teardown.Won(entry) | Teardown.Lost` decision with the absorbed cancelHttp+zeroize hygiene BEFORE the Won return) / entryFor
- `proxy/src/main/java/smpp/companion/proxy/relay/ConnectionEntry.java` — the two absorbed adjudication handles (4→6 things, volatile, package-private `RelayStateManager`-routed transitions) + the derived `answered()` predicate; javadoc amended
- `proxy/src/main/java/smpp/companion/proxy/relay/ConnectionRegistry.java` — bean javadoc pointer (production transitions route through the manager; the registry stays AD-8's storage home + the test seam)
- `proxy/src/main/java/smpp/companion/proxy/relay/BindInterceptor.java` — manager injection (registry→manager); the pending fields + `cancelAndWipePending` + `teardownForPreCoupleViolation` DELETED; the four teardown arms consume the sealed decision; `adjudicate`/`onVerdict` re-pointed at the manager's store/settle (the settle is the continuation's first act); `EgressLeg` carries the entry ref, its `answered` shadow bit + write site deleted; class javadoc re-pointed
- `proxy/src/main/java/smpp/companion/proxy/relay/CoupledRelayHandler.java` — `protected manager` field (ctor swap); the three teardown sites consume the sealed decision; the three lookups re-pointed; the teardown javadoc bullet names the single-sited ordering
- `proxy/src/main/java/smpp/companion/proxy/relay/RelayIngressHandler.java` — the pipeline-lookup delegation DELETED; the pre-couple violation teardown calls the manager directly (full ordering, no interceptor needed)
- `proxy/src/main/java/smpp/companion/proxy/relay/RelayEgressHandler.java` — ctor param swap (manager)
- `proxy/src/main/java/smpp/companion/proxy/relay/netty/RelayIngressInitializer.java`, `RelayEgressInitializer.java` — manager injection + wiring/javadoc re-points
- `proxy/src/test/java/smpp/companion/proxy/relay/RelayStateManagerTest.java` — NEW (4 rows: hygiene-before-Won + idempotence, Lost-without-a-pair, settle-wipes-without-cancelling, the register assert)
- `proxy/src/test/java/smpp/companion/proxy/relay/ConnectionRegistryTest.java` — +1 row (`answeredDerivesFromCoupledOrTearingDown`)
- `proxy/src/test/java/smpp/companion/proxy/relay/CoupledPairHarness.java`, `BindInterceptorTest.java`, `BindInterceptorForwardRoleTest.java`, `RelayIngressHandlerTest.java`, `netty/RelayPipelineInitializersTest.java`, `testsupport/RelayTestFixtures.java` — manager wiring re-points (harness records carry the manager; zero assertion changes)
- `_bmad-output/planning-artifacts/architecture/…/ARCHITECTURE-SPINE.md` — AD-8 dated AMENDED marker + the module-map `relay/` line's dated note
- `_bmad-output/planning-artifacts/architecture/…/.memlog.md` — one T6 `(amendment)` entry (append-only) with the full Propagated list + gates

**T8 (2026-08-29):**
- `proxy/src/test/java/smpp/companion/proxy/security/RopcSlice.java` — the three interop arms deleted (JWKS verify/fetch/cache/refresh + the forked subtask; `adjudicateByIntrospection`/`introspectRequest`; the `useMtlsClientAuth` no-secret path); `SliceConfig` → `(tokenEndpoint, clientId, clientSecret)` with mandatory secret; `adjudicateIssuedToken` (the structural three-segment gate) added; `cancelHttp` re-worded (token exchange alone); Nimbus imports pruned to `JSONObjectUtils`; class javadoc re-pointed to the amended contract
- `proxy/src/test/java/smpp/companion/proxy/security/KeycloakFixture.java` — `INTROSPECTION_ENDPOINT`/`JWKS_URI`/`ISSUER`/`CLIENT_B_ID` deleted; Client A + SSLContext javadocs re-pointed (transport identity)
- `proxy/src/test/java/smpp/companion/proxy/security/RopcSliceUnitTest.java` — kid-miss + 8 claim-deny rows retired with the Nimbus apparatus; the unreachable row re-pointed (10 → 1)
- `proxy/src/test/java/smpp/companion/proxy/security/RopcSliceFailClosedTest.java` — the two 7662 rows + JWKS/introspection serving retired; the D6/D7/mandatory-secret pins added (7 → 8); `slice()` re-pointed; class javadoc re-pointed
- `proxy/src/test/java/smpp/companion/proxy/security/RopcSliceCancelTest.java` — config re-pointed (3-arg); STS-teardown prose note (zero assertion changes)
- `proxy/src/test/java/smpp/companion/proxy/security/RopcSliceLiveTest.java` — paths 2 (7662) + 3 (mTLS) retired; path 1 re-pointed to the structural gate (5 → 3); helper + javadoc re-pointed
- `proxy/src/test/java/smpp/companion/proxy/security/NoRolledCryptoArchitectureTest.java` — the JOSE rows + class javadoc re-worded to the amended Nimbus role (provider-JSON parser; rules unchanged)
- `proxy/src/test/java/smpp/companion/proxy/security/KeycloakContainer.java`, `TrustOnlyKeycloakContainer.java` — transport-mTLS comment/javadoc re-points (the client-x509 rationale retired)
- `proxy/src/test/java/smpp/companion/proxy/security/RopcBindCredentialVerifierLiveTest.java` — the "slice keeps the historical ratification" living cross-references swept
- `proxy/src/test/resources/keycloak/realm-smpp-companions.json` — the client-x509 Client B block deleted; Client A name swept
- `proxy/src/test/resources/keycloak/README.md` — amended-contract banner, 3-path tables, transport-cert wording; the dated Findings kept with a historical preamble
- `proxy/src/test/resources/keycloak/certs/generate.sh` — client-cert comments re-worded (transport identity; the cert itself unchanged)
- `_bmad-output/planning-artifacts/architecture/…/ARCHITECTURE-SPINE.md` — AD-12 ratification marker EXTENDED (2026-08-29 supersession) + the register ROPC row's dated update
- `_bmad-output/planning-artifacts/architecture/…/.memlog.md` — one T8 `(amendment)` entry (append-only): the disposition reversal + the full removal/pin/fixture record + gates

**T9 (2026-08-29):**
- `proxy/src/main/java/smpp/companion/proxy/security/OidcStartupDiscovery.java` — DELETED whole (the probe + the nested `OidcProviderMetadata` record)
- `proxy/src/main/java/smpp/companion/proxy/security/RopcBindCredentialVerifier.java` — the derivation (`TOKEN_ENDPOINT_PATH` + `deriveTokenEndpoint`, append-after-last-segment join) + the `providerUrl`/`tokenEndpoint` fields; ctor pair drops the `discovery` param (public 1-arg / recording 2-arg); the starred `OPERATOR_WARNING` + `unmappedStatus` + the IOException-gated connection-error WARN + `transportFailure`; `tokenRequest` targets the derived endpoint; the "no startup provider call" class-javadoc paragraph
- `proxy/src/main/java/smpp/companion/proxy/security/VerifierWiringConfig.java` — the `OidcStartupDiscovery` injection dropped; javadoc re-pointed
- `proxy/src/main/java/smpp/companion/proxy/config/ProxyCompanionProperties.java` — the `Oidc.providerUrl` javadoc mirror (realm-base MUST + the DAG operator contract + the token-path join) + the compact-ctor javadoc's join wording re-pointed
- `proxy/src/main/java/smpp/companion/proxy/config/CompanionConfigValidator.java`, `security/IdpSslContextFactory.java` — javadoc re-points (the discovery build is gone; the token call is the link's one runtime use)
- `proxy/src/main/resources/application.yml` — the dated T9 policy block after the T2 block
- `proxy/src/test/java/smpp/companion/proxy/security/OidcStartupDiscoveryTest.java` — DELETED (−10 rows)
- `proxy/src/test/java/smpp/companion/proxy/security/RopcBindCredentialVerifierTest.java` — ctor re-points; realm-segment provider-urls; `standInIdP` re-shaped (no discovery context/`tokenEndpointBase`); the unreachable + constructor-fail-fast rows need no server; +3 fresh pins (derivation / never-hit / WARN)
- `proxy/src/test/java/smpp/companion/proxy/security/AdjudicationLifecycleTest.java` — ctor re-point; realm base; the parked fixture's discovery context deleted
- `proxy/src/test/java/smpp/companion/proxy/security/VerifierWiringConfigTest.java` — the runner drops the probe bean; javadoc re-pointed
- `proxy/src/test/java/smpp/companion/proxy/security/RopcBindCredentialVerifierLiveTest.java` — ctor re-point; "fully live" javadoc clause re-pointed to the derived endpoint
- `proxy/src/test/java/smpp/companion/proxy/testsupport/OidcDiscoveryStandIn.java` — re-purposed (dated javadoc note; `discoveryHits()` counter on the never-hit metadata context; `url()`/`fixtureServerSslContext()` unchanged)
- `proxy/src/test/java/smpp/companion/proxy/config/TestCompanionConfigs.java`, `testsupport/RelayTestFixtures.java` — javadoc re-points (no startup wire call; the CA-anchor trust story)
- `_bmad-output/planning-artifacts/architecture/…/ARCHITECTURE-SPINE.md` — AD-12 probe-clause SUPERSEDED marker (the surviving https clause re-worded to bind-time) + AD-36 dated wording note + the module-map `security/` line re-worded
- `_bmad-output/planning-artifacts/architecture/…/.memlog.md` — one T9 `(amendment)` entry (append-only): the full removal/derivation/WARN/documentation record + Propagated list + gates
- `_bmad-output/planning-artifacts/epics.md` — the operative SEC-3 row's dated note (the `[AD-12]` mirror bullets stay frozen, flagged)
- `_bmad-output/implementation-artifacts/deferred-work.md` — "Evaluate an OIDC-discovery/parsing library" `**✅ RESOLVED … SUPERSEDED BY REMOVAL**` marker (the verbatim owner note kept as history)

**T7 (2026-08-29):**
- `_bmad-output/implementation-artifacts/deferred-work.md` — the F13 RESOLVED marker (Story 3.3 T6/`35fd64d` attribution + the not-claimed idle-timeout clause re-homed to Epic-4); the four 2026-08-27 story-text-pass RESOLVED markers (executed by `cda2013`); the F7 DISPOSITION marker (D4 → RE-HOMED, with the adapter settlement-guarantee → T6 hygiene → losing-race-release chain as the source-level mitigation)
- `_bmad-output/test-artifacts/test-design/test-coverage-scenarios.md` — SEC-030 dated Status-RETIRED note (the T2-flagged TEA gap: both-tier 8705 removal, mandatory `client_secret`, the transport-cert role, the SEC-095 parenthetical flagged as history)

**T10 (2026-08-30):**
- `_bmad-output/implementation-artifacts/3-4-relay-refactoring-round.md` — this file only: T10 ticked; the RED-on-neuter mutation ledger filled (17 fresh rows + the struck retired-guard rows + the stands inventory); Dev Agent Record / Debug Log / Completion Notes / File List entries. **ZERO source changes** — a mutation-only round: every neuter was restored `cmp`-verified byte-identical from its `/tmp/t10-backups` master (`git status proxy/src` empty; `grep -rn "MUTATED" proxy/src` empty).

**T11 (2026-08-31):**
- `_bmad-output/implementation-artifacts/3-4-relay-refactoring-round.md` — this file only: T11 ticked; the Dev Agent Record / Debug Log / Completion Notes / File List entries (the per-question goals-met evidence, the scatter-map diff, and the finder-pass dispositions AC12 requires). **ZERO source changes** — an analysis-only round: every claim verified by grep/read against the working tree and the `cda2013` baselines (`RelayHandler.java`, `BindInterceptor.java`); the final gates re-proven green on the unchanged post-T10 tree; `git status proxy/src` stays empty.
