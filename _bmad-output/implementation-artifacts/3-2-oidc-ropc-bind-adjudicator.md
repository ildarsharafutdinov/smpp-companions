---
baseline_commit: 92964ebaf62787a47cb607f7393c2878828d0888
epic: 3
story: 2
story_key: 3-2-oidc-ropc-bind-adjudicator
status: in-progress
---

# Story 3.2: OIDC ROPC bind adjudicator — production adapter behind the AD-12 port

Status: in-progress

> **The Epic 3 production-security slice.** Story 2.1 shipped the AD-12 port types + an always-allow stub in `main` and ratified the contract shape against real Keycloak 26.7.0 with a *test-tier* slice (`RopcSlice`, under `proxy/src/test`). This story ships the **production ROPC adapter** in `proxy/src/main/.../security/`, wired via Spring DI behind the **ratified-immutable port** (AC8, 2026-08-10 — "wire, never re-author"), plus the `companion.*` config surface it needs. **Scope discipline (non-negotiable): `proxy/src/main/.../relay/` is NOT modified** (epics.md §Epic 3: the swap is "behind the unchanged AD-12 port via Spring DI"); the forward-role acceptor widening that would exercise the adapter through a live relay belongs to the NEXT story (3.3). The load-bearing evidence is **AC2** (the AD-11 verdict mapping, incl. the 429/404/403 ledger refinement) and **AC5** (AD-28(4) concurrency + the F7 settlement-on-cancel decision).

## Story

**As a** proxy operator running Keycloak (or any OIDC provider with Direct Access Grants),
**I want** every accepted legacy bind's password-grant adjudicated by a real ROPC call to my provider — with local Nimbus JWKS defense-in-depth, RFC 7662 introspection for opaque tokens, fail-closed on every indeterminate condition, and zero credential bytes surviving the adjudication —
**so that** my proxy tier stays credential-free (FR-SEC-1/AD-10): it brokers and delegates trust instead of holding it, denies closed when the provider is unreachable or ambiguous (FR-SEC-5/AD-11), and never leaks a working carrier credential from a compromise.

## Acceptance Criteria

> Load-bearing: **AC2** (verdict mapping — resolves deferred-work "429/404/403" refinement) and **AC5** (concurrency + F7 settlement decision). ACs cite the spine (ARCHITECTURE-SPINE.md) and existing code by `file:line`; verify signatures in code before use — do not trust this document over the source tree.

1. **[AC1] Production adapter behind the unchanged port, wired by cell.** A production `RopcBindCredentialVerifier` (final class, `smpp.companion.proxy.security`) implements `BindCredentialVerifier` with **zero changes to any port type** (`BindCredentialVerifier`, `Verdict` + 3 permits, `VerdictRequest`, `BindCredential`, `SystemId`, `Password`, `RequestContext` — the AC8-ratified shape; `SecurityPortShapeTest`/`VerdictShapeTest` stay green unmodified). Spring wiring selects exactly one `BindCredentialVerifier` bean per cell: **forward.mode-a / forward.mode-c → ROPC adapter** (the forward role performs OIDC verification, AD-12); **any reverse.* cell → `AlwaysAllowBindCredentialVerifier`** (the reverse role does not adjudicate, AD-12 two-proxy flow). `AlwaysAllow` loses its unconditional `@Component` (its wiring moves to the new security-side configuration; update `AlwaysAllowBindCredentialVerifierTest`'s `@Component` assertion accordingly). A wiring test proves **exactly one** verifier bean of the expected type for each of the five cells via `ApplicationContextRunner`. No main-source file outside `proxy/security/` and `proxy/config/` changes (`relay/` untouched — verified by the File List). *(AD-12; ARCHITECTURE-SPINE.md:135–142; epics.md:377.)*

2. **[AC2] AD-11 verdict mapping — the normative production table.** The token-endpoint call's outcome maps to `Verdict` exactly per this table (this RESOLVES deferred-work "429/404/403 → DenyInvalid misclassification"): `DenyInvalid` is reserved for the provider's **positive invalid-credential signal** — HTTP 401 (a bare 401 from a token endpoint IS a positive auth-layer rejection per RFC 6749 §5.2, even with an unparseable body), or HTTP 400 whose parsed OAuth `error` is `invalid_grant` or `invalid_client` (2-1 fixture finding #6: bad user → 400 `invalid_grant`, bad client → 401 `invalid_client`; its rule is "do not key on 401 **alone**" — bad-user credentials arrive as 400, so map by error semantics: parse the 400 body as JSON and key on the `error` field). **Everything else is `DenyIndeterminate`**: 200-with-non-JWT/HTML/empty body, 200-with-malformed-JWT, 3xx, any other 4xx (incl. 403/404/429 and 400s with other/absent error codes — rate-limit, config and authorization errors are not credential verdicts), 5xx, timeout, network error, cancellation, admission saturation, and scope shutdown. **DENY always wins on disagreement**: a 200 verdict overridden by failed local JWT verification (signature/iss/aud/exp/nbf/typ/kid) → `DenyIndeterminate` (defense-in-depth failures are *unverifiable*, not *invalid* — matches the ratified slice). Introspection: **only** HTTP 200 + JSON + boolean `active:true` → `Allow`; every other outcome (incl. `active:false`) → `DenyIndeterminate`. `kid` absent from cached JWKS → `DenyIndeterminate` + background refresh scheduled; **no foreground refresh-and-retry on the bind path**. Every row of this table has a test with a RED-on-neuter control (bind the exact bad response through the in-process IdP stand-in, never a removed code path). **Supersession note:** this refined mapping intentionally supersedes the shorthand "4xx → `DenyInvalid`" in the spine's AD-12 ratification bullet and the `Verdict.DenyInvalid` javadoc example ("e.g. an IdP 4xx") — the port types stay untouched (AC8-immutable; the javadoc example is stale by design), and the refinement is recorded as a dated entry appended to the architecture `.memlog.md` when T3 lands. Wire impact: none (AD-33 collapses both `Deny*` permits identically); the distinction surfaces only in logs/metrics labels. *(AD-11; ARCHITECTURE-SPINE.md:130–133; deferred-work §2.1 item 1.)*

3. **[AC3] JWT defense-in-depth via cached JWKS (Nimbus).** On a 200 JWT verdict the adapter locally verifies with Nimbus JOSE+JWT (already `com.nimbusds:nimbus-jose-jwt:10.9.1` in `proxy/build.gradle.kts`; floor ≥10.0.2, CVE-2025-53864, enforced by `smpp.dependency-floors`): signature against the **cached** JWKS, `iss` == the configured issuer, `aud` containing the configured client-id, `exp`/`nbf` per clock (NTP precondition A-4), `typ` header per RFC 8725 §3.9 (absent → tolerated, present-but-unexpected → deny — **verify the pinned fixture's actual `typ` value live before hardcoding**; do not trust training data for the literal). The JWKS cache is an `AtomicReference<JWKSet>` swapped **whole** on successful refresh (never nulled before a successful fetch — deferred-work §2.1 item 6b), refreshed by a hand-rolled virtual thread on a `ScheduledExecutorService` owned by `security/` (**not** `@Scheduled`, and **not** through the adjudication pool or its admission semaphore — §2.1 item 6a, Task 4) with TTL/refresh-ahead (config, spine-deferred values → dev-selected defaults with positivity guards). **No verdict cache of any kind; introspection results never cached** (AD-12). *(AD-12; ARCHITECTURE-SPINE.md:135–142; AD-28(2) ibid:226–229.)*

4. **[AC4] Opaque-token fallback = RFC 7662 introspection, cancellation-visible.** When the 200 body is not a locally-verifiable JWT (opaque token), the adapter calls the provider's introspection endpoint (endpoint URIs come from OIDC discovery of `provider-url` — no per-endpoint override keys); the introspection `sendAsync` future is registered in the same active-call slot as the token call so `cancelHttp()` aborts it too (2-1 review fix **F3**). Introspection verdict per AC2's table; never cached. Operators should prefer JWT issuance (introspection is online-only with no offline cryptographic backstop — emit that guidance in the config key's javadoc/docs comment, not runtime noise). *(AD-12; 2-1 Dev Notes F3.)*

5. **[AC5] Concurrency, cancellation, and the F7 settlement decision.** Entry bounding per AD-28(4): a **single bounded hand-managed VT `ExecutorService` owned by `security/`**, sized by `oidc.max-in-flight`, admission via `tryAcquire` semaphore — **a saturated pool returns `DenyIndeterminate` without starting any wire call** (2-1 review fix **F2**: `sendAsync` deferred until AFTER `tryAcquire`); spawning a VT per bind outside this pool is forbidden; never `@Async`. `verify()` never blocks the caller's thread (the relay pins this: `BindInterceptorTest`'s class-level `@Timeout` with `SEPARATE_THREAD` mode is the anti-blocking pin). Inside one adjudication the fan-out uses **JDK 25 StructuredTaskScope (JEP 505, fifth preview)** with the `open(Joiner)` API — `StructuredTaskScope<T,R>`, `static open(Joiner)`, `fork(Callable)`, `join()`, `close()`, `Joiner.awaitAllSuccessfulOrThrow()` — **NOT the JDK-21/22 `new ShutdownOnFailure()` subclass pattern** (2-1 verified the live-JDK-25 shape; also web-confirmed vs JEP 505). `RequestContext` is re-bound on the pool thread **via the same `ScopedValue` handle passed to `verify()`** (`ScopedValue.where(ctx, rc).call(...)`), never `ThreadLocal`. **Capture rule: read `ctx.get()` synchronously inside `verify()` on the caller's thread** — the relay binds `REQUEST_CONTEXT` only for the dynamic extent of the call (`BindInterceptor.java:256-257`), so dereferencing the handle on a pool thread without re-binding throws `NoSuchElementException`; F12's `requireNonNull(ctx)` guards the handle, not bound-ness. The scope is **shuttable from the teardown path** (AD-25: case-3 teardown shuts the adjudication's STS) and `cancelHttp()` binds to `HttpClient.sendAsync(...).cancel(true)` aborting the real wire exchange (2-1 verified `MinimalFuture.cancel(true)` → `MultiExchange.cancel` → `connection.close()`, requires `mayInterruptIfRunning==true`). **F7 DECISION (this story owns it): the production adapter GUARANTEES `future()` settles (with `DenyIndeterminate`) after `cancelHttp()`** — an adapter-level contract that resolves the relay's cancelled-verdict frame-leak (2-2 F7) at the source **without amending the AC8-immutable port** (the port is silent on settlement; promising it is additive). Proven by a test asserting settlement + `CancellationException` in the aborted wire future's cause chain (reuse 2-1's `RecordingHttpClient` delegating-wrapper pattern and the "assert anywhere in the cause chain" rule). *(AD-4, AD-5, AD-25, AD-28(4), AD-32; ARCHITECTURE-SPINE.md:82–90, 214, 226–229, 253; deferred-work 2-2 F7.)*

6. **[AC6] Secret hygiene — raw-byte encoding and token zeroization.** The ROPC form encodes the password **from the raw backing bytes** (`value.array()`/`arrayOffset()`/`length()`), **never `toString()`** — the `AsciiString.toString()` lazy-immortal-String hazard (2-1 F1: the throwaway slice's `pw.toString()` shortcut is FORBIDDEN in production). The **password's `zeroize()` stays CALLER-OWNED** (relay `BindInterceptor` continuation `finally` + `cancelAndWipePending`, settled 2.2 T7 — the adapter reads the password asynchronously while its future is pending, so it must NOT zeroize at `verify()`-return; codec `SmppBytes` copies C-octets so a later wipe cannot corrupt the forwarded frame). The adapter owns and zeroizes **its own** secret material on every completion path — access-token bytes and any form-encoded request buffer in `char[]`/`byte[]`, never `String` — including the use-after-close hardening: `try/catch(RejectedExecutionException)` → release permit + zeroize (deferred-work §2.1 item 2). `NoStringFromPasswordTest` (CODEC-024 P2 scan, already rooted on `security/` main) passes over the new adapter source unchanged, with a fresh mutation control proving it bites on the adapter. *(AD-10(3), AD-12 secret-hygiene; PRIV-1; deferred-work §2.1 items 2–3.)*

7. **[AC7] Provider link TLS — https-only, AD-13/26 trust posture, AD-34 intersection, RFC 8705.** The proxy→provider link uses a single shared `java.net.http.HttpClient` with an `SSLContext` built from operator config: **trust = a dedicated IdP trust store (file-path-injected, AD-18) with the full AD-13 posture — never JDK `cacerts`, fail-fast on absent/empty/wrong-format/zero-`trustedCertEntry`** (reuse `CompanionConfigValidator.requireTrustStore`'s 5-state check pattern); **client auth = exactly one of** `client_secret` (file-path) **or** mTLS keystore (RFC 8705 `tls_client_auth`, path+password leaf) — exactly-one-of enforced fail-fast. Non-`https` `provider-url` already fails (SEC-053/054) — keep. **At startup, intersect `companion.tls.*` configured suites with the IdP client `SSLContext`'s supported suites; empty intersection → fail-fast** (this discharges deferred-work **D2/AD-34** for the IdP mTLS client context — the ingress/egress SMSC contexts land with story 3.3's TLS work). Startup OIDC discovery of `provider-url` (`/.well-known/openid-configuration`) runs **once at adapter bean initialization** (constructor/init — a refusal surfaces as the standard Spring startup failure, non-zero exit) and is **hard-required**: discovery failure → refuse to start (fail-closed: a misconfigured provider URL is the common case; a provider outage blocks binds either way per A-2). Discovered `issuer` must equal `provider-url` (the fixture's deterministic-issuer property — 2-1 Dev Notes, the unnumbered note following fixture findings #1–#6) — mismatch → refuse. If `grant_types_supported` omits `password` (DAG off-by-default since KC 26.2) → **loud warning, defer to runtime** (grant-type error → DENY per AC2). **Existing full-context tests survive this via a test-support HTTPS stand-in discovery server** (Project Structure Notes): `TestCompanionConfigs.forwardA()/forwardC()` default `provider-url` to the stand-in (live Keycloak tests override with the fixture URL), so the four existing full-context boots (`BootstrapLifecycleTest`, `RelayServerLifecycleTest`, `DirectMemoryBudgetStartupCheckTest`, `CompanionTlsBindingTest`) stay green against a bootable forward cell — no test is weakened or skipped. *(AD-12, AD-13, AD-26, AD-34; ARCHITECTURE-SPINE.md:135–147, 216–219, 260–263; deferred-work 1-3 D2.)*

8. **[AC8] Config surface — structural, fail-fast, 1-3 pattern.** Extend the shared `Oidc` record (`ProxyCompanionProperties.java:200-206`, used by `forward.mode-a`/`forward.mode-c`) — `ignoreUnknownFields=false` means **every new key must land in the record or every boot fails**: `client-id` (`@NotNull`), `provider-url` (existing), `client-secret-path` (`@Nullable`), `client-mtls-keystore.{path,password}` (optional node — path `@NotNull` when the node binds, password `@Nullable` (`KeyStore.load` tolerates null), mirroring the `TrustStore` leaf at `ProxyCompanionProperties.java:282-287`; **replaces the underspecified `client-credential-path`** — rename recorded in the Change Log), `trust-store.{path,password}` (`@NotNull`, AD-13 posture), `timeout` (`@Nullable` Duration → **compact-ctor normalized to `4s` when absent**), validator-enforced inclusive range **2s–5s** per PERF-3, and **≤ `companion.bind.adjudication-deadline`** (cross-field check in `CompanionConfigValidator`, null-safe on both sides — normalize before comparing, never NPE), `max-in-flight` (`@Nullable` Integer → ctor default 64; `@Min(1)` semantics on the normalized value), `jwks-cache-ttl` (`@Nullable` Duration → ctor default, positive). Exactly-one-of `client-secret-path` vs `client-mtls-keystore` in the validator; all file paths through the 4-state readability check (`requireReadableFile`); messages follow `"<key> … — refusing to start (<AD/SEC id>)."`. **Defaulting mechanism (deliberate):** the forward branches are commented out in `application.yml` and single-branch selection forbids shipping them uncommented, so branch-scoped keys **cannot carry yml defaults** — defaults live in the record's compact ctor (normalize absent/`null` → documented default), NOT `@DefaultValue` (1-3 T5b's whole-record-null-semantics discipline) and NOT yml. Tests follow the **null-vs-blank trap** rule: every refusal test binds the exact invalid value through real Spring binding (a removed key proves nothing); add an **omitted-keys default pin** (runner boot without the optional keys asserts the ctor-normalized values — runner boots don't load `application.yml` anyway) and update `TestCompanionConfigs.forwardA/forwardC` builders. The structural single-branch matrix and all existing matrix tests stay green. *(1-3 Dev Notes; SEC-053/060; PERF-3.)*

9. **[AC9] Four-path live validation through the production adapter.** Docker-gated (`@Testcontainers(disabledWithoutDocker = true)` — an environment gate, never a disable-to-pass dodge) live tests drive the **production bean** through the real port against the existing pinned fixture (`quay.io/keycloak/keycloak:26.7.0`, fixed `:8443`, realm `smpp-companions`): (1) JWT happy path → `Allow`; (2) RFC 7662 introspection `active:true` → `Allow`; (3) RFC 8705 mTLS provider auth, **no** `client_secret` (client `smpp-client-mtls`, `x509.subjectdn=CN=smpp-mtls-client`) → `Allow`; (4) ≥1 DENY branch end-to-end (bad user password → `DenyInvalid`) — mirroring `RopcSliceLiveTest`'s five cases. A live Docker-less fail-closed suite (in-process `com.sun.net.httpserver` stand-in IdPs, `RopcSliceFailClosedTest` patterns) covers the AC2 table rows + saturation + cancel + zeroization. *(AD-12 ratification paths 1–4; ARCHITECTURE-SPINE.md:142.)*

10. **[AC10] Gates, mutation pass, clean build.** `NoRolledCryptoArchitectureTest` rules 3–4 (currently `RopcSlice`-specific) widen to pin the **production adapter** to `java.net.http` + `com.nimbusds` (rules 1–2 unchanged: no `sun.security..`/`javax.crypto..`/`java.security.MessageDigest`, no third-party HTTP/JWT stacks); **SEC-090 tightens for Epic 3** with a positive control (in `security/`, `javax.net.ssl` is sanctioned for JDK `SSLContext`/`SSLParameters` use only — no hand-rolled PKIX, no custom `X509TrustManager` logic; `HttpsParameters` remains a `com.sun.net.httpserver` test-tier concern, out of main). No Keycloak SDK, no BouncyCastle, no third-party HTTP client on any classpath (AD-36; `smpp.runtime-purity` + ArchUnit enforce). Standing RED-on-neuter gate (AI-1): every named guard in AC1–AC9 gets a neuter→RED→revert→GREEN control recorded in Completion Notes; source-scan/biters need `clean build` (UP-TO-DATE trap) — final gate `./gradlew clean build :buildSrc:test` GREEN on JDK 25 + `--enable-preview`, **no NEW `// FIXME` introduced by this story** (five owner-FIXMEs pre-exist in `BindInterceptor`/`ProxyCompanionProperties` — leave them for their owners), no test removed/`@Disabled` to pass. *(SEC-4; AD-36; ARCHITECTURE-SPINE.md:270–275; epic-1 retro AI-1/AI-7.)*

## Tasks / Subtasks

> Ordering note: T1 (config) first — `ignoreUnknownFields=false` means nothing boots with half-added keys; the adapter lands T3–T6 against the config; wiring (T7) last before gates. Each task ends with its tests green and a per-task commit when directed (house convention: commit only when asked).

- [x] **Task 1 (AC: 8) — Config surface + validator.**
  - [x] Reshape `Oidc` record per AC8; remove `client-credential-path`; update **every** binding of the old key — the full blast radius: `TestCompanionConfigs.java:48`, `CompanionConfigMatrixTest.java:257`, `BootstrapLifecycleTest.java:85`, `RelayServerLifecycleTest.java:224`, `DirectMemoryBudgetStartupCheckTest.java:232`, `CompanionTlsBindingTest.java:54` (with `ignoreUnknownFields=false`, one missed file = hard boot failure).
  - [x] Validator additions: exactly-one-of client auth; IdP trust-store 5-state; timeout range 2s–5s + ≤ `adjudication-deadline` (null-safe, post-normalization); `max-in-flight`/`jwks-cache-ttl` positivity; message convention. *(Superseded by user directive mid-task — see Completion Notes: annotation-level conformance + file-existence-only deep checks; the timeout window became a yml-documented operator contract.)*
  - [x] Refusal tests binding exact invalid values (null-vs-blank rule); omitted-keys ctor-default pin; all 5 cells' matrix suites green. *(The "omitted-keys ctor-default pin" is moot — the budget keys are required `@NotNull` (user directive); absent-key refusals replace the pin.)*
  - [x] Test-support HTTPS stand-in discovery server (AC7): `com.sun.net.httpserver.HttpsServer` + `HttpsConfigurator` reusing the fixture's server cert/key (`proxy/src/test/resources/keycloak/certs/`), serving a minimal discovery document whose `issuer` echoes its own base URL; `TestCompanionConfigs.forwardA()/forwardC()` default `provider-url` to it (lazy-shared, free-port-probed). *(Superseded by the AD-12 amendment mid-task — see Completion Notes: `forwardA()/forwardC()` bind no oidc keys post-amendment; the stand-in is consumed by `reverseA()/reverseB()/reverseC()` via `oidcKeys()` + the two reverse mode-b boot builders + `RelayTestFixtures.testOidc()`.)*
- [ ] **Task 2 (AC: 7) — IdP SSLContext factory + AD-34 intersection + startup discovery.**
  - [ ] `security/` component: trust store + optional client keystore → `SSLContext` (JDK only); startup cipher/protocol intersection vs `companion.tls.*` → empty = fail-fast (D2 discharge for the IdP context).
  - [ ] Discovery at adapter bean init (fail-closed refuse on failure), issuer==provider-url check, `grant_types_supported` DAG warning; the four full-context boots run against the T1 stand-in.
- [ ] **Task 3 (AC: 2, 5) — Adapter core: pool, admission, STS fan-out, ROPC call, verdict mapping.**
  - [ ] Bounded VT `ExecutorService` + semaphore admission (fail-closed saturation, F2 ordering); `RejectedExecutionException` hardening (AC6).
  - [ ] `HttpClient` request build with raw-byte password encoding (F1 rule); response → AC2 table.
  - [ ] STS JEP 505 `open(Joiner)` fan-out; `ScopedValue` capture-on-caller + re-bind on pool thread (AC5 capture rule); scope shuttable from teardown.
  - [ ] Append the AC2 supersession entry (refined 4xx mapping) to the architecture `.memlog.md`.
- [ ] **Task 4 (AC: 3) — Nimbus JWT defense-in-depth + JWKS cache.**
  - [ ] Cached-`JWKSet` verify: signature/iss/aud/exp/nbf + `typ` (fixture-verified literal); kid-miss → deny + background refresh (no foreground retry).
  - [ ] Refresh executor (AD-28(2) hand-rolled VT, not `@Scheduled`); whole-swap-only cache invariant (§2.1 item 6b). **Item 6a — refresh bounding:** the background refresh runs on the AD-28(2) scheduler and **never routes through the adjudication pool or its admission semaphore** (a single-flight scheduler task is its own bound); it must neither consume bind-admission permits (bind saturation must not block kid-rotation refresh) nor be starvable by binds.
- [ ] **Task 5 (AC: 4) — Opaque-token introspection fallback.**
  - [ ] Discovery-derived endpoint; F3 active-call registration; AC2 introspection rows.
- [ ] **Task 6 (AC: 5, 6) — Cancellation + zeroization.**
  - [ ] `cancelHttp()` → `sendAsync(...).cancel(true)` + STS shutdown + **guaranteed settlement `DenyIndeterminate`** (F7 decision) — `RecordingHttpClient` test.
  - [ ] Token/request-buffer zeroization on every completion path; `NoStringFromPasswordTest` green over adapter source + fresh mutation control.
- [ ] **Task 7 (AC: 1) — Spring DI wiring + lifecycle.**
  - [ ] Security-side `@Configuration`: forward cells → ROPC adapter, reverse cells → `AlwaysAllow` (lose its `@Component`; update its test); exactly-one wiring test per cell.
  - [ ] `SmartLifecycle stop()`: DENY-in-flight (`shutdownNow()` + `awaitTermination` + fail-closed log on timeout — §2.1 item 5), stop JWKS refresh **before** cache close (AD-22 ordering), all inside `security/` (no `bootstrap/` changes). **Phase discipline:** `getPhase()` must be strictly **below** `RelayServerLifecycle.RELAY_ACCEPTOR_PHASE` (`APP_PHASE+1000`) — Spring stops higher phases first, so the acceptor stops BEFORE the adjudicator (AD-22 step order: acceptor → deny in-flight → … → JWKS refresh → cache close); `APP_PHASE` is the natural slot. `isRunning()` pinned; `stop()` idempotent (house pattern from `RelayServerLifecycle`).
- [ ] **Task 8 (AC: 10) — Gates widened.**
  - [ ] `NoRolledCryptoArchitectureTest` rules 3–4 → production adapter; SEC-090 tightening + positive control.
- [ ] **Task 9 (AC: 9) — Live Keycloak suite (production adapter).**
  - [ ] Docker-gated 4-path suite mirroring `RopcSliceLiveTest`; keep `RopcSlice*` tests green (reference slice stays).
- [ ] **Task 10 (AC: 10) — RED-on-neuter mutation pass + clean build + CR.**
  - [ ] Named-guard mutation ledger (exception-safe `finally` cleanup in biters; unique `/tmp` backup paths; marker-grep zero residue; `clean build` after restores).

## Dev Notes

### Architecture compliance (MUST follow)

- **AD-12 (ROPC adjudication, spine:135–142)** — token-endpoint status 200/401 **is** the verdict; local JWT verify is defense-in-depth (AD-11 precedence, DENY wins); **discard the token, relay the ORIGINAL bind** (the adapter never substitutes the verdict for the SMSC's own check — the relay already forwards `originalFrame()` verbatim); **re-validate every bind — no verdict cache, cache JWKS only; introspection never cached**. Port is immutable since AC8 (2026-08-10): `VerdictRequest verify(BindCredential, ScopedValue<RequestContext>)`, `future()`/`cancelHttp()`, sealed `Verdict permits Allow, DenyInvalid, DenyIndeterminate`, **no Throwable/Nimbus/free-form-reason type crosses the port** — the adapter absorbs Nimbus.
- **AD-11 (fail-closed, spine:130–133)** — every auth-adjacent decision denies on indeterminate; AC2's table is the exhaustive instantiation. Unknown `system_id` → DENY (routing-miss) is **relay/story-3.3 territory** — NOT this story.
- **AD-4 / AD-28(4) (no blocking on event loops; bounded VT pool)** — all adapter I/O off event loops; single bounded hand-managed VT pool owned by `security/`, config-sized, fail-closed on saturation; **never `@Async`, never a VT-per-bind outside the pool, never `CallerRunsPolicy`**.
- **AD-5 (STS + ScopedValue, spine:87–90)** — STS structures the fan **within one adjudication**; the pool gates entry — STS does not replace bounding. Preview confinement: STS lives in the control plane (`security/` + `bootstrap/`) only. JDK 25 STS = JEP 505 fifth preview (`open(Joiner)` API — see AC5); ScopedValue is final (JEP 506). Pin risk is absorbed: `smpp.java-conventions` already runs `--enable-preview` process-wide and 2.2 T1's compiler gate enforces it.
- **AD-10(3) / PRIV-1 (credential-free at rest)** — runtime holdings are exactly: OIDC client credential (file-path), cached JWKS public keys, per-bind password in memory during adjudication+forward (never persisted/logged/cached), own TLS keys. Zeroization events: adjudication completion, connection teardown, JVM shutdown, exception paths.
- **AD-13 / AD-26 (trust posture)** — IdP trust store never `cacerts`; PKIX defaults, no custom chain-validation code, no `PKIXBuilderParameters` overrides.
- **AD-22 / AD-28(2) (shutdown ordering, spine:196–199, 226–229)** — stop JWKS refresh **before** closing the JWKS cache; DENY in-flight adjudications on shutdown; pool drain = `shutdownNow()` + await with fail-closed log.
- **AD-25 (teardown race, spine:214)** — case-3 teardown shuts the adjudication's STS and calls `cancelHttp()` (the relay's `BindInterceptor.teardownForPreCoupleViolation` + `cancelAndWipePending` already do this from the caller side — `BindInterceptor.java:348-359`); a late verdict reaching a torn-down entry is a no-op relay-side. The adapter's settlement guarantee (AC5/F7) makes the "late verdict" window settle cleanly.
- **AD-27 (observer contract)** — `onBindReject` fires only for actual `Verdict` values returned by the verifier (relay-side, `BindInterceptor.java:314`); the adapter changes nothing here — but it must never return `null` from `verify()` and never complete `future()` exceptionally-as-normal-flow (relay treats exceptional/null as fail-closed deny — `BindInterceptor.java:258-280`; the adapter settles every future with a `Verdict`).
- **AD-33 (deny wire collapse)** — already shipped by 2.2 (`ESME_RBINDFAIL 0x0000000D` header-only `bind_*_resp` + close, `BindInterceptor.java:110, 333-345`, identical across all denial arms). **This story does not touch it**; the adapter's rich `DenyInvalid`/`DenyIndeterminate` distinction surfaces only in logs/metrics (Epic 4 impl) and never on the wire.
- **AD-34 / D2 (cipher allowlist)** — AC7's startup intersection for the IdP client context; SMSC-leg/ingress contexts → story 3.3.
- **AD-36 (adapter stack, spine:270–275)** — plain OIDC over JDK `java.net.http.HttpClient` + Nimbus local verify; **no `keycloak-authz-client`/`keycloak-admin-client`/any vendor SDK** (drags JAX-RS/RESTEasy → banned by `smpp.runtime-purity`; no per-request cancel handle; obstructs zeroization). "The production adapter (Epic 3) stays on this same HttpClient+Nimbus stack the test slice ratifies."
- **AD-18 (secrets)** — every credential/token is a **file path** in config, read at startup, never inline values.

### Seeded contract surfaces (as shipped — verify exact signatures in code before use)

```java
// proxy/src/main/java/smpp/companion/proxy/security/ (Story 2.1/2.2 — UNCHANGED by this story)
interface BindCredentialVerifier { VerdictRequest verify(BindCredential cred, ScopedValue<RequestContext> ctx); }
sealed interface Verdict permits Verdict.Allow, Verdict.DenyInvalid, Verdict.DenyIndeterminate { }  // 3 payload-less records
interface VerdictRequest { CompletableFuture<Verdict> future(); void cancelHttp(); }  // cancelHttp idempotent
record BindCredential(SystemId systemId, Password password) { }  // compact-ctor null-guards; toString redacts
record SystemId(AsciiString value)  { /* MAX_LENGTH = 15; non-secret (AD-14) */ }
record Password(AsciiString value)  { /* MAX_LENGTH = 8; zeroize() wipes shared backing array; toString = Password[***] */ }
record RequestContext(SystemId systemId, ChannelId channelId, Instant deadline) { }
@Component final class AlwaysAllowBindCredentialVerifier implements BindCredentialVerifier { }  // AC1 re-wires this
```

Caller side (READ ONLY — do not modify): `BindInterceptor.java:242-244` builds the credential + deadline (`companion.bind.adjudication-deadline`, default 4s — **the verifier's budget**; relay arms no timeout of its own, F14 open, relay-side); `:256-257` binds `ScopedValue` and calls `verify()`; `:283-286` hops the continuation to the ingress event loop (AD-25 re-check); `:294-323` verdict mapping; `:319-322` continuation `finally` zeroizes the password; `:348-359` `cancelAndWipePending()`.

### Library / framework specifics

- **Nimbus JOSE+JWT 10.9.1** — already pinned in `proxy/build.gradle.kts` (`com.nimbusds:nimbus-jose-jwt:10.9.1`; dependency-floor 10.0.2 wired into `check`). Latest upstream is 10.9.1 — **no version change needed; do not churn it.**
- **JDK `java.net.http.HttpClient`** — one shared client per adapter (connect timeout from config, per-request `Timeout` from `oidc.timeout`); `sendAsync` + `cancel(true)` is the ONLY sanctioned abort path (2-1 JDK-25-source-verified). `isCancelled()` is false by design; assert `CancellationException` **anywhere in the cause chain**.
- **JDK 25 StructuredTaskScope (JEP 505)** — `StructuredTaskScope.open(Joiner)`, `Joiner.awaitAllSuccessfulOrThrow()`, `fork`/`join`/`close`/`isCancelled`; `Configuration` carries name/timeout/thread-factory. **The `new StructuredTaskScope.ShutdownOnFailure()` pattern in most training data does not compile on JDK 25.** Web-verified: [JEP 505](https://openjdk.org/jeps/505).
- **Spring Boot 4.1.0** (proxy), Gradle 9.6.1, JDK 25 toolchain, Lombok via `io.freefair.lombok` 9.5.0 (`@RequiredArgsConstructor` idiom is house style). Testcontainers 2.x via SB's `testcontainers-bom` — the JUnit module is `testcontainers-junit-jupiter` (renamed from `junit-jupiter` in 2.x). jqwik/Jazzer exist **only in codec** — do not add them to proxy.
- **Null-safety**: `@NullMarked` is per-package — new sub-packages under `security/` need their own `package-info.java` (1-4 finding). NullAway 0.13.8 ERROR-severity on `compileJava` only (`AnnotatedPackages=smpp.companion`, `JSpecifyMode=true`); zeroized secret fields are `@Nullable` (nulled after use — 1-4 precedent); `@SuppressWarnings("NullAway")` only with `// reason:` comment. Expect ErrorProne `FutureReturnValueIgnored` on bare `close()`/`cancel` returns — class-level suppression with reason, 2-2 precedent.
- **Keycloak fixture facts (verified, 2-1)**: DAG is **per-client and off by default since KC 26.2** — realm JSON enables it on both fixture clients; `KC_HTTPS_CLIENT_AUTH=required` (NOT `request` — peer cert must reach `client-x509`); KC 26 uses `x509.subjectdn` + `x509.casubjectdn`; User Profile requires email/firstName/lastName or ROPC fails "Account is not fully set up"; fixed `:8443` + `KC_HOSTNAME=localhost` gives a deterministic issuer (sidesteps keycloak#49967 `iss`-reflection).

### Verdict mapping — normative quick table (AC2)

| Token endpoint outcome | Verdict |
|---|---|
| 200 + locally-verifiable JWT (defense-in-depth passes) | `Allow` |
| 200 + opaque token → introspection 200+JSON+`active:true` | `Allow` |
| 401 · 400 with `error` ∈ {`invalid_grant`, `invalid_client`} | `DenyInvalid` |
| 200 non-JWT/HTML/empty · malformed JWT · 3xx · any other 4xx (403/404/429, 400 other error) · 5xx · timeout · network error | `DenyIndeterminate` |
| 200 JWT but sig/iss/aud/exp/nbf/typ fail · kid-miss | `DenyIndeterminate` (DENY wins; background JWKS refresh, no foreground retry) |
| Introspection anything ≠ 200+JSON+`active:true` (incl. `active:false`) | `DenyIndeterminate` |
| Admission saturation · `cancelHttp()` · scope/executor shutdown | `DenyIndeterminate` (saturated: **no wire call started**) |

### Previous-story intelligence (2-1 → this story, directly)

- **`RopcSlice` is the ratified reference** — productionize its shape, do not redesign. Six LOW items to fix in production (all open in deferred-work §2.1): (1) 429/404/403 mapping → AC2 table; (2) use-after-close permit leak → `try/catch(RejectedExecutionException){release(); zeroize();}`; (3) JWT `typ` validation (RFC 8725 §3.9) → AC3; (5) `close()` drain `awaitTermination` + fail-closed log → T7; (6a) `asyncRefreshJwks` bypasses the admission gate → refresh lives on the AD-28(2) scheduler, never the adjudication pool (Task 4); (6b) unconditional cache-null → whole-swap-only (AC3). Item (4) `AlwaysAllow` zeroization is RESOLVED caller-owned (2.2 T7) — **do not re-litigate; the adapter must NOT zeroize at `verify()`-return**.
- **2-1 review fixes the production adapter MUST keep**: F2 (sendAsync only after `tryAcquire`), F3 (introspection registered in active-call for cancel), F5 (`requireNonNull` compact-ctor fail-fast), F6 (JWKS fork only when the token is a JWT, not on the introspection path), F12 (`Objects.requireNonNull(ctx, "ctx")`).
- **2-1 test assets to reuse, not duplicate**: `KeycloakContainer`/`KeycloakFixture` (fixture constants: `CLIENT_A_ID=smpp-client-confidential`/secret, `CLIENT_B_ID=smpp-client-mtls` no-secret, `TEST_USER=testuser`/`testpass`, endpoints under `https://localhost:8443/realms/smpp-companions`), in-process `com.sun.net.httpserver` stand-in IdPs (`RopcSliceFailClosedTest`), `RecordingHttpClient` delegating wrapper (`RopcSliceCancelTest`), forged-JWT helpers (`RSAKeyGenerator`/`RSASSASigner`, `RopcSliceUnitTest`). Keep `RopcSlice*` green as the contract reference; production tests live beside them.
- **2-2 facts the adapter depends on**: the relay already handles exceptional/null futures fail-closed (`BindInterceptor.java:258-280`) — the adapter settles every future with a `Verdict` instead; caller-owned zeroization sites are `:319-322` + `:348-359`; `LatchedBindCredentialVerifier` (relay test fake) shows the fake-verifier idiom if needed.
- **Sprint-status bookkeeping drift (cite, don't re-do)**: action items AI-3 (Keycloak fixture), AI-4 (contract types), AI-5 (password seam/CODEC-024 P2) are still marked `open` in sprint-status.yaml but were completed by 2.1/2.2.

### Codified test traps (epic-1 retro AI-7 — apply by default)

(a) **null-vs-blank**: refusal tests bind the exact invalid value through real Spring binding — removing a key proves nothing (killed 14/33 config guards in 1.3); (b) gate-must-run-in-`check`: any new Gradle gate needs a TestKit positive control and must be wired into `check`; (c) jqwik rules are codec-only (N/A here); plus: RED-on-neuter biters release latches/resources in `finally` (exception-safe cleanup — a stranded in-process IdP handler hangs the test JVM); unique `/tmp` backup paths per mutation; marker-grep proves zero mutation residue; `clean build` after restores (source-scan tests are UP-TO-DATE-skipped on incremental runs); full-context boots need `TestCompanionConfigs.common()` (memory budget `1/1/1.0`, `adjudication-deadline=4s`, TLS lists, free port — runner boots don't load `application.yml`); `SpringApplicationBuilder.properties()` is lowest precedence — overrides go as `run("--key=val")` args; console counts lie — trust the XML.

### External research (2026-08-18, web-verified)

- **Keycloak 26.7.0** (GA 2026-07-09) is the latest 26.x — matches the fixture pin and Story 3.1's verdict (viability MET, ≥26.7.0, re-validate each minor; no LTS, minors EOL ~3 months). **ROPC removal-watch is live**: [deprecation-path discussion keycloak#24585](https://github.com/keycloak/keycloak/discussions/24585), OAuth 2.1 omits ROPC, RFC 9700 §2.4 "MUST NOT", Keycloak docs discourage DAG, and RFC 7523 JWT Authorization Grant (KC 26.5+) is the emerging replacement. **CVE-2026-9792** (medium; ROPC client-policy bypass; fixed ≥26.7.0/26.6.3/26.4.13) — the spine already records it as "unverified signal, not asserted"; the ≥26.7.0 pin covers it. v1 stays **explicitly ROPC-conditional** (PRD §11) — the deployment-guide disclosure is Epic 6 (OPS-1), NOT this story.
- **Nimbus JOSE+JWT 10.9.1** is current — no change.
- **JEP 505 fifth preview** — `open(Joiner)` API shape as above; [JEP 505](https://openjdk.org/jeps/505), [Rock the JVM JDK-25 guide](https://rockthejvm.com/articles/structured-concurrency-jdk-25).

### Out of scope (explicitly — do NOT expand into these)

- **Forward-role acceptor wiring + `BindInterceptor` role-split + routing-miss deny arm + per-target `tlsContextId` + `RelayServerLifecycle` acceptor widening** — story 3.3 (relay/ changes; owner-deferred "execute the split when Epic 3's forward role actually lands").
- **TLS modes A/B/C on the SMPP legs, ingress TLS, per-egress-context cipher intersection beyond the IdP context, wildcard-listener/connection-cap hardening (F13)** — story 3.3.
- **Relay-side F14 deadline arm, F10 `CONNECT_TIMEOUT_MILLIS`, F1 ingress-leg `bind_resp` drop, the relay owner-refactor cluster (couple-vocabulary, `RelayHandler` split, `ConnectionEntry` state manager)** — relay/ untouched by this story.
- **Structured-JSON logs + metrics impl (AD-27 impl, Verdict-type counters), AD-22 full shutdown body** — Epic 4.
- **PERF-3 p99 measurement harness** — Epic 6 (this story only honors the timeout budget).
- **ROPC fallback DECISION** — dormant (probe MET); deployment-guide ROPC-conditional disclosure — Epic 6.

### Risk notes

- **Blast radius, honestly stated**: reverse cells are unchanged (acceptor is reverse.mode-b-only today, `RelayServerLifecycle.java:70-82`, and keeps `AlwaysAllow`). Forward cells DO gain one startup behavior: a hard-required discovery probe at boot (AC7 — fail-closed by design) plus the adapter bean, though no acceptor feeds it binds until 3.3. The four existing full-context forward boots survive via the T1 stand-in discovery server — verified green in T1/T2, not weakened or skipped. AC9's live/port tests are the story's end-to-end proof. All existing tests must stay green — the File List proves the blast radius.
- **`client-credential-path` rename** is a config-surface break — acceptable pre-release (no deployments); the six binding sites enumerated in Task 1 are updated in the same task, never via a transition shim.
- **`AsciiString.toString()` immortal-cache hazard**: one stray `toString()` on `Password.value()` leaks the secret permanently — the `NoStringFromPasswordTest` scan + F1 raw-byte rule are the guards; never "temporarily" log the password even in tests' failure paths.
- **STS preview shape**: if the compiler rejects `ShutdownOnFailure`-style code from memory, that is expected — use the AC5 API. Do not "fix" it by pinning an older API or suppressing preview flags.

### Project Structure Notes

- **NEW** — `proxy/src/main/java/smpp/companion/proxy/security/`: the adapter, its wiring `@Configuration`, JWKS cache/refresh component, IdP `SSLContext` factory (naming: follow the port's noun style — `RopcBindCredentialVerifier` suggested; spine's walkthrough term is `BindAdjudicator` — pick one, use it consistently).
- **NEW (test)** — `proxy/src/test/java/smpp/companion/proxy/testsupport/` (or `security/`): the HTTPS stand-in discovery server (T1) + production-adapter test suites (unit fail-closed/cancel/zeroization; Docker-gated live 4-path).
- **MODIFIED (main)** — `proxy/src/main/java/smpp/companion/proxy/config/ProxyCompanionProperties.java` (Oidc record + new leaves), `config/CompanionConfigValidator.java` (deep checks). `application.yml` only if a comment/doc line needs updating — branch-scoped defaults do NOT go in yml (AC8 defaulting mechanism).
- **MODIFIED (test)** — the `client-credential-path` rename blast radius: `config/TestCompanionConfigs.java:48`, `config/CompanionConfigMatrixTest.java:257`, `bootstrap/BootstrapLifecycleTest.java:85`, `relay/netty/RelayServerLifecycleTest.java:224`, `relay/netty/DirectMemoryBudgetStartupCheckTest.java:232`, `config/CompanionTlsBindingTest.java:54`; plus `security/AlwaysAllowBindCredentialVerifierTest` (@Component assertion), `security/NoRolledCryptoArchitectureTest` (rules 3–4), matrix suites for the new refusal cases. `proxy/build.gradle.kts` only if a test dep is genuinely missing (expect none).
- **DO NOT TOUCH (main)** — `proxy/relay/**`, `proxy/observability/**`, `proxy/bootstrap/**`, `codec/**`, all AD-12 port types in `security/`.
- **Test fixtures** — reuse `proxy/src/test/.../security/Keycloak*` + `resources/keycloak/` as-is; new production-adapter tests live in the same package; Docker-gated classes carry `@Tag("integration", "security", "p1")` + `@Testcontainers(disabledWithoutDocker = true)`; unit suites `@Tag("unit", "security", "p1")`.
- **Config keys** are kebab-case under `companion.`; the AD/SEC id goes in the refusal message.

### References

- [Source: ARCHITECTURE-SPINE.md#AD-12 (135–142) — ROPC rule, port contract, ratification AC8]
- [Source: ARCHITECTURE-SPINE.md#AD-11 (130–133), #AD-5 (87–90), #AD-28 (226–229), #AD-22 (196–199), #AD-25 (214), #AD-33 (255–258), #AD-34 (260–263), #AD-36 (270–275), #AD-10 (125–128), #AD-13 (144–147), #AD-26 (216–219); Accepted-Risk Register (288–296 — ROPC hard-dependency + 3.1 verdict + CVE-2026-9792 note)]
- [Source: prd.md §6.2/6.3 (FR-SEC-1..5, FR-AUTH-1..4), §7 (SEC-1/3/4/5, PERF-3, PRIV-1, COMP-2), §8 (modes), §10 (A-2), §11 (accepted risks)]
- [Source: epics.md §Epic 3 (369–378 — scope, fallback tree, package ownership); epics.md:286 (3.1 verdict)]
- [Source: 2-1-security-port-contract-validation-slice.md — fixture findings #1–#6 + the unnumbered deterministic-issuer note, review fixes F2/F3/F5/F6/F12, F1 raw-byte mandate]
- [Source: 2-2-stateless-relay-and-a1-session-affinity-smoke.md — caller-owned zeroization, AD-33 collapse as shipped, F7 deferral]
- [Source: deferred-work.md — §2.1 items 1/2/3/5/6; 1-3 D2; 2-2 F7; SEC-090]
- [Source: proxy/src/main/java/smpp/companion/proxy/relay/BindInterceptor.java (110, 242–244, 256–257, 283–286, 294–345, 348–359)]
- [Source: RFC 6749 §4.3 (ROPC), RFC 7662 (introspection), RFC 8705 (mTLS client auth), RFC 8725 §3.9 (typ), RFC 9700 §2.4; JEP 505 (STS fifth preview), JEP 506 (ScopedValue final), JEP 444 (VT)]
- Web (2026-08-18): [keycloak#24585](https://github.com/keycloak/keycloak/discussions/24585), [JEP 505](https://openjdk.org/jeps/505), [Nimbus 10.9.1](https://connect2id.com/products/nimbus-jose-jwt)

## Dev Agent Record

### Agent Model Used

GLM 5.2 (Claude Code harness, 2026-08-18).

### Debug Log References

- `./gradlew :proxy:compileJava :proxy:compileTestJava` — GREEN (one pre-existing `UnusedVariable` warning in `RelayHandler.java:333`, one pre-existing `ArrayRecordComponent` warning in `JsmppSmscServer.java:355`; both untouched by this story).
- `./gradlew :proxy:test` — **226 tests, 0 failures, 0 errors, 0 skipped** (XML-verified; includes `RopcSlice*` reference suites and the six blast-radius classes: `CompanionConfigMatrixTest` 67, `CompanionTlsBindingTest` 7, `CompanionRoleFailFastTest` 3, `BootstrapLifecycleTest` 2, `RelayServerLifecycleTest` 5, `DirectMemoryBudgetStartupCheckTest` 6). *(Pre-amendment count — the same-day AD-12 amendment added `amendment_strayForwardOidcKeyRefuses` → 227; the 2026-08-19 RFC 8705 removal (−2/+1) and review patch pass (+4) re-baselined the suite to 230 — see Change Log.)*
- `./gradlew :proxy:check` — GREEN.
- RED-on-neuter spot mutations (T1 guards; full consolidated pass is T10): **MUTANT-1** removed the `timeout` component's `@NotNull` → `ac8_absentOidcBudgetKeyRefuses[timeout]` RED (the max-in-flight/jwks-cache-ttl cases stayed green — per-key bite); **MUTANT-2** neutered `requireReadableFile(oidc.trustStore().path(), …)` → `ac8_idpTrustStoreMissingFileRefuses` RED. Both restored from unique `/tmp` backups, `grep -rn "MUTANT" proxy/src/` = zero residue, full `:proxy:test` re-run GREEN (226/0).

### Completion Notes List

- **Config surface shipped (T1).** `Oidc` reshaped to 8 components: `provider-url`, `client-id`, `client-secret-path` (nullable), `client-mtls-keystore` (nullable node, new `ClientMtlsKeystore` record mirroring the `TrustStore` leaf), `trust-store` (@NotNull, reused `TrustStore` record), `timeout`, `max-in-flight` (@NotNull + `@Min(1)`), `jwks-cache-ttl`. `client-credential-path` removed; blast radius updated in all 7 enumerated files **plus `CompanionRoleFailFastTest.java:63`** (direct Oidc construction the enumeration missed) and the two `application.yml` comment examples.
- **USER-DIRECTED DESIGN AMENDMENTS (5 — 1–4 applied mid-task, 5 applied post-review 2026-08-19; they supersede the corresponding AC8/task text above, which is left as-written per story-file rules):**
  1. *Budget keys required, not ctor-defaulted:* `timeout`/`max-in-flight`/`jwks-cache-ttl` are NON-NULL `@NotNull` components with NO compact-ctor normalization and NO public default constants. Defaults live in `application.yml`'s forward template (uncommenting a branch yields working `4s`/`64`/`5m`) — the `companion.bind.adjudication-deadline` pattern. Structural note recorded in the record javadoc: live yml defaults for branch keys are impossible (any uncommented `companion.forward.*` key binds the Forward branch for every cell → every reverse boot refuses "found 2"), so yml is the documentation surface and the annotations are the enforcement.
  2. *Validator = file existence only:* `requireOidc` keeps the pre-existing provider-url https/blank checks and adds `requireReadableFile` for `client-secret-path`, `client-mtls-keystore.path`, `trust-store.path`. NO exactly-one-of pair check, NO IdP trust-store 5-state PKIX load, NO `jwks-cache-ttl` positivity in the validator — conformance is annotation-level; the exactly-one-of resolution and the store/keystore PKIX loads move to the T2+ adapter's SSLContext build (a bad store refuses startup at bean init, fail-closed). **T2 must pick these up** (AC7's "exactly-one-of enforced fail-fast" now lands at the adapter factory, not the config validator).
  3. *Timeout window not validated:* the 2s–5s PERF-3 bounds and the ≤ `adjudication-deadline` relation are documented as the `OIDC_TIMEOUT` comment in `application.yml` — an operator contract, deliberately unenforced (no validator code, no cross-field check).
  4. *`max-in-flight` described:* full component javadoc (AD-28(4) admission cap; rejection threshold, not a queue depth — saturated = fail-closed `DenyIndeterminate` with no wire call) + one-line yml comment.
  5. *RFC 8705 IdP mTLS client-auth option REMOVED (2026-08-19, user-directed, post-review):* the `client-mtls-keystore` component and `ClientMtlsKeystore` record are deleted from `Oidc`; `client-secret-path` is the sole provider client auth and is now `@NotNull`-required (validator: unconditional file-existence check; yml template updated). Supersedes AC7's "client auth = **exactly one of** `client_secret` … or mTLS keystore", AC8's `client-mtls-keystore` bullet, and AC9's live path 3 — the production live suite becomes **3-path** (JWT / introspection / bad-credentials DENY). The 2-1 test-tier slice stays untouched and green (`RopcSliceLiveTest` path 3 + fixture client `smpp-client-mtls` remain as the historical contract ratification — the slice builds its own wire calls, not through this config surface). Matrix tests −2/+1: deleted `ac8_mtlsKeystoreMissingFileRefuses` + `ac8_mtlsArmedReverseCellBoots`, added `ac8_absentClientSecretPathRefuses` (@NotNull, the absent-budget-key pattern). Two direct-construction sites fixed for the new 7-component arity (`CompanionConfigMatrixTest.reverseAProps`, `CompanionRoleFailFastTest`). `:proxy:test` 226/0/0 (XML), `:proxy:check` GREEN. Review-finding interaction: the F1 patch item's ":237 exactly-one-of" sub-claim is moot (text deleted with the option); F1's remaining javadoc claims (5-state depth, timeout window, defaults, forward-template wording) still stand.
- **New T1 tests (12 in `CompanionConfigMatrixTest`):** absent `client-id` / absent budget keys (×3, @NotNull); `max-in-flight` 0/−3 (@Min, exact invalid values bound); `max-in-flight=1` boundary boots; missing-file refusals for `client-secret-path`, `client-mtls-keystore.path`, `trust-store.path` (bad paths bound, never removed keys); absent `trust-store` node; mTLS-armed forward cell boots (empty keystore file is a valid T1 bind — PKCS12 load is T2+'s). Dropped from the original plan by amendment 2/3: exactly-one-of both/neither, timeout window/boundary/deadline cross-field, ttl positivity, IdP 5-state. `requiredSecretTypes` lost the oidc entry (option-typed now — removal no longer refuses; its missing-file case is `ac8_clientSecretMissingFileRefuses`).
- **HTTPS stand-in discovery server (`OidcDiscoveryStandIn`, testsupport):** lazy-shared singleton (double-checked volatile), free-port-probed loopback `HttpsServer`, `HttpsConfigurator` over the fixture's `server.pem`/PKCS#8 `server-key.pem` (JDK `KeyFactory` PEM parse — no BouncyCastle, AD-36; SEC-090: `com.sun.net.httpserver` is test-tier only), serves `/.well-known/openid-configuration` whose `issuer` echoes its own base URL (the T2 `issuer == provider-url` check), shutdown-hooked. `TestCompanionConfigs.forwardA()/forwardC()` AND the four full-context boots default `provider-url` to it, with `oidc.trust-store` pointing at the fixture CA store (`RelayTestFixtures.idpTrustStoreFixture`) so the same configs stay valid from T2 on, when discovery actually handshakes (the stand-in's cert chains to that CA). Forward cells are bootable without Docker; no test weakened or skipped.
- **Deferred to T2 (now load-bearing there):** exactly-one-of client-auth resolution + PKCS loads at the SSLContext factory; discovery probe consumption of the stand-in; `grant_types_supported` DAG warning.

### File List

**Modified (main):**
- proxy/src/main/java/smpp/companion/proxy/config/ProxyCompanionProperties.java
- proxy/src/main/java/smpp/companion/proxy/config/CompanionConfigValidator.java
- proxy/src/main/resources/application.yml

**Modified (test):**
- proxy/src/test/java/smpp/companion/proxy/config/TestCompanionConfigs.java
- proxy/src/test/java/smpp/companion/proxy/config/CompanionConfigMatrixTest.java
- proxy/src/test/java/smpp/companion/proxy/config/CompanionRoleFailFastTest.java
- proxy/src/test/java/smpp/companion/proxy/config/CompanionTlsBindingTest.java
- proxy/src/test/java/smpp/companion/proxy/bootstrap/BootstrapLifecycleTest.java
- proxy/src/test/java/smpp/companion/proxy/relay/netty/RelayServerLifecycleTest.java
- proxy/src/test/java/smpp/companion/proxy/relay/netty/DirectMemoryBudgetStartupCheckTest.java
- proxy/src/test/java/smpp/companion/proxy/testsupport/RelayTestFixtures.java

**Added (test):**
- proxy/src/test/java/smpp/companion/proxy/testsupport/OidcDiscoveryStandIn.java

**Modified (planning artifacts — AD-12 amendment, same day):**
- _bmad-output/planning-artifacts/architecture/architecture-smpp-companions-2026-07-19/ARCHITECTURE-SPINE.md (AD-12 two-proxy-flow, AD-17 matrix, AD-26)
- _bmad-output/planning-artifacts/architecture/architecture-smpp-companions-2026-07-19/.memlog.md (dated amendment entry)
- _bmad-output/planning-artifacts/epics.md (AD-17:188, AD-12:197 bullets)

- **ARCHITECTURE AMENDMENT (2026-08-18, user-directed, mid-T1): AD-12 adjudication moved to the REVERSE role.** The user's corrected trust model, adopted exactly: `client → forward → (internet) → reverse → SMSC`; the `client→forward` leg is trusted (no TLS/mTLS/IdP — the forward is a trusted-side relay holding NO OIDC material); the **reverse is the sole enforcement point** — it ROPC-adjudicates every bind via the IdP before forwarding anything to the SMSC ("the reverse protects the SMSC"); the modes (A TLS / B plaintext / C mTLS) apply to the **internet leg**; the `reverse→SMSC` leg is **always plaintext** (trusted network); **Mode B has no forward proxy** — the legacy client connects directly to the reverse over the plaintext internet leg and the reverse still adjudicates ("plaintext + ROPC", a strengthening vs the pre-amendment model where Mode B had no OIDC anywhere). Propagated: spine AD-12 two-proxy-flow + AD-17 matrix (both rows) + AD-26 peer wording; epics.md AD-17/AD-12 bullets; architecture .memlog.md dated entry. **T1 reworked same day**: the `oidc` node moved from `forward.mode-a/mode-c` to ALL THREE reverse cells (`@NotNull` on each; forward cells structurally carry no oidc component — a stray `companion.forward.*.oidc.*` key is refused by `ignoreUnknownFields=false`, matrix-pinned by `amendment_strayForwardOidcKeyRefuses`); validator `requireOidc` re-hung on the reverse validators; yml examples moved; all OIDC surface tests re-targeted to `reverse.mode-a`; the mode-b full-context boots now carry the oidc node (reverse cells adjudicate end-to-end); `RelayTestFixtures.testOidc()` added for direct-construction fixtures. **Consequences for the remaining story text (ACs left as-written per story-file rules):** AC1's wiring INVERTS — reverse.* cells → ROPC adapter, forward.* cells → `AlwaysAllow` (lands in T2/T7); the four full-context boots' stand-in dependency moves to the reverse boots (done); AC9's live 4-path suite drives the production bean on a reverse cell. Unchanged: the AD-12 port (AC8-immutable, location-agnostic), the `Oidc` record, the `OidcDiscoveryStandIn`, and all T3–T9 adapter work. Verified: `:proxy:test` 227/0/0 (XML), `:proxy:check` GREEN.

## Change Log

- 2026-08-19 — **Code-review patch pass — all 8 remaining findings applied** (Review Findings all `[x]`): F1 `Oidc` javadoc rewritten to the amended reality (dropped-enforcement claims removed — file-existence only, timeout window = documented operator contract; phantom "Default 4s/64/5m" → "yml reverse template documents … no in-record default"; the promised structural note on why branch keys cannot carry live yml defaults added; the ":237 exactly-one-of" sub-claim was already moot — deleted with the RFC 8705 arm). F2 stale "forward" role wording swept to reverse (3 `@NotNull` messages, the blank provider-url refusal, `requireOidc` javadoc) + the weakened sec054 assertion restored to strict ("provider-url is required for the reverse role"). F4 `@NotBlank` on client-id + blank/whitespace refusal tests (null-vs-blank rule). F5 positivity via `@DurationMin(nanos = 1)` (HV `org.hibernate.validator.constraints.time` — jakarta `@Positive` has no Duration validator (HV000030, empirically 42 red) and HV 9 moved `@DurationMin` out of `constraints`) + `0s`/`-30s` refusal tests. F7 stand-in key-parse `IllegalArgumentException` → contextual `IllegalStateException`. F3 stand-in javadoc re-targeted to the reverse consumers. F9 subtask-4 supersession annotation. F11 226/227 count contradiction annotated at both sites. `:proxy:test` **230/0/0** (XML), `:proxy:check` GREEN. The two new guards (`@NotBlank`, `@DurationMin`) await T10's consolidated RED-on-neuter pass (AI-1).

- 2026-08-19 — **RFC 8705 IdP mTLS client-auth option removed** (user-directed, post-review): `client-mtls-keystore` + `ClientMtlsKeystore` deleted from the `Oidc` record; `client-secret-path` is the sole, now-`@NotNull`-required provider client auth; validator/yml/tests swept (2 direct-construction arity fixes; matrix −2/+1). Recorded as Completion Notes amendment 5. `:proxy:test` 226/0/0 (XML), `:proxy:check` GREEN. Earlier the same day a code review ran (2 layers: Edge Case Hunter + Acceptance Auditor; Blind Hunter skipped per user directive): 1 decision (D1 → full AD-12 amendment sweep) + 8 patches left as action items, 1 defer, 1 dismissed — see Review Findings. Spine/epics orphan references from this removal (AD-10 :128, AD-12 :138, AD-34 :263, risk-register :294, epics :211) pending the same propagation decision as D1. *(Follow-up same day: D1 resolved → full sweep — spine AD-13/AD-20/AD-26 + AD-10/AD-12/AD-34/register + epics AD-12/AD-13/AD-20/AD-26 + the epics AD-20 table row; `.memlog.md` dated entry.)*

- 2026-08-18 — **AD-12 amendment applied + T1 re-targeted** (user-directed "follow my claim exactly"): adjudication moved to the reverse role (sole enforcement point before the SMSC; forward = trusted-side relay with no OIDC; internet-leg modes A/B/C; SMSC leg always plaintext; Mode B = direct client→reverse, still adjudicates). Spine (AD-12/AD-17/AD-26) + epics + .memlog amended in place; the `oidc` config node moved from forward cells to all three reverse cells; matrix/yml/boots/fixtures re-targeted; new structural test refuses any stray forward oidc key. `:proxy:test` 227/0/0, `:proxy:check` GREEN. Full detail in the Dev Agent Record amendment note above.

- 2026-08-18 — Story 3.2 created (status: backlog → ready-for-dev). Ultimate context-engine analysis completed: 4 parallel research agents (codebase recon, spine AD extraction, previous-story intelligence, PRD analysis) + web research (Keycloak 26.7.0/CVE-2026-9792, JEP 505 STS shape, Nimbus 10.9.1). Decomposition decision: adjudicator-first per user selection; TLS modes A/B/C + forward-acceptor wiring → story 3.3. F7 settlement-on-cancel decision pre-ratified in AC5; deferred-work absorption set: §2.1 items 1/2/3/5/6 + D2 (IdP context) + SEC-090. Config note: `oidc.client-credential-path` renamed/replaced by the exactly-one-of auth pair (pre-release, no deployments). Story 3.1 (ROPC viability probe) was executed out-of-band as a sprint action item (2026-08-08, verdict MET) and is intentionally not a story key in sprint-status.yaml. Story status: ready-for-dev.
- 2026-08-18 — **T1 implemented** (AC8 + the AC7 test-support leg): `Oidc` record reshaped (client-id, exactly-one-of client-auth pair, IdP trust-store, three required budget keys), `client-credential-path` removed across the full blast radius (7 enumerated sites + `CompanionRoleFailFastTest`), validator reduced to file-existence checks for the oidc paths, defaults documented in `application.yml`'s forward template incl. the OIDC_TIMEOUT 2s–5s comment. 4 user-directed design amendments recorded in Completion Notes (required non-null budget keys / yml-carried defaults; file-existence-only validation; timeout window as documented contract; maxInFlight description). New `OidcDiscoveryStandIn` HTTPS discovery server; `TestCompanionConfigs` + the four full-context boots now boot forward cells against it. 12 new AC8 tests; `:proxy:test` 226/0/0 (XML-verified; pre-amendment count — the same-day AD-12 amendment's stray-forward-oidc-key test took it to 227, see the amendment entry below); `:proxy:check` GREEN; 2 RED-on-neuter spot mutations confirmed and restored. Status: ready-for-dev → in-progress (T2 next).

## Review Findings

<!-- Code review 2026-08-19: 2 layers (Edge Case Hunter + Acceptance Auditor; Blind Hunter skipped by user
     directive). 20 raw findings → 10 after dedup/triage: 1 decision, 8 patch, 1 defer, 1 dismissed. -->

- [x] [Review][Decision] AD-12 amendment sweep missed residual SMSC-trust wording contradicting "SMSC leg always plaintext" — spine AD-26's first sentence was amended but its remainder keeps "(b) the operator SMSC trust store being the sole gate", "cacerts/public-PKI SMSC trust", "the threat — trusting a public root for the SMSC peer"; spine AD-13 rule text still says "Mode A egress validation of the SMSC server cert (AD-26)" (:147); spine AD-20 still says "SMSC-leg TLS is operator-choice, default off … when on, the SMSC is authenticated via the operator SMSC trust store (AD-26)" (:189); epics.md:199 still says "Mode C client certs AND Mode A egress SMSC validation, AD-26". Under the amended model the SMSC leg is plaintext in every mode — four sites describe validation that cannot happen. Decide: full sweep now vs record-and-defer to story 3.3. — **RESOLVED 2026-08-19 (user: full sweep, folded with the RFC 8705 removal propagation)**: spine AD-13/AD-20 (retitled)/AD-26 + epics AD-13:199/AD-20:204/AD-26:205-206 + the epics AD-20 summary-table row :267 swept in place; dated `.memlog.md` entry records both.
- [x] [Review][Patch] Oidc record javadoc asserts enforcement and defaults the user amendments dropped [proxy/src/main/java/smpp/companion/proxy/config/ProxyCompanionProperties.java:208-258] — claims "the validator enforces exactly-one-of" (:237), "validated at the full 5-state PKIX-load depth" (:239-240), "Validator-enforced to the inclusive window [2s,5s] … AND <= adjudication-deadline" (:241-243): all three are amendment-2/3 droppings (validator is file-existence-only; the timeout window is a documented operator contract per application.yml:94-95). Also "Default 4s/64/5m" on @NotNull-required keys with no defaults anywhere (:244,:253,:257) and "application.yml's forward template ships them" (:216) — the oidc template now lives under reverse. Fix: rewrite the javadoc to the amended reality + add the structural note Completion Notes amendment 1 claims is recorded there (why branch keys cannot carry live yml defaults).
- [x] [Review][Patch] Stale "forward" role wording in live refusal messages + requireOidc javadoc; covering test's assertion relaxed in the same diff to mask it [proxy/src/main/java/smpp/companion/proxy/config/CompanionConfigValidator.java:284-294; ProxyCompanionProperties.java:268-273; CompanionConfigMatrixTest.java:583] — blank provider-url refusal still says "for the forward role (AD-12)" (:294), requireOidc javadoc still says "@NotNull on forward A/C" (:285) while oidc is @NotNull on reverse A/B/C; the three @NotNull messages say "(forward, PERF-3/AD-28(4)/AD-28(2))" (:268,:270,:273); and sec054_blankOidcProviderUrlRefuses was simultaneously narrowed from asserting "provider-url is required for the forward role" to the substring "provider-url is required" — exactly what keeps the stale message green (brushes AC7 "no test is weakened"). Fix: sweep messages/javadoc to reverse + restore the strict assertion against the corrected message.
- [x] [Review][Patch] Blank/whitespace client-id passes the @NotNull-only guard — the codified null-vs-blank trap (epic-1 retro AI-7a) [proxy/src/main/java/smpp/companion/proxy/config/ProxyCompanionProperties.java:262; CompanionConfigValidator.java:291-306] — the only client-id test is the absent-key/null case (CompanionConfigMatrixTest:358-361); a bound "" or " " boots T1 and would send an empty client_id at T2. Fix: @NotBlank on clientId (annotation-level per amendment 2) + a refusal test binding the blank value.
- [x] [Review][Patch] jwks-cache-ttl positivity enforced nowhere although amendment 2 claims "conformance is annotation-level" [proxy/src/main/java/smpp/companion/proxy/config/ProxyCompanionProperties.java:273-274] — @NotNull only: no @Positive, no validator check; zero/negative TTL boots and spins the T2 refresh scheduler. Fix: @Positive + a zero-value refusal test.
- [x] [Review][Patch] OidcDiscoveryStandIn javadoc documents the pre-amendment wiring it was re-targeted away from [proxy/src/test/java/smpp/companion/proxy/testsupport/OidcDiscoveryStandIn.java:34-39,63-65] — "from T2 the forward cells' adapter bean…", "TestCompanionConfigs.forwardA()/forwardC() bases default provider-url here", "use it verbatim as companion.forward.mode-*.oidc.provider-url"; actual consumers (verified) are TestCompanionConfigs.oidcKeys() on reverseA/B/C, the two reverse mode-b boot builders, and RelayTestFixtures.testOidc() — a stray forward oidc key refuses (amendment_strayForwardOidcKeyRefuses). Fix: rewrite to the reverse consumers.
- [x] [Review][Patch] Stand-in private-key parse lets a raw IllegalArgumentException escape with no context on a corrupt/non-PKCS#8 fixture key [proxy/src/test/java/smpp/companion/proxy/testsupport/OidcDiscoveryStandIn.java:141-146] — Base64.getDecoder().decode throws unchecked IAE outside both catch blocks (:91,:134). Fix: catch IllegalArgumentException → IllegalStateException("fixture server key is not raw-base64 PKCS#8", e).
- [x] [Review][Patch] Story Task 1 subtask 4 is [x]-checked with pre-amendment text and, unlike subtasks 2-3, carries no supersession annotation [3-2-oidc-ropc-bind-adjudicator.md:53] — "TestCompanionConfigs.forwardA()/forwardC() default provider-url to it" is false post-amendment (oidc hangs off reverseA/B/C). Fix: annotate like subtasks 2-3.
- [x] [Review][Patch] Story record self-contradicts on the test count: 226 vs 227 [3-2-oidc-ropc-bind-adjudicator.md:201,:249 vs :242] — the amendment's added amendment_strayForwardOidcKeyRefuses (+1) explains the delta; the earlier Debug Log/Change Log bullets were never refreshed. Fix: annotate the 226 entries as pre-amendment counts.
- [x] [Review][Defer] Hostless provider-url ("https://", "https://:8443") passes the scheme-only isHttps check [proxy/src/main/java/smpp/companion/proxy/config/CompanionConfigValidator.java:295-297] — deferred, pre-existing (2.1-era guard this story merely re-hung on the reverse validators); natural home is T2's discovery/HttpClient build.
