---
baseline_commit: 92964ebaf62787a47cb607f7393c2878828d0888
epic: 3
story: 2
story_key: 3-2-oidc-ropc-bind-adjudicator
status: in-progress
---

# Story 3.2: OIDC ROPC bind adjudicator — production adapter behind the AD-12 port

Status: in-progress

> **The Epic 3 production-security slice.** Story 2.1 shipped the AD-12 port types + an always-allow stub in `main` and ratified the contract shape against real Keycloak 26.7.0 with a *test-tier* slice (`RopcSlice`, under `proxy/src/test`). This story ships the **production ROPC adapter** in `proxy/src/main/.../security/`, wired via Spring DI behind the **ratified-immutable port** (AC8, 2026-08-10 — "wire, never re-author"), plus the `companion.*` config surface it needs. **Scope discipline (non-negotiable): `proxy/src/main/.../relay/` is NOT modified** (epics.md §Epic 3: the swap is "behind the unchanged AD-12 port via Spring DI"); the forward-role acceptor widening that would exercise the adapter through a live relay belongs to the NEXT story (3.3). The load-bearing evidence is **AC2** (the AD-11 verdict mapping, incl. the 429/404/403 ledger refinement) and **AC5** (AD-28(4) concurrency + the F7 settlement-on-cancel decision).

## Story

**As a** proxy operator running Keycloak (or any OIDC provider with Direct Access Grants),
**I want** every accepted legacy bind's password-grant adjudicated by a real ROPC call to my provider — with local Nimbus JWKS defense-in-depth, RFC 7662 introspection for opaque tokens, fail-closed on every indeterminate condition, and zero credential bytes surviving the adjudication —
**so that** my proxy tier stays credential-free (FR-SEC-1/AD-10): it brokers and delegates trust instead of holding it, denies closed when the provider is unreachable or ambiguous (FR-SEC-5/AD-11), and never leaks a working carrier credential from a compromise.

## Acceptance Criteria

> Load-bearing: **AC2** (verdict mapping — resolves deferred-work "429/404/403" refinement) and **AC5** (concurrency + F7 settlement decision). ACs cite the spine (ARCHITECTURE-SPINE.md) and existing code by `file:line`; verify signatures in code before use — do not trust this document over the source tree.

1. **[AC1] Production adapter behind the unchanged port, wired by cell.** A production `RopcBindCredentialVerifier` (final class, `smpp.companion.proxy.security`) implements `BindCredentialVerifier` with **zero changes to any port type** (`BindCredentialVerifier`, `Verdict` + 3 permits, `VerdictRequest`, `BindCredential`, `SystemId`, `Password`, `RequestContext` — the AC8-ratified shape; `SecurityPortShapeTest`/`VerdictShapeTest` stay green unmodified). Spring wiring selects exactly one `BindCredentialVerifier` bean per cell: **forward.mode-a / forward.mode-c → ROPC adapter** (the forward role performs OIDC verification, AD-12); **any reverse.* cell → `AlwaysAllowBindCredentialVerifier`** (the reverse role does not adjudicate, AD-12 two-proxy flow). `AlwaysAllow` loses its unconditional `@Component` (its wiring moves to the new security-side configuration; update `AlwaysAllowBindCredentialVerifierTest`'s `@Component` assertion accordingly). A wiring test proves **exactly one** verifier bean of the expected type for each of the five cells via `ApplicationContextRunner`. No main-source file outside `proxy/security/` and `proxy/config/` changes (`relay/` untouched — verified by the File List). *(AD-12; ARCHITECTURE-SPINE.md:135–142; epics.md:377.)*

2. **[AC2] AD-11 verdict mapping — the normative production table.** The token-endpoint call's outcome maps to `Verdict` exactly per this table (this RESOLVES deferred-work "429/404/403 → DenyInvalid misclassification"): `DenyInvalid` is reserved for the provider's **positive invalid-credential signal** — HTTP 401 (a bare 401 from a token endpoint IS a positive auth-layer rejection per RFC 6749 §5.2, even with an unparseable body), or HTTP 400 whose parsed OAuth `error` is `invalid_grant` or `invalid_client` (2-1 fixture finding #6: bad user → 400 `invalid_grant`, bad client → 401 `invalid_client`; its rule is "do not key on 401 **alone**" — bad-user credentials arrive as 400, so map by error semantics: parse the 400 body as JSON and key on the `error` field). **Everything else is `DenyIndeterminate`**: 200-with-non-JWT/HTML/empty body, 200-with-malformed-JWT, 3xx, any other 4xx (incl. 403/404/429 and 400s with other/absent error codes — rate-limit, config and authorization errors are not credential verdicts), 5xx, timeout, network error, cancellation, admission saturation, and scope shutdown. **DENY always wins on disagreement**: a 200 verdict overridden by failed local JWT verification (signature/iss/aud/exp/nbf/typ/kid) → `DenyIndeterminate` (defense-in-depth failures are *unverifiable*, not *invalid* — matches the ratified slice). Introspection: **only** HTTP 200 + JSON + boolean `active:true` → `Allow`; every other outcome (incl. `active:false`) → `DenyIndeterminate`. `kid` absent from cached JWKS → `DenyIndeterminate` + background refresh scheduled; **no foreground refresh-and-retry on the bind path**. Every row of this table has a test with a RED-on-neuter control (bind the exact bad response through the in-process IdP stand-in, never a removed code path). **Supersession note:** this refined mapping intentionally supersedes the shorthand "4xx → `DenyInvalid`" in the spine's AD-12 ratification bullet and the `Verdict.DenyInvalid` javadoc example ("e.g. an IdP 4xx") — the port types stay untouched (AC8-immutable; the javadoc example is stale by design), and the refinement is recorded as a dated entry appended to the architecture `.memlog.md` when T3 lands. Wire impact: none (AD-33 collapses both `Deny*` permits identically); the distinction surfaces only in logs/metrics labels. *(AD-11; ARCHITECTURE-SPINE.md:130–133; deferred-work §2.1 item 1.)*

3. **[AC3] JWT defense-in-depth via cached JWKS (Nimbus).** On a 200 JWT verdict the adapter locally verifies with Nimbus JOSE+JWT (already `com.nimbusds:nimbus-jose-jwt:10.9.1` in `proxy/build.gradle.kts`; floor ≥10.0.2, CVE-2025-53864, enforced by `smpp.dependency-floors`): signature against the **cached** JWKS, `iss` == the configured issuer, `aud` containing the configured client-id, `exp`/`nbf` per clock (NTP precondition A-4), `typ` header per RFC 8725 §3.9 (absent → tolerated, present-but-unexpected → deny — **verify the pinned fixture's actual `typ` value live before hardcoding**; do not trust training data for the literal). The JWKS cache is an `AtomicReference<JWKSet>` swapped **whole** on successful refresh (never nulled before a successful fetch — deferred-work §2.1 item 6b), refreshed by a hand-rolled virtual thread on a `ScheduledExecutorService` owned by `security/` (**not** `@Scheduled`, and **not** through the adjudication pool or its admission semaphore — §2.1 item 6a, Task 4) with TTL/refresh-ahead (config, spine-deferred values → dev-selected defaults with positivity guards). **No verdict cache of any kind; introspection results never cached** (AD-12). *(AD-12; ARCHITECTURE-SPINE.md:135–142; AD-28(2) ibid:226–229.)*

4. **[AC4] Opaque-token fallback = RFC 7662 introspection, cancellation-visible.** When the 200 body is not a locally-verifiable JWT (opaque token), the adapter calls the provider's introspection endpoint (endpoint URIs come from OIDC discovery of `provider-url` — no per-endpoint override keys); the introspection `sendAsync` future is registered in the same active-call slot as the token call so `cancelHttp()` aborts it too (2-1 review fix **F3**). Introspection verdict per AC2's table; never cached. Operators should prefer JWT issuance (introspection is online-only with no offline cryptographic backstop — emit that guidance in the config key's javadoc/docs comment, not runtime noise). *(AD-12; 2-1 Dev Notes F3.)*

5. **[AC5] Concurrency, cancellation, and the F7 settlement decision.** Entry bounding per AD-28(4): a **single bounded hand-managed VT `ExecutorService` owned by `security/`**, sized by `oidc.max-in-flight`, admission via `tryAcquire` semaphore — **a saturated pool returns `DenyIndeterminate` without starting any wire call** (2-1 review fix **F2**: `sendAsync` deferred until AFTER `tryAcquire`); spawning a VT per bind outside this pool is forbidden; never `@Async`. `verify()` never blocks the caller's thread (the relay pins this: `BindInterceptorTest`'s class-level `@Timeout` with `SEPARATE_THREAD` mode is the anti-blocking pin). Inside one adjudication the fan-out uses **JDK 25 StructuredTaskScope (JEP 505, fifth preview)** with the `open(Joiner)` API — `StructuredTaskScope<T,R>`, `static open(Joiner)`, `fork(Callable)`, `join()`, `close()`, `Joiner.awaitAllSuccessfulOrThrow()` — **NOT the JDK-21/22 `new ShutdownOnFailure()` subclass pattern** (2-1 verified the live-JDK-25 shape; also web-confirmed vs JEP 505). `RequestContext` is re-bound on the pool thread **via the same `ScopedValue` handle passed to `verify()`** (`ScopedValue.where(ctx, rc).call(...)`), never `ThreadLocal`. **Capture rule: read `ctx.get()` synchronously inside `verify()` on the caller's thread** — the relay binds `REQUEST_CONTEXT` only for the dynamic extent of the call (`BindInterceptor.java:256-257`), so dereferencing the handle on a pool thread without re-binding throws `NoSuchElementException`; F12's `requireNonNull(ctx)` guards the handle, not bound-ness. The scope is **shuttable from the teardown path** (AD-25: case-3 teardown shuts the adjudication's STS) and `cancelHttp()` binds to `HttpClient.sendAsync(...).cancel(true)` aborting the real wire exchange (2-1 verified `MinimalFuture.cancel(true)` → `MultiExchange.cancel` → `connection.close()`, requires `mayInterruptIfRunning==true`). **F7 DECISION (this story owns it): the production adapter GUARANTEES `future()` settles (with `DenyIndeterminate`) after `cancelHttp()`** — an adapter-level contract that resolves the relay's cancelled-verdict frame-leak (2-2 F7) at the source **without amending the AC8-immutable port** (the port is silent on settlement; promising it is additive). Proven by a test asserting settlement + `CancellationException` in the aborted wire future's cause chain (reuse 2-1's `RecordingHttpClient` delegating-wrapper pattern and the "assert anywhere in the cause chain" rule). *(AD-4, AD-5, AD-25, AD-28(4), AD-32; ARCHITECTURE-SPINE.md:82–90, 214, 226–229, 253; deferred-work 2-2 F7.)*

6. **[AC6] Secret hygiene — raw-byte encoding and token zeroization.** The ROPC form encodes the password **from the raw backing bytes** (`value.array()`/`arrayOffset()`/`length()`), **never `toString()`** — the `AsciiString.toString()` lazy-immortal-String hazard (2-1 F1: the throwaway slice's `pw.toString()` shortcut is FORBIDDEN in production). The **password's `zeroize()` stays CALLER-OWNED** (relay `BindInterceptor` continuation `finally` + `cancelAndWipePending`, settled 2.2 T7 — the adapter reads the password asynchronously while its future is pending, so it must NOT zeroize at `verify()`-return; codec `SmppBytes` copies C-octets so a later wipe cannot corrupt the forwarded frame). The adapter owns and zeroizes **its own** secret material on every completion path — access-token bytes and any form-encoded request buffer in `char[]`/`byte[]`, never `String` — including the use-after-close hardening: `try/catch(RejectedExecutionException)` → release permit + zeroize (deferred-work §2.1 item 2). `NoStringFromPasswordTest` (CODEC-024 P2 scan, already rooted on `security/` main) passes over the new adapter source unchanged, with a fresh mutation control proving it bites on the adapter. *(AD-10(3), AD-12 secret-hygiene; PRIV-1; deferred-work §2.1 items 2–3.)*

7. **[AC7] Provider link TLS — https-only, AD-13/26 trust posture, AD-34 intersection, RFC 8705.** The proxy→provider link uses a single shared `java.net.http.HttpClient` with an `SSLContext` built from operator config: **trust = a dedicated IdP trust store (file-path-injected, AD-18) with the full AD-13 posture — never JDK `cacerts`, fail-fast on absent/empty/wrong-format/zero-`trustedCertEntry`** (reuse `CompanionConfigValidator.requireTrustStore`'s 5-state check pattern); **client auth = exactly one of** `client_secret` (file-path) **or** mTLS keystore (RFC 8705 `tls_client_auth`, path+password leaf) — exactly-one-of enforced fail-fast. Non-`https` `provider-url` already fails (SEC-053/054) — keep. **At startup, intersect `companion.tls.*` configured suites with the IdP client `SSLContext`'s supported suites; empty intersection → fail-fast** (this discharges deferred-work **D2/AD-34** for the IdP mTLS client context — the ingress/egress SMSC contexts land with story 3.3's TLS work). Startup OIDC discovery of `provider-url` (`/.well-known/openid-configuration`) runs **once at adapter bean initialization** (constructor/init — a refusal surfaces as the standard Spring startup failure, non-zero exit) and is **hard-required**: discovery failure → refuse to start (fail-closed: a misconfigured provider URL is the common case; a provider outage blocks binds either way per A-2). Discovered `issuer` must equal `provider-url` (the fixture's deterministic-issuer property — 2-1 Dev Notes, the unnumbered note following fixture findings #1–#6) — mismatch → refuse. If `grant_types_supported` omits `password` (DAG off-by-default since KC 26.2) → **loud warning, defer to runtime** (grant-type error → DENY per AC2). **Existing full-context tests survive this via a test-support HTTPS stand-in discovery server** (Project Structure Notes): `TestCompanionConfigs.forwardA()/forwardC()` default `provider-url` to the stand-in (live Keycloak tests override with the fixture URL), so the four existing full-context boots (`BootstrapLifecycleTest`, `RelayServerLifecycleTest`, `DirectMemoryBudgetStartupCheckTest`, `CompanionTlsBindingTest`) stay green against a bootable forward cell — no test is weakened or skipped. *(AD-12, AD-13, AD-26, AD-34; ARCHITECTURE-SPINE.md:135–147, 216–219, 260–263; deferred-work 1-3 D2.)*

8. **[AC8] Config surface — structural, fail-fast, 1-3 pattern.** Extend the shared `Oidc` record (`ProxyCompanionProperties.java:200-206`, used by `forward.mode-a`/`forward.mode-c`) — `ignoreUnknownFields=false` means **every new key must land in the record or every boot fails**: `client-id` (`@NotNull`), `provider-url` (existing), `client-secret-path` (`@Nullable`), `client-mtls-keystore.{path,password}` (optional node — path `@NotNull` when the node binds, password `@Nullable` (`KeyStore.load` tolerates null), mirroring the `TrustStore` leaf at `ProxyCompanionProperties.java:282-287`; **replaces the underspecified `client-credential-path`** — rename recorded in the Change Log), `trust-store.{path,password}` (`@NotNull`, AD-13 posture), `timeout` (`@Nullable` Duration → **compact-ctor normalized to `4s` when absent**), validator-enforced inclusive range **2s–5s** per PERF-3, and **≤ `companion.bind.adjudication-deadline`** (cross-field check in `CompanionConfigValidator`, null-safe on both sides — normalize before comparing, never NPE), `max-in-flight` (`@Nullable` Integer → ctor default 64; `@Min(1)` semantics on the normalized value), `jwks-cache-ttl` (`@Nullable` Duration → ctor default, positive). Exactly-one-of `client-secret-path` vs `client-mtls-keystore` in the validator; all file paths through the 4-state readability check (`requireReadableFile`); messages follow `"<key> … — refusing to start (<AD/SEC id>)."`. **Defaulting mechanism (deliberate):** the forward branches are commented out in `application.yml` and single-branch selection forbids shipping them uncommented, so branch-scoped keys **cannot carry yml defaults** — defaults live in the record's compact ctor (normalize absent/`null` → documented default), NOT `@DefaultValue` (1-3 T5b's whole-record-null-semantics discipline) and NOT yml. Tests follow the **null-vs-blank trap** rule: every refusal test binds the exact invalid value through real Spring binding (a removed key proves nothing); add an **omitted-keys default pin** (runner boot without the optional keys asserts the ctor-normalized values — runner boots don't load `application.yml` anyway) and update `TestCompanionConfigs.forwardA/forwardC` builders. The structural single-branch matrix and all existing matrix tests stay green. *(1-3 Dev Notes; SEC-053/060; PERF-3.)*

9. **[AC9] Four-path live validation through the production adapter.** Docker-gated (`@Testcontainers(disabledWithoutDocker = true)` — an environment gate, never a disable-to-pass dodge) live tests drive the **production bean** through the real port against the existing pinned fixture (`quay.io/keycloak/keycloak:26.7.0`, fixed `:8443`, realm `smpp-companions`): (1) JWT happy path → `Allow`; (2) RFC 7662 introspection `active:true` → `Allow`; (3) RFC 8705 mTLS provider auth, **no** `client_secret` (client `smpp-client-mtls`, `x509.subjectdn=CN=smpp-mtls-client`) → `Allow`; (4) ≥1 DENY branch end-to-end (bad user password → `DenyInvalid`) — mirroring `RopcSliceLiveTest`'s five cases. A live Docker-less fail-closed suite (in-process `com.sun.net.httpserver` stand-in IdPs, `RopcSliceFailClosedTest` patterns) covers the AC2 table rows + saturation + cancel + zeroization. *(AD-12 ratification paths 1–4; ARCHITECTURE-SPINE.md:142.)*

10. **[AC10] Gates, mutation pass, clean build.** `NoRolledCryptoArchitectureTest` rules 3–4 (currently `RopcSlice`-specific) widen to pin the **production adapter** to `java.net.http` + `com.nimbusds` (rules 1–2 unchanged: no `sun.security..`/`javax.crypto..`/`java.security.MessageDigest`, no third-party HTTP/JWT stacks); **SEC-090 tightens for Epic 3** with a positive control (in `security/`, `javax.net.ssl` is sanctioned for JDK `SSLContext`/`SSLParameters` use only — no hand-rolled PKIX, no custom `X509TrustManager` logic; `HttpsParameters` remains a `com.sun.net.httpserver` test-tier concern, out of main). No Keycloak SDK, no BouncyCastle, no third-party HTTP client on any classpath (AD-36; `smpp.runtime-purity` + ArchUnit enforce). Standing RED-on-neuter gate (AI-1): every named guard in AC1–AC9 gets a neuter→RED→revert→GREEN control recorded in Completion Notes; source-scan/biters need `clean build` (UP-TO-DATE trap) — final gate `./gradlew clean build :buildSrc:test` GREEN on JDK 25 + `--enable-preview`, **no NEW `// FIXME` introduced by this story** (five owner-FIXMEs pre-exist in `BindInterceptor`/`ProxyCompanionProperties` — leave them for their owners), no test removed/`@Disabled` to pass. *(SEC-4; AD-36; ARCHITECTURE-SPINE.md:270–275; epic-1 retro AI-1/AI-7.)*

## Tasks / Subtasks

> Ordering note: T1 (config) first — `ignoreUnknownFields=false` means nothing boots with half-added keys; the adapter lands T3–T6 against the config; wiring (T7) last before gates. Each task ends with its tests green and a per-task commit when directed (house convention: commit only when asked).

- [x] **Task 1 (AC: 8) — Config surface + validator.**
  - [x] Reshape `Oidc` record per AC8; remove `client-credential-path`; update **every** binding of the old key — the full blast radius: `TestCompanionConfigs.java:48`, `CompanionConfigMatrixTest.java:257`, `BootstrapLifecycleTest.java:85`, `RelayServerLifecycleTest.java:224`, `DirectMemoryBudgetStartupCheckTest.java:232`, `CompanionTlsBindingTest.java:54` (with `ignoreUnknownFields=false`, one missed file = hard boot failure).
  - [x] Validator additions: exactly-one-of client auth; IdP trust-store 5-state; timeout range 2s–5s + ≤ `adjudication-deadline` (null-safe, post-normalization); `max-in-flight`/`jwks-cache-ttl` positivity; message convention. *(Superseded by user directive mid-task — see Completion Notes: annotation-level conformance + file-existence-only deep checks; the timeout window became a yml-documented operator contract.)*
  - [x] Refusal tests binding exact invalid values (null-vs-blank rule); omitted-keys ctor-default pin; all 5 cells' matrix suites green. *(The "omitted-keys ctor-default pin" is moot — the budget keys are required `@NotNull` (user directive); absent-key refusals replace the pin.)*
  - [x] Test-support HTTPS stand-in discovery server (AC7): `com.sun.net.httpserver.HttpsServer` + `HttpsConfigurator` reusing the fixture's server cert/key (`proxy/src/test/resources/keycloak/certs/`), serving a minimal discovery document whose `issuer` echoes its own base URL; `TestCompanionConfigs.forwardA()/forwardC()` default `provider-url` to it (lazy-shared, free-port-probed). *(Superseded by the AD-12 amendment mid-task — see Completion Notes: `forwardA()/forwardC()` bind no oidc keys post-amendment; the stand-in is consumed by `reverseA()/reverseB()/reverseC()` via `oidcKeys()` + the two reverse mode-b boot builders + `RelayTestFixtures.testOidc()`.)*
- [x] **Task 2 (AC: 7) — IdP SSLContext factory + AD-34 intersection + startup discovery.**
  - [x] `security/` component: trust store + optional client keystore → `SSLContext` (JDK only); startup cipher/protocol intersection vs `companion.tls.*` → empty = fail-fast (D2 discharge for the IdP context). *(The "optional client keystore" arm is moot — the RFC 8705 option was removed 2026-08-19 (Completion Notes amendment 5): `client_secret` is the sole provider client auth, so the shipped `IdpSslContextFactory` builds a TRUST-ONLY context — no key managers. Everything else shipped as specified: 5-state store load at bean init (the T1 amendment-2 handoff), JDK-default TrustManagerFactory/PKIX, both-axis intersection.)*
  - [x] Discovery at adapter bean init (fail-closed refuse on failure), issuer==provider-url check, `grant_types_supported` DAG warning; the four full-context boots run against the T1 stand-in. *(Shipped as `OidcStartupDiscovery`, a dedicated bean the T3 adapter will constructor-inject — the probe runs at refresh, so the refusal is the same standard Spring startup failure. Field-completeness precedes the issuer equality (order pinned by the missing-endpoint test). Also discharges the deferred hostless-provider-url review finding at the URI build.)*
- [x] **Task 3 (AC: 2, 5) — Adapter core: pool, admission, STS fan-out, ROPC call, verdict mapping.**
  - [x] Bounded VT `ExecutorService` + semaphore admission (fail-closed saturation, F2 ordering); `RejectedExecutionException` hardening (AC6). *(Shipped as `RopcBindCredentialVerifier` — named-VT pool + `oidc.max-in-flight` semaphore, sendAsync only after `tryAcquire`; the use-after-close catch covers `RejectedExecutionException | IllegalStateException` (rejected pool task OR closed shared client) → release permit + abort fired exchange + settle `DenyIndeterminate`. A minimal `close()` shutdown seam (shutdownNow + client close + client-secret wipe) lands now so the hardening is test-reachable; T7's SmartLifecycle stop body wraps it.)*
  - [x] `HttpClient` request build with raw-byte password encoding (F1 rule); response → AC2 table. *(Form built into a `byte[]` via a raw-octet percent-encoder — password AND client-secret encoded from `array()/arrayOffset()/length()`, never `toString()`; bodies arrive as `ofByteArray` (no String materialization of token bytes). Per-request budget = `oidc.timeout` clamped to the remaining `RequestContext.deadline` (≤ 0 → deny with no wire call). The 200 arm fails closed `DenyIndeterminate` pending T4 (JWT verify) / T5 (introspection) — the story's own task ordering; body bytes deliberately unread.)*
  - [x] STS JEP 505 `open(Joiner)` fan-out; `ScopedValue` capture-on-caller + re-bind on pool thread (AC5 capture rule); scope shuttable from teardown. *(T3's fan is the token exchange alone — T4/T5 fork their arms into the same scope; capture-on-caller with fail-closed settle on an unbound handle; re-bind via the same handle wraps the STS so forked subtasks inherit it; `cancelHttp()` → exchange `cancel(true)` → forked join throws → `join()` fails → `DenyIndeterminate` + guaranteed settlement (F7 structure — T6 proves it with `RecordingHttpClient`).)*
  - [x] Append the AC2 supersession entry (refined 4xx mapping) to the architecture `.memlog.md`.
- [x] **Task 4 (AC: 3) — Nimbus JWT defense-in-depth + JWKS cache.**
  - [x] Cached-`JWKSet` verify: signature/iss/aud/exp/nbf + `typ` (fixture-verified literal); kid-miss → deny + background refresh (no foreground retry). *(The typ literal was verified LIVE against the pinned fixture (docker-run probe, 2026-08-19): the JOSE **header** `typ` is `"JWT"` — NOT `"Bearer"`, which is the token's payload *claim*; recorded on `EXPECTED_TYP`'s javadoc. The cold-cache arm (empty `current()`) also denies + triggers the background refresh — the bind path never fetches, foreground or otherwise.)*
  - [x] Refresh executor (AD-28(2) hand-rolled VT, not `@Scheduled`); whole-swap-only cache invariant (§2.1 item 6b). **Item 6a — refresh bounding:** the background refresh runs on the AD-28(2) scheduler and **never routes through the adjudication pool or its admission semaphore** (a single-flight scheduler task is its own bound); it must neither consume bind-admission permits (bind saturation must not block kid-rotation refresh) nor be starvable by binds. *(Shipped as `JwksCache`: `newScheduledThreadPool(1, virtual "jwks-refresh-")`, cadence = TTL/2 refresh-ahead (dev-selected, floored 1ms), initial fetch async at construction — cold-cache binds deny fail-closed until it lands, deliberately NOT a startup refusal (a transient JWKS outage is runtime, unlike discovery). §6a isolation is STRUCTURAL — `JwksCache` holds no pool/semaphore reference at all. Extra design decisions recorded in Completion Notes: no age-eviction, empty-set-200 retained, one write site.)*
- [x] **Task 5 (AC: 4) — Opaque-token introspection fallback.**
  - [x] Discovery-derived endpoint; F3 active-call registration; AC2 introspection rows. *(Shipped as the `introspect`/`mapIntrospection` arm: discovery-derived endpoint + Basic client auth raw-byte assembled + `token=` form via the shared raw-octet encoder; `activeCall.set(intro)` F3 registration; ONLY 200+JSON+boolean `active:true` → Allow, everything else — incl. introspection 401 — `DenyIndeterminate`. ONE DESIGN CORRECTION recorded: the T3 plan to fork round 2 into the same STS is impossible on JDK 25 — the fifth-preview scope is SINGLE-JOIN (second `join()` throws ISE, masked by the fail-closed catches) — round 2 joins INLINE (the ratified slice's shape), bounded by the clamped request timeout. See Completion Notes + Debug Log.)*
- [x] **Task 6 (AC: 5, 6) — Cancellation + zeroization.**
  - [x] `cancelHttp()` → `sendAsync(...).cancel(true)` + STS shutdown + **guaranteed settlement `DenyIndeterminate`** (F7 decision) — `RecordingHttpClient` test. *(Shipped behavior PROVEN, not added: the T6 `RecordingHttpClient` (production-tier, the 2-1 wrapper pattern + the always-inject ctor — the 2-arg delegates to `IdpSslContextFactory.newClient()`, the 3-arg takes the built client; no nullable branch) asserts the synchronous F7 settlement, the `CancellationException`-in-cause-chain abort of the REAL wire future for BOTH rounds (the T5-deferred round-2 proof), and the permit-free/STS-unwind observable — all green pre-wipe because T3/T5 shipped the behavior; the bite is proven by M-CANCEL → 3 RED. **Honest finding:** the F7 settlement is deliberately DOUBLE-COVERED (cancelHttp completes the pin AND every pool path settles) — a drop-the-completion mutation stays GREEN in repeated runs (the pool unwind wins the race), so the settlement pin is a contract assertion, not a single-arm mutation control; recorded on the test comment.)*
  - [x] Token/request-buffer zeroization on every completion path; `NoStringFromPasswordTest` green over adapter source + fresh mutation control. *(Shipped as the per-adjudication `Adjudication` holder — pin + activeCall + rc + a sensitive-buffer registry threaded as ONE object (replacing the T5 three-deep param threading) — with exactly TWO wipe sites: the pool task's `finally` (permit frees first, then every registered array wipes — Allow rows, every deny row, cancel, timeout, introspection) and the use-after-close catch (§2.1 item 2's "release permit + zeroize" now literal). Registered: both form arrays, the introspection token bytes, both response bodies. **JDK finding that shaped the design:** `BodyPublishers.ofByteArray` COPIES the form into fresh heap chunks at subscribe time (RequestPublishers.java, verified in the 25.0.3 source) — un-wipeable copies of the credential form — so the forms now publish ZERO-COPY via `FormPublisher`/`FormBuffer` (one bound-allocated array, `ByteBuffer.wrap` delivery), making the registered array the ONLY copy and wire-observable by identity; the BAOS + `toByteArray` double-buffer is gone. The password stays caller-owned (never registered — the diff's directions held). `NoStringFromPasswordTest` green over the adapter unchanged; M-TOSTRING (a planted `cred.password().value().toString()`) → 1 RED on Rule 1.)*
- [x] **Task 7 (AC: 1) — Spring DI wiring + lifecycle.**
  - [x] Security-side `@Configuration`: forward cells → ROPC adapter, reverse cells → `AlwaysAllow` (lose its `@Component`; update its test); exactly-one wiring test per cell. *(Direction INVERTED by the AD-12 amendment — see the Dev Agent Record amendment note: **reverse.* cells → ROPC adapter, forward.mode-a/c → `AlwaysAllow`**. Shipped as `VerifierWiringConfig`: the populated `reverse` branch IS the selector (the AD-17 compact-ctor single-branch guarantee), so selection cannot drift from the config model; `AlwaysAllow` lost `@Component`; `VerifierWiringConfigTest` pins exactly-one per all five cells via `ApplicationContextRunner` + the blank-secret bean-construction refusal (SEC-060) + the lifecycle's phase in every context; the `@Component` assertion in `AlwaysAllowBindCredentialVerifierTest` inverted to a NOT-annotated pin.)*
  - [x] `SmartLifecycle stop()`: DENY-in-flight (`shutdownNow()` + `awaitTermination` + fail-closed log on timeout — §2.1 item 5), stop JWKS refresh **before** cache close (AD-22 ordering), all inside `security/` (no `bootstrap/` changes). **Phase discipline:** `getPhase()` must be strictly **below** `RelayServerLifecycle.RELAY_ACCEPTOR_PHASE` (`APP_PHASE+1000`) — Spring stops higher phases first, so the acceptor stops BEFORE the adjudicator (AD-22 step order: acceptor → deny in-flight → … → JWKS refresh → cache close); `APP_PHASE` is the natural slot. `isRunning()` pinned; `stop()` idempotent (house pattern from `RelayServerLifecycle`). *(Shipped as `AdjudicationLifecycle` (`ADJUDICATION_PHASE == APP_PHASE`, running-flag idempotence, `stop(Runnable)` callback in `finally`) whose stop wraps the AD-22 body that now lives in the adapter's `close()` — REORDERED from the T3/T4 seam to the literal AD-22 step order: deny-in-flight first (`shutdownNow()` + `awaitTermination(oidc.timeout+1s)` + fail-closed WARN on timeout/interrupt), THEN the JWKS refresh stop, then the shared client + client-secret wipe; the load-bearing T4 invariant "refresh stops before the client it fetches through closes" is preserved. Two honest scope notes in Completion Notes: the drain-timeout WARN arm is structurally shadowed by the per-request timeout clamp, and M-DRAIN is not a mutation control — the F7/M-SETTLE double-coverage precedent.)*
- [ ] **Task 8 (AC: 10) — Gates widened.**
  - [ ] `NoRolledCryptoArchitectureTest` rules 3–4 → production adapter; SEC-090 tightening + positive control.
- [ ] **Task 9 (AC: 9) — Live Keycloak suite (production adapter).**
  - [ ] Docker-gated 4-path suite mirroring `RopcSliceLiveTest`; keep `RopcSlice*` tests green (reference slice stays).
- [ ] **Task 10 (AC: 10) — RED-on-neuter mutation pass + clean build + CR.**
  - [ ] Named-guard mutation ledger (exception-safe `finally` cleanup in biters; unique `/tmp` backup paths; marker-grep zero residue; `clean build` after restores).

## Dev Notes

### Architecture compliance (MUST follow)

- **AD-12 (ROPC adjudication, spine:135–142)** — token-endpoint status 200/401 **is** the verdict; local JWT verify is defense-in-depth (AD-11 precedence, DENY wins); **discard the token, relay the ORIGINAL bind** (the adapter never substitutes the verdict for the SMSC's own check — the relay already forwards `originalFrame()` verbatim); **re-validate every bind — no verdict cache, cache JWKS only; introspection never cached**. Port is immutable since AC8 (2026-08-10): `VerdictRequest verify(BindCredential, ScopedValue<RequestContext>)`, `future()`/`cancelHttp()`, sealed `Verdict permits Allow, DenyInvalid, DenyIndeterminate`, **no Throwable/Nimbus/free-form-reason type crosses the port** — the adapter absorbs Nimbus.
- **AD-11 (fail-closed, spine:130–133)** — every auth-adjacent decision denies on indeterminate; AC2's table is the exhaustive instantiation. Unknown `system_id` → DENY (routing-miss) is **relay/story-3.3 territory** — NOT this story.
- **AD-4 / AD-28(4) (no blocking on event loops; bounded VT pool)** — all adapter I/O off event loops; single bounded hand-managed VT pool owned by `security/`, config-sized, fail-closed on saturation; **never `@Async`, never a VT-per-bind outside the pool, never `CallerRunsPolicy`**.
- **AD-5 (STS + ScopedValue, spine:87–90)** — STS structures the fan **within one adjudication**; the pool gates entry — STS does not replace bounding. Preview confinement: STS lives in the control plane (`security/` + `bootstrap/`) only. JDK 25 STS = JEP 505 fifth preview (`open(Joiner)` API — see AC5); ScopedValue is final (JEP 506). Pin risk is absorbed: `smpp.java-conventions` already runs `--enable-preview` process-wide and 2.2 T1's compiler gate enforces it.
- **AD-10(3) / PRIV-1 (credential-free at rest)** — runtime holdings are exactly: OIDC client credential (file-path), cached JWKS public keys, per-bind password in memory during adjudication+forward (never persisted/logged/cached), own TLS keys. Zeroization events: adjudication completion, connection teardown, JVM shutdown, exception paths.
- **AD-13 / AD-26 (trust posture)** — IdP trust store never `cacerts`; PKIX defaults, no custom chain-validation code, no `PKIXBuilderParameters` overrides.
- **AD-22 / AD-28(2) (shutdown ordering, spine:196–199, 226–229)** — stop JWKS refresh **before** closing the JWKS cache; DENY in-flight adjudications on shutdown; pool drain = `shutdownNow()` + await with fail-closed log.
- **AD-25 (teardown race, spine:214)** — case-3 teardown shuts the adjudication's STS and calls `cancelHttp()` (the relay's `BindInterceptor.teardownForPreCoupleViolation` + `cancelAndWipePending` already do this from the caller side — `BindInterceptor.java:348-359`); a late verdict reaching a torn-down entry is a no-op relay-side. The adapter's settlement guarantee (AC5/F7) makes the "late verdict" window settle cleanly.
- **AD-27 (observer contract)** — `onBindReject` fires only for actual `Verdict` values returned by the verifier (relay-side, `BindInterceptor.java:314`); the adapter changes nothing here — but it must never return `null` from `verify()` and never complete `future()` exceptionally-as-normal-flow (relay treats exceptional/null as fail-closed deny — `BindInterceptor.java:258-280`; the adapter settles every future with a `Verdict`).
- **AD-33 (deny wire collapse)** — already shipped by 2.2 (`ESME_RBINDFAIL 0x0000000D` header-only `bind_*_resp` + close, `BindInterceptor.java:110, 333-345`, identical across all denial arms). **This story does not touch it**; the adapter's rich `DenyInvalid`/`DenyIndeterminate` distinction surfaces only in logs/metrics (Epic 4 impl) and never on the wire.
- **AD-34 / D2 (cipher allowlist)** — AC7's startup intersection for the IdP client context; SMSC-leg/ingress contexts → story 3.3.
- **AD-36 (adapter stack, spine:270–275)** — plain OIDC over JDK `java.net.http.HttpClient` + Nimbus local verify; **no `keycloak-authz-client`/`keycloak-admin-client`/any vendor SDK** (drags JAX-RS/RESTEasy → banned by `smpp.runtime-purity`; no per-request cancel handle; obstructs zeroization). "The production adapter (Epic 3) stays on this same HttpClient+Nimbus stack the test slice ratifies."
- **AD-18 (secrets)** — every credential/token is a **file path** in config, read at startup, never inline values.

### Seeded contract surfaces (as shipped — verify exact signatures in code before use)

```java
// proxy/src/main/java/smpp/companion/proxy/security/ (Story 2.1/2.2 — UNCHANGED by this story)
interface BindCredentialVerifier { VerdictRequest verify(BindCredential cred, ScopedValue<RequestContext> ctx); }
sealed interface Verdict permits Verdict.Allow, Verdict.DenyInvalid, Verdict.DenyIndeterminate { }  // 3 payload-less records
interface VerdictRequest { CompletableFuture<Verdict> future(); void cancelHttp(); }  // cancelHttp idempotent
record BindCredential(SystemId systemId, Password password) { }  // compact-ctor null-guards; toString redacts
record SystemId(AsciiString value)  { /* MAX_LENGTH = 15; non-secret (AD-14) */ }
record Password(AsciiString value)  { /* MAX_LENGTH = 8; zeroize() wipes shared backing array; toString = Password[***] */ }
record RequestContext(SystemId systemId, ChannelId channelId, Instant deadline) { }
@Component final class AlwaysAllowBindCredentialVerifier implements BindCredentialVerifier { }  // AC1 re-wires this
```

Caller side (READ ONLY — do not modify): `BindInterceptor.java:242-244` builds the credential + deadline (`companion.bind.adjudication-deadline`, default 4s — **the verifier's budget**; relay arms no timeout of its own, F14 open, relay-side); `:256-257` binds `ScopedValue` and calls `verify()`; `:283-286` hops the continuation to the ingress event loop (AD-25 re-check); `:294-323` verdict mapping; `:319-322` continuation `finally` zeroizes the password; `:348-359` `cancelAndWipePending()`.

### Library / framework specifics

- **Nimbus JOSE+JWT 10.9.1** — already pinned in `proxy/build.gradle.kts` (`com.nimbusds:nimbus-jose-jwt:10.9.1`; dependency-floor 10.0.2 wired into `check`). Latest upstream is 10.9.1 — **no version change needed; do not churn it.**
- **JDK `java.net.http.HttpClient`** — one shared client per adapter (connect timeout from config, per-request `Timeout` from `oidc.timeout`); `sendAsync` + `cancel(true)` is the ONLY sanctioned abort path (2-1 JDK-25-source-verified). `isCancelled()` is false by design; assert `CancellationException` **anywhere in the cause chain**.
- **JDK 25 StructuredTaskScope (JEP 505)** — `StructuredTaskScope.open(Joiner)`, `Joiner.awaitAllSuccessfulOrThrow()`, `fork`/`join`/`close`/`isCancelled`; `Configuration` carries name/timeout/thread-factory. **The `new StructuredTaskScope.ShutdownOnFailure()` pattern in most training data does not compile on JDK 25.** Web-verified: [JEP 505](https://openjdk.org/jeps/505).
- **Spring Boot 4.1.0** (proxy), Gradle 9.6.1, JDK 25 toolchain, Lombok via `io.freefair.lombok` 9.5.0 (`@RequiredArgsConstructor` idiom is house style). Testcontainers 2.x via SB's `testcontainers-bom` — the JUnit module is `testcontainers-junit-jupiter` (renamed from `junit-jupiter` in 2.x). jqwik/Jazzer exist **only in codec** — do not add them to proxy.
- **Null-safety**: `@NullMarked` is per-package — new sub-packages under `security/` need their own `package-info.java` (1-4 finding). NullAway 0.13.8 ERROR-severity on `compileJava` only (`AnnotatedPackages=smpp.companion`, `JSpecifyMode=true`); zeroized secret fields are `@Nullable` (nulled after use — 1-4 precedent); `@SuppressWarnings("NullAway")` only with `// reason:` comment. Expect ErrorProne `FutureReturnValueIgnored` on bare `close()`/`cancel` returns — class-level suppression with reason, 2-2 precedent.
- **Keycloak fixture facts (verified, 2-1)**: DAG is **per-client and off by default since KC 26.2** — realm JSON enables it on both fixture clients; `KC_HTTPS_CLIENT_AUTH=required` (NOT `request` — peer cert must reach `client-x509`); KC 26 uses `x509.subjectdn` + `x509.casubjectdn`; User Profile requires email/firstName/lastName or ROPC fails "Account is not fully set up"; fixed `:8443` + `KC_HOSTNAME=localhost` gives a deterministic issuer (sidesteps keycloak#49967 `iss`-reflection).

### Verdict mapping — normative quick table (AC2)

| Token endpoint outcome | Verdict |
|---|---|
| 200 + locally-verifiable JWT (defense-in-depth passes) | `Allow` |
| 200 + opaque token → introspection 200+JSON+`active:true` | `Allow` |
| 401 · 400 with `error` ∈ {`invalid_grant`, `invalid_client`} | `DenyInvalid` |
| 200 non-JWT/HTML/empty · malformed JWT · 3xx · any other 4xx (403/404/429, 400 other error) · 5xx · timeout · network error | `DenyIndeterminate` |
| 200 JWT but sig/iss/aud/exp/nbf/typ fail · kid-miss | `DenyIndeterminate` (DENY wins; background JWKS refresh, no foreground retry) |
| Introspection anything ≠ 200+JSON+`active:true` (incl. `active:false`) | `DenyIndeterminate` |
| Admission saturation · `cancelHttp()` · scope/executor shutdown | `DenyIndeterminate` (saturated: **no wire call started**) |

### Previous-story intelligence (2-1 → this story, directly)

- **`RopcSlice` is the ratified reference** — productionize its shape, do not redesign. Six LOW items to fix in production (all open in deferred-work §2.1): (1) 429/404/403 mapping → AC2 table; (2) use-after-close permit leak → `try/catch(RejectedExecutionException){release(); zeroize();}`; (3) JWT `typ` validation (RFC 8725 §3.9) → AC3; (5) `close()` drain `awaitTermination` + fail-closed log → T7; (6a) `asyncRefreshJwks` bypasses the admission gate → refresh lives on the AD-28(2) scheduler, never the adjudication pool (Task 4); (6b) unconditional cache-null → whole-swap-only (AC3). Item (4) `AlwaysAllow` zeroization is RESOLVED caller-owned (2.2 T7) — **do not re-litigate; the adapter must NOT zeroize at `verify()`-return**.
- **2-1 review fixes the production adapter MUST keep**: F2 (sendAsync only after `tryAcquire`), F3 (introspection registered in active-call for cancel), F5 (`requireNonNull` compact-ctor fail-fast), F6 (JWKS fork only when the token is a JWT, not on the introspection path), F12 (`Objects.requireNonNull(ctx, "ctx")`).
- **2-1 test assets to reuse, not duplicate**: `KeycloakContainer`/`KeycloakFixture` (fixture constants: `CLIENT_A_ID=smpp-client-confidential`/secret, `CLIENT_B_ID=smpp-client-mtls` no-secret, `TEST_USER=testuser`/`testpass`, endpoints under `https://localhost:8443/realms/smpp-companions`), in-process `com.sun.net.httpserver` stand-in IdPs (`RopcSliceFailClosedTest`), `RecordingHttpClient` delegating wrapper (`RopcSliceCancelTest`), forged-JWT helpers (`RSAKeyGenerator`/`RSASSASigner`, `RopcSliceUnitTest`). Keep `RopcSlice*` green as the contract reference; production tests live beside them.
- **2-2 facts the adapter depends on**: the relay already handles exceptional/null futures fail-closed (`BindInterceptor.java:258-280`) — the adapter settles every future with a `Verdict` instead; caller-owned zeroization sites are `:319-322` + `:348-359`; `LatchedBindCredentialVerifier` (relay test fake) shows the fake-verifier idiom if needed.
- **Sprint-status bookkeeping drift (cite, don't re-do)**: action items AI-3 (Keycloak fixture), AI-4 (contract types), AI-5 (password seam/CODEC-024 P2) are still marked `open` in sprint-status.yaml but were completed by 2.1/2.2.

### Codified test traps (epic-1 retro AI-7 — apply by default)

(a) **null-vs-blank**: refusal tests bind the exact invalid value through real Spring binding — removing a key proves nothing (killed 14/33 config guards in 1.3); (b) gate-must-run-in-`check`: any new Gradle gate needs a TestKit positive control and must be wired into `check`; (c) jqwik rules are codec-only (N/A here); plus: RED-on-neuter biters release latches/resources in `finally` (exception-safe cleanup — a stranded in-process IdP handler hangs the test JVM); unique `/tmp` backup paths per mutation; marker-grep proves zero mutation residue; `clean build` after restores (source-scan tests are UP-TO-DATE-skipped on incremental runs); full-context boots need `TestCompanionConfigs.common()` (memory budget `1/1/1.0`, `adjudication-deadline=4s`, TLS lists, free port — runner boots don't load `application.yml`); `SpringApplicationBuilder.properties()` is lowest precedence — overrides go as `run("--key=val")` args; console counts lie — trust the XML.

### External research (2026-08-18, web-verified)

- **Keycloak 26.7.0** (GA 2026-07-09) is the latest 26.x — matches the fixture pin and Story 3.1's verdict (viability MET, ≥26.7.0, re-validate each minor; no LTS, minors EOL ~3 months). **ROPC removal-watch is live**: [deprecation-path discussion keycloak#24585](https://github.com/keycloak/keycloak/discussions/24585), OAuth 2.1 omits ROPC, RFC 9700 §2.4 "MUST NOT", Keycloak docs discourage DAG, and RFC 7523 JWT Authorization Grant (KC 26.5+) is the emerging replacement. **CVE-2026-9792** (medium; ROPC client-policy bypass; fixed ≥26.7.0/26.6.3/26.4.13) — the spine already records it as "unverified signal, not asserted"; the ≥26.7.0 pin covers it. v1 stays **explicitly ROPC-conditional** (PRD §11) — the deployment-guide disclosure is Epic 6 (OPS-1), NOT this story.
- **Nimbus JOSE+JWT 10.9.1** is current — no change.
- **JEP 505 fifth preview** — `open(Joiner)` API shape as above; [JEP 505](https://openjdk.org/jeps/505), [Rock the JVM JDK-25 guide](https://rockthejvm.com/articles/structured-concurrency-jdk-25).

### Out of scope (explicitly — do NOT expand into these)

- **Forward-role acceptor wiring + `BindInterceptor` role-split + routing-miss deny arm + per-target `tlsContextId` + `RelayServerLifecycle` acceptor widening** — story 3.3 (relay/ changes; owner-deferred "execute the split when Epic 3's forward role actually lands").
- **TLS modes A/B/C on the SMPP legs, ingress TLS, per-egress-context cipher intersection beyond the IdP context, wildcard-listener/connection-cap hardening (F13)** — story 3.3.
- **Relay-side F14 deadline arm, F10 `CONNECT_TIMEOUT_MILLIS`, F1 ingress-leg `bind_resp` drop, the relay owner-refactor cluster (couple-vocabulary, `RelayHandler` split, `ConnectionEntry` state manager)** — relay/ untouched by this story.
- **Structured-JSON logs + metrics impl (AD-27 impl, Verdict-type counters), AD-22 full shutdown body** — Epic 4.
- **PERF-3 p99 measurement harness** — Epic 6 (this story only honors the timeout budget).
- **ROPC fallback DECISION** — dormant (probe MET); deployment-guide ROPC-conditional disclosure — Epic 6.

### Risk notes

- **Blast radius, honestly stated**: reverse cells are unchanged (acceptor is reverse.mode-b-only today, `RelayServerLifecycle.java:70-82`, and keeps `AlwaysAllow`). Forward cells DO gain one startup behavior: a hard-required discovery probe at boot (AC7 — fail-closed by design) plus the adapter bean, though no acceptor feeds it binds until 3.3. The four existing full-context forward boots survive via the T1 stand-in discovery server — verified green in T1/T2, not weakened or skipped. AC9's live/port tests are the story's end-to-end proof. All existing tests must stay green — the File List proves the blast radius.
- **`client-credential-path` rename** is a config-surface break — acceptable pre-release (no deployments); the six binding sites enumerated in Task 1 are updated in the same task, never via a transition shim.
- **`AsciiString.toString()` immortal-cache hazard**: one stray `toString()` on `Password.value()` leaks the secret permanently — the `NoStringFromPasswordTest` scan + F1 raw-byte rule are the guards; never "temporarily" log the password even in tests' failure paths.
- **STS preview shape**: if the compiler rejects `ShutdownOnFailure`-style code from memory, that is expected — use the AC5 API. Do not "fix" it by pinning an older API or suppressing preview flags.

### Project Structure Notes

- **NEW** — `proxy/src/main/java/smpp/companion/proxy/security/`: the adapter, its wiring `@Configuration`, JWKS cache/refresh component, IdP `SSLContext` factory (naming: follow the port's noun style — `RopcBindCredentialVerifier` suggested; spine's walkthrough term is `BindAdjudicator` — pick one, use it consistently).
- **NEW (test)** — `proxy/src/test/java/smpp/companion/proxy/testsupport/` (or `security/`): the HTTPS stand-in discovery server (T1) + production-adapter test suites (unit fail-closed/cancel/zeroization; Docker-gated live 4-path).
- **MODIFIED (main)** — `proxy/src/main/java/smpp/companion/proxy/config/ProxyCompanionProperties.java` (Oidc record + new leaves), `config/CompanionConfigValidator.java` (deep checks). `application.yml` only if a comment/doc line needs updating — branch-scoped defaults do NOT go in yml (AC8 defaulting mechanism).
- **MODIFIED (test)** — the `client-credential-path` rename blast radius: `config/TestCompanionConfigs.java:48`, `config/CompanionConfigMatrixTest.java:257`, `bootstrap/BootstrapLifecycleTest.java:85`, `relay/netty/RelayServerLifecycleTest.java:224`, `relay/netty/DirectMemoryBudgetStartupCheckTest.java:232`, `config/CompanionTlsBindingTest.java:54`; plus `security/AlwaysAllowBindCredentialVerifierTest` (@Component assertion), `security/NoRolledCryptoArchitectureTest` (rules 3–4), matrix suites for the new refusal cases. `proxy/build.gradle.kts` only if a test dep is genuinely missing (expect none).
- **DO NOT TOUCH (main)** — `proxy/relay/**`, `proxy/observability/**`, `proxy/bootstrap/**`, `codec/**`, all AD-12 port types in `security/`.
- **Test fixtures** — reuse `proxy/src/test/.../security/Keycloak*` + `resources/keycloak/` as-is; new production-adapter tests live in the same package; Docker-gated classes carry `@Tag("integration", "security", "p1")` + `@Testcontainers(disabledWithoutDocker = true)`; unit suites `@Tag("unit", "security", "p1")`.
- **Config keys** are kebab-case under `companion.`; the AD/SEC id goes in the refusal message.

### References

- [Source: ARCHITECTURE-SPINE.md#AD-12 (135–142) — ROPC rule, port contract, ratification AC8]
- [Source: ARCHITECTURE-SPINE.md#AD-11 (130–133), #AD-5 (87–90), #AD-28 (226–229), #AD-22 (196–199), #AD-25 (214), #AD-33 (255–258), #AD-34 (260–263), #AD-36 (270–275), #AD-10 (125–128), #AD-13 (144–147), #AD-26 (216–219); Accepted-Risk Register (288–296 — ROPC hard-dependency + 3.1 verdict + CVE-2026-9792 note)]
- [Source: prd.md §6.2/6.3 (FR-SEC-1..5, FR-AUTH-1..4), §7 (SEC-1/3/4/5, PERF-3, PRIV-1, COMP-2), §8 (modes), §10 (A-2), §11 (accepted risks)]
- [Source: epics.md §Epic 3 (369–378 — scope, fallback tree, package ownership); epics.md:286 (3.1 verdict)]
- [Source: 2-1-security-port-contract-validation-slice.md — fixture findings #1–#6 + the unnumbered deterministic-issuer note, review fixes F2/F3/F5/F6/F12, F1 raw-byte mandate]
- [Source: 2-2-stateless-relay-and-a1-session-affinity-smoke.md — caller-owned zeroization, AD-33 collapse as shipped, F7 deferral]
- [Source: deferred-work.md — §2.1 items 1/2/3/5/6; 1-3 D2; 2-2 F7; SEC-090]
- [Source: proxy/src/main/java/smpp/companion/proxy/relay/BindInterceptor.java (110, 242–244, 256–257, 283–286, 294–345, 348–359)]
- [Source: RFC 6749 §4.3 (ROPC), RFC 7662 (introspection), RFC 8705 (mTLS client auth), RFC 8725 §3.9 (typ), RFC 9700 §2.4; JEP 505 (STS fifth preview), JEP 506 (ScopedValue final), JEP 444 (VT)]
- Web (2026-08-18): [keycloak#24585](https://github.com/keycloak/keycloak/discussions/24585), [JEP 505](https://openjdk.org/jeps/505), [Nimbus 10.9.1](https://connect2id.com/products/nimbus-jose-jwt)

## Dev Agent Record

### Agent Model Used

GLM 5.2 (Claude Code harness, 2026-08-18).

### Debug Log References

- `./gradlew :proxy:compileJava :proxy:compileTestJava` — GREEN (one pre-existing `UnusedVariable` warning in `RelayHandler.java:333`, one pre-existing `ArrayRecordComponent` warning in `JsmppSmscServer.java:355`; both untouched by this story).
- `./gradlew :proxy:test` — **226 tests, 0 failures, 0 errors, 0 skipped** (XML-verified; includes `RopcSlice*` reference suites and the six blast-radius classes: `CompanionConfigMatrixTest` 67, `CompanionTlsBindingTest` 7, `CompanionRoleFailFastTest` 3, `BootstrapLifecycleTest` 2, `RelayServerLifecycleTest` 5, `DirectMemoryBudgetStartupCheckTest` 6). *(Pre-amendment count — the same-day AD-12 amendment added `amendment_strayForwardOidcKeyRefuses` → 227; the 2026-08-19 RFC 8705 removal (−2/+1) and review patch pass (+4) re-baselined the suite to 230 — see Change Log.)*
- `./gradlew :proxy:check` — GREEN.
- RED-on-neuter spot mutations (T1 guards; full consolidated pass is T10): **MUTANT-1** removed the `timeout` component's `@NotNull` → `ac8_absentOidcBudgetKeyRefuses[timeout]` RED (the max-in-flight/jwks-cache-ttl cases stayed green — per-key bite); **MUTANT-2** neutered `requireReadableFile(oidc.trustStore().path(), …)` → `ac8_idpTrustStoreMissingFileRefuses` RED. Both restored from unique `/tmp` backups, `grep -rn "MUTANT" proxy/src/` = zero residue, full `:proxy:test` re-run GREEN (226/0).
- **T2 (2026-08-19):** RED phase on stubs — 21 new tests, **19 RED / 2 green** (the two forward-cell inactive-invariants, which pin the real contract). GREEN: all 21 pass. **MUTANT-A** `trusted == 0` → `trusted < 0` (zero-trustedCertEntry refusal neutered) → `zeroTrustedCertEntryStoreRefusesStartup` RED (11 tests, 1 failed — per-guard bite); **MUTANT-B** `if (suites.isEmpty())` → `if (false)` (AD-34 suite-intersection refusal neutered) → `emptyCipherIntersectionRefusesStartup` RED; **MUTANT-C** `!issuer.equals(providerUrl)` → `if (false)` → `issuerMismatchRefusesStartup` RED (10 tests, 1 failed); **MUTANT-D** `log.warn(DAG_WARNING, …)` commented out → `dagMissingPasswordGrantWarnsButDiscovers` RED. All restored from `/tmp/t2-mutations/` backups, `grep -rn "MUTANT" proxy/src/` = zero residue.
- **T2 final gates:** `./gradlew :proxy:check` GREEN; `:proxy:test` **251/0/0/0** (XML-verified; 230 at T1 close + 21 new T2 tests).
- **T2 user-directed FIXME pass (2026-08-19, same day):** RED-first hostless matrix test (booted pre-refactor → refused post); MUTANT-E neutered `requireOidc`'s hostless check → `sec053_hostlessOidcProviderUrlRefuses` RED (72 tests, 1 failed); MUTANT-F neutered the `Oidc` ctor slash-strip → `trailingSlashProviderUrlNormalizesAndDiscovers` RED (10 tests, 1 failed). Both restored from `/tmp/t2-fixme-mutations/`, zero residue. Final: `:proxy:check` GREEN, **252/0/0/0** (XML). One behavior note discovered en route: a compact-ctor `requireNonNull` on a URI-typed component PREEMPTS the `@NotNull` message with a bare NPE (binding instantiates before validating, and empty strings convert to null for non-String targets) — the `Oidc` ctor is deliberately null-tolerant, matching every other `@NotNull` component; see the Change Log entry.
- **Owner deferral (2026-08-19, same day):** evaluate an OIDC-discovery/parsing library (preferred candidate `com.nimbusds:oauth2-oidc-sdk` — `OIDCProviderMetadata.parse`) to replace `OidcStartupDiscovery.{probe,parseDocument}` — recorded in deferred-work.md §"owner note during Story 3.2 T2 FIXME pass", with the constraint set (keep the JDK HttpClient + TLS context; preserve the refusal matrix / issuer equality / DAG warning; no SDK type crosses the security/ boundary; runtime-purity + dependency-floors; Keycloak SDKs stay banned) and the timing note: decide BEFORE T4/T5, which face the same buy-vs-build for JWKS and RFC 7662.
- **T3 (2026-08-19):** RED phase on the stub (`verify()` throwing `UnsupportedOperationException`) — 15 new tests, **15 RED / 0 green** (the cleanest split: the harness infrastructure — fixture-cert stand-in IdP, real factory+discovery construction — is exercised and proven live by every failure). GREEN: 15/15. **MUTANT-1** reverted the 400 arm to the crude slice shorthand (`400 → DenyInvalid` unconditionally) → `other400ErrorsYieldDenyIndeterminate` RED (15 tests, 1 failed — the AC2 refinement's own guard); **MUTANT-2** neutered `if (!admission.tryAcquire())` → `if (false)` (+ unconditional `tryAcquire()`) → `saturationDeniesWithoutWireCall` RED (the F2 no-wire-call counter caught the saturated bind transmitting); **MUTANT-3** neutered the secret-file not-readable refusal (`catch (IOException) → raw = new byte[0]`) → initially **MASKED** (the failure fell through to the empty-file guard, whose message satisfied the shared substrings) — fixed by tightening the test to the arm-specific substrings (`does not exist or is not readable` vs `empty`), re-run → `constructorFailsFastOnBadConfig` RED. All restored from `/tmp/t3-mutations/` backups, `grep -rn "MUTANT" proxy/src/` = zero residue.
- **T3 final gates:** `./gradlew :proxy:check` GREEN; `:proxy:test` **267/0/0/0** (XML-verified; 252 at T2 close + 15 new T3 tests); `:proxy:compileJava` zero warnings (one `EscapedEntity` javadoc warning found and fixed pre-gates).
- **T3 user-directed FIXME pass (2026-08-19, same day; 4 user-marked FIXMEs on the adapter):** RED on the `ClientSecret` stub — 4 new tests, **3 RED / 16 green** (trim, zeroize, form-capture; the rewire kept all 15 T3 tests green). GREEN 19/19. **M-TRIM** (neutered ASCII trim) → 3 RED, not 1: `loadTrimsAsciiWhitespace` + `formEncodesIdentityAndSecretFromRawBytes` + `constructorFailsFastOnBadConfig` — a finding, not noise: the whitespace-only refusal and the trim are ONE mechanism (`from == to` after trimming), so both guards share the bite. **M-ZEROIZE** (no-op `zeroize()`) → `zeroizeWipesBackingArray` RED (3 tests, 1 failed); **M-ENCODE** (raw octets, no `%XX`) → the form-capture test RED (16 tests, 1 failed — the encoder pin). All restored from `/tmp/t3-fixme-mutations/`, `grep -rn "MUTANT\|M-TRIM\|M-ZEROIZE\|M-ENCODE" proxy/src/` = zero residue. Final: `:proxy:check` GREEN, **271/0/0/0** (XML; 267 + 4 new), zero FIXME markers in the T3 files (the two `OidcStartupDiscovery` grep hits are javadoc references to the T2 FIXME pass, not markers).
- **T4 (2026-08-19):** AC3's "verify the fixture's `typ` live" discharged FIRST — the pinned `quay.io/keycloak/keycloak:26.7.0` booted via a throwaway `docker run` (fixture certs + realm mounted read-only, `:8443`, removed immediately after — the Docker-gated suites need the port), a real ROPC call issued a token, and the header decoded off the wire: `{"alg":"RS256","typ":"JWT","kid":...}` — header `typ` = **"JWT"** (the payload CLAIM on the same token is `"Bearer"`; the header/claim distinction is exactly why the AC banned training-data literals). RED phase on the `JwksCache` stub (`current()`/`requestRefresh()` UOE, inert ctor — the T3 pattern) with the adapter's 200 arm wired through it: 16 new tests, **15 RED / 1 green** (the green one is `malformedAndOpaqueTokens`, a deny-row pin whose bite arrives with the implementation; the 15 RED include both Allow flips and every `awaitCachePopulated` precondition — the forged-JWT/JWKS-endpoint harness is proven live by the failures). GREEN 16/16. **Infrastructure finding en route:** `com.sun.net.httpserver`'s default executor runs ALL handlers on ONE dispatcher thread — a parked latch handler starved the token endpoint itself (the cold-cache bind timed out at exactly the 4s `oidc.timeout`), so the stand-ins now carry an 8-thread DAEMON pool (`stop(0)` does not shut an explicit executor down — daemon threads cannot strand the test JVM; latch discipline in `finally` unchanged). 5 RED-on-neuter spot mutations, each biting EXACTLY one targeted test (1 RED, 0 collateral): **M-TYP** (typ guard → `if (false)`) → `typHeaderUnexpectedYieldsDenyIndeterminate`; **M-KIDMISS** (kid-miss `requestRefresh()` dropped) → `kidMissDeniesNowAndRefreshesInBackground`; **M-6B** (the slice's `keys.set(null)`-on-failed-refresh reintroduced) → `failedRefreshRetainsLastGoodSet`; **M-6A** (periodic cycle neutered — initial fetch only) → `bindSaturationDoesNotStarveJwksRefresh`; **M-EMPTY** (empty-set guard → `if (false)`) → `emptyKeySetIsNotSwapped`. All restored from `/tmp/t4-mutations/`, marker-grep = zero residue.
- **T4 final gates:** `cleanTest` + full re-run — `./gradlew :proxy:check` GREEN; `:proxy:test` **287/0/0/0** (XML-verified; 271 at T3 close + 16 new T4 tests; 0 skipped proves the Docker-gated live suites ran); `:proxy:compileJava` zero warnings (`FutureReturnValueIgnored` suppressed with reason on `JwksCache`'s ctor — AC10 predicted it; `JavaUtilDate` suppressed on `verifyJwt` — Nimbus's Date-typed claim-API boundary).
- **T5 (2026-08-20):** RED phase — 10 new AC4 tests, **10 RED / 25 green** (the one green newcomer is the rewritten malformed-JWT boundary pin, green by design: it pins that JWT-shaped tokens never REACH introspection — its bite is under a dispatch-direction mutation). GREEN required a design correction: the first implementation forked the introspection join into the SAME STS scope (the T3 subtask note's stated intent) — **JDK 25's JEP 505 fifth-preview scope is SINGLE-JOIN**: the second `scope.join()` threw `IllegalStateException` ("Already joined or scope is closed"), and the failure was MASKED by the fail-closed catches (every deny row green, only the Allow rows red — diagnosed by temporary instrumentation, since removed). The shipped shape joins round 2 INLINE on the pool thread (the ratified slice's own shape), bounded by the clamped per-request timeout (the join is uninterruptible — a lost cancel cannot park the pool thread); the API fact is recorded on the `adjudicate`/`introspect` javadocs. A second test-authoring correction: an aborted exchange's LATE handler write does NOT fail (TCP half-close still accepts one local write — verified empirically), so the F3 test's abort observable became the PERMIT: max-in-flight=1, and a cancelled round 2 must free the permit instantly — bind 2 then runs to `DenyInvalid`; a neutered registration leaves bind 2 saturated `DenyIndeterminate` with no wire call. 3 RED-on-neuter spot mutations, each biting its targeted guard: **M-REG** (`activeCall.set(intro)` dropped) → the F3 test RED (1/35, no collateral); **M-ACTIVE** (`active` semantics neutered — any parseable 200 allows) → 3 RED (active:false + the never-cached bind-1 flip + the STRING-active row — one mechanism, the T3 M-TRIM precedent); **M-401** (the token-table 401→DenyInvalid shorthand leaked into round 2) → the non-200 test RED (1/35). All restored from `/tmp/t5-mutations/`, `grep -rn "MUTANT\|M-REG\|M-ACTIVE\|M-401\|T5DBG" proxy/src/` = zero residue.
- **T5 final gates:** post-`clean` `./gradlew :proxy:check` GREEN; `:proxy:test` **297/0/0/0** (XML-verified; 287 at T4 close + 10 new T5 tests; 0 skipped — the Docker-gated live suites ran); `:proxy:compileJava` warnings unchanged (the one pre-existing `RelayHandler.java:333`; the 5 test-compile warnings all pre-existing in untouched files — the slice, `JsmppSmscServer`, `A1CarrierPlanDocsTest`).
- **T6 (2026-08-20):** two JDK facts verified from the 25.0.3 sources before any code, both shaping the design: (1) `HttpRequest.BodyPublishers.ofByteArray` **copies** the body into fresh `ByteBuffer.allocate` chunks at subscribe time (`RequestPublishers.ByteArrayPublisher.subscribe` → `copy(...)`) — so the adapter's exact form array is invisible to the wire AND the JDK retains un-wipeable copies of the credential form — the fix serves both hygiene and observability: `FormBuffer` (one bound-allocated array, cursor-based, no BAOS/`toByteArray` double buffer) + `FormPublisher` (zero-copy `ByteBuffer.wrap` single-item delivery; hand-rolled `Flow` subscription); (2) base `HttpClient.close()` is concrete — the recording wrapper overrides it to delegate. RED phase: 5 new tests — **3 RED / 2 green-before-wipe** (the zeroization tests failed on the missing wipe; the two cancel tests' F7/wire-abort assertions were green pre-implementation because T3/T5 SHIPPED that behavior — their bite is the mutation control, not the RED run). GREEN: the `Adjudication` bundle (pin/activeCall/rc/registry — one object, replacing the T5 activeCall+rc+pin threading) with registrations at both builders, both response sites, and `tokenBytes`, and exactly two wipe sites (task `finally`: permit-release-then-wipe; the use-after-close catch: §2.1 item 2's "release + zeroize" made literal). 4 mutation controls: **M-WIPE** (wipe neutered) → 5 RED (all zeroization assertions, one mechanism); **M-CANCEL** (`cancel(true)` dropped from `cancelHttp`) → 3 RED (both T6 wire-abort assertions + T5's permit observable — downstream of the abort); **M-SETTLE** (cancelHttp's pin completion dropped) → **0 RED in 3 repeated runs — deliberately NOT a ledger control**: the pool task's cancel→join-throw→complete unwind wins the race every time; the F7 guarantee is double-covered by design, the test pins the contract, and the test comment records this honestly; **M-TOSTRING** (a planted `cred.password().value().toString()` in `tokenRequest`) → 1 RED on `NoStringFromPasswordTest` Rule 1 (the fresh adapter bite the subtask names). All restored from `/tmp/t6-mutations/`, `grep -rn "MUTANT\|M-WIPE\|M-CANCEL\|M-SETTLE\|M-TOSTRING\|T6DBG" proxy/src/` = zero residue.
- **T6 final gates:** post-`clean` `./gradlew :proxy:check` GREEN; `:proxy:test` **302/0/0/0** (XML-verified; 297 at T5 close + 5 new T6 tests; 0 skipped — the Docker-gated live suites ran); warnings unchanged (the one pre-existing main `RelayHandler.java:333`; the 5 test-compile warnings all pre-existing — three transient `NullablePrimitiveArray` warnings from the first test draft were eliminated by plain `byte[]` + explicit null checks before the gates).
- **T6 post-pass (2026-08-20, user-directed "consider always injecting http client"):** the nullable injection branch is GONE — `IdpSslContextFactory.newClient()` now owns the 4-line provider-facing client recipe (sslContext + sslParameters + `oidc.timeout` connectTimeout; fails fast on forward cells via the existing accessors), the adapter's public 2-arg ctor delegates `this(tlsFactory, discovery, tlsFactory.newClient())`, and the package-private 3-arg ctor takes a non-null client — every construction path is injection, the TLS posture stays single-sourced, and the test's `recordingClient` helper shrinks to wrapping `factory.newClient()` (recipe dedup). **The companion proposal — replace `RecordingHttpClient` with a Mockito spy — rejected with rationale:** (1) `spy()` shallow-copies fields into a ByteBuddy subclass while `HttpClientImpl`'s ctor starts a `SelectorManager` thread bound to the original instance — the spy aliases a live thread across two objects and it surfaces at `close()` (test teardown), exactly the stranded-thread trap class this repo memoizes; (2) the response-identity capture must SWAP the `BodyHandler` argument before it reaches the real client, which `callRealMethod()` cannot do — the `doAnswer` needs an explicit real-client reference anyway, i.e. the hand-built delegating wrapper in Mockito clothing plus unchecked `Answer` generics; Mockito saves only the ~15 dumb accessor forwards; (3) zero Mockito in proxy tests today (grep-verified) — house style is hand-rolled deterministic fakes (2-1's `RopcSliceCancelTest` wrapper is the ratified pattern). Gates re-run GREEN: `:proxy:check`, `:proxy:test` **302/0/0/0** (XML), warnings unchanged; mutation controls unaffected (they target `cancelHttp`/`zeroizeSensitive`/`tokenRequest`, not construction).
- **T7 (2026-08-20):** RED phase on stubs (an empty `VerifierWiringConfig` + a flag-only `AdjudicationLifecycle.stop()`) — 14 targeted tests, **8 RED / 6 green** (RED: the 5-cell wiring matrix, the blank-secret bean-construction refusal, deny-in-flight settlement, the inverted `@Component` assertion; green-on-stub by design: the phase pins and the flag/callback/idempotence contract, whose bite is the M-PHASE control). GREEN: `VerifierWiringConfig` (the populated `reverse` branch is the selector) + `AdjudicationLifecycle` (stop wraps the adapter's `close()`) + the adapter's `close()` REORDERED to the literal AD-22 step order (deny-in-flight first — the T3/T4 seam stopped the JWKS refresh first; the "refresh before the client closes" invariant is preserved), `AlwaysAllow` de-`@Component`ed. **Blast radius surfaced and fixed in the same pass:** the reverse full-app boots now CONSTRUCT the adapter bean, which loads `oidc.client-secret-path` — `TestCompanionConfigs.oidcKeys()` and the two reverse mode-b boot builders wrote EMPTY secret files (a guaranteed SEC-060 refusal); all three now write real content (trailing newline keeps the trim path exercised). `TestCompanionConfigs` went PUBLIC for the wiring suite (one canonical source for the five-cell bases, no drifted copies). 4 RED-on-neuter spot mutations: **M-SELECT** (selection → `if (false)`) → 4 RED (the three reverse cells + the blank-secret refusal — one mechanism); **M-CLOSE** (`adapter.close()` dropped from the lifecycle stop) → 1 RED (deny-in-flight), no collateral; **M-PHASE** (`getPhase()` → the implicit `Integer.MAX_VALUE` default) → 6 RED (the phase pin + the wiring matrix's phase assertion in all five cells); **M-ALLOW-COMPONENT** (`@Component` re-added to `AlwaysAllow`) → **1 RED in the runner suites but 4 RED in the component-scanned full-app boots** (both forward AND reverse boots die on the two-verifier collision — the runner's `withUserConfiguration` never scans, so the annotation pin + the full boots carry the bite; 5 RED total). **M-DRAIN deliberately NOT a control** (dropping `awaitTermination` is flaky-by-race: the `shutdownNow()` interrupt usually settles the parked task before the assertion — the F7/M-SETTLE double-coverage precedent; the settlement-at-return pin + M-CLOSE carry the guarantee, and the drain-timeout WARN arm is structurally shadowed by the per-request timeout clamp). All restored from `/tmp/t7-mutations/`, `grep -rn "M-SELECT\|M-CLOSE\|M-PHASE\|M-ALLOW-COMPONENT\|MUTANT" proxy/src/` = zero residue.
- **T7 final gates:** post-`cleanTest` `./gradlew :proxy:check` GREEN; `:proxy:test` **311/0/0/0** (XML-verified; 302 at T6 close + 9 net-new: 6 wiring incl. the 5-cell matrix, 3 lifecycle; the AlwaysAllow annotation test was a 1:1 rewrite; 0 skipped — the Docker-gated live suites ran); `:proxy:compileJava` warnings unchanged (the one pre-existing `RelayHandler.java:333`; the 5 test-compile warnings all pre-existing in untouched files).

### Completion Notes List

- **Config surface shipped (T1).** `Oidc` reshaped to 8 components: `provider-url`, `client-id`, `client-secret-path` (nullable), `client-mtls-keystore` (nullable node, new `ClientMtlsKeystore` record mirroring the `TrustStore` leaf), `trust-store` (@NotNull, reused `TrustStore` record), `timeout`, `max-in-flight` (@NotNull + `@Min(1)`), `jwks-cache-ttl`. `client-credential-path` removed; blast radius updated in all 7 enumerated files **plus `CompanionRoleFailFastTest.java:63`** (direct Oidc construction the enumeration missed) and the two `application.yml` comment examples.
- **USER-DIRECTED DESIGN AMENDMENTS (5 — 1–4 applied mid-task, 5 applied post-review 2026-08-19; they supersede the corresponding AC8/task text above, which is left as-written per story-file rules):**
  1. *Budget keys required, not ctor-defaulted:* `timeout`/`max-in-flight`/`jwks-cache-ttl` are NON-NULL `@NotNull` components with NO compact-ctor normalization and NO public default constants. Defaults live in `application.yml`'s forward template (uncommenting a branch yields working `4s`/`64`/`5m`) — the `companion.bind.adjudication-deadline` pattern. Structural note recorded in the record javadoc: live yml defaults for branch keys are impossible (any uncommented `companion.forward.*` key binds the Forward branch for every cell → every reverse boot refuses "found 2"), so yml is the documentation surface and the annotations are the enforcement.
  2. *Validator = file existence only:* `requireOidc` keeps the pre-existing provider-url https/blank checks and adds `requireReadableFile` for `client-secret-path`, `client-mtls-keystore.path`, `trust-store.path`. NO exactly-one-of pair check, NO IdP trust-store 5-state PKIX load, NO `jwks-cache-ttl` positivity in the validator — conformance is annotation-level; the exactly-one-of resolution and the store/keystore PKIX loads move to the T2+ adapter's SSLContext build (a bad store refuses startup at bean init, fail-closed). **T2 must pick these up** (AC7's "exactly-one-of enforced fail-fast" now lands at the adapter factory, not the config validator).
  3. *Timeout window not validated:* the 2s–5s PERF-3 bounds and the ≤ `adjudication-deadline` relation are documented as the `OIDC_TIMEOUT` comment in `application.yml` — an operator contract, deliberately unenforced (no validator code, no cross-field check).
  4. *`max-in-flight` described:* full component javadoc (AD-28(4) admission cap; rejection threshold, not a queue depth — saturated = fail-closed `DenyIndeterminate` with no wire call) + one-line yml comment.
  5. *RFC 8705 IdP mTLS client-auth option REMOVED (2026-08-19, user-directed, post-review):* the `client-mtls-keystore` component and `ClientMtlsKeystore` record are deleted from `Oidc`; `client-secret-path` is the sole provider client auth and is now `@NotNull`-required (validator: unconditional file-existence check; yml template updated). Supersedes AC7's "client auth = **exactly one of** `client_secret` … or mTLS keystore", AC8's `client-mtls-keystore` bullet, and AC9's live path 3 — the production live suite becomes **3-path** (JWT / introspection / bad-credentials DENY). The 2-1 test-tier slice stays untouched and green (`RopcSliceLiveTest` path 3 + fixture client `smpp-client-mtls` remain as the historical contract ratification — the slice builds its own wire calls, not through this config surface). Matrix tests −2/+1: deleted `ac8_mtlsKeystoreMissingFileRefuses` + `ac8_mtlsArmedReverseCellBoots`, added `ac8_absentClientSecretPathRefuses` (@NotNull, the absent-budget-key pattern). Two direct-construction sites fixed for the new 7-component arity (`CompanionConfigMatrixTest.reverseAProps`, `CompanionRoleFailFastTest`). `:proxy:test` 226/0/0 (XML), `:proxy:check` GREEN. Review-finding interaction: the F1 patch item's ":237 exactly-one-of" sub-claim is moot (text deleted with the option); F1's remaining javadoc claims (5-state depth, timeout window, defaults, forward-template wording) still stand.
- **New T1 tests (12 in `CompanionConfigMatrixTest`):** absent `client-id` / absent budget keys (×3, @NotNull); `max-in-flight` 0/−3 (@Min, exact invalid values bound); `max-in-flight=1` boundary boots; missing-file refusals for `client-secret-path`, `client-mtls-keystore.path`, `trust-store.path` (bad paths bound, never removed keys); absent `trust-store` node; mTLS-armed forward cell boots (empty keystore file is a valid T1 bind — PKCS12 load is T2+'s). Dropped from the original plan by amendment 2/3: exactly-one-of both/neither, timeout window/boundary/deadline cross-field, ttl positivity, IdP 5-state. `requiredSecretTypes` lost the oidc entry (option-typed now — removal no longer refuses; its missing-file case is `ac8_clientSecretMissingFileRefuses`).
- **HTTPS stand-in discovery server (`OidcDiscoveryStandIn`, testsupport):** lazy-shared singleton (double-checked volatile), free-port-probed loopback `HttpsServer`, `HttpsConfigurator` over the fixture's `server.pem`/PKCS#8 `server-key.pem` (JDK `KeyFactory` PEM parse — no BouncyCastle, AD-36; SEC-090: `com.sun.net.httpserver` is test-tier only), serves `/.well-known/openid-configuration` whose `issuer` echoes its own base URL (the T2 `issuer == provider-url` check), shutdown-hooked. `TestCompanionConfigs.forwardA()/forwardC()` AND the four full-context boots default `provider-url` to it, with `oidc.trust-store` pointing at the fixture CA store (`RelayTestFixtures.idpTrustStoreFixture`) so the same configs stay valid from T2 on, when discovery actually handshakes (the stand-in's cert chains to that CA). Forward cells are bootable without Docker; no test weakened or skipped.
- **Deferred to T2 (now load-bearing there):** exactly-one-of client-auth resolution + PKCS loads at the SSLContext factory; discovery probe consumption of the stand-in; `grant_types_supported` DAG warning.
- **IdP link shipped (T2) — two `security/` components, JDK-only (SEC-090-clean: `javax.net.ssl` only as JDK `SSLContext`/`SSLParameters`/`TrustManagerFactory`; no `HttpsParameters` in main).** `IdpSslContextFactory` (@Component, eager ctor): resolves the reverse-cell oidc node via a shared `ResolvedOidc(keyPrefix, oidc)` (single resolution site — the leaf-selection cannot drift between IdP-link components; forward cells → inactive, accessors throw ISE, never null); loads the IdP trust store at the full 5-state depth (not-readable re-check, zero-byte, wrong-format/wrong-password via `KeyStore.load`, zero-`trustedCertEntry` — all refuse at bean init with SEC-050/AD-13 messages; password `char[]` zeroized on every path, AD-10); builds the context on the JDK-default `TrustManagerFactory` (PKIX defaults, no custom chain code, no key managers — client auth is the ROPC `client_secret`, so the context is trust-only); intersects `companion.tls.*` with THIS context's supported suites AND protocols on both axes → empty refuses (D2 discharge for the IdP context), and the INTERSECTION (configured order preserved) becomes the effective `SSLParameters` so an unsupported suite name can never reach an `SSLEngine`.
- **Discovery shipped (T2).** `OidcStartupDiscovery` (@Component, eager ctor, runs after the factory bean): GETs `<provider-url>/.well-known/openid-configuration` once at refresh over a transient TLS-armed `HttpClient` (closed after the one call — the steady-state shared client is T3's) with `oidc.timeout` as both connect and request timeout; any failure refuses (non-200 / unparseable / missing field / unreachable / interrupted — all "(AD-12, fail-closed A-2)"); field-completeness first (issuer + token + RFC 7662 introspection + jwks_uri — a provider omitting one cannot back the AD-12 contract), then the exact `issuer == provider-url` equality, then the DAG loud warning (starred WARN banner, the Mode B/over-budget pattern) when `grant_types_supported` lacks `password`. Exposes `OidcProviderMetadata(issuer, tokenEndpoint, introspectionEndpoint, jwksUri)` (F5-style compact-ctor null-guards) for T3/T4/T5. Discovery JSON parses via Nimbus `JSONObjectUtils` — no Jackson on the classpath (non-web `spring-boot-starter`), the `RopcSlice` precedent.
- **T2 discharges/notes:** (a) the T1 amendment-2 deferrals are settled — the PKCS load + DAG warning landed here, and the "exactly-one-of client-auth resolution" item is MOOT (amendment 5 removed the mTLS arm; `client-secret-path` is sole + `@NotNull`); (b) the 2-1-era deferred hostless-provider-url finding discharged at the discovery URI build ("https://:8443" refuses SEC-053/054); (c) **the four full-context boots**: the reverse-B boots (`RelayServerLifecycleTest`, `DirectMemoryBudgetStartupCheckTest`, `CompanionConfigMatrixTest`'s two full-app boots) now run REAL TLS discovery against the T1 stand-in at every boot (fixture-CA trust store handshakes with the stand-in's cert chain — the first real client of the stand-in, green); `BootstrapLifecycleTest`/`CompanionTlsBindingTest`'s full boots are forward-A (no oidc — beans inactive, never refuse); the runner-based matrix suites load `@EnableConfigurationProperties`-only configs, so the new beans do not load there. (d) `OidcDiscoveryStandIn` gained `fixtureServerSslContext()` so the discovery failure-matrix tests spin ad-hoc fixture-cert HTTPS servers (base-URL-aware body — a well-formed doc can echo its own issuer); no T1 behavior changed.
- **New T2 tests (21):** `IdpSslContextFactoryTest` (11): reverse×A/B/C active + keyPrefix pins, forward inactive + accessor fail-fast, 5 store refusals (wrong password / garbage file / zero-`trustedCertEntry` / missing file / zero-byte — exact bad values bound, never removed keys), empty suite + empty protocol intersection refusals; `OidcStartupDiscoveryTest` (10): stand-in happy path (issuer + all three endpoints pinned), forward inactive, issuer mismatch / 503 / malformed body / missing `token_endpoint` / unreachable dead port / hostless URL refusals, DAG-missing warn-but-discover (output-captured), and the full-app-boot refusal (standard Spring startup failure, chain-verified). Suite 230 → **251/0/0/0**, `:proxy:check` GREEN.
- **Adapter core shipped (T3) — `RopcBindCredentialVerifier`, one final class in `security/`, no Spring annotation (T7's per-cell `@Configuration` constructs it: `new RopcBindCredentialVerifier(new IdpSslContextFactory(props), new OidcStartupDiscovery(props, factory))` — the exact shape the test suite constructs).** Key decisions:
  - **Refined AC2 table as the only `DenyInvalid` sources:** bare 401 (even unparseable body) and 400 whose parsed `error` ∈ {`invalid_grant`, `invalid_client`}; 3xx/403/404/429/other-400/5xx/timeout/network/cancel/saturation → `DenyIndeterminate`. The `.memlog.md` supersession entry (subtask 4) records it against the spine's shorthand.
  - **Caller-owned zeroization honored — the adapter NEVER calls `cred.password().zeroize()`** (2.2 T7 settlement; the slice's saturation/completion zeroizes were dropped deliberately). The adapter's own material: the file-loaded client secret (`byte[]`, ASCII-trimmed, read-buffer wiped at load, field wiped on `close()`); form buffers are `byte[]`/`ByteArrayOutputStream`, never `String`.
  - **200 arm fails closed** (`DenyIndeterminate`, body bytes unread) until T4 lands JWT verify and T5 introspection — deliberate per the story's T3→T6 ordering; the pinned 200 rows (empty/HTML body) are final, the JWT-shaped-200 row is deliberately NOT pinned so T4 flips it consciously.
  - **Per-request budget = `oidc.timeout` clamped to remaining `RequestContext.deadline`** (`RequestContext`'s own javadoc: "the adapter derives per-call timeouts from it"); exhausted deadline → settled deny with no wire call (pinned test).
  - **F2 ordering kept from the slice:** `sendAsync` on the caller thread only after `tryAcquire`, `activeCall` published before `verify()` returns (race-free cancel); the pool task re-binds the context via the same `ScopedValue` handle around the STS (`open(Joiner.awaitAllSuccessfulOrThrow())` — verified compiling shape, matching the ratified slice).
  - **Use-after-close catch is `RejectedExecutionException | IllegalStateException`** — the pool may reject the task OR the closed shared client may refuse `sendAsync`; both arms release the permit, abort whatever fired, settle `DenyIndeterminate` (test drives the closed adapter through TWO verifies — no hang, no throw, and no permit-strand).
  - **`close()` minimal shutdown seam** (pool `shutdownNow()` + client close + secret wipe) so the hardening is test-reachable now; T7's SmartLifecycle stop body (deny-in-flight await + fail-closed log, AD-22 ordering) wraps it.
- **New T3 tests (15 in `RopcBindCredentialVerifierTest`, all unit-tier in-process):** the AC2 status rows (bare-401-unparseable, 400 invalid_grant/invalid_client/other-error/unparseable, 403/404/429 rotation, 5xx, 302-with-Location, 200-empty/HTML), timeout (250ms budget vs latched stand-in), unreachable token endpoint (discovery doc pointing `token_endpoint` at a probed dead port — discovery stays well-formed), saturation/F2 (max-in-flight=1, latched handler, `tokenHits` counter + <1s elapsed), exhausted-deadline (no wire call), unbound-`ScopedValue` handle (settled deny, no throw, no wire call), closed-adapter double-verify, and the ctor fail-fast triple (max-in-flight 0 / missing secret file / blank secret file — arm-specific message assertions after the MUTANT-3 masking lesson).
- **USER-DIRECTED FIXME PASS on the T3 adapter (2026-08-19, same day; 4 user-marked FIXMEs — resolutions):**
  1. *Record-wrapper around the secret byte[]* + 4. *Extract utility class* — **merged into one type**: package-private `ClientSecret` record in `security/` (own file = the extraction; the `Password` pattern for the adapter's own material): `value()` (raw bytes for form encoding), `zeroize()` (single canonical wipe; `close()` now calls it instead of a bare `Arrays.fill`), redacting `toString()`, identity-based `equals` documented + `@SuppressWarnings("ArrayRecordComponent") // reason:` (ErrorProne warning-level — the byte[] component is load-bearing and comparison is never meaningful), and `static load(Path, configKey)` carrying the AD-18 read with the refusal messages byte-identical (the adapter-ctor tests pin them unchanged).
  2. *Form capacity* — the magic `160` was NOT worst-case-sufficient (a percent-heavy form runs ≈330 B; `ByteArrayOutputStream` would grow silently). Replaced with the exact bound `62 + 3×(clientId + username + secret + password lengths)` (62 = the four literal keys; 3 = `%XX` per octet) — single allocation, never grows.
  3. *Apache Commons Codec for the encoder* — **resolved as won't-do with rationale** (the T2 FIXME-pass pattern): commons-codec is on NO proxy classpath (verified `runtimeClasspath` — a new runtime dependency for a twelve-line encoder), `URLCodec`'s `www-form-url` safe set differs from RFC 3986 unreserved (`*` allowed, `~` escaped), and `security/` stays JDK+Nimbus by design (AD-36 dependency-minimalism — the same rule that kept `oauth2-oidc-sdk` out). Rationale lives on `urlEncode`'s javadoc; the encoder is now pinned end-to-end by the form-capture test (`username=test%20user`, `password=p%40ss%2Bw`).
- **New FIXME-pass tests (4):** `ClientSecretTest` (3 — load trims ASCII whitespace incl. the trailing-newline case, zeroize wipes + idempotent, toString never renders); `RopcBindCredentialVerifierTest.formEncodesIdentityAndSecretFromRawBytes` — captures the stand-in-received form body end-to-end and pins the raw-byte encoder (reserved octets percent-encoded: space/`@`/`+`), the trimmed secret (`client_secret=…&` with no `%0A`), and the verbatim grant/client-id fields.
- **JWT defense-in-depth + JWKS cache shipped (T4, AC3) — the adapter's 200 arm is live.** `verifyJwt` (adapter): parse &rarr; `typ` (absent tolerated; present &ne; `"JWT"` &rarr; deny, RFC 8725 &sect;3.9) &rarr; `kid` &rarr; cached-set lookup &rarr; RS256 signature &rarr; `iss` == the DISCOVERED issuer &rarr; `aud` &sup; client-id &rarr; `exp` present + not-past-skew &rarr; `nbf` not-future-skew (60s `CLAIM_SKEW`, the ratified slice's value) &rarr; `Allow`; EVERY failure — malformed JWT, cold cache, kid miss, wrong signer, claim misses — is `DenyIndeterminate` (unverifiable &ne; invalid, AD-11; DENY wins over the provider's 200). The 200-arm dispatch is structural: no `access_token` &rarr; deny (final row); a NON-3-segment token is OPAQUE &rarr; deny pending T5 (the malformed-vs-opaque boundary is test-pinned so T5's flip of the opaque arm is conscious); 3 segments &rarr; `verifyJwt`. The verify is pure CPU against the cached set — no fork in the STS, no foreground fetch ever (the `adjudicate` javadoc now records why).
- **`JwksCache` (new package-private component, `security/`) — the ratified slice's cache, productionized against its two filed defects.** Single `AtomicReference<@Nullable JWKSet>` with exactly ONE write site (`keys.set(fetched)` on a successful fetch) — never nulled anywhere (§6b), never evicted for AGE (staleness is bounded by the refresh cadence, not by dropping keys: an empty cache denies every bind indeterminately, strictly worse than a stale one that still rejects unknown kids), and an empty-set-200 is treated as provider misbehavior (retained, WARN). Scheduler: `newScheduledThreadPool(1, virtual "jwks-refresh-")` — hand-rolled VT per AD-28(2), NOT `@Scheduled`, and with NO reference to the adjudication pool or admission semaphore (§6a isolation is structural — the strongest form the guard can take); initial refresh at construction (ASYNC — cold-cache binds deny fail-closed until it lands; deliberately NOT a startup refusal: discovery misconfiguration is permanent, a JWKS outage is transient, A-2); cadence = TTL/2 refresh-ahead (dev-selected default per AC3's "spine-deferred values", floored 1ms against `scheduleWithFixedDelay`'s zero rejection); kid-miss/cold triggers `requestRefresh()` — serialized single-flight on the same one-thread scheduler (the bound); every refresh arm is internally guarded (a periodic task that throws would suppress the WHOLE schedule). `close()` = `shutdownNow()`, invoked FIRST by the adapter's `close()` (AD-22 refresh-before-cache). JWKS material is PUBLIC — no zeroization obligation (AD-10 covers secrets).
- **§6a honest scope note (recorded, not hidden):** the M-6A spot control neutered the PERIODIC cycle (what the saturation test observes) because faithfully emulating pool-routing would require a constructor seam — `JwksCache` structurally holds no pool/semaphore reference to mutate. The guard is carried jointly by the structural isolation (reviewable: the class cannot route through what it cannot see) and the behavioral pin (refresh lands while ALL admission permits are held). T10's consolidated adversarial pass owns any sharper control.
- **Test seam:** `RopcBindCredentialVerifier.jwksCache()` package-private accessor (the slice's `verifyWithJwks` precedent) — cache-population preconditions in the adapter suite, and the whole `JwksCache` contract asserted component-level in `JwksCacheTest` against a plain-HTTP stand-in (the component takes any `HttpClient`/URI; the TLS leg runs end-to-end in the adapter suite).
- **New T4 tests (16):** `JwksCacheTest` (7): initial populate; §6b retention under non-200 AND unparseable-200; whole-swap (rotated-OUT kid disappears); empty-set retention; periodic (TTL/2) swap without any bind or kid miss; close-stops-the-refresh (AD-22); non-positive-ttl ctor guard (the T3 typed-guard pattern, `@DurationMin` bypass). `RopcBindCredentialVerifierTest` (+9): the Allow flip (full defense-in-depth pass); typ absent &rarr; Allow; typ unexpected ("Bearer" — the conflation literal) &rarr; deny; kid miss &rarr; deny NOW with the triggered refresh held on a latch the verdict never waited on, then same-cache Allow; wrong signer; the claim battery (iss/aud/missing-exp/expired/future-nbf); malformed + opaque boundary rows; cold cache &rarr; fast fail-closed deny (<2s, no wire wait on JWKS) + recovery-Allows on a healthy cache (no verdict caching); bind saturation does not starve the refresh (§6a).
- **Introspection fallback shipped (T5, AC4) — the adapter's issued-token dispatch is complete.** `RopcBindCredentialVerifier.introspect`: a non-3-segment 200 token goes to `metadata.introspectionEndpoint()` (discovery-derived — no override keys), `sendAsync` registered in the SAME active-call slot (`activeCall.set(intro)` — F3: the token exchange is complete by then, so a late `cancelHttp()` aborts round 2), the join INLINE on the pool thread, and `mapIntrospection` instantiates the AC2 rows: ONLY 200 + JSON + BOOLEAN `active:true` &rarr; `Allow`; `active:false` / missing / STRING-typed `active` / non-JSON / every non-200 (incl. 401 — that is CLIENT-auth trouble, never the user's invalid password) &rarr; `DenyIndeterminate`. The round-2 budget is re-clamped to the REMAINING adjudication deadline (&le;0 &rarr; deny with no wire call — the deadline is the verifier's WHOLE budget, not a per-round allowance). F6 is structural: the arm holds no JWKS reference at all (dead-JWKS-leg test). Results never cached — two binds = two round trips, pinned with a provider flip (`active:false` &rarr; deny, flip to `active:true` &rarr; Allow).
- **T5 design correction (the T3 note said "T5 forks its arm into the same scope" — the JDK forbids it):** JDK 25 `StructuredTaskScope` is SINGLE-JOIN — a second `scope.join()` after the token arm's throws `IllegalStateException` ("Already joined or scope is closed", verified empirically; the ISE landed in the fail-closed catch and MASKED itself: all deny rows green, only Allow rows red). Round 2 therefore joins inline (the ratified slice's own shape — `adjudicateByIntrospection` never forked either), and the uninterruptible `CompletableFuture.join()` is bounded by the clamped per-request timeout, so a lost cancel cannot park the pool thread. Recorded on both javadocs + session auto-memory.
- **Introspection client auth = RFC 6749 &sect;2.3.1 Basic, raw-byte assembled** (the ratified slice's header shape, ratified live in 2-1 path 2): `clientId:secret` concatenated in a `byte[]` &rarr; base64 (the JDK client's only header representation — the PLAIN secret never becomes a `String`; the concatenated raw buffer is wiped immediately after, AD-10); the `token=` form body percent-encoded from the token's UTF-8 bytes by the same raw-octet encoder as the ROPC form (AC6 discipline; the full zeroization sweep over both forms' buffers is T6's subtask).
- **AC4 operator guidance placement:** the `Oidc` record javadoc already carried it since T1 ("prefer JWT-issuing providers — introspection is online-only, never cached"); T5 adds the yml docs line (reverse mode-a template, after the budget keys). No runtime warning is emitted (docs-time guidance only, per AC4).
- **New T5 tests (10):** the Allow row (200 + JSON + boolean `active:true`); `active:false`; the malformed-body rotation (non-JSON / missing `active` / STRING `"true"` — the boolean-typing pin); the non-200 rotation 401/404/500 &rarr; `DenyIndeterminate` NEVER `DenyInvalid`; round-2 timeout (250ms budget vs parked handler); F3 cancel — the PERMIT observable (max-in-flight=1: a cancelled round 2 frees the permit instantly &rarr; bind 2 runs to `DenyInvalid`; a neutered registration saturates bind 2 with no wire call); F6 (dead JWKS leg, opaque bind still Allows); never-cached (the provider flip); request shape (POST + the discovered path + Basic decoding to the configured id:secret pair + `token=` form + form-urlencoded content type); the round-2 budget clamp (700ms deadline vs 4s `oidc.timeout`). The T4 boundary test was rewritten `malformedAndOpaqueTokens…` &rarr; `malformedJwtDeniesWithoutIntrospection` (introHits==0 — the dispatch boundary pin now guards the introspection direction).
- **Cancellation + zeroization shipped (T6, AC5/AC6) — F7 proven, the adapter's own buffers wipe on every completion path.** The F7 half is a PROOF task, not new behavior: the production-tier `RecordingHttpClient` (the 2-1 wrapper pattern, injected through the adapter's always-inject ctor — the 2-arg delegates to `IdpSslContextFactory.newClient()`, the single client recipe) drives both cancel windows against real TLS exchanges and asserts the synchronous `DenyIndeterminate` settlement, the `CancellationException`-anywhere-in-chain abort of the UNWRAPPED JDK future for round 1 AND round 2 (the T5-deferred wire proof — T5's TCP-half-close finding made the wire the only reliable abort signal, which is exactly what the recording client sees), and the permit-free/STS-unwind observable (bind 2 runs to `DenyInvalid` at max-in-flight=1). The zeroization half: a per-adjudication registry on the new `Adjudication` holder, wiped at exactly two sites — the pool task's `finally` (permit frees FIRST so a waiting bind never queues behind a wipe; then every array) and the use-after-close catch (§2.1 item 2's "release permit + zeroize" made literal — the form's only completion path when the pool rejected the task).
- **`Adjudication` (T6 refactor): one holder replaces the T5 param threading.** `pin` + `activeCall` + `rc` + the sensitive-buffer registry traveled as three loose params through `settleAdjudication` &rarr; `adjudicate` &rarr; `mapTokenResponse` &rarr; `adjudicateIssuedToken` &rarr; `introspect` since T3/T5; T6's registry would have made it four. The holder is the honest object boundary (all four share one lifecycle: created on the caller's thread in `verify()`, handed to the pool task, dead at settlement). `RopcVerdictRequest` keeps its shipped (pin, activeCall) shape, built from the holder's fields. Not registered, deliberately: the bind password (caller-owned, F1/2.2 T7 — the adapter reads it asynchronously while its future is pending) and the file-loaded client secret (wiped once at `close()`); the registry holds only per-adjudication transient copies.
- **`FormBuffer` + `FormPublisher` (T6): zero-copy form publishing — a JDK-verified finding, not a style choice.** `BodyPublishers.ofByteArray` COPIES the whole body into fresh `ByteBuffer.allocate` chunks at subscribe time (verified in the 25.0.3 sources, `RequestPublishers.ByteArrayPublisher.subscribe` &rarr; `copy(...)`) — two consequences: (a) hygiene — the JDK would hold un-wipeable copies of the credential-bearing form; (b) observability — the adapter's array is invisible to the wire, so no recording client could prove the wipe. Both are served by publishing the adapter's exact array: `FormBuffer` (bound-allocated `byte[]` + cursor — also removes the BAOS-internal-buffer + `toByteArray` SECOND copy the T3 shape carried) and `FormPublisher` (single-item `Flow` delivery of `ByteBuffer.wrap(buffer, 0, length)`; a plain final class, not a record — ErrorProne `ArrayRecordComponent`). The wire bytes are unchanged (the form-encoder tests stayed green through the refactor), and the recording client captures the adapter's array BY IDENTITY at `sendAsync` time — the wipe assertions run on the exact array that went on the wire. The response bodies are captured by identity too (a handler-wrapping `thenApply` passes the same reference the adapter's `response.body()` returns), proving the access-token bytes wipe.
- **F7 honest scope note (recorded, not hidden):** the guaranteed settlement is deliberately DOUBLE-COVERED — `cancelHttp()` completes the pin itself AND every pool path (task `finally`, cancel-unwound join, timeout-bounded join) settles fail-closed. The M-SETTLE mutation (drop cancelHttp's own completion) therefore stays GREEN across repeated runs: the pool task's unwind wins the race. The synchronous-settlement assertion pins the CONTRACT (settled the moment `cancelHttp()` returns) but is not a single-arm mutation control; the bite-able cancel-arm control is M-CANCEL (the wire abort). The test comment records this so nobody "fixes" the comment into an overclaim later.
- **New T6 tests (5):** round-1 cancel (parked token handler: synchronous settlement + wire abort cause-chain + permit freed &rarr; bind 2 `DenyInvalid` + form zeroized); round-2 cancel (parked introspection handler — the T5-deferred F3 WIRE proof: the round-2 future aborts with `CancellationException` in the chain, both rounds' forms wipe, permit frees); zeroization on the JWT-Allow path (form + access-token response body); on a `DenyInvalid` row (the password-bearing form wipes even when the verdict is deny); on the introspection-Allow path (both forms + both response bodies). Observables: request arrays by identity through `FormPublisher`, response arrays by identity through the handler wrap — RED-on-neuter via M-WIPE &rarr; 5 RED.
- **User FIXME observed mid-T6 (2026-08-20, uncommitted, `codec/…/SmppBindRequest.java:51` — left for its owner, NOT absorbed; the marker itself was removed by its owner later the same day, the direction stands recorded here):** *"create wrapper record around byte[] with zeroize() method and decode bind request to the records instead of AsciiString"* — a codec-layer direction to carry the password as a zeroizable byte[]-backed record instead of `AsciiString` (the root of the lazy-`toString` immortal-cache hazard the F1 rule and `NoStringFromPasswordTest` work around). Out of 3.2 scope on two independent grounds: `codec/**` is this story's DO-NOT-TOUCH list, and `Password(AsciiString)` is an AC8-immutable port type — the change needs its own story + port-shape decision. T6 is CONSISTENT with the direction and does not pre-commit against it: the adapter's F1 arm reads the password only through `array()/arrayOffset()/length()` (raw backing bytes — the same surface a byte[]-wrapper record would expose), never wipes it (caller-owned), and registers nothing password-backed in the wipe set; when the wrapper lands, `tokenRequest`'s three accessor calls are the single touch point. Not counted by AC10's "no NEW FIXME" gate (owner-authored, not story-introduced; codec untouched by this story).

- **Wiring shipped (T7, AC1) — `VerifierWiringConfig`, one `@Configuration` in `security/`.** Exactly one `BindCredentialVerifier` `@Bean` per cell, selected by `properties.reverse() != null` — the populated reverse branch IS the selector, so the selection cannot drift from the AD-17 config model (and cannot be bypassed by a stray key: `ignoreUnknownFields=false` refuses those at bind time). **The selection direction is the AMENDED one** (AD-12, 2026-08-18): every reverse cell → `new RopcBindCredentialVerifier(tlsFactory, discovery)` (fail-closed at bean construction — a blank secret file refuses refresh with the SEC-060 message, matrix-pinned); forward.mode-a/c → `new AlwaysAllowBindCredentialVerifier()` (the T2 beans construct on forward cells too, but inactive — the adapter branch is simply not taken). **`AlwaysAllow` lost its `@Component`** (an unconditional component would collide with the adapter in every reverse context — M-ALLOW-COMPONENT proves both forward AND reverse scanned boots die on the collision); its test's assertion inverted to a NOT-annotated pin. The adapter bean's **inferred `close()` destroy method is deliberate**: the lifecycle stop runs the drain first (phase-ordered), and the destroy call is the idempotent backstop for a boot that fails before the lifecycle ever starts (the `RelayNettyConfig` pattern). No `relay/` file changed — the acceptor/interceptor keep injecting the unchanged port type.
- **Lifecycle shipped (T7, AC1) — `AdjudicationLifecycle` + the adapter's full AD-22 `close()`.** The lifecycle (`ADJUDICATION_PHASE == ProxyCompanionLifecycle.APP_PHASE`, strictly below `RELAY_ACCEPTOR_PHASE`; running-flag idempotence; `stop(Runnable)` runs the callback in `finally` — the `RelayServerLifecycle` house pattern) delegates the drain to the adapter's `close()`, which was **REORDERED from the T3/T4 seam to the literal AD-22 step order**: (1) deny in-flight — `shutdownNow()` (each pool task settles fail-closed in its catch) + `awaitTermination(oidc.timeout + 1s)` with the fail-closed WARN on timeout/interrupt (§2.1 item 5); (2) `jwks.close()` (the AD-22 refresh-before-cache invariant — the refresh stops before the client it fetches through closes); (3) `http.close()` (the hard close that aborts anything the drain missed — every aborted join settles `DenyIndeterminate`); (4) `clientSecret.zeroize()`. The prior seam stopped the JWKS refresh FIRST; the reorder is safe because a draining task's `requestRefresh()` can at worst enqueue onto a still-live scheduler whose fetches happen before the client closes.
- **T7 honest scope notes (recorded, not hidden):** (a) the **drain-timeout WARN arm is structurally shadowed** — every in-flight task is self-bounded by the per-request timeout clamp (≤ `oidc.timeout` < the drain bound `timeout+1s`), so the arm is defensive belt-and-braces, not a reachable row; (b) **M-DRAIN is not a mutation control** — dropping `awaitTermination` is flaky-by-race (the interrupt settles the parked task before the assertion in most runs; the F7/M-SETTLE double-coverage precedent — the settlement-at-return pin plus M-CLOSE carry the guarantee); (c) the runner-based wiring suites use `withUserConfiguration` (no component scan), so a stray-`@Component` regression cannot surface there — the bite lives in the annotation pin + the component-scanned full-app boots (proven by M-ALLOW-COMPONENT's split: 1 runner RED, 4 full-boot RED).
- **T7 blast radius (all green, none weakened):** the reverse full-app boots (`RelayServerLifecycleTest`, `DirectMemoryBudgetStartupCheckTest`, `CompanionConfigMatrixTest`'s SEC-052 ack boot) now construct the adapter bean end-to-end (TLS factory → stand-in discovery → secret load → JwksCache async initial fetch — a 404 on the stand-in's un-served `jwks_uri`, WARN-only by design); the three secret fixtures went from empty files to real content (an empty file is a guaranteed SEC-060 refusal once the adapter loads it). `TestCompanionConfigs` is now PUBLIC (the wiring suite consumes the same five-cell bases cross-package — one canonical source). Forward full boots (`BootstrapLifecycleTest`, `CompanionTlsBindingTest`, `RelayServerLifecycleTest`'s forward case) wire `AlwaysAllow` from the config — exactly one verifier, no oidc keys, unchanged.
- **New T7 tests (9):** `VerifierWiringConfigTest` (6): the five-cell exactly-one matrix (reverseA/B/C → `RopcBindCredentialVerifier`, forwardA/C → `AlwaysAllowBindCredentialVerifier`; each context also pins the `AdjudicationLifecycle` running + below-acceptor phase) and the blank-secret bean-construction refusal (the exact bad value bound through real Spring binding). `AdjudicationLifecycleTest` (3): the phase pins (== `APP_PHASE`, < `RELAY_ACCEPTOR_PHASE`); the flag/callback/idempotence contract; **deny-in-flight** — a real adapter over a parked in-process TLS stand-in (4s request budget ≫ the test window), asserting the in-flight future is SETTLED `DenyIndeterminate` the moment `stop()` returns (only the drain can settle it inside the window; the request timeout is the backstop) and every post-stop verify settles fail-closed. Plus the 1:1 rewrite of `AlwaysAllowBindCredentialVerifierTest`'s annotation test.

### File List

**Modified (main):**
- proxy/src/main/java/smpp/companion/proxy/config/ProxyCompanionProperties.java
- proxy/src/main/java/smpp/companion/proxy/config/CompanionConfigValidator.java
- proxy/src/main/java/smpp/companion/proxy/security/AlwaysAllowBindCredentialVerifier.java   *(T7 — `@Component` removed + javadoc re-targeted to the amended forward-cell role)*
- proxy/src/main/resources/application.yml   *(T1 config-surface docs; T5 adds the AC4 prefer-JWT-issuance guidance line to the reverse mode-a oidc template)*

**Modified (test):**
- proxy/src/test/java/smpp/companion/proxy/config/TestCompanionConfigs.java   *(T7: class + the five-cell factories/`put`/`propertyValues` went PUBLIC for the cross-package wiring suite; the oidc secret file now carries real content — the adapter bean loads it on full-app boots)*
- proxy/src/test/java/smpp/companion/proxy/config/CompanionConfigMatrixTest.java
- proxy/src/test/java/smpp/companion/proxy/config/CompanionRoleFailFastTest.java
- proxy/src/test/java/smpp/companion/proxy/config/CompanionTlsBindingTest.java
- proxy/src/test/java/smpp/companion/proxy/bootstrap/BootstrapLifecycleTest.java
- proxy/src/test/java/smpp/companion/proxy/relay/netty/RelayServerLifecycleTest.java   *(T7: the mode-b boot builder's secret file is non-blank — the boot now constructs the adapter bean)*
- proxy/src/test/java/smpp/companion/proxy/relay/netty/DirectMemoryBudgetStartupCheckTest.java   *(T7: same non-blank secret fix in its mode-b boot builder)*
- proxy/src/test/java/smpp/companion/proxy/security/AlwaysAllowBindCredentialVerifierTest.java   *(T7: the `@Component` assertion inverted to a NOT-annotated pin + javadoc/DisplayName re-targeted)*
- proxy/src/test/java/smpp/companion/proxy/testsupport/RelayTestFixtures.java
- proxy/src/test/java/smpp/companion/proxy/testsupport/OidcDiscoveryStandIn.java   *(T2: `fixtureServerSslContext()` exposure for the ad-hoc failure-matrix servers; no behavior change)*

**Added (test):**
- proxy/src/test/java/smpp/companion/proxy/testsupport/OidcDiscoveryStandIn.java
- proxy/src/test/java/smpp/companion/proxy/security/IdpSslContextFactoryTest.java   *(T2)*
- proxy/src/test/java/smpp/companion/proxy/security/OidcStartupDiscoveryTest.java   *(T2)*
- proxy/src/test/java/smpp/companion/proxy/security/RopcBindCredentialVerifierTest.java   *(T3; +1 test in the same-day FIXME pass; +9 AC3 tests in T4 — 200-arm JWT rows, and the stand-ins gained a JWKS endpoint + an 8-thread daemon executor; +10 AC4 introspection tests in T5 — the stand-ins gained the introspection context at the discovered path, and the T4 malformed/opaque boundary test was rewritten to guard the intro direction; +5 AC5/AC6 cancellation/zeroization tests in T6 — the nested production-tier `RecordingHttpClient` (identity captures of the adapter's request arrays via `FormPublisher` and response arrays via a handler wrap) + the recording-client/adapter fixtures)*
- proxy/src/test/java/smpp/companion/proxy/security/ClientSecretTest.java   *(T3 FIXME pass)*
- proxy/src/test/java/smpp/companion/proxy/security/JwksCacheTest.java   *(T4 — the component contract: §6b whole-swap, empty-set policy, periodic refresh, close, ctor guard)*
- proxy/src/test/java/smpp/companion/proxy/security/VerifierWiringConfigTest.java   *(T7 — the five-cell exactly-one matrix + the blank-secret bean-construction refusal; consumes the public TestCompanionConfigs bases)*
- proxy/src/test/java/smpp/companion/proxy/security/AdjudicationLifecycleTest.java   *(T7 — phase pins, flag/callback/idempotence, deny-in-flight over a parked stand-in)*

**Added (main):**
- proxy/src/main/java/smpp/companion/proxy/security/IdpSslContextFactory.java   *(T2; T6 post-pass: `newClient()` — the single provider-facing client recipe the adapter's 2-arg ctor and the recording tests share)*
- proxy/src/main/java/smpp/companion/proxy/security/OidcStartupDiscovery.java   *(T2)*
- proxy/src/main/java/smpp/companion/proxy/security/RopcBindCredentialVerifier.java   *(T3 — the production adapter core; T4 adds the JWT defense-in-depth arm, the 200-token dispatch, the jwksCache() seam, and AD-22 close ordering; T5 adds the RFC 7662 introspection arm — inline join after the single-join STS correction, F3 activeCall registration, round-2 budget clamp, Basic-auth/raw-byte introspectRequest; T6 adds the `Adjudication` holder (pin/activeCall/rc/sensitive-registry, one object replacing the param threading), the two wipe sites (task `finally` + use-after-close catch), zero-copy `FormBuffer`/`FormPublisher` form publishing, and the always-inject ctor — 2-arg delegates to `IdpSslContextFactory.newClient()`, 3-arg package-private takes the built client, no nullable branch; T7 rewrites `close()` to the full AD-22 stop body — deny-in-flight `shutdownNow()` + `awaitTermination(oidc.timeout+1s)` + fail-closed WARN, then JWKS-refresh stop before client close — and adds `@Slf4j` for the drain log)*
- proxy/src/main/java/smpp/companion/proxy/security/ClientSecret.java   *(T3 FIXME pass — typed zeroizable client secret + the AD-18 loader, extracted from the adapter)*
- proxy/src/main/java/smpp/companion/proxy/security/JwksCache.java   *(T4 — the whole-swap-only JWKS cache + its isolated AD-28(2) refresh scheduler)*
- proxy/src/main/java/smpp/companion/proxy/security/AdjudicationLifecycle.java   *(T7 — the adjudicator's SmartLifecycle: APP_PHASE slot strictly below the relay acceptor, running-flag idempotence, stop() wraps the adapter's AD-22 close)*
- proxy/src/main/java/smpp/companion/proxy/security/VerifierWiringConfig.java   *(T7 — the security-side wiring: exactly one BindCredentialVerifier per cell, the populated reverse branch as the selector; reverse.* → ROPC adapter, forward.mode-a/c → AlwaysAllow; registers the lifecycle bean)*

**Modified (planning artifacts — AD-12 amendment, same day):**
- _bmad-output/planning-artifacts/architecture/architecture-smpp-companions-2026-07-19/ARCHITECTURE-SPINE.md (AD-12 two-proxy-flow, AD-17 matrix, AD-26)
- _bmad-output/planning-artifacts/architecture/architecture-smpp-companions-2026-07-19/.memlog.md (dated amendment entry)
- _bmad-output/planning-artifacts/epics.md (AD-17:188, AD-12:197 bullets)
- _bmad-output/planning-artifacts/architecture/architecture-smpp-companions-2026-07-19/.memlog.md   *(T3 — appended the AC2 verdict-mapping supersession entry; append-only, nothing above it touched)*

- **ARCHITECTURE AMENDMENT (2026-08-18, user-directed, mid-T1): AD-12 adjudication moved to the REVERSE role.** The user's corrected trust model, adopted exactly: `client → forward → (internet) → reverse → SMSC`; the `client→forward` leg is trusted (no TLS/mTLS/IdP — the forward is a trusted-side relay holding NO OIDC material); the **reverse is the sole enforcement point** — it ROPC-adjudicates every bind via the IdP before forwarding anything to the SMSC ("the reverse protects the SMSC"); the modes (A TLS / B plaintext / C mTLS) apply to the **internet leg**; the `reverse→SMSC` leg is **always plaintext** (trusted network); **Mode B has no forward proxy** — the legacy client connects directly to the reverse over the plaintext internet leg and the reverse still adjudicates ("plaintext + ROPC", a strengthening vs the pre-amendment model where Mode B had no OIDC anywhere). Propagated: spine AD-12 two-proxy-flow + AD-17 matrix (both rows) + AD-26 peer wording; epics.md AD-17/AD-12 bullets; architecture .memlog.md dated entry. **T1 reworked same day**: the `oidc` node moved from `forward.mode-a/mode-c` to ALL THREE reverse cells (`@NotNull` on each; forward cells structurally carry no oidc component — a stray `companion.forward.*.oidc.*` key is refused by `ignoreUnknownFields=false`, matrix-pinned by `amendment_strayForwardOidcKeyRefuses`); validator `requireOidc` re-hung on the reverse validators; yml examples moved; all OIDC surface tests re-targeted to `reverse.mode-a`; the mode-b full-context boots now carry the oidc node (reverse cells adjudicate end-to-end); `RelayTestFixtures.testOidc()` added for direct-construction fixtures. **Consequences for the remaining story text (ACs left as-written per story-file rules):** AC1's wiring INVERTS — reverse.* cells → ROPC adapter, forward.* cells → `AlwaysAllow` (lands in T2/T7); the four full-context boots' stand-in dependency moves to the reverse boots (done); AC9's live 4-path suite drives the production bean on a reverse cell. Unchanged: the AD-12 port (AC8-immutable, location-agnostic), the `Oidc` record, the `OidcDiscoveryStandIn`, and all T3–T9 adapter work. Verified: `:proxy:test` 227/0/0 (XML), `:proxy:check` GREEN.

## Change Log

- 2026-08-20 — **T7 implemented** (AC1): Spring DI wiring + lifecycle — the production adapter is now a bean. New **`VerifierWiringConfig`** (`security/`): exactly ONE `BindCredentialVerifier` per cell, selected by the populated `reverse` branch itself (AD-12 amended — reverse.* → `RopcBindCredentialVerifier`, constructed fail-closed over the T2 provider link incl. the AD-18 secret load; forward.mode-a/c → `AlwaysAllow`, which lost its unconditional `@Component` — M-ALLOW-COMPONENT proves both forward AND reverse component-scanned boots die on the two-verifier collision). New **`AdjudicationLifecycle`**: the AD-22 stop window at `APP_PHASE` (strictly below `RELAY_ACCEPTOR_PHASE` — the acceptor closes first), running-flag idempotence, `stop(Runnable)` callback in `finally` (the `RelayServerLifecycle` house pattern); its stop wraps the adapter's `close()`, **rewritten to the literal AD-22 step order**: deny in-flight FIRST (`shutdownNow()` + `awaitTermination(oidc.timeout+1s)` + the fail-closed WARN on timeout/interrupt — §2.1 item 5), THEN JWKS-refresh stop before the shared-client close, then the secret wipe (the T3/T4 seam stopped the refresh first; the "refresh before the client it fetches through closes" invariant is preserved; the inferred destroy `close()` stays as the failed-boot backstop — both paths idempotent). **Blast radius surfaced and fixed:** the reverse full-app boots now CONSTRUCT the adapter bean → their `oidc-client-secret` fixtures went from empty files to real content (`TestCompanionConfigs.oidcKeys` + the two mode-b boot builders — an empty file is a guaranteed SEC-060 refusal); `TestCompanionConfigs` went PUBLIC (the wiring suite consumes the same five-cell bases — one canonical source). +9 tests (`VerifierWiringConfigTest` 6 — the five-cell exactly-one matrix + the blank-secret bean-construction refusal; `AdjudicationLifecycleTest` 3 — phase pins, flag/callback/idempotence, deny-in-flight: a parked 4s-budget bind settles `DenyIndeterminate` the moment `stop()` returns) + the 1:1 rewrite of the AlwaysAllow annotation pin; RED 8/6-green-on-stub → GREEN. 4 mutation controls (M-SELECT → 4 RED, M-CLOSE → 1 RED, M-PHASE → 6 RED, M-ALLOW-COMPONENT → 5 RED incl. the full-app boots); M-DRAIN recorded as flaky-by-race, not a control (the F7/M-SETTLE double-coverage precedent); restored from `/tmp/t7-mutations/`, zero residue. Post-`cleanTest` `:proxy:check` GREEN, `:proxy:test` **311/0/0/0** (XML; 302 + 9; 0 skipped — the Docker-gated live suites ran), compile warnings unchanged (all pre-existing in untouched files). T8 next.

- 2026-08-20 — **T6 implemented** (AC5/AC6): cancellation proven + zeroization shipped. The F7 half PROVES shipped behavior through a production-tier `RecordingHttpClient` (2-1 wrapper pattern, injected through the always-inject ctor): synchronous `DenyIndeterminate` settlement after `cancelHttp()`, `CancellationException`-in-cause-chain abort of the real wire future for BOTH rounds (the T5-deferred round-2 F3 wire proof — T5's permit observable is retained), permit-free/STS-unwind (bind 2 runs to `DenyInvalid` at max-in-flight=1). **Honest finding:** the F7 settlement is double-covered (cancelHttp completes the pin AND every pool path settles) — the drop-the-completion mutation stays GREEN across repeated runs; the test pins the contract and the comment records why (the bite-able control is M-CANCEL → 3 RED). The zeroization half: per-adjudication `Adjudication` holder (pin + activeCall + rc + sensitive-buffer registry — one object replacing the T5 param threading), wiped at exactly two sites (pool task `finally` — permit frees first; the use-after-close catch — §2.1 item 2's "release permit + zeroize" made literal); registered: both form arrays, the introspection token bytes, both response bodies; the password stays caller-owned (never registered). **JDK-verified design driver:** `BodyPublishers.ofByteArray` COPIES the form at subscribe time (25.0.3 source) — un-wipeable credential copies + no wire identity — so forms now publish ZERO-COPY (`FormBuffer` bound-allocated single array + `FormPublisher` `ByteBuffer.wrap` delivery; the BAOS/`toByteArray` double buffer is gone; wire bytes unchanged), and the recording client asserts the wipe on the EXACT array that went on the wire (response arrays by identity via a handler wrap). `NoStringFromPasswordTest` green over the adapter unchanged; M-TOSTRING (planted `password().value().toString()`) → 1 RED. +5 tests (RED 3/2-green-before-wipe — the cancel assertions proved shipped behavior; zeroization RED pre-wipe); mutations M-WIPE → 5 RED, M-CANCEL → 3 RED, M-SETTLE → 0 (recorded as double-covered, not a control), M-TOSTRING → 1 RED; restored from `/tmp/t6-mutations/`, zero residue. Post-clean `:proxy:check` GREEN, `:proxy:test` **302/0/0/0** (XML; 297 + 5; 0 skipped — the Docker-gated suites ran), warnings unchanged. T7 next.

- 2026-08-20 — **T5 implemented** (AC4): the opaque-token RFC 7662 introspection fallback — the adapter's second wire arm and the completion of the issued-token dispatch. A non-3-segment 200 token goes to the DISCOVERY-derived introspection endpoint (no override keys) with RFC 6749 §2.3.1 Basic client auth raw-byte assembled (the plain secret never becomes a String; the concat buffer wiped immediately) and the `token=` form percent-encoded from UTF-8 bytes by the shared raw-octet encoder; the `sendAsync` future takes over the active-call slot (F3 — a late `cancelHttp()` aborts round 2); the round-2 budget is re-clamped to the remaining adjudication deadline. Verdict rows: ONLY 200 + JSON + boolean `active:true` → `Allow`; `active:false`, missing/STRING `active`, non-JSON, 401/404/500, timeout, cancel → `DenyIndeterminate` (an introspection 401 is client-auth/config trouble — NEVER the token endpoint's positive-invalid 401). F6 structural (the arm holds no JWKS reference); results never cached (provider-flip pin). **Design correction:** JDK 25's JEP 505 STS is single-join — the planned fork-round-2-into-the-same-scope arm threw `IllegalStateException` ("Already joined or scope is closed"), masked by the fail-closed catches (deny rows green, Allow rows red; diagnosed by instrumentation, since removed) — round 2 joins INLINE (the ratified slice's own shape), bounded by the clamped request timeout; recorded in both javadocs + auto-memory. The AC4 prefer-JWT guidance gained its yml docs line (the `Oidc` javadoc carried it since T1). +10 tests (RED 10/25-green pre-impl → GREEN 35/35; the rewritten malformed-JWT boundary pin green by design — introHits==0); 3 RED-on-neuter spot mutations (M-REG → F3 test RED; M-ACTIVE → 3 active-semantics guards RED; M-401 → non-200 RED); restored from `/tmp/t5-mutations/`, zero residue. Post-clean `:proxy:check` GREEN, `:proxy:test` **297/0/0/0** (XML; 287 + 10; 0 skipped — the Docker-gated suites ran), compile warnings unchanged. T6 next.

- 2026-08-19 — **T4 implemented** (AC3): local JWT defense-in-depth + the JWKS cache. The pinned fixture was booted via a throwaway `docker run` to discharge AC3's live-verification mandate — the ROPC-issued token's JOSE **header** `typ` is **`"JWT"`** (the payload *claim* is `"Bearer"`; recorded on `EXPECTED_TYP`), with `iss` == provider-url and `aud` == client-id confirmed on the same decode. `RopcBindCredentialVerifier.verifyJwt`: typ (absent tolerated / unexpected deny) → kid → cached-JWKS RS256 signature → iss/aud/exp/nbf (60s skew) → `Allow`; every failure is `DenyIndeterminate` (unverifiable ≠ invalid; DENY wins over the 200); the bind path NEVER fetches JWKS — kid miss / cold cache deny now and trigger the background refresh; the opaque-token arm stays fail-closed pending T5 (boundary test-pinned). New **`JwksCache`**: whole-swap-only `AtomicReference` with one write site (never nulled — §6b, the slice's invalidation bug productionized away; no age eviction; empty-set-200 retained), `newScheduledThreadPool(1, virtual)` owned by `security/` (AD-28(2); NOT `@Scheduled`; structurally NO pool/semaphore reference — §6a), async initial fetch at construction + TTL/2 refresh-ahead cadence (dev-selected, floor 1ms), kid-miss/cold `requestRefresh()` single-flight on the same scheduler, guarded refresh arms (a throw would kill the whole schedule), `close()` first in the adapter's shutdown (AD-22). Stand-in IdPs gained a JWKS endpoint and an 8-thread daemon executor (the JDK server's default single dispatcher thread starved the token endpoint behind a parked latch handler — diagnosed from a cold-cache bind timing out at exactly `oidc.timeout`). +16 tests (RED 15/1 on the `JwksCache` UOE stub → GREEN 16/16); 5 RED-on-neuter spot mutations (M-TYP, M-KIDMISS, M-6B, M-6A, M-EMPTY) each bit exactly one targeted test; restored, zero residue. `:proxy:check` GREEN, `:proxy:test` **287/0/0/0** (XML; 271 + 16; 0 skipped — the Docker-gated suites ran), zero compile warnings. T5 next.

- 2026-08-19 — **T3 user-directed FIXME pass (4 user-marked FIXMEs on the adapter):** (1)+(4) merged — the secret `byte[]` field + its loader extracted into package-private **`ClientSecret`** (the `Password` pattern for the adapter's own material): `zeroize()` replaces the bare `Arrays.fill` in `close()`, redacting `toString()`, identity-`equals` documented with a reasoned `@SuppressWarnings("ArrayRecordComponent")`, and `static load(Path, configKey)` carrying the AD-18 read (refusal messages byte-identical — the adapter-ctor tests pin them unchanged). (2) The magic form capacity `160` was NOT worst-case-sufficient (percent-heavy form ≈330 B; BAOS grows silently) — replaced by the exact bound `62 + 3×(field lengths)` (single allocation, never grows). (3) Apache Commons Codec — **won't-do with rationale** on `urlEncode`'s javadoc (commons-codec on no proxy classpath — a new runtime dependency for a twelve-line encoder; `URLCodec`'s safe set differs from RFC 3986 unreserved; `security/` stays JDK+Nimbus, AD-36 minimalism). Zero FIXME markers remain in the T3 files. +4 tests (RED 3/4 on the stub → GREEN): `ClientSecretTest` (trim/zeroize/toString) + the end-to-end form-capture pin (raw-byte encoder `%20/%40/%2B`, trimmed secret, verbatim fields). Mutations: M-TRIM → 3 RED (trim + form + the blank-file refusal — one mechanism, `from == to`), M-ZEROIZE → 1 RED, M-ENCODE → 1 RED; restored, zero residue. `:proxy:test` **271/0/0/0** (XML), `:proxy:check` GREEN.

- 2026-08-19 — **T3 implemented** (AC2/AC5): `RopcBindCredentialVerifier` — the production adapter core. Named-VT pool + `oidc.max-in-flight` semaphore admission (F2: `sendAsync` only after `tryAcquire`; saturation = settled `DenyIndeterminate`, no wire call), use-after-close hardening (`RejectedExecutionException | IllegalStateException` → release + abort + settle, §2.1 item 2), ROPC form built from RAW BYTES (F1: password and client-secret percent-encoded from `array()/arrayOffset()/length()` via a raw-octet encoder — no `toString()`, no `String` form, `ofByteArray` response bodies), the **refined AC2 verdict table** (`DenyInvalid` = bare 401 (RFC 6749 §5.2, unparseable body included) or 400 + `error` ∈ {`invalid_grant`,`invalid_client`}; everything else `DenyIndeterminate` — the 429/404/403 refinement, deferred-work §2.1 item 1 discharged; recorded as the dated supersession entry in the architecture `.memlog.md`), STS JEP 505 `open(Joiner)` fan-out with `ScopedValue` capture-on-caller + re-bind-on-pool (AC5 capture rule; unbound handle → settled deny), per-request budget clamped to the remaining `RequestContext.deadline`, minimal `close()` seam for T7's lifecycle. The 200 arm fails closed pending T4 (JWT verify) / T5 (introspection); the password is NEVER zeroized by the adapter (caller-owned, 2.2 T7). +15 tests (RED 15/15 on the stub → GREEN 15/15); 3 RED-on-neuter spot mutations — MUTANT-3's missing-file refusal was initially MASKED by the empty-file arm (shared substrings), fixed by arm-specific message assertions before re-proving; restored, zero residue. `:proxy:test` **267/0/0/0** (XML), `:proxy:check` GREEN, zero compile warnings. T4 next.

- 2026-08-19 — **T2 user-directed FIXME pass (3 FIXMEs on the T2 files; 1 implemented as a refactor, 2 resolved as already-done / cannot-work with rationale comments):** (1) `Oidc.providerUrl` is now **`URI`-typed** — a non-URI string refuses at BIND time (conversion failure); the **host + scheme checks moved from the discovery build into `CompanionConfigValidator.requireOidc`** (the deferred 2.1-era scheme-only gap's final home; new matrix test `sec053_hostlessOidcProviderUrlRefuses` binding the exact bad value `https://:8443`); the **trailing-`/` strip moved into the `Oidc` compact ctor** (a BV constraint cannot rewrite the bound value — the ctor is the only config-layer home; canonicalization only, no value invented); `discoveryUri` is now a trivial join. **Consequence (inherent to the type change):** empty-string provider-urls convert to NULL at bind (non-String target), so the blank and absent arms COLLAPSE into the component `@NotNull` refusal — `sec054_blankOidcProviderUrlRefuses` re-pinned to the consolidated message (the exact bad value `""` is still bound — the null-vs-blank discipline holds). Direct-construction sites swept (5: `RelayTestFixtures.testOidc`, `CompanionConfigMatrixTest.reverseAProps`, `CompanionRoleFailFastTest`, both T2 test helpers); Spring's String→URI binding handles every yml/args/runner path unchanged. (2) "parse with nimbus" — already satisfied (`com.nimbusds.jose.util.JSONObjectUtils` IS the Nimbus parse; the OIDC SDK would be a new dependency); stale comment removed. (3) "use jakarta validation annotations" on the `ResolvedOidc`/`OidcProviderMetadata` ctors — **cannot work as written**: BV fires only on Spring-bound records; these are programmatic/wire-derived, so the annotations would be inert while `requireNonNull` actually throws (and F5 is a story MUST-keep); FIXMEs replaced with one-line rationale comments. New: +`sec053_hostlessOidcProviderUrlRefuses` (RED-first: booted pre-refactor), +`trailingSlashProviderUrlNormalizesAndDiscovers` (migration pin), −the discovery-level hostless test (moved up a layer). MUTANT-E (hostless check neutered) → matrix hostless RED; MUTANT-F (ctor strip neutered) → trailing-slash pin RED; restored, zero residue. `:proxy:test` **252/0/0/0** (XML), `:proxy:check` GREEN.

- 2026-08-19 — **T2 implemented** (AC7): `IdpSslContextFactory` (reverse-cell oidc resolution → 5-state AD-13 trust-store load at bean init → JDK-default-PKIX trust-only SSLContext → AD-34 both-axis intersection, empty = refuse — **D2 discharged for the IdP context**; forward cells inactive) + `OidcStartupDiscovery` (hard-required startup probe over the real TLS link: fail-closed refusals for non-200/malformed/missing-field/unreachable/hostless-URL, exact `issuer == provider-url` check, loud DAG warning on a missing `password` grant; exposes `OidcProviderMetadata` for T3/T4/T5). The T1 amendment-2 deferrals settled (exactly-one-of moot per amendment 5); the 2-1 deferred hostless-provider-url finding discharged. The reverse-B full-context boots now run real TLS discovery against the T1 stand-in (its first real client). +21 tests (`IdpSslContextFactoryTest` 11, `OidcStartupDiscoveryTest` 10); RED phase on stubs 19/21; 4 RED-on-neuter spot mutations (A zero-trustedCertEntry, B AD-34 suites, C issuer equality, D DAG warning) each bit exactly one targeted test, restored, zero residue. `:proxy:test` **251/0/0/0** (XML), `:proxy:check` GREEN. No main-source file outside `security/` touched; `relay/` untouched. T3 next. *(The hostless refusal + trailing-slash strip later moved into the config layer the same day — see the FIXME-pass entry above.)*

- 2026-08-19 — **Code-review patch pass — all 8 remaining findings applied** (Review Findings all `[x]`): F1 `Oidc` javadoc rewritten to the amended reality (dropped-enforcement claims removed — file-existence only, timeout window = documented operator contract; phantom "Default 4s/64/5m" → "yml reverse template documents … no in-record default"; the promised structural note on why branch keys cannot carry live yml defaults added; the ":237 exactly-one-of" sub-claim was already moot — deleted with the RFC 8705 arm). F2 stale "forward" role wording swept to reverse (3 `@NotNull` messages, the blank provider-url refusal, `requireOidc` javadoc) + the weakened sec054 assertion restored to strict ("provider-url is required for the reverse role"). F4 `@NotBlank` on client-id + blank/whitespace refusal tests (null-vs-blank rule). F5 positivity via `@DurationMin(nanos = 1)` (HV `org.hibernate.validator.constraints.time` — jakarta `@Positive` has no Duration validator (HV000030, empirically 42 red) and HV 9 moved `@DurationMin` out of `constraints`) + `0s`/`-30s` refusal tests. F7 stand-in key-parse `IllegalArgumentException` → contextual `IllegalStateException`. F3 stand-in javadoc re-targeted to the reverse consumers. F9 subtask-4 supersession annotation. F11 226/227 count contradiction annotated at both sites. `:proxy:test` **230/0/0** (XML), `:proxy:check` GREEN. The two new guards (`@NotBlank`, `@DurationMin`) await T10's consolidated RED-on-neuter pass (AI-1).

- 2026-08-19 — **RFC 8705 IdP mTLS client-auth option removed** (user-directed, post-review): `client-mtls-keystore` + `ClientMtlsKeystore` deleted from the `Oidc` record; `client-secret-path` is the sole, now-`@NotNull`-required provider client auth; validator/yml/tests swept (2 direct-construction arity fixes; matrix −2/+1). Recorded as Completion Notes amendment 5. `:proxy:test` 226/0/0 (XML), `:proxy:check` GREEN. Earlier the same day a code review ran (2 layers: Edge Case Hunter + Acceptance Auditor; Blind Hunter skipped per user directive): 1 decision (D1 → full AD-12 amendment sweep) + 8 patches left as action items, 1 defer, 1 dismissed — see Review Findings. Spine/epics orphan references from this removal (AD-10 :128, AD-12 :138, AD-34 :263, risk-register :294, epics :211) pending the same propagation decision as D1. *(Follow-up same day: D1 resolved → full sweep — spine AD-13/AD-20/AD-26 + AD-10/AD-12/AD-34/register + epics AD-12/AD-13/AD-20/AD-26 + the epics AD-20 table row; `.memlog.md` dated entry.)*

- 2026-08-18 — **AD-12 amendment applied + T1 re-targeted** (user-directed "follow my claim exactly"): adjudication moved to the reverse role (sole enforcement point before the SMSC; forward = trusted-side relay with no OIDC; internet-leg modes A/B/C; SMSC leg always plaintext; Mode B = direct client→reverse, still adjudicates). Spine (AD-12/AD-17/AD-26) + epics + .memlog amended in place; the `oidc` config node moved from forward cells to all three reverse cells; matrix/yml/boots/fixtures re-targeted; new structural test refuses any stray forward oidc key. `:proxy:test` 227/0/0, `:proxy:check` GREEN. Full detail in the Dev Agent Record amendment note above.

- 2026-08-18 — Story 3.2 created (status: backlog → ready-for-dev). Ultimate context-engine analysis completed: 4 parallel research agents (codebase recon, spine AD extraction, previous-story intelligence, PRD analysis) + web research (Keycloak 26.7.0/CVE-2026-9792, JEP 505 STS shape, Nimbus 10.9.1). Decomposition decision: adjudicator-first per user selection; TLS modes A/B/C + forward-acceptor wiring → story 3.3. F7 settlement-on-cancel decision pre-ratified in AC5; deferred-work absorption set: §2.1 items 1/2/3/5/6 + D2 (IdP context) + SEC-090. Config note: `oidc.client-credential-path` renamed/replaced by the exactly-one-of auth pair (pre-release, no deployments). Story 3.1 (ROPC viability probe) was executed out-of-band as a sprint action item (2026-08-08, verdict MET) and is intentionally not a story key in sprint-status.yaml. Story status: ready-for-dev.
- 2026-08-18 — **T1 implemented** (AC8 + the AC7 test-support leg): `Oidc` record reshaped (client-id, exactly-one-of client-auth pair, IdP trust-store, three required budget keys), `client-credential-path` removed across the full blast radius (7 enumerated sites + `CompanionRoleFailFastTest`), validator reduced to file-existence checks for the oidc paths, defaults documented in `application.yml`'s forward template incl. the OIDC_TIMEOUT 2s–5s comment. 4 user-directed design amendments recorded in Completion Notes (required non-null budget keys / yml-carried defaults; file-existence-only validation; timeout window as documented contract; maxInFlight description). New `OidcDiscoveryStandIn` HTTPS discovery server; `TestCompanionConfigs` + the four full-context boots now boot forward cells against it. 12 new AC8 tests; `:proxy:test` 226/0/0 (XML-verified; pre-amendment count — the same-day AD-12 amendment's stray-forward-oidc-key test took it to 227, see the amendment entry below); `:proxy:check` GREEN; 2 RED-on-neuter spot mutations confirmed and restored. Status: ready-for-dev → in-progress (T2 next).

## Review Findings

<!-- Code review 2026-08-19: 2 layers (Edge Case Hunter + Acceptance Auditor; Blind Hunter skipped by user
     directive). 20 raw findings → 10 after dedup/triage: 1 decision, 8 patch, 1 defer, 1 dismissed. -->

- [x] [Review][Decision] AD-12 amendment sweep missed residual SMSC-trust wording contradicting "SMSC leg always plaintext" — spine AD-26's first sentence was amended but its remainder keeps "(b) the operator SMSC trust store being the sole gate", "cacerts/public-PKI SMSC trust", "the threat — trusting a public root for the SMSC peer"; spine AD-13 rule text still says "Mode A egress validation of the SMSC server cert (AD-26)" (:147); spine AD-20 still says "SMSC-leg TLS is operator-choice, default off … when on, the SMSC is authenticated via the operator SMSC trust store (AD-26)" (:189); epics.md:199 still says "Mode C client certs AND Mode A egress SMSC validation, AD-26". Under the amended model the SMSC leg is plaintext in every mode — four sites describe validation that cannot happen. Decide: full sweep now vs record-and-defer to story 3.3. — **RESOLVED 2026-08-19 (user: full sweep, folded with the RFC 8705 removal propagation)**: spine AD-13/AD-20 (retitled)/AD-26 + epics AD-13:199/AD-20:204/AD-26:205-206 + the epics AD-20 summary-table row :267 swept in place; dated `.memlog.md` entry records both.
- [x] [Review][Patch] Oidc record javadoc asserts enforcement and defaults the user amendments dropped [proxy/src/main/java/smpp/companion/proxy/config/ProxyCompanionProperties.java:208-258] — claims "the validator enforces exactly-one-of" (:237), "validated at the full 5-state PKIX-load depth" (:239-240), "Validator-enforced to the inclusive window [2s,5s] … AND <= adjudication-deadline" (:241-243): all three are amendment-2/3 droppings (validator is file-existence-only; the timeout window is a documented operator contract per application.yml:94-95). Also "Default 4s/64/5m" on @NotNull-required keys with no defaults anywhere (:244,:253,:257) and "application.yml's forward template ships them" (:216) — the oidc template now lives under reverse. Fix: rewrite the javadoc to the amended reality + add the structural note Completion Notes amendment 1 claims is recorded there (why branch keys cannot carry live yml defaults).
- [x] [Review][Patch] Stale "forward" role wording in live refusal messages + requireOidc javadoc; covering test's assertion relaxed in the same diff to mask it [proxy/src/main/java/smpp/companion/proxy/config/CompanionConfigValidator.java:284-294; ProxyCompanionProperties.java:268-273; CompanionConfigMatrixTest.java:583] — blank provider-url refusal still says "for the forward role (AD-12)" (:294), requireOidc javadoc still says "@NotNull on forward A/C" (:285) while oidc is @NotNull on reverse A/B/C; the three @NotNull messages say "(forward, PERF-3/AD-28(4)/AD-28(2))" (:268,:270,:273); and sec054_blankOidcProviderUrlRefuses was simultaneously narrowed from asserting "provider-url is required for the forward role" to the substring "provider-url is required" — exactly what keeps the stale message green (brushes AC7 "no test is weakened"). Fix: sweep messages/javadoc to reverse + restore the strict assertion against the corrected message.
- [x] [Review][Patch] Blank/whitespace client-id passes the @NotNull-only guard — the codified null-vs-blank trap (epic-1 retro AI-7a) [proxy/src/main/java/smpp/companion/proxy/config/ProxyCompanionProperties.java:262; CompanionConfigValidator.java:291-306] — the only client-id test is the absent-key/null case (CompanionConfigMatrixTest:358-361); a bound "" or " " boots T1 and would send an empty client_id at T2. Fix: @NotBlank on clientId (annotation-level per amendment 2) + a refusal test binding the blank value.
- [x] [Review][Patch] jwks-cache-ttl positivity enforced nowhere although amendment 2 claims "conformance is annotation-level" [proxy/src/main/java/smpp/companion/proxy/config/ProxyCompanionProperties.java:273-274] — @NotNull only: no @Positive, no validator check; zero/negative TTL boots and spins the T2 refresh scheduler. Fix: @Positive + a zero-value refusal test.
- [x] [Review][Patch] OidcDiscoveryStandIn javadoc documents the pre-amendment wiring it was re-targeted away from [proxy/src/test/java/smpp/companion/proxy/testsupport/OidcDiscoveryStandIn.java:34-39,63-65] — "from T2 the forward cells' adapter bean…", "TestCompanionConfigs.forwardA()/forwardC() bases default provider-url here", "use it verbatim as companion.forward.mode-*.oidc.provider-url"; actual consumers (verified) are TestCompanionConfigs.oidcKeys() on reverseA/B/C, the two reverse mode-b boot builders, and RelayTestFixtures.testOidc() — a stray forward oidc key refuses (amendment_strayForwardOidcKeyRefuses). Fix: rewrite to the reverse consumers.
- [x] [Review][Patch] Stand-in private-key parse lets a raw IllegalArgumentException escape with no context on a corrupt/non-PKCS#8 fixture key [proxy/src/test/java/smpp/companion/proxy/testsupport/OidcDiscoveryStandIn.java:141-146] — Base64.getDecoder().decode throws unchecked IAE outside both catch blocks (:91,:134). Fix: catch IllegalArgumentException → IllegalStateException("fixture server key is not raw-base64 PKCS#8", e).
- [x] [Review][Patch] Story Task 1 subtask 4 is [x]-checked with pre-amendment text and, unlike subtasks 2-3, carries no supersession annotation [3-2-oidc-ropc-bind-adjudicator.md:53] — "TestCompanionConfigs.forwardA()/forwardC() default provider-url to it" is false post-amendment (oidc hangs off reverseA/B/C). Fix: annotate like subtasks 2-3.
- [x] [Review][Patch] Story record self-contradicts on the test count: 226 vs 227 [3-2-oidc-ropc-bind-adjudicator.md:201,:249 vs :242] — the amendment's added amendment_strayForwardOidcKeyRefuses (+1) explains the delta; the earlier Debug Log/Change Log bullets were never refreshed. Fix: annotate the 226 entries as pre-amendment counts.
- [x] [Review][Defer] Hostless provider-url ("https://", "https://:8443") passes the scheme-only isHttps check [proxy/src/main/java/smpp/companion/proxy/config/CompanionConfigValidator.java:295-297] — deferred, pre-existing (2.1-era guard this story merely re-hung on the reverse validators); natural home is T2's discovery/HttpClient build.
