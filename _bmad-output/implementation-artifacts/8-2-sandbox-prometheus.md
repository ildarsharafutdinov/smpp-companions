---
title: 'Story 8.2 — Sandbox Prometheus'
type: 'feature'
created: '2026-09-20'
status: 'in-progress'
route: 'dispatch'
baseline_commit: '597702dc0d6ba0802a39b20946652f7a2b9b9384'
review_loop_iteration: 1
context:
  - {project-root}/_bmad-output/implementation-artifacts/epic-8-context.md
  - {project-root}/_bmad-output/planning-artifacts/architecture/architecture-smpp-companions-2026-07-19/ARCHITECTURE-SPINE.md
---

<frozen-after-approval reason="human-owned intent — do not modify unless human renegotiates">

## Intent

**Problem:** Nothing consumes the proxy's metrics in the sandbox rig — the loopback-only `/metrics` (AD-19) is reachable only by hand (`curl` from the host shell), so the rig never demonstrates the observability story end-to-end and the Story-8.1 histogram surface has no live consumer.

**Approach:** Add a pinned Prometheus service to `sandbox/compose.yml` that scrapes the loopback endpoint via a sandbox-only network-namespace pattern — the service joins the host namespace, the namespace both supported proxy postures run in, so the target is the literal `127.0.0.1:9090` — proven live before it lands. Fixture scope per addendum A7: the product's loopback-only binding and fail-closed posture stay byte-intact.

**Owner decisions (2026-09-20):** Q1 = always-on (the service starts with the plain `docker compose up`; no compose profile). Q2 = the web UI binds loopback-only, `127.0.0.1:9095`. Q3 = BOTH static targets always configured — `127.0.0.1:9090` and `127.0.0.1:9091` — accepting that every single-instance rig shows the 9091 target DOWN until a second instance launches. Spec kept whole above the token guideline (single goal; the overage is Code Map citations).

## Boundaries & Constraints

**Always:**
- AD-19 byte-intact: nothing under `proxy/` changes; the metrics bind stays the literal loopback + `companion.metrics.port` (yml default 9090); access comes from namespace sharing, never from bind widening or port publishing.
- Fixture scope (A7; sprint-change-proposal §conflict): the Prometheus service and its web UI are rig fixtures; the product §13 "no metrics dashboard / telemetry backend / management API" non-goal stands.
- Image pinned exactly (the Keycloak 26.7.0 precedent); the new host port lands in README §3 EN+RU; EN normative, RU structural mirror with the terminology-glossary conformance sweep.
- The namespace pattern is proven live (rig up + running proxy, target UP, both histogram families flowing) BEFORE the service lands; evidence recorded in Implementation Notes.

**Never:**
- No `-p 9090`/`-p 9091`, no `ports:` entry for metrics, no `EXPOSE` change, no `networks:` key added to compose for this; no product meter/seam work (`proxy/observability/` untouched).
- No CI, `.github/`, Docker Hub, pull-based compose, deployment-guide or `docs/runbooks.md` product-doc work (Story 8.3 owns those).
- No closing of ledgered items (sandbox supply-chain hardening, the secrets `check-ignore` guard, 8.1's G3/G6/G7) — they stay ledgered.
- No auto-reformat of untouched files — hand style stands.

## I/O & Edge-Case Matrix

| Scenario | Input / State | Expected Output / Behavior | Error Handling |
|----------|---------------|----------------------------|----------------|
| Default posture scrape | Rig up + host-run proxy (README §5.2), metrics on yml-default 9090 | `smpp-proxy` target UP; counters + both histogram families queryable in the UI | Scrape failures surface as `up == 0` in the UI |
| Runbook docker shape | Proxy container `--network host` (§5.6 / runbooks) | Same target UP — same (host) namespace | Same |
| Proxy not launched | Rig up, no proxy process | Target DOWN (connection refused); rig otherwise healthy | Documented in README §5.3/§5.4 |
| Two-instance runbooks | Second proxy on 9091 | Both targets always configured: the 9091 job goes UP when the second instance launches | Single-instance rigs show the 9091 target DOWN — accepted (Q3) |
| Host 9090 held by another process | Proxy boot | Proxy refuses startup (AD-17) — unchanged; target stays DOWN | Product behavior untouched |
| §5.5 bridge variant | Proxy on the compose bridge network | Loopback metrics unreachable from a host-ns Prometheus — stays a documented variant limitation | N/A |

</frozen-after-approval>

## Code Map

- `sandbox/compose.yml` -- 8 services, NO `networks:` key (implicit default only), per-service comment-block style; the Keycloak 26.7.0 pin (:137-160) is the image-pinning precedent; kannel `extra_hosts: host.docker.internal:host-gateway` reaches the host's routable interface, NOT its loopback — insufficient for `/metrics`.
- `sandbox/README.md` -- §3 port table :155-170 (compose-published vs host-must-be-free — the ledger the new port joins); §5.2 :295-315 host-run launch (metrics rides yml-default 9090); §5.5 :407-426 bridge variant (metrics structurally unreachable — limitation to state); §5.6 :441-470 docker shape + `--network host` rationale; mermaid topology :57-86.
- `sandbox/README.ru.md` -- structural §-mirror of the EN README; owner terminology glossary binds the new text.
- `sandbox/runbooks/{reverse-mode-b,forward-reverse-mode-a,forward-reverse-mode-c}.md` + `.ru.md` twins -- the docker-shape usage surface; `reverse-mode-b.md:82-99` the `--network host` run, :150-156 "Why `--network host`", :128 the hand-curl scrape; the a/c runbooks run the second instance on 9091.
- `proxy/.../observability/MetricsEndpointLifecycle.java` -- DO NOT CHANGE: literal `LOOPBACK_BIND_HOST` :77, `companion.metrics.port`, absent node = endpoint down, occupied port = AD-17 fail-fast.
- `.../prds/prd-smpp-companions-2026-07-18/addendum.md` -- A7 :75 fixture-scope ruling; A8 :79-86 the scrape-arithmetic baseline (121 forward / 120 reverse / N-id formula) this story's live check consumes.
- `_bmad-output/planning-artifacts/epics.md` :427-437 -- Epic 8 charter: the netns-join recommendation, packages owned, no new FRs.
- `_bmad-output/implementation-artifacts/deferred-work.md` -- :878-883 open sandbox supply-chain + secrets-guard entries (adjacent, stay ledgered per Never); :887-895 8.1 gaps G3/G6/G7 (owned elsewhere).
- `docs/runbooks.md` :187-272 -- product `/metrics` reference; Story 8.3 territory, untouched here.

## Tasks & Acceptance

**Execution:**
- [x] `sandbox/compose.yml` + `sandbox/prometheus/prometheus.yml` -- add the pinned Prometheus service (`network_mode: host`; UI loopback-only on `127.0.0.1:9095` — 9090/9091 taken, 9093 is the Alertmanager convention; named TSDB volume + short retention; short scrape interval), always-on (no compose profile), with TWO static targets under the `smpp-proxy` job — `http://127.0.0.1:9090/metrics` and `http://127.0.0.1:9091/metrics`; prove the pattern live FIRST and record the evidence in Implementation Notes. -- The fixture lands only after its access pattern is proven (the proposal's named risk).
- [x] Live conformance check vs A8 -- run the rig's journeys with real traffic, reconcile the live scrape rows against A8's baseline, append the dated first-live-check note to addendum A8. -- A8 declares itself the baseline for the next live conformance check; 8.2 is its first consumer.
- [x] `sandbox/README.md` -- §1 topology (mermaid + prose), §2 layout table, §3 port table row, §4 bring-up (always-on — no extra command), §5.3 expected observations (UI + target states incl. DOWN-when-no-proxy and the DOWN 9091 in single-instance rigs), §10 deltas honesty list. -- The rig doc must match the rig.
- [x] `sandbox/README.ru.md` -- mirror the T3 sections; run the terminology-glossary conformance sweep over the new text. -- RU twins stay structurally identical.
- [ ] `sandbox/runbooks/*.md` + `.ru.md` twins (3 pairs) -- one pointer each in "What you should see": the hand-curl observation gains the Prometheus UI route; the two-instance runbooks (a/c) note the always-configured 9091 target — UP only while the second instance runs, DOWN otherwise; PLUS one sentence in `sandbox/README.md` §5.6 + the RU twin — while a two-instance runbook pair runs, the rig Prometheus's 9091 target is UP and both instances' metrics land in the TSDB (closes the Q3 loop at the section where the second instance actually runs; added by review round 1, 2026-09-21). -- The runbooks are the docker-shape usage surface.

**Acceptance Criteria:**
- Given the rig up and a host-run proxy (§5.2 defaults), when browsing the Prometheus UI on its listen address, then the `smpp-proxy` target is UP and after a journey both histogram families (`relay_binds_adjudication_seconds`, `relay_pdus_transit_seconds`) carry data.
- Given the rig up with no proxy launched, then the target reads DOWN and every other service is unaffected — stated in README §5.3.
- Given the post-journey live scrape, its row set reconciles with A8's arithmetic, or the delta is explained in the A8 dated note.
- Given `sandbox/compose.yml`, no `ports:` entry publishes 9090/9091; the story's whole diff touches nothing under `proxy/`.
- Given the RU mirror, the glossary conformance sweep passes (owner terminology; TLS/mTLS untranslated).

## Implementation Notes

**T1 live proof (2026-09-20, executed BEFORE the compose service landed).** Environment: Docker 29.6.1, rig found fully down, ports 9090/9091/9095/2775 free. Sequence and observations:

- Rig up: `cd sandbox && docker compose up -d --build` (cached Kannel images) → all 8 services up; `keycloak` healthy at ~25 s. §5.1 bootstrap re-run (fresh container → regenerated client secret fetched via `kcadm` into `sandbox/secrets/oidc-client-secret`, mode 0444 — the found file's 0444 mode had to be made writable first to overwrite).
- Proxy: `./gradlew :proxy:bootJar` (up-to-date), then launched verbatim per README §5.2 (host-run `java -jar`, reverse.mode-b, everything else yml-default → `companion.metrics.port=9090`). §5.3 held: Mode B WARN banner → `startup_summary` (`"metrics_port":9090`, AD-30 interlock `memory_budget_bytes` == `direct_memory_ceiling_bytes` == 6442450944) → `bind_accept usr1 coupled` at launch + 9 s; front status page `smsc1 … (online`; `curl 127.0.0.1:9090/metrics` → 200.
- Journey (§6.1, real traffic): `curl "http://127.0.0.1:8080/cgi-bin/sendsms?…&text=hello+8-2+proof"` → `202`/`0: Accepted for delivery`; fakesmsc printed `Got message 1: <79876543210 79033374423 text hello 8-2 proof>` (byte-intact). Raw `/metrics` post-journey: `relay_pdus_total{direction="INGRESS"|"EGRESS"}` 3/3 (submit+resp, DLR deliver_sm+resp), growing to 9/9 with §6.3 keepalives during the scrape window; pre-registered histogram grids already all-zero rows at idle (37 rows across both families).
- Proof Prometheus — the EXACT landing config (`sandbox/prometheus/prometheus.yml` staged at its final path, mounted `:ro`; pin `prom/prometheus:v3.14.0`, the newest stable on 2026-09-20 — v3.15 is rc-only, v3.13.3 an older-line patch; Docker Hub tags API):
  `docker run -d --name prometheus-82-proof --network host -v …/sandbox/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro prom/prometheus:v3.14.0 --config.file=/etc/prometheus/prometheus.yml --storage.tsdb.path=/prometheus --storage.tsdb.retention.time=24h --web.listen-address=127.0.0.1:9095`
  - `curl 127.0.0.1:9095/-/ready` → `Prometheus Server is Ready.`; `ss -tln` → `LISTEN 127.0.0.1:9095` and nothing else on 9095 (UI loopback-only; `/graph` answers 302 → the UI).
  - `GET /api/v1/targets` (job `smpp-proxy`): `http://127.0.0.1:9090/metrics health=up lastError=""` and `http://127.0.0.1:9091/metrics health=down — dial tcp 127.0.0.1:9091: connect: connection refused` — the Q3 single-instance state, observed live.
  - `GET /api/v1/query`: `relay_binds_adjudication_seconds_count{instance="127.0.0.1:9090",job="smpp-proxy"} = 1`; `relay_pdus_transit_seconds_count` = 9 (`EGRESS`) / 9 (`INGRESS`); buckets populated — `relay_binds_adjudication_seconds_bucket{le="0.5"} = 1`, `count(relay_pdus_transit_seconds_bucket) = 20`.
- Teardown: proof container `docker rm -f`; proxy `kill -TERM` → the §5.3 drain WARN (`shutdown drain deadline (PT10S) expired — force-closed 1 live pair(s) as SHUTDOWN_DRAIN`) → process exited; `docker compose down` → rig back to its found state, 0 containers, ports free again.

Ops notes (environmental, not spec deviations): (1) staging the proof config under the shell's `/tmp` failed — the daemon cannot see the sandboxed shell's private `/tmp`, so it silently created a DIRECTORY at the mount source (README §8's documented `-v` failure class); staging at the final repo path fixed it, and the stray daemon-side `/tmp/8-2-prometheus-proof/` was removed via a root throwaway container. (2) `docker compose down` at the end regenerated the Keycloak realm → `sandbox/secrets/oidc-client-secret` is stale again for the NEXT `up` (§5.1 regeneration semantics — documented rig behavior, re-fetch on next bring-up).

**Landed artifact:** the `prometheus` service (9th) in `sandbox/compose.yml` — `network_mode: host`, no `ports:` entry (Design Notes: silently ignored under host mode), named volume `prometheus-tsdb` + 24 h retention, UI `127.0.0.1:9095`, always-on, image `prom/prometheus:v3.14.0` — plus `sandbox/prometheus/prometheus.yml` (job `smpp-proxy`, `metrics_path: /metrics`, targets `127.0.0.1:9090` + `127.0.0.1:9091`, `scrape_interval: 5s` / `scrape_timeout: 3s`). Verification per spec §Verification: `docker compose config` parses (prometheus shows `network_mode: host`, zero published ports); `grep -nE '909[01]' sandbox/compose.yml` hits comment lines only (the service comment block), never a `ports:` publish.

**T2 live conformance check vs A8 (2026-09-20, the landed service's own first live run).** Rig up (9 services) → §5.1 re-fetch (the T1 teardown's `down` had regenerated the secret, as its ops note predicted; file overwritten at `0600`) → §5.2 launch verbatim (jar up-to-date): Mode B banner → `startup_summary` (`"metrics_port":9090`, interlock equal) → `bind_accept coupled` at +2.1 s; front `smsc1 … (online`; `/-/ready` → `Prometheus Server is Ready.`; `/graph` → 302 to the UI; targets: `127.0.0.1:9090 … health=up`, `127.0.0.1:9091 … down — connection refused` (the Q3 state, live). Journeys with real traffic: §6.1 → `202`/`0: Accepted for delivery`, fakesmsc `Got message 1: <79876543210 79033374423 text hello 8-2 A8 check>` (byte-intact), DLR round trip (opensmppbox `deliver_sm`, front `removing DLR from database`), counters 1/1 → 3/3; §6.2 via the README's scripted fifo + `script -qec "docker attach --sig-proxy=false …"` (opensmppbox `deliver_sm` `source_addr "79033374423"`/`destination_addr "79876543210"`, the same octets in the front's `Got PDU` dump, +1/+1); §6.3 keepalives (INGRESS == EGRESS at every observation, never out of step). Post-journey scrape (uptime ~200 s): **the product-owned rows reconcile with A8 row-for-row** — 36 one-row observer meters (2 PDU + 32 close over all 16 CloseReasons × 2 directions + reject + unknown) + 34 timer rows (adjudication 8 `_bucket` + `_count`/`_sum`; transit 2 × (10 `_bucket` + `_count`/`_sum`); both grids' edges verbatim) + 2 resource gauges = **72 = A8's 120 − 48** (reframed 2026-09-21, review: the exactness scopes to the 36 one-row meters + 2 gauges; the 34 timer rows are the pre-correction count — live they render 37 incl. the 3 `_max`, product-owned live total 75 = the corrected 123 − 48; the A8 note's dated bullet is reworded to match); consecutive scrapes shape-identical after value-stripping; zero duplicate series. Two deltas, explained in the dated A8 note: every Timer's extra `_max` row (A8's fan-out clause undercounted → corrected totals **124 forward / 123 reverse**) and the JVM binder block floating 48 → 57 (lazily-materialized ZGC GC families, per-(action,cause,gc) combos) — live whole-scrape total **132**; Prometheus-side `count({job="smpp-proxy",instance="127.0.0.1:9090"})` = **137** = 132 + the 5 per-target scrape-health series. Both histogram families carried live data through the TSDB: adjudication `_count`=1 (`_sum` 0.183 s, in (0.1, 0.5]); transit `_count` 9/9 = journeys 3/3 + 6 keepalive pairs, every observation < 100 µs; transit `_count` tracked `relay_pdus_total` exactly throughout. Teardown: `kill -TERM` → the §5.3 drain WARN verbatim (`PT10S … SHUTDOWN_DRAIN`) → process exited; `docker compose down` → 0 containers, ports free, the named `prometheus-tsdb` volume surviving by design. The dated note itself: PRD addendum A8's two appended 2026-09-20 bullets (row-for-row reconciliation + the `_max` correction + the JVM-float explanation; the Prometheus-side counts + journey data). Unexercised: the forward-cell/N-id arm (needs the §5.6 composed-runbook shape, not the rig's default lone-reverse posture) — recorded as deferred-to-first-forward-rig in the A8 note.

**T3 `sandbox/README.md` (2026-09-20, docs-only — the EN rig doc now matches the landed rig).** Sections touched, per the task list: §1 — the mermaid gains the `prom` node + the dashed scrape edge (`scrape /metrics — 127.0.0.1:9090 (loopback, shared namespace)`), and a prose paragraph names the service the one compose-side exception to the `host.docker.internal` pattern (`network_mode: host`, loopback literals 9090/9091, UI loopback-only 9095, fixture-not-product per A7). §2 — the `compose.yml` row says the 9-service chain (prometheus: always-on, host net, UI 9095, named `prometheus-tsdb` volume, no healthcheck) and a NEW `prometheus/prometheus.yml` row documents the two-target scrape config. §3 — the 9090/9091 rows note the namespace-shared scrape / always-configured second target, and the NEW 9095 row lands the UI port (a direct host-namespace bind, not a compose publish; 9093 left to the Alertmanager convention). §4 — a `prometheus` readiness-table row (always-on, no profile, no healthcheck/`depends_on` by design, `/-/ready`), the no-healthcheck paragraph extended, the `/-/ready` curl in the smoke block, and the honest-idle paragraph's Prometheus counterpart (both targets DOWN while no proxy runs, every other service unaffected; 9091 DOWN for good on single-instance rigs). §5.3 — item 7, the Prometheus vantage point: 9090 UP / 9091 DOWN with the exact `dial tcp … connection refused` text, the Q3 both-always-configured rationale, both 8.1 histogram families carrying the couple (`relay_binds_adjudication_seconds_count` = 1; transit ticking with the keepalives and equal to `relay_pdus_total` per direction), and the no-proxy → `up == 0` + rig-otherwise-unaffected statement (AC2's README §5.3 requirement). §5.5 — the bridge-variant limitation sentence (in-container loopback unreachable from the host-ns Prometheus; I/O-matrix row). §10 — delta 7, the honesty-list entry (pinned image, always-on, scrape config, named volume + 24 h retention incl. the survives-`down`/only-`down -v`-removes semantics, namespace-only access, nothing under `proxy/`, pattern proven live pre-landing). No other section touched; §9 left as-is (the volume teardown semantics live in §10's delta). Verification per spec §Verification: `docker compose config` parses — the prometheus service resolves `network_mode: host` with ZERO published ports; `grep -nE '909[01]' sandbox/compose.yml` hits only the service comment block (lines 170/171/177), never a `ports:` publish; `./gradlew clean build` GREEN (BUILD SUCCESSFUL in 2m 58s; 550 tests, 0 failures/errors, 0 skipped). Left for T4/T5: the RU mirror + the runbook pointers (untouched).

**T4 `sandbox/README.ru.md` (2026-09-21, docs-only — the RU twin mirrors the T3 sections).** Mirrored one-for-one against the T3 diff (commit b887e80, `sandbox/README.md` only): §1 — the mermaid gains the `prom` node + the dashed scrape edge (`scrape /metrics — 127.0.0.1:9090 (loopback, общий namespace)`), plus the compose-side-exception prose paragraph (`network_mode: host`, loopback literals 9090/9091, UI loopback-only 9095, fixture-not-product per A7); §2 — the `compose.yml` row goes 8 → 9 services (prometheus: always-on, host net, UI 9095, named `prometheus-tsdb` volume, no healthcheck) and the NEW `prometheus/prometheus.yml` row (two always-configured loopback targets, 5 s / 3 s); §3 — the 9090/9091 rows gain the namespace-shared-scrape / always-configured-second-target clauses and the NEW 9095 row lands the UI port (direct host-namespace bind, not a compose publish; 9093 = the Alertmanager convention); §4 — the `prometheus` readiness-table row, the no-healthcheck paragraph extension, the `/-/ready` curl in the smoke block, and the honest-idle Prometheus paragraph (both targets DOWN while no proxy runs; 9091 DOWN for good on single-instance rigs); §5.3 — item 7, the Prometheus vantage point (9090 UP / 9091 DOWN with the verbatim `dial tcp … connection refused`, the Q3 both-always-configured rationale, both 8.1 histogram families carrying the couple, the no-proxy → `up == 0` + rig-otherwise-unaffected statement — AC2/AC5's README side); §5.5 — the bridge-variant limitation sentence; §10 — delta 8 (the RU list's own numbering: the RU-only translation delta sits at 6), the honesty-list entry. Glossary conformance sweep over the NEW text (the task's oracle): grep of the diff's added lines returns ZERO hits for спаривани, дозвон, взаимный TLS, TLS-рукопожатие, ворота, сценарий использования, владеет, открытый текст, and «бинд-»-shaped transliteration hybrids (also zero for несёт/несущ and одинокий). New-text term choices: couple → сопряжение; wire-reach → достигает/недостижим (the quoted `dial tcp …` error stays verbatim); scrape → сбор метрик / цели сбора, interval «интервал сбора 5 с»; "by design" → намеренно; "carries" → не имеет/содержит-style rephrasing; targets → цели (UP/DOWN UI states and quoted strings verbatim; the fresh §2 use-case gloss reads «the single reverse instance» / «единственный инстанс reverse» — the adjective dropped in review, the glossary's no-adjective rule admits none). Borderline, deliberately LEFT ALONE — pre-existing RU text in sentences T3 did not touch (their EN originals still say "owns"/"carries"/"the lone" too, so EN and RU stand even): «Владеет своим плечом DLR» (§2.1) and «владеющий эпик» (§11) for "owns"; «несёт три работы» (§2.1), «каждое несущее нагрузку» (§5.5's pre-existing deltas sentence), «несёт свою таблицу» / «несут живые плечи» (§5.6) for "carries"; «одинокий reverse» (§3's 2775 row) — the banned adjective's one pre-existing instance. Review fixes (2026-09-21, EN + RU twins + the `prometheus.yml` comment): §1 "both default proxy postures" (was "every supported" — the §5.5 bridge variant is supported but unreachable) + the Linux-host assumption sentence + the second mermaid edge (the always-configured 9091 target); §4's `/-/ready` curl carries `--retry 30 --retry-connrefused` (no healthcheck to wait on); §5.3 item 7's failure wording names the target DOWN (`up == 0`) with the connection-refused error on the Targets page ("and nothing else" was wrong — the other scrape-health series also move); §9 gains the one-sentence `prometheus-tsdb` volume-teardown note; the §2/`prometheus.yml` use-case glosses drop the adjective (see above). Verification per spec §Verification: `(cd sandbox && docker compose config)` parses — the prometheus service resolves `network_mode: host`, pinned `prom/prometheus:v3.14.0`, `127.0.0.1:9095` listen, `prometheus.yml` mounted read-only, named `prometheus-tsdb` volume, ZERO published 909x ports anywhere; `grep -nE '909[01]' sandbox/compose.yml` hits only the service comment block (lines 170/171/177), never a `ports:` publish; `./gradlew clean build` GREEN (BUILD SUCCESSFUL in 3m 7s; 550 tests = 17 buildSrc + 82 codec + 451 proxy, 0 failures/errors/skipped). Left for T5: the runbook pointers (untouched).

## Spec Change Log

- 2026-09-21, review round 1 (finding BH7 → bad_spec): T5's task extended — README §5.6 (EN+RU) gains the one-sentence "9091 goes UP while a two-instance runbook pair runs" statement. Root cause: the task decomposition covered the runbook files but not the README's own §5.6 section, so the Q3 "no config edit needed" rationale pointed at a section that never completed the loop. Known-bad state avoided: the always-configured 9091 target's payoff undocumented at the only place a second instance runs. KEEP: all T1–T4 output as landed (live proofs, the A8 reconciliation, the EN/RU mirrors, the pinned service) — untouched; T5's re-derivation stays owner-paced (one task per step).

## Review Triage Log

### Round 1 (2026-09-21, T4 round; layers: blind-hunter, edge-case-hunter, verification-gap)

- VG1 `_max` fan-out pinned by no test — MEDIUM (pre-verified gap finding): `MetricsEndpointTest` asserts only `_bucket`/`_count`/`_sum` (`proxy/.../MetricsEndpointTest.java:198-229`); A8's corrected 124/123 arithmetic lives only in markdown. → **defer**: frozen AC4 forbids touching `proxy/` in-story; ledgered for a standalone test-only commit or 8.3.
- VG2 no repeatable verification of the compose service — LOW (pre-verified): no test/Gradle/CI path reads `compose.yml` or `prometheus.yml`; silent failure modes (mount-path typo → daemon-made directory) ship green. → **defer**: the Never clause assigns CI/`.github` to Story 8.3; ledgered.
- EC1 9095-collision / config-load death silent — LOW, rejected: post-bring-up death is rare and §4's `/-/ready` smoke curl is the designed detection point; the meaningful fix (a restart policy) changes ratified fixture behavior.
- EC2 bind-source directory trap — LOW, rejected: the scrape config is committed, so the trap needs the file to vanish; §8 documents the class; long-syntax `required: true` would alter the exact config proven live in T1.
- EC3 smoke curl races Prometheus startup — LOW → **patch**: `--retry 30 --retry-connrefused` on the `/-/ready` curl (EN+RU) — the service has no healthcheck to wait on.
- EC4 overridden metrics port invisible to the scrape — LOW, rejected: off-recipe override; §3's 9090 row already ties the port to the documented launches.
- EC5 foreign listener on 9090 masks an absent proxy — LOW, rejected: AD-17 fail-fast makes the proxy's own death loud; the mask requires ignoring it.
- EC6 non-Linux Docker hosts undocumented — LOW → **patch**: one Linux-host prerequisite sentence (EN+RU); `network_mode: host` semantics differ on Docker Desktop-class runtimes.
- EC7+BH8+BH9 fresh "the lone reverse" / «одиночный reverse» — MEDIUM → **patch**: the new §2 row (EN+RU) and the new `prometheus.yml` comment breach the glossary's use-case-names-as-is rule; the T4 note's "sanctioned adjective" claim was wrong — no such sanction exists. Drop the adjective from all fresh instances; the pre-existing §3 «одинокий» stays untouched, which also restores RU to a single form.
- BH1 A8 bullet's "70 rows exactly … = 72 … exact" contradicts its own `_max` delta — MEDIUM → **patch**: exactness scopes to the 36 one-row meters + 2 gauges; live timers render 37 rows (34 + 3 `_max`), product-owned live total 75 = 123 − 48 under the corrected arithmetic. Reword the A8 bullet + the T2 note's framing.
- BH2 superseded A8 bullet unmarked — MEDIUM → **patch**: the old "scrape arithmetic" bullet still self-describes as "the baseline for the next live conformance check"; add an in-place corrected-marker pointing at the 2026-09-20 bullet.
- BH3 T1/T2 secret-file mode 0444-vs-0600 ambiguity — LOW, rejected: the fix edits this spec's Implementation Notes (rejected by rule); surfaced to the owner in the round report.
- BH4 `_sum` 0.183 s identical across T1/T2 boots — MAYBE-FALSE (would-be MEDIUM, evidence integrity) → **defer**: what settles it — a fresh §6.1 journey's `_sum`, or the T2 session's raw scrape output.
- BH5 §1 "every supported proxy posture" vs the §5.5 bridge variant — LOW → **patch**: "both default proxy postures" (EN+RU); the bridge variant is supported but unreachable.
- BH6 §9 teardown never mentions `prometheus-tsdb` — LOW → **patch**: one sentence (EN+RU) — the named volume survives `down`, only `down -v` removes it; augments T3's recorded §10-delta rationale rather than overturning it.
- BH7 §5.6 never says the 9091 target goes UP — MEDIUM → **bad_spec**: no task covers the README's own §5.6 section (T5 owned the runbook files only). T5's task amended — see Spec Change Log; T5 unstarted, so no revert; re-derivation owner-paced.
- BH10 "up == 0 and nothing else" is false — LOW → **patch**: failures also move the four other scrape-health series and show the error string in the Targets page; reword (EN+RU).
- BH11 in-review status vs open T5 — LOW → **patch (state)**: status restored to in-progress at round close; the T5 remainder is explicit here and in the change log.
- BH12 no Prometheus self-scrape job — LOW, rejected: fixture minimalism is the ratified config shape (the task enumerates the two-target config; Design Notes: no-durability dev rig).
- BH13 mermaid omits the always-configured 9091 target — LOW → **patch**: second dashed edge with the DOWN-until-second-instance label (EN+RU), completing Q3's visual story.

## Design Notes

- Why `network_mode: host` IS "joins the proxy's network namespace": both supported proxy postures (host-run jar §5.2; runbook `--network host` containers §5.6) live in the host namespace, so a compose service with `network_mode: host` shares the proxy's namespace and scrapes the literal `127.0.0.1:9090` — the only in-repo precedent where an external scraper touches the loopback endpoint (runbooks' "Why `--network host`"). The §5.5 bridge variant's metrics stay in-container-only (DEPLOY-011 posture) and unreachable — documented, not fixed.
- The service needs no `ports:` mapping at all (host namespace; the UI binds directly); a `ports:` block under `network_mode: host` is silently ignored by compose and would mislead readers — omit it.
- TSDB on a named compose volume with a short retention (~24h) — dev rig, no durability promise.

## Verification

**Commands:**
- `(cd sandbox && docker compose config)` -- expected: parses; the prometheus service shows `network_mode: host` and no ports publishing
- `grep -nE '909[01]' sandbox/compose.yml` -- expected: metrics-port references appear only in comments/scrape context, never in a `ports:` publish
- `./gradlew clean build` -- expected: GREEN (nothing under `proxy/` changed; house rule: docs-only rounds still build clean)

**Manual checks (if no CLI):**
- The T1 live proof and the T2 A8 reconciliation records in Implementation Notes (commands run, observations, row counts).
