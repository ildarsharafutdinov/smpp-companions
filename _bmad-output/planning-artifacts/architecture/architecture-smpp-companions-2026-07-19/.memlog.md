---
topic: Companions v1 architecture spine (smpp-companions)
goal: Fix the architecture-level invariants for the SMPP 3.4 security-transit proxy; resolve the PRD's deferred-to-architecture items (OQ-4, OQ-11, relay/splice core, module boundary, config/fail-fast, OIDC flow, threat model)
scope: Dual-role single-codebase runtime; relay/splice core; module boundaries; trust/auth (OIDC) integration; config, mode/role & deployment shapes; observability
purpose: build-substrate
altitude: feature
updated: 2026-07-19T20:20
---

- (event) Spine run started. Inputs: brief+addendum, PRD+addendum (status: final, authoritative), brainstorm-intent. Greenfield (no source code). Working mode = Coaching; deliverable = spine + walkthrough artifact (audience TBD).
- (constraint) Stack INHERITED binding from final PRD (won't re-litigate): JDK 25 (Loom-era, JVM-only); Netty (latest) for async I/O + pipelines; virtual threads + structured concurrency; ZGC; from-scratch SMPP layer only (codec/PDU/session); MATURE libs mandatory for TLS (JDK SSLEngine or Netty tcnative/BoringSSL) and OIDC (Nimbus JOSE+JWT) -- never roll own crypto (SEC-4, Decision B). GraalVM native-image = compatibility-stretch, not a v1 commitment (OQ-11 open).
- (constraint) Dual-role single codebase INHERITED: one runnable JAR; role (enterprise/ingress + carrier/egress) and mode (A/B/C) are DEPLOYMENT-TIME CONFIG of that JAR, not forks (FR-DEPLOY-1/2, MAINT-1). Docker image packages the same JAR.
- (constraint) Hybrid PDU model INHERITED (OQ-2 resolved): proxy inspects/handles ONLY the bind family + unbind(+resp) (session establishment + auth); ALL other PDUs spliced transparently as opaque bytes -- not inspected, not filtered. Payload-transparent; MT-only is a deployment expectation, not proxy-enforced (FR-TRANSIT-1..4).
- (constraint) Credential-free trust model INHERITED: proxy tier holds NO SMPP passwords / no vault; SMSC is sole credential authority; password-grant validation DELEGATED to operator OIDC provider (Keycloak=reference); FAIL-CLOSED (DENY on unavailable/indeterminate verdict); end-to-end identity (legacy system_id == carrier system_id, no pooling/mapping). Authority provider external/BYO; certs consumed at runtime, source-agnostic; no CA/issuance/bundled-IdP (FR-SEC-1..5, FR-AUTH-1/4).
- (constraint) Statelessness INHERITED: relay holds socket-pairing/connection state ONLY; NO message_id->system_id correlation (REL-4). Load-bearing on assumption A-1 (carrier allows multiple concurrent binds under one system_id + DLR session-affinity; author-confirmed). If A-1 false, design must become stateful -- architect smoke-tests early (PRD direction).
- (constraint) Performance targets INHERITED (locked): PERF-1 >=10K submit_sm/sec sustained (~25K stretch), mTLS both legs + OIDC cached + VT relay; PERF-2 10K concurrent idle socket pairs (~20K sockets) <1GB heap/<1vCPU idle; PERF-3 p99 bind ~250ms warm / <=2s cold / fail-closed DENY beyond 2-5s; PERF-4 sub-ms per-PDU added relay latency. Ref hw: 4-8 vCPU, Linux, JDK 25 GA, ZGC, 2-4 GiB heap.
- (constraint) Observability posture INHERITED: structured JSON-lines ops logging (startup/errors/bind accept-reject, system_id where relevant; full PDU/body only at TRACE, off by default) + read-only HTTP /metrics (Prometheus exposition) bound loopback IPv4 by default, NEVER on SMPP transit legs, system_id labels permitted, message content NEVER emitted. No dashboard/telemetry-backend/management-API (FR-OBS-1/2, OBS-1..3).
- (constraint) Deployment + platform INHERITED: two feature-equivalent shapes -- application (runnable JAR) + Docker image (packages JAR); Linux only; IPv4 only; single-instance v1 (no HA/failover); Apache-2.0; validate config at startup, FAIL-FAST on ambiguous/insecure/missing config; certs provisioned at deploy (no runtime ACME/SPIFFE); Mode C cert rotation = re-deploy (FR-DEPLOY-1..4, COMP-3/4, OPS-2).
- (question) OQ-4 (PRD-deferred): Mode C mTLS chain-validation depth + trust-anchor handling. Revocation (CRL/OCSP) already OUT (re-deploy-to-rotate, OQ-5). Only chain-depth + trust anchors remain. Validate client-cert chains against runtime trust store; how deep, what trust anchors.
- (question) OQ-11 (PRD-deferred): ship a GraalVM native-image BUILD TARGET in v1, or keep native-image as compatibility-stretch only (MAINT-5)? Compatibility preserved either way; the call is whether v1 produces a native binary artifact.
- (question) A6 cluster (PRD-deferred mechanisms): (1) per-leg TLS wiring + cipher/protocol negotiation specifics; (2) OIDC token-flow -- local JWT via cached JWKS vs remote introspection (resolve by what operator provider issues); (3) DLR-splice mechanics (deliver_sm back to originating bind w/o message-state, contingent on hybrid-splice + A-1); (4) A-1 carrier-multi-bind smoke-test method; (5) config-file format + Docker secret-injection contract (DEP-1).
- (direction) Headline architecture call = the DESIGN PARADIGM: how Netty ChannelPipelines, virtual threads + structured concurrency, and the relay/splice core compose. One model carries relay + backpressure + graceful shutdown + the bind->raw-splice transition. Coaching turn 1. NOTE tension to reconcile: PRD positioning line 'virtual-thread relay' vs perf reality that the steady-state byte splice wants the Netty event-loop ByteBuf-forward path (codec ~60x faster than net) -- likely VT-owns-control-plane, Netty-owns-data-plane.
- (decision) DESIGN PARADIGM = EVENT-LOOP RELAY (user chose Option A over my Option-C lean). Steady-state byte splice runs on Netty event loops via ByteBuf forwarding (HexdumpProxy-style channel coupling + autoRead backpressure) -- the proven low-overhead path PERF-1/2/4 require (codec ~60x faster than wire). Virtual threads + structured concurrency own the CONTROL PLANE ONLY: a shared VT service for bind adjudication (OIDC, off the event loop) + a StructuredTaskScope for the acceptor + graceful drain on SIGTERM. VTs never carry steady-state bytes. Binds: relay core, lifecycle, OIDC path. Prevents: per-PDU parsing on the hot path; event-loop blocking under OIDC; unbounded buffering on slow egress.
- (change) CONSEQUENCE / CROSS-ARTIFACT ITEM: PRD positioning line 'virtual-thread relay' (PRD S2; addendum A4 'VT carry per-connection work') is now MISLEADING under Option A -- VTs are control-plane only, not the relay. Reconcile wording to: 'Netty event-loop relay; virtual threads + structured concurrency own the control plane (bind adjudication, OIDC, graceful shutdown).' Same class of consistency fix as MT-only->payload-transparent. Loom story remains (structured concurrency for lifecycle + blocking OIDC off the event loop) but as control-plane, not per-connection VTs. Offer PRD-update at finalize.
- (decision) Walkthrough artifact audience = AUTHOR + TECH-FLUENT REVIEWERS (recruiters/contributors/future-self). Deep on trust model + relay internals; JVM/Netty/SMPP fluency assumed. Right-sized to portfolio piece (SM-1).
- (decision) Splice transition = FLIP-FLAG BYPASS (confirmed). RelayHandler always present flips a flag at couple-time: pre-bind passes reads to codec; post-couple consumes the framed ByteBuf and forwards to peer WITHOUT fireChannelRead (codec bypassed/dormant, zero cost). Transition = one atomic flag flip on the event loop. No live pipeline surgery (avoids pipeline.remove() races on a live channel).
- (decision) REFINEMENT (user): forward SMPP-FRAMED ByteBufs on the splice path, NOT raw stream bytes. SmppFrameDecoder (PDU length-framing) stays ACTIVE on both legs during splice; relay forwards whole-PDU framed ByteBufs; SmppCodec (object decode) bypassed/dormant. Binds: relay core, PERF-1, payload-transparency. Bonus: framed chunks enable free PDU-count metrics without content inspection (counting chunks != inspecting payload). Prevents: arbitrary byte-boundary forwarding; keeps PDU-grain observability + clean per-PDU write chunking.
- (decision) MODULE NAMES (user): companions-smpp -> companions-codec (scope = codec + PDU model + bind-family framing; the pure extractable protocol-layer seam, MAINT-2; zero dep on relay/TLS/OIDC/config/metrics/app); companions-app -> companions-proxy (relay + security + config + observability + bootstrap; the runnable proxy application = the JAR + Docker image). 2-module Gradle structure CONFIRMED; inward-only deps: companions-proxy -> companions-codec.
- (decision) State-ownership invariant CONFIRMED (user): MUTABLE at runtime = (a) ephemeral per-bind connection-pair state (RelayHandler/coupling, keyed by ingress channel, gone on teardown), (b) monotonic metrics counters/gauges, (c) bounded TTL OIDC verdict cache. IMMUTABLE after startup validation = config + routing table (system_id->egress target) + TLS material (rotation=re-deploy, OPS-2). NEVER EXISTS = message_id->system_id correlation (REL-4). AD: the only mutable runtime state is ephemeral per-bind + monotonic counters + bounded cache.
- (event) Launching 6-agent web fact-sweep (Netty, JDK25, GraalVM native-image, OIDC/Keycloak password-grant flow, mTLS chain-validation depth, SMPP conformance/mock-SMSC tooling) before coaching the deferred questions (OIDC token-flow, OQ-4, OQ-11). Front-loads the finalize gate's web-verify; coaching grounded in current 2026-07 facts.
- (version) Netty 4.2.16.Final (06-Jul-2026) via netty-bom [pending user confirm vs 4.1.136]. 4.2 is GA (since 03-Apr-2025, ~15mo production, community-recommended for new projects); Netty 5 is DEAD/abandoned (last 5.0.0.Alpha5 Sep-2022) -- do not consider. Use the 4.2 IoHandler API: MultiThreadIoEventLoopGroup(NioIoHandler.newFactory()) -- NioEventLoopGroup deprecated. NIO/Epoll only, NOT io_uring (Java-25 event-loop-group shutdown regression, issue #16174). HexDumpProxy pattern (shared event loop both legs, AUTO_READ=false, write-complete-gates-read) = the relay model.
- (version) JDK 25 LTS (GA 2025-09-16); pin a specific build (25.0.x) so 'latest 25' can't silently advance preview semantics. STABLE on 25: virtual threads (JEP 444 final), JEP 491 sync-no-pin (final 24->25), generational ZGC (the ONLY mode; -XX:+UseZGC, legacy -ZGenerational flag removed), ScopedValue (JEP 506 FINAL), jlink (~45-66MB).
- (version) Nimbus JOSE+JWT 10.9.1 (>=10.0.2 fixes CVE-2025-53864). jSMPP org.jsmpp:jsmpp:3.0.2 (Maven Central 2026-04; NOT 2.3.11 which GitHub misleadingly shows as Latest) -- interop/test counterpart only. Keycloak 26.x reference (26.7 Jul-2026). GraalVM for JDK 25 (Oracle 25.1.3 / CE 25.0.2) -- compatibility-stretch only.
- (decision) OQ-11 RESOLVED [pending confirm]: NO native-image build target in v1; keep MAINT-5 compatibility-stretch, revisit post-v1. Rationale: (a) always-on single-instance -> startup/footprint wins are noise; (b) ZGC UNAVAILABLE in native-image (SerialGC/G1 only) -> native build = strict regression on flagship GC; (c) Netty 4.2 reachability-metadata gaps (graalvm-reachability-metadata #655/#727) + TLS/JCA tracing-agent re-runs = maintenance burden + 2nd build matrix; (d) reflection-free codec/splice makes MAINT-5 nearly free to keep. Revisit trigger: a concrete cold-start-sensitive deployment (edge/serverless/memory-hard container).
- (decision) OQ-4 RESOLVED [pending confirm]: Mode C validates peer client-cert to operator-supplied trust anchor via JDK PKIX -- SslContextBuilder.trustManager(<operator trust store>) + .clientAuth(ClientAuth.REQUIRE). Rely on PKIX defaults for chain depth (maxPathLength=5, tightened by BasicConstraints.pathLenConstraint) -- do NOT override, no custom PKIXBuilderParameters, NO custom chain-validation code. Hard rules: trust store MUST NOT fall back to JDK cacerts (fail-fast if absent/empty -- else any publicly-trusted cert authenticates as a peer); REQUIRE never WANT (fail-closed if no cert / cert not reaching anchor); minimal single-purpose trust store (operator issuing-CA roots only) prevents cross-cert/bridge chasing; endpoint-identification is a no-op on client certs by JSSE design (correct for baked identity certs). Revocation OUT (OQ-5; rotation=re-deploy).
- (decision) OIDC token-flow RESOLVED [pending confirm]: ROPC (Keycloak Direct Access Grants) as v1 PRIMARY password-validation grant -- the ONLY standard grant validating a raw username+password (no browser, no pre-existing token; Auth-Code/Client-Creds/Token-Exchange/JWT-Bearer/Device-Code/Introspection all need a browser or an existing token). Flow: intercept bind (system_id+password in-memory, never persisted) -> ROPC POST /token with proxy confidential-client cred (mTLS RFC8705 or client_secret) -> 200=valid / 401=invalid (token-endpoint STATUS is the verdict) -> verify returned JWT locally via cached JWKS (defense-in-depth, not source of truth) -> DISCARD token, relay ORIGINAL SMPP bind to SMSC (sole credential authority). Re-validate EVERY bind (no verdict cache); cache JWKS only. Opaque-token fallback = RFC 7662 introspection. Fail-closed on unreachable/timeout/5xx/malformed/sig-mismatch; fail-fast at startup if provider lacks Direct Access Grants (non-default since Keycloak 26.2). Abstract behind a pluggable BindCredentialVerifier port so the ROPC adapter is swappable (hedge deprecation).
- (assumption) ACCEPTED RISK to own explicitly (SM-2 will probe): ROPC is DEPRECATED (RFC 9700 sec 4.1.2 'MUST NOT be used'; OAuth 2.1 removes it). Defensible because the deprecation rationale targets 3rd-party END-USER apps (raw-password exposure to untrusted clients, MFA/consent/WebAuthn incompatibility) -- NONE of which maps to a first-party, headless, operator-controlled M2M trust broker that already receives the password from the un-modifiable legacy wire + holds it in-memory per-bind only (no vault). Keycloak maintainer stianst (Dec-2025) endorsed this framing; Keycloak 26.7 still ships ROPC. Mitigated by BindCredentialVerifier port (swappable adapter). Must live in threat model + accepted-risk register.
- (change) CORRECTION to paradigm control plane (research-forced): StructuredTaskScope (JEP 505) is NOT stable on JDK 25 -- 5th PREVIEW, requires --enable-preview (all-or-nothing process flag forcing Netty/Nimbus/codec into preview semantics), major Joiner API revamp, not final until ~JDK 27. So the control plane MUST build on STABLE primitives only: virtual threads (JEP 444) + ScopedValue (JEP 506 final) + plain ExecutorService/CompletableFuture / try-with-resources ExecutorService (per JEP 444) for structured fan-out (concurrent OIDC, bind adjudication, graceful shutdown). StructuredTaskScope treated as opt-in/flag-gated behind a thin internal seam, NEVER load-bearing, never in the default build path. Paradigm name (Event-loop relay) unchanged; control-plane AD refined. [pending confirm]
- (change) CORRECTION to state-ownership invariant (research-forced): REMOVE 'bounded TTL OIDC verdict cache' from mutable runtime state. Cache JWKS ONLY (public keys, safe); re-validate EVERY bind via ROPC -- a cached 'valid' after the operator revokes/rotates the credential at the IdP is a security hole. Binds are infrequent session-setup events vs ~10K submit_sm/sec, so re-validation is cheap. REVISED mutable runtime state = (a) ephemeral per-bind connection-pair state, (b) monotonic metrics counters/gauges, (c) JWKS cache (public keys). Everything structural stays immutable post-startup.
- (constraint) NEW AD candidate -- egress TLS endpoint-identification: Netty 4.2 flips SslContextBuilder.endpointIdentificationAlgorithm default ON ('HTTPS', was null in 4.1) for client TLS. Hits the egress leg (Netty = TLS client to carrier SMSC, frequently a raw IP) -> must EXPLICITLY set endpointIdentificationAlgorithm (null for IP-based SMSC egress, or provision SMSC certs with IP SANs + keep verification on) or egress handshakes FAIL. sysprop escape hatch: io.netty.handler.ssl.defaultEndpointVerificationAlgorithm=NONE. Single most likely 4.2 bring-up breaker. (Applies to server-cert validation on the egress TLS client; orthogonal to OQ-4 client-cert side.)
- (constraint) NEW AD candidate -- SslHandler delegated-task offload: SSLEngine delegated tasks (CPU-heavy handshake crypto) run ON the EventExecutor (event loop) by DEFAULT; construct SslHandler with a bounded delegating Executor to avoid pinning event loops on TLS crypto during ingress accept-storm + egress connect burst. Safe with JDK SSLEngine (PRD default); test explicitly if tcnative/BoringSSL (issue #11850). Reinforces the existing invariant: NO blocking work on the event loop (OIDC/adjudication on VTs).
- (constraint) ByteBuf allocator posture (operational): de-risk 4.2 adaptive allocator (now default, 'designed for virtual threads') by starting io.netty.allocator.type=pooled, load-test BOTH (PERF-1 10K submit_sm/s + PERF-2 10K idle-pair soak) before picking; verify direct-memory/GC under ZGC. One shared PooledByteBufAllocator; size -XX:MaxDirectMemorySize; expose ByteBufAllocatorMetric. (addendum A4 + research.)
- (decision) TEST/CONFORMANCE TOOLCHAIN [pending confirm]: (1) AUTHOR a from-scratch SMPP 3.4 conformance/regression suite in the test module from SMPP v3.4 spec sec3/sec4 -- NO OSS suite exists to adopt (verified; FR-TRANSIT-3 cannot be satisfied by adopting one, only by authoring -- itself a portfolio contribution); optional JQF/jqwik structural fuzzing of the bind-PDU parser (only parsed surface, SEC-2). (2) Primary mock SMSC = purpose-built in-JVM mock on Companions' OWN codec (dogfoods codec; A-1 fixture + PERF-1..4 sink; emulates N concurrent binds/system_id + deliver_sm session affinity). (3) INDEPENDENT interop counterpart = jSMPP 3.0.2 (Apache-2.0) -- alternate ESME client vs Companions-egress + server-side mock (SMPPServerSimulator/StressServer examples); codec-bug backstop a self-mock cannot provide. NOT the production codec (stays from-scratch, MAINT-3). (4) A-1 carrier-side check = separate non-CI ops step vs real target carrier (mock validates the proxy splice half only; cannot validate the real carrier's affinity policy). (5) SM-3 benchmark honesty: scope 'first-of-kind' to 'no published SMPP PROXY/stateless-relay benchmark'; cite smpp-core/cloudhopper as library-level priors to preempt reviewer debunk.
- (event) 6-agent web fact-sweep COMPLETED (337K tokens, 107 tool-uses, ~12min, 0 errors; full per-agent results in workflow journal). 3 deferred Qs resolved (OQ-4, OQ-11, OIDC); 2 corrections to prior decisions (StructuredTaskScope preview -> stable control-plane primitives; no-verdict-cache -> JWKS-only); new ADs surfaced (egress endpoint-identification, SslHandler delegating executor); 1 inter-agent conflict to resolve (Netty 4.2 greenfield-lean vs 4.1 battle-tested). All grounded in 2026-07 verified sources. Full output: /tmp/claude-1000/.../tasks/wwf1u0205.output .
- (event) log-count-marker
- (decision) Netty line = 4.2.16.Final (USER CONFIRMED; resolves inter-agent conflict -- 4.2 greenfield-lean wins over 4.1 battle-tested). Via netty-bom; 4.2 IoHandler API (MultiThreadIoEventLoopGroup(NioIoHandler.newFactory())); NIO/Epoll only (NOT io_uring -- Java-25 shutdown regression); Netty 5 abandoned (never consider). Brings the load-bearing ADs already logged: egress endpointIdentificationAlgorithm must be explicit (4.2 default-on); SslHandler built with bounded delegating Executor; adaptive-vs-pooled allocator load-tested.
- (question) Config format OPEN: user answered 'springboot env vars' -- AMBIGUOUS, two readings. (A) Adopt Spring Boot platform: externalized config (application.yml + env-var relaxed binding) + DI + lifecycle + Actuator/Micrometer for /metrics; drive Netty directly (NOT WebFlux/Reactor). Diverges from PRD from-scratch/lean ethos; framework weight. (B) Env-var 12-factor config with Spring-Boot-style relaxed binding (COMPANIONS__ROLE, COMPANIONS__MODE, nested via __) + optional base file -> typed config record + fail-fast; NO Spring Boot framework; tiny hand-rolled Prometheus /metrics handler. My lean = (B) for the showcase/lean/Reactor-avoidance reasons; (A)'s Micrometer-for-/metrics is the main draw but achievable without Spring Boot. INVARIANT EITHER WAY (DEP-1): secrets (certs/keys/OIDC client secret) stay file-path-injected (env var holds the PATH to a mounted secret), never the secret value in an env var (leak via /proc/environ, docker inspect). Clarifying with user.
- (version) Spring Boot 4.1.x (current line; 4.0.0 GA 2025-11-20, 4.1.x is the mid-2026 line) on Spring Framework 7.0.8+, JDK 25 LTS -- web-verified: 'Java 25 + Spring Boot 4 = clean 2026 baseline'; first-class Java 25 support; min Java 17, compatible to JDK 26. Micrometer ships with Boot 4. Pin exact 4.1.x patch at build time.
- (decision) CONFIG APPROACH = Spring Boot platform (USER chose Reading A over my lean B). Spring Boot OWNS: externalized config (application.yml + env-var relaxed binding -> @ConfigurationProperties typed record + fail-fast via @Validated/JSR-380), DI (beans: RelayServer, BindAdjudicator, BindCredentialVerifier port + ROPC adapter, RoutingTable, MeterRegistry), lifecycle (SpringApplication + graceful shutdown), metrics (Micrometer). Mode/role/mode-B-acknowledgment + cert-missing + routing/SMSC-endpoint + provider-URL checks -> @ConfigurationProperties validation (FR-DEPLOY-3). Secrets stay file-path-injected (env var holds the PATH to a mounted secret, DEP-1) under Spring's config too.
- (constraint) SPRING BOOT HARD INVARIANTS: (1) NO WebFlux, NO Reactor -- drive Netty DIRECTLY (own ServerBootstrap/Bootstrap in a SmartLifecycle bean); Reactor would fight the event-loop relay + VT control plane. (2) Relay/adjudication threading is HAND-MANAGED virtual threads (ExecutorService per JEP 444) + ScopedValue; spring.threads.virtual.enabled is incidental (Spring's own managed executors only); Netty event loops stay platform threads. (3) No Spring web server on the SMPP path. Three threading models (Netty event loops / hand-managed VTs / Spring managed executors) kept coherent by this rule.
- (decision) GRACEFUL SHUTDOWN (REL-3) coordinates three layers: Spring lifecycle (SmartLifecycle/GracefulShutdownCallback) -> stop the Netty SMPP acceptor (no new binds) -> drain in-flight splices up to a configured timeout (the coupled channel set) -> VT control-plane drain -> exit. Spring's graceful shutdown timeout bounds the drain window.
- (question) /METRICS MECHANISM sub-decision (FR-OBS-2) [pending user]: (a) Actuator + Micrometer on a separate loopback management port (embedded server -- Tomcat by default -- serving ONLY /actuator/prometheus, all other endpoints disabled, loopback IPv4) = idiomatic Spring Boot but adds an embedded web server; OR (b) my lean = Micrometer metric model (no Actuator web stack) + a tiny loopback HTTP scrape handler on our OWN Netty (PrometheusMeterRegistry.scrape()) -- reuses Netty, NO Tomcat, NO WebFlux, leaner fit for the ethos. EITHER: loopback IPv4 only, system_id tags permitted (cardinality-controlled per FR-OBS-2), message content NEVER emitted, never on SMPP ports (OBS-1/2/3). NOTE: the 'active virtual threads' gauge (addendum A2) needs custom tracking -- Micrometer jvm_threads_* does NOT count virtual threads.
- (change) CROSS-ARTIFACT ITEM: Spring Boot adoption DIVERGES from the brief's stack positioning ('modern JVM stack: JDK 25, Netty, virtual threads' -- no Spring named). NOT a Decision-B violation ('from scratch' scoped to the SMPP layer only), but the brief/PRD stack framing should be reconciled to name Spring Boot as the config/DI/lifecycle/Micrometer substrate. Flag for finalize; offer to update brief/PRD wording. Same class of consistency fix as MT-only->payload-transparent and 'virtual-thread relay'.
- (decision) THREAT-MODEL INVARIANTS FIXED (user confirmed): (1) proxy tier holds NO trust root (no passwords/vault/CA/issuing keys; trust roots = OIDC provider + operator PKI + SMSC; proxy compromise = network position only); (2) fail-closed UNIVERSAL default for every auth-adjacent decision (enumerated indeterminate OIDC cases -> DENY; REQUIRE never WANT for mTLS; fail-fast on missing/empty trust store); (3) operator trust store NEVER falls back to JDK cacerts (else trust-root collapse); (4) payload transparency is NOT a content-level security control (no inspection/filtering/type-enforcement; adding non-bind parsing FORBIDDEN); (5) bind-family parser is the ONLY parsed attack surface (fuzz via JQF/jqwik; splice backpressures oversized frames); (6) no persistence of message bodies/passwords (passwords in-memory per-bind; content never in metrics/logs except TRACE off-by-default); (7) legacy<->proxy ingress leg UNAUTHENTICATED by the proxy (trusted network = sole gate; routes on system_id; A-3 isolation = deployer responsibility).
- (decision) ACCEPTED-RISK REGISTER OWNED (SM-2 probes these): INHERITED from PRD S11 (password-only legacy ingress; system_id spoofing on trusted net -> sole control A-3; authority-provider compromise = full impersonation A-2; Mode B plaintext over public internet; payload transparency = no content protection; long-lived baked Mode C certs rotation=re-deploy; provider outage blocks new binds; no local brute-force/rate-limit; no dashboard/management API; single-instance no HA) + NEW from research: ROPC deprecation (RFC 9700 'MUST NOT'; OAuth 2.1 removes) owned as accepted risk (deprecation targets 3rd-party end-user apps; doesn't map to first-party headless M2M broker receiving password off legacy wire in-memory per-bind; hedged by swappable BindCredentialVerifier port; Keycloak 26.7 still ships). Register lives in spine + walkthrough threat-model section.
- (event) ALL DEFERRED ITEMS CLOSED: design paradigm (event-loop relay); relay/splice core + state-ownership + module boundary (companions-codec / companions-proxy); config (Spring Boot platform, no WebFlux/Reactor) + /metrics (own Netty, Micrometer); OQ-4 (PKIX defaults, trust-store-never-cacerts); OQ-11 (no v1 native-image); OIDC flow (ROPC + JWKS + BindCredentialVerifier port); threat model + accepted-risk register. 44+ memlog entries. NEXT: distill ARCHITECTURE-SPINE.md from the memlog (Finalize step 1).
- (event) SPINE DISTILLED (draft): ARCHITECTURE-SPINE.md written from the memlog -- 24 ADs (AD-1..24) + Accepted-Risk Register + Consistency Conventions + web-verified Stack + 4 mermaid diagrams (paradigm data/control-plane, module deps, two-proxy topology+modes, deployment envelope) + source tree + Capability->Architecture Map + Deferred + Cross-artifact items. All deferred items resolved and captured as ADs (OQ-4=AD-13, OQ-11=AD-23, OIDC=AD-12, config=AD-16/17, threat model=AD-10/11/14/15/19+register). Launching the reviewer gate (lint + rubric + 2 configured reviewers + 3 reconcilers + security lens).
- (event) REVIEWER GATE COMPLETED (8 parallel lenses; post-loop log() had a 'serv' typo that threw after all agents finished -- no content lost, read 8 review files directly). VERDICTS: lint PASS (0); rubric pass-with-findings (6F: OIDC-transport-not-TLS, routing-cardinality doubly-deferred, default cipher allowlist, Docker base, PRIV-1 not bound, bind_resp mapping); webverify pass-with-findings (3F: 'Netty 5 abandoned' rationale WRONG -- it's alpha-only/main-active, no GA; GraalVM CE patch floor; IoHandler precision); adversarial FAIL (9F -- internal CONTRADICTION: paradigm diagram + AD-2 named two splice-flag flippers; + undefined BindCredentialVerifier port contract; role×mode config matrix undefined; intra-proxy ownership; executor assignment; per-bind state home; /metrics event-loop group; routing value schema; frame-size/memory budget); reconcile-prd pass-with-findings (F1 CONTRADICTION: AD-17 refuses Mode B but PRD S8 says warn-don't-refuse; F2 AD-10 overclaim; F4 VT-relay reconcile must extend to PERF-1/A1; F5 SEC-3 link encryption; F6 OPS-1 docs surface; + PRIV-1/REL-1 binds); reconcile-brief pass (Spring Boot correctly flagged); reconcile-brainstorm pass-with-findings (F1 unified-trust-root silently replaced by 3 roots -- user-confirmed but undisclosed; F2 proxy->provider TLS dropped); security pass-with-findings (15F: S1 AD-10 overclaim; S2 egress trust unowned -> cacerts collapse; S3 port is NOT a ROPC hedge; S4 frame-decoder is the exposed parser not in fuzz; S5 verdict-vs-JWT precedence; S6 two-proxy auth flow + AD-12/15 contradiction; S7 OIDC client cred not enumerated; S8 /metrics handler hardening; S9 system_id cardinality; + S10-S15 lows). APPLYING all critical/high/medium + cheap-low fixes inline.
- (decision) GATE FIX -- judgment calls made (surfaced to user, reversible): (1) ROUTING CARDINALITY = v1 INGRESS FRONTS EXACTLY ONE CARRIER EGRESS (1:1); routing table = system_id allow-list -> the single egress target; multi-carrier deferred post-v1 (simplicity + single-instance + statelessness + metrics cardinality). (2) MODE B = restored to PRD S8 posture: loud startup warning + explicit opt-in acknowledgment, STARTS (NOT refused); additionally Mode B is EGRESS-ONLY (ingress+ModeB rejected at fail-fast -- no ingress leg exists in Mode B topology). (3) UNIFIED-TRUST-ROOT -> 3 trust roots: DISCLOSED as brainstorm departure (user-confirmed memlog) + register 'mint certs' conditioned on OIDC-provider==operator-PKI-CA. (4) ROPC hard-dependency OWNED HONESTLY: BindCredentialVerifier port localizes rework to one adapter but does NOT eliminate the ROPC dependency (no standard replacement grant); register rewritten. (5) AD-10 = credential-free AT REST (qualified, honest); blast radius qualified. (6) EGRESS TLS trust = operator SMSC trust store, NEVER cacerts (new AD-26) -- closes the S2 trust-root-collapse-on-egress hole.
- (decision) GATE FIX -- spine structural changes: 7 NEW ADs (AD-25 bind->splice transition state machine [single flipper=RelayHandler, decoded bind_*_resp ROK trigger, post-couple unbind=opaque, teardown on TCP half-close]; AD-26 egress TLS trust anchoring [operator SMSC trust store, never cacerts, or explicit opt-in]; AD-27 intra-proxy ownership seams [bind-family command_id set in codec, SpliceObserver in observability]; AD-28 bounded delegating executors hand-managed/platform-thread/shared + JWKS refresh on hand-rolled VT; AD-29 routing cardinality 1:1 + value schema + Mode C cert per-instance; AD-30 max frame size + per-channel inbound budget + direct-memory derivation; AD-31 OPS-1 docs-as-operator-surface). ~14 ADs TIGHTENED (AD-2 transition+REL-1; AD-3 unbind-scope+5.x; AD-4 executor rejection; AD-8 ConnectionRegistry+JWKS shape+PRIV-1; AD-10 reword+enumeration; AD-11 enumerate+precedence+kid-miss+unknown-sysid; AD-12 TLS-link+port-contract+zeroize+two-proxy-flow+ROPC-honesty; AD-13 broaden-to-all-peer-paths+FR-AUTH-3+trust-store-enum; AD-15 local-check reword; AD-17 2x3 matrix+Mode-B-warn+egress-only+trust-enum; AD-19 dedicated-event-loop+handler-hardening+cardinality-mechanism; AD-20 egress-anchoring+SMSC-default-off; AD-22 deny-in-flight-adjudications; AD-24 fuzz-frame-decoder). Register rewrites (AD-10 blast radius, provider-compromise conditional, ROPC honesty). Cross-artifact additions (Netty-5 rationale, unified-trust-root disclosure, PERF-1/A1 reconcile, PRD-A2 /metrics opt-in, PRD-S11 ROPC). PRIV-1+REL-1 to binds. Spine 24->31 ADs.
- (event) VERIFY (2-lens re-review) COMPLETED: adversarial ALL 9 PRIOR (F-1..F-9) CLOSED + 5 new interaction-gap findings [NF-1 JWKS shutdown-ordering conflict AD-22 vs AD-28 HIGH; NF-2 SpliceObserver trigger events unpinned -> cardinality leak via the seam MEDIUM; NF-3 adjudication executor undefined (AD-12 refs a pool AD-28 never defines) MEDIUM; NF-4 bind_*_resp forwarder unnamed -> double bind_resp MEDIUM; NF-5 cacerts opt-in scope ambiguous LOW]; security ALL 15 PRIOR (S1..S15) CLOSED + 4 new [N1 Mode A two-proxy ingress-cannot-authenticate-egress -> plaintext relay to any peer MEDIUM; N2 introspection fallback not in AD-11 fail-closed enum + cache-forbidden + JWT-recommend MEDIUM; N3 AD-10 omits proxy TLS end-entity private keys LOW; N4 AD-12 DAG fail-fast overclaim (not startup-detectable) LOW]. APPLYING all 9 (AD-22 stop-JWKS-refresh step; AD-28(4) adjudication executor pin; AD-27 trigger-event pin; AD-25 bind_resp forwarder; AD-26 cacerts opt-in any-mode; AD-12 Mode-A-two-proxy ACL both-directions + register; AD-11 introspection clause; AD-12 introspection no-cache+JWT-rec; AD-10 enumeration(4) TLS keys; AD-12 DAG fail-fast honesty).
- (event) REVIEWER GATE PASSED (2 rounds). Round 1 (8 lenses): 33 findings (adversarial FAIL on the flipper contradiction + undefined seams; security 15F incl AD-10 overclaim + egress trust + ROPC-hedge theater; PRD-contradiction on Mode B; brainstorm unified-trust-root undisclosed; webverify Netty-5 rationale wrong) -- ALL closed by the 24->31 AD revision. Round 2 verify (adversarial + security): all 33 prior CLOSED; 9 new interaction-gap findings (NF-1..5, N1..4) -- ALL closed via 11 targeted edits. Spine = 31 ADs, lint 0 findings, mermaid valid. Gate verdict PASS. Next: build walkthrough HTML+SVG deck (audience: author + tech-fluent reviewers; deep on trust model + relay internals), then finalize.
- (change) Walkthrough artifact format switched per user request: HTML+SVG -> MARKDOWN + MERMAID (walkthrough.md). Mermaid sequenceDiagram used for the bind->splice lifecycle and inline OIDC (clearer than static SVG); flowcharts for trust roots / topology / paradigm / module seam; tables for stack + perf targets. walkthrough.html removed. Spine frontmatter companions updated to [walkthrough.md].
- (event) SPINE FINALIZED. Deliverables in workspace: ARCHITECTURE-SPINE.md (status: final, 31 ADs), walkthrough.md (mermaid+md deck, audience=author+tech-fluent), .memlog.md (54-entry decision trail), reviews/ (8-lens gate + 2-lens verify, all findings closed). Gate PASSED (2 rounds). Open reversible default pending user confirm: v1 ingress routing = 1:1 (AD-29). Cross-artifact items flagged for brief/PRD reconcile: Spring Boot stack naming; 'virtual-thread relay' wording (extend to PERF-1 condition + addendum A1 harness); unified-trust-root -> 3 roots disclosure; /metrics loopback-only (supersedes PRD A2 opt-in); ROPC hard-dependency framing. Next recommended: bmad-spec (adopt spine as PRD companion, stable AD ids) -> bmad-create-epics-and-stories.
- (decision) Routing cardinality CONFIRMED by user: v1 ingress = 1:1 (one carrier egress). No longer a provisional default -- locked. Multi-carrier routing remains deferred post-v1 (AD-29). ALL architecture open items now closed; spine fully settled (status: final, 31 ADs).
- (event) USER REVISION PASS (2026-07-20) -- 5 changes to the finalized spine + walkthrough:
- (decision) RENAME proxy1/proxy2 -> FORWARD PROXY / REVERSE PROXY (user). Semantically apt: the forward proxy fronts the legacy CLIENT (proxies outbound, ingress role); the reverse proxy fronts the SMSC SERVER (proxies inbound, egress role). Applied: AD-10, AD-17 matrix (ingress->reverse proxy; forward-proxy/SMSC trust), AD-26 (peer = forward proxy / SMSC), spine Structural Seed topology diagram, walkthrough §2 + §3 topology. Role CONFIG values (ingress|egress) unchanged -- only the proxy1/proxy2 shorthand labels are renamed.
- (change) REVERSAL -- control plane ADOPTS StructuredTaskScope (JEP 505) + ScopedValue (JEP 506) per user instruction, reversing the prior research-forced 'stable-primitives-only / STS-opt-in-behind-seam' stance (memlog earlier). AD-5 rewritten (header now 'Control plane on StructuredTaskScope + ScopedValue'); Accepted-Risk Register entry added; design-paradigm prose + frontmatter paradigm updated; walkthrough §4 + header updated. COMPOSITION: STS structures the intra-adjudication fan-out (fork ROPC + JWKS-verify, join, collapse to Verdict); the bounded hand-managed VT entry pool (AD-28(4)) STILL gates how many adjudications run concurrently + fail-closed-on-saturation (AD-11) -- STS does not replace that bounding. ScopedValue = per-bind RequestContext carrier (system_id, ChannelId, deadlines), never ThreadLocal. [USER CONFIRMED 2026-07-20: 'JDK 25 + --enable-preview' intent confirmed -- the STS adoption + process-wide preview is a deliberate, accepted decision, not an oversight. No JDK-pin bump; no revert.]
- (version) STRUCTURED CONCURRENCY status web-verified 2026-07-20: ScopedValue FINAL in JDK 25 (JEP 506; only change vs preview = ScopedValue.orElse no longer accepts null). StructuredTaskScope PREVIEW on JDK 25 (JEP 505, 5TH preview -- major 'joiner' API revamp) AND JDK 26 (JEP 525, 6TH preview, GA 2026-03-17, polishing timeouts/joiners); no firm final date (~JDK 27+). --enable-preview is all-or-nothing PROCESS flag -> forces Netty/Nimbus/codec under preview semantics. Sources: openjdk.org/jeps/505, /506, /525.
- (fix) TOPOLOGY BUG (user-caught): walkthrough §3 forward->reverse edge WRONGLY listed Mode B among A/C ('A: one-way TLS · B: plaintext · C: mTLS'); corrected to 'A: one-way TLS · C: mTLS' ONLY. Mode B is the SEPARATE legacy->reverse dashed path (no forward proxy in that topology). Spine Structural Seed diagram was already correct (A/C only). Clarifying prose added to walkthrough §3. NOTE: the rename (proxy1/proxy2 -> forward/reverse) was applied to BOTH topology diagrams in the same pass.
- (addition) BIND FRAMING DEFINED in AD-7 (user asked 'what exactly is bind framing in companions-codec'). Four-part definition: (1) PDU length-framing via SmppFrameDecoder -- read 4-octet command_length, accumulate exactly one PDU, emit one framed ByteBuf/PDU (the unit the relay forwards, AD-2); generic to every PDU on both legs; stays active during splice; enforces max command_length 65536, rejects <16, overflow-guards pre-allocation (AD-30). (2) Bind-family parsing via SmppCodec -- parses ONLY bind_transmitter/_receiver/_transceiver + their _resp into typed objects exposing header command_id/command_status/sequence_number + system_id/password/system_type/interface_version (what lets BindInterceptor route on system_id, hand password to BindAdjudicator AD-12, and let RelayHandler detect command_status==ROK = the AD-25 flip trigger). (3) SmppCommandIds.BIND_FAMILY = single source of truth for parsed-vs-opaque command_ids (AD-27). (4) OUT OF SCOPE: every non-bind PDU (submit_sm, deliver_sm, unbind...) is an opaque framed ByteBuf, never typed (AD-3). NET: parsed attack surface = exactly TWO decoders (generic framer + bind-family parser), both structurally fuzzed (AD-24).
- (fix) MERMAID PARSE ERROR (user-caught): walkthrough §5 sequenceDiagram Note had a U+2192 arrow glyph ('flips the flag ... -> COUPLE') that the sequenceDiagram lexer mis-parses as arrow syntax (error: line 20 'Expecting (), SOLID_OPEN_ARROW'). Replaced '-> COUPLE' with 'to COUPLE'. NOTE for future: arrow glyphs (->, <-, <->) are fine inside FLOWGRAPH quoted node labels and in prose, but BREAK unquoted sequenceDiagram Note/message text -- keep sequenceDiagram text ASCII (no -> / <- glyphs).
- (event) BRIEF/PRD RECONCILIATION PASS (2026-07-20) -- all 5 cross-artifact items folded into brief.md + brief addendum + prd.md + prd addendum (edits carry '(architecture AD-..)' / 'supersedes ...' markers): (1) 'virtual-thread relay' -> 'Netty event-loop relay (VT own the control plane)' in PRD tagline + PERF-1 + addendum A1 harness. (2) Spring Boot named as config/DI/lifecycle/Micrometer substrate (brief stack line + new brief-addendum bullet + new PRD-addendum A3 bullet under Decision B = SMPP-layer-only). (3) unified-trust-root -> THREE trust roots (brief-addendum topology + PRD-addendum A5 + PRD S11 authority-provider entry: cert-minting conditioned on provider==operator PKI CA). (4) PRD-addendum A2 '/metrics opt-in to bind elsewhere' SUPERSEDED -> loopback IPv4 only in v1 (AD-19); FR-OBS-2 tightened 'by default' -> 'in v1'. (5) ROPC hard-dependency ADDED to PRD S11 (port localizes-but-doesn't-eliminate). PLUS: native-image 'escape hatch' -> 'no v1 build, stretch' (brief + brief addendum; AD-23); OQ-4 + OQ-11 marked DECIDED in PRD S12 (PKIX defaults / no v1 native build); STS adoption reflected in brief/PRD stack framing (JDK 25 --enable-preview, StructuredTaskScope+ScopedValue) + COMP-2; proxy1/proxy2 -> forward/reverse proxy in brief addendum + PRD addendum A5.
- (decision) ROLE CONFIG VALUES CHANGED (user): the literal role enum ingress|egress -> forward|reverse (not just the proxy1/proxy2 shorthand). Applied: spine AD-12 two-proxy flow, AD-17 (enum `forward`|`reverse` + role x mode matrix rows + 'Mode B is reverse-only'), AD-26, AD-29, register Mode-A entry, topology diagram+prose; walkthrough (S3 diagram labels, S3 prose, S5 participant 'forward proxy', S10, S11); brief ('enterprise/forward' + 'carrier/reverse'); PRD (S2, S5, FR-DEPLOY-2, S8). DIRECTIONAL terms KEPT (leg/data-flow, not role): 'ingress leg'/'egress leg', 'ingress gate', 'Ingress routing', 'ingress/egress channel', 'ingress/egress TLS', 'egress target/endpoint', 'egress side', 'slow/pooled egress', 'harden ingress', 'ingress factor'. GLOSS: ROLE = forward|reverse (the instance's job); LEG = ingress|egress (data-flow direction within an instance). The forward proxy sits at ingress; its legacy-facing leg is the ingress leg.
- (decision) JAVA PACKAGE rename (user): base package prefix io -> smpp, i.e. io.companions.* -> smpp.companions.*. Applied: AD-12 port contract (smpp.companions.proxy.security.BindCredentialVerifier), Consistency Conventions/Naming (smpp.companions.codec.* / smpp.companions.proxy.*), Structural Seed source tree (src/main/java/smpp/companions/{codec,proxy}/). Sub-packages unchanged (relay/security/config/observability/bootstrap). CONFIG KEYS stay companions.* (separate Spring relaxed-binding namespace, not the java package) -- left as-is unless user wants those aligned too.
- (decision) JAVA PACKAGE rename (user): smpp.companions.* -> smpp.companion.* (plural -> singular base segment). Supersedes the prior io -> smpp.companions rename. Applied: AD-12 port contract (smpp.companion.proxy.security.BindCredentialVerifier), Naming (smpp.companion.codec.* / smpp.companion.proxy.*), source tree (src/main/java/smpp/companion/{codec,proxy}/). NET package base = smpp.companion. NOTE: the project/family NAME stays 'Companions' (plural); Gradle module names stay companions-codec / companions-proxy; config keys stay companions.* -- only the java package base went singular. Flag if user wants those aligned.
- (decision) MODULE NAMES + CONFIG KEYS aligned to singular (user): Gradle modules companions-codec / companions-proxy -> companion-codec / companion-proxy; Spring config keys companions.* -> companion.*. Applied across spine (Design Paradigm + AD-7 + AD-24 + AD-27 + Consistency Conventions/Modules+Naming + Structural Seed source tree + deployment mermaid + capability map + deferred) and walkthrough (S7 module mermaid). LEFT AS-IS (NOT module/config identifiers): the project/family NAME 'Companions' (plural, prose); the repo root dir 'companions/' (the Gradle ROOT project -- the doc defines 'the two modules' as the -codec/-proxy submodules, not the root); the doc frontmatter 'companions:' key (lists companion DOCS like walkthrough.md, not a Spring config key); the project slug 'smpp-companions'. NET: all code/Gradle/config identifiers now singular (smpp.companion.* packages, companion-codec/companion-proxy modules, companion.* config keys); the product/family name stays plural Companions.
- (decision) GRADLE MODULE names shortened (user): companion-codec -> codec, companion-proxy -> proxy (dropped the companion- prefix; bare module names). Applied via replace_all across spine + walkthrough (Design Paradigm, AD-7, AD-24, AD-27, Consistency Conventions/Modules, Structural Seed source-tree dirs codec/ + proxy/, deployment + module mermaid node labels, capability map, deferred). NET module names = codec + proxy. Java packages stay smpp.companion.{codec,proxy}.* (final package segment now == module name -- clean alignment); config keys stay companion.*; repo root dir stays companions/ (plural project dir, not a module). Module-naming history: io.companions -> smpp.companions -> smpp.companion -> companion-codec/proxy -> codec/proxy.
- (fix) MERMAID walkthrough S5 sequenceDiagram STILL would not render (user) even after the earlier U+2192 ->-glyph fix. ROOT CAUSE: the Mermaid sequenceDiagram grammar is strict about non-ASCII / special chars in UNQUOTED positions (participant aliases, message text, Note text, alt conditions) -- unlike flowgraph node labels, which are QUOTED ("...") and tolerate anything. Removed from S5: <br/> in participant aliases + Note text, the · (U+00B7) middot, the ; semicolon (a Mermaid statement TERMINATOR -- the likely persistent culprit, it cut the Note mid-sentence), the — em-dash in the alt condition, and the '/' separator in 'Allow / Deny' and 'indeterminate / invalid'. KEPT parens + commas (safe). Aliases now 'BindInterceptor (forward proxy, event loop)' / 'BindAdjudicator (virtual thread)'. STANDING RULE for these docs: sequenceDiagram text = ASCII only, no <br/> in aliases, no ·/—/;/arrow-glyphs anywhere in sequence text; flowgraph (graph/flowchart) labels may stay rich because they are quoted.
