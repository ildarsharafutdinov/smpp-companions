---
topic: Companions v1 architecture spine (smpp-companions)
goal: Fix the architecture-level invariants for the SMPP 3.4 security-transit proxy; resolve the PRD's deferred-to-architecture items (OQ-4, OQ-11, relay/splice core, module boundary, config/fail-fast, OIDC flow, threat model)
scope: Dual-role single-codebase runtime; relay/splice core; module boundaries; trust/auth (OIDC) integration; config, mode/role & deployment shapes; observability
purpose: build-substrate
altitude: feature
updated: 2026-07-23T22:24
---

- (event) Spine run started. Inputs: brief+addendum, PRD+addendum (status: final, authoritative), brainstorm-intent. Greenfield (no source code). Working mode = Coaching; deliverable = spine + walkthrough artifact (audience TBD).
- (constraint) Stack INHERITED binding from final PRD (won't re-litigate): JDK 25 (Loom-era, JVM-only); Netty (latest) for async I/O + pipelines; virtual threads + structured concurrency; ZGC; from-scratch SMPP layer only (codec/PDU/session); MATURE libs mandatory for TLS (JDK SSLEngine or Netty tcnative/BoringSSL) and OIDC (Nimbus JOSE+JWT) -- never roll own crypto (SEC-4, Decision B). GraalVM native-image = compatibility-stretch, not a v1 commitment (OQ-11 open).
- (constraint) Dual-role single codebase INHERITED: one runnable JAR; role (enterprise/ingress + carrier/egress) and mode (A/B/C) are DEPLOYMENT-TIME CONFIG of that JAR, not forks (FR-DEPLOY-1/2, MAINT-1). Docker image packages the same JAR.
- (constraint) Hybrid PDU model INHERITED (OQ-2 resolved): proxy inspects/handles ONLY the bind family + unbind(+resp) (session establishment + auth); ALL other PDUs spliced transparently as opaque bytes -- not inspected, not filtered. Payload-transparent; MT-only is a deployment expectation, not proxy-enforced (FR-TRANSIT-1..4).
- (constraint) Credential-free trust model INHERITED: proxy tier holds NO SMPP passwords / no vault; SMSC is sole credential authority; password-grant validation DELEGATED to operator OIDC provider (Keycloak=reference); FAIL-CLOSED (DENY on unavailable/indeterminate verdict); end-to-end identity (legacy system_id == carrier system_id, no pooling/mapping). Authority provider external/BYO; certs consumed at runtime, source-agnostic; no CA/issuance/bundled-IdP (FR-SEC-1..5, FR-AUTH-1/4).
- (constraint) Statelessness INHERITED: relay holds socket-pairing/connection state ONLY; NO message_id->system_id correlation (REL-4). Load-bearing on assumption A-1 (carrier allows multiple concurrent binds under one system_id + DLR session-affinity; author-confirmed). If A-1 false, design must become stateful -- architect smoke-tests early (PRD direction).
- (constraint) Performance targets INHERITED (locked): PERF-1 >=10K submit_sm/sec sustained (~25K stretch), mTLS both legs + OIDC cached + VT relay; PERF-2 10K concurrent idle socket pairs (~20K sockets) <1GB heap/<1vCPU idle; PERF-3 p99 bind ~250ms warm / <=2s cold / fail-closed DENY beyond 2-5s; PERF-4 sub-ms per-PDU added relay latency. Ref hw: 4-8 vCPU, Linux, JDK 25 GA, ZGC, 2-4 GiB heap.
- (constraint) Observability posture INHERITED: structured JSON-lines ops logging (startup/errors/bind accept-reject, system_id where relevant; full PDU/body only at TRACE, off by default) + read-only HTTP /metrics (Prometheus exposition) bound loopback IPv4 by default, NEVER on SMPP transit legs, system_id labels permitted, message content NEVER emitted. No dashboard/telemetry-backend/management-API (FR-OBS-1/2, OBS-1..3).
- (constraint) Deployment + platform INHERITED: two feature-equivalent shapes -- application (runnable JAR) + Docker image (packages JAR); Linux only; IPv4 only; single-instance v1 (no HA/failover); Apache-2.0; validate config at startup, FAIL-FAST on ambiguous/insecure/missing config; certs provisioned at deploy (no runtime ACME/SPIFFE); Mode C cert rotation = re-deploy (FR-DEPLOY-1..4, COMP-3/4, OPS-2).
- (question) OQ-4 (PRD-deferred): Mode C mTLS chain-validation depth + trust-anchor handling. Revocation (CRL/OCSP) already OUT (re-deploy-to-rotate, OQ-5). Only chain-depth + trust anchors remain. Validate client-cert chains against runtime trust store; how deep, what trust anchors.
- (question) OQ-11 (PRD-deferred): ship a GraalVM native-image BUILD TARGET in v1, or keep native-image as compatibility-stretch only (MAINT-5)? Compatibility preserved either way; the call is whether v1 produces a native binary artifact.
- (question) A6 cluster (PRD-deferred mechanisms): (1) per-leg TLS wiring + cipher/protocol negotiation specifics; (2) OIDC token-flow -- local JWT via cached JWKS vs remote introspection (resolve by what operator provider issues); (3) DLR-splice mechanics (deliver_sm back to originating bind w/o message-state, contingent on hybrid-splice + A-1); (4) A-1 carrier-multi-bind smoke-test method; (5) config-file format + Docker secret-injection contract (DEP-1).
- (direction) Headline architecture call = the DESIGN PARADIGM: how Netty ChannelPipelines, virtual threads + structured concurrency, and the relay/splice core compose. One model carries relay + backpressure + graceful shutdown + the bind->raw-splice transition. Coaching turn 1. NOTE tension to reconcile: PRD positioning line 'virtual-thread relay' vs perf reality that the steady-state byte splice wants the Netty event-loop ByteBuf-forward path (codec ~60x faster than net) -- likely VT-owns-control-plane, Netty-owns-data-plane.
- (decision) DESIGN PARADIGM = EVENT-LOOP RELAY (user chose Option A over my Option-C lean). Steady-state byte splice runs on Netty event loops via ByteBuf forwarding (HexdumpProxy-style channel coupling + autoRead backpressure) -- the proven low-overhead path PERF-1/2/4 require (codec ~60x faster than wire). Virtual threads + structured concurrency own the CONTROL PLANE ONLY: a shared VT service for bind adjudication (OIDC, off the event loop) + a StructuredTaskScope for the acceptor + graceful drain on SIGTERM. VTs never carry steady-state bytes. Binds: relay core, lifecycle, OIDC path. Prevents: per-PDU parsing on the hot path; event-loop blocking under OIDC; unbounded buffering on slow egress.
- (change) CONSEQUENCE / CROSS-ARTIFACT ITEM: PRD positioning line 'virtual-thread relay' (PRD S2; addendum A4 'VT carry per-connection work') is now MISLEADING under Option A -- VTs are control-plane only, not the relay. Reconcile wording to: 'Netty event-loop relay; virtual threads + structured concurrency own the control plane (bind adjudication, OIDC, graceful shutdown).' Same class of consistency fix as MT-only->payload-transparent. Loom story remains (structured concurrency for lifecycle + blocking OIDC off the event loop) but as control-plane, not per-connection VTs. Offer PRD-update at finalize.
- (decision) Walkthrough artifact audience = AUTHOR + TECH-FLUENT REVIEWERS (recruiters/contributors/future-self). Deep on trust model + relay internals; JVM/Netty/SMPP fluency assumed. Right-sized to portfolio piece (SM-1).
- (decision) Splice transition = FLIP-FLAG BYPASS (confirmed). RelayHandler always present flips a flag at couple-time: pre-bind passes reads to codec; post-couple consumes the framed ByteBuf and forwards to peer WITHOUT fireChannelRead (codec bypassed/dormant, zero cost). Transition = one atomic flag flip on the event loop. No live pipeline surgery (avoids pipeline.remove() races on a live channel).
- (decision) REFINEMENT (user): forward SMPP-FRAMED ByteBufs on the splice path, NOT raw stream bytes. SmppFrameDecoder (PDU length-framing) stays ACTIVE on both legs during splice; relay forwards whole-PDU framed ByteBufs; SmppCodec (object decode) bypassed/dormant. Binds: relay core, PERF-1, payload-transparency. Bonus: framed chunks enable free PDU-count metrics without content inspection (counting chunks != inspecting payload). Prevents: arbitrary byte-boundary forwarding; keeps PDU-grain observability + clean per-PDU write chunking.
- (decision) MODULE NAMES (user): companions-smpp -> companions-codec (scope = codec + PDU model + bind-family framing; the pure extractable protocol-layer seam, MAINT-2; zero dep on relay/TLS/OIDC/config/metrics/app); companions-app -> companions-proxy (relay + security + config + observability + bootstrap; the runnable proxy application = the JAR + Docker image). 2-module Gradle structure CONFIRMED; inward-only deps: companions-proxy -> companions-codec.
- (decision) State-ownership invariant CONFIRMED (user): MUTABLE at runtime = (a) ephemeral per-bind connection-pair state (RelayHandler/coupling, keyed by ingress channel, gone on teardown), (b) monotonic metrics counters/gauges, (c) bounded TTL OIDC verdict cache. IMMUTABLE after startup validation = config + routing table (system_id->egress target) + TLS material (rotation=re-deploy, OPS-2). NEVER EXISTS = message_id->system_id correlation (REL-4). AD: the only mutable runtime state is ephemeral per-bind + monotonic counters + bounded cache.
- (event) Launching 6-agent web fact-sweep (Netty, JDK25, GraalVM native-image, OIDC/Keycloak password-grant flow, mTLS chain-validation depth, SMPP conformance/mock-SMSC tooling) before coaching the deferred questions (OIDC token-flow, OQ-4, OQ-11). Front-loads the finalize gate's web-verify; coaching grounded in current 2026-07 facts.
- (version) Netty 4.2.16.Final (06-Jul-2026) via netty-bom [pending user confirm vs 4.1.136]. 4.2 is GA (since 03-Apr-2025, ~15mo production, community-recommended for new projects); Netty 5 is DEAD/abandoned (last 5.0.0.Alpha5 Sep-2022) -- do not consider. Use the 4.2 IoHandler API: MultiThreadIoEventLoopGroup(NioIoHandler.newFactory()) -- NioEventLoopGroup deprecated. NIO/Epoll only, NOT io_uring (Java-25 event-loop-group shutdown regression, issue #16174). HexDumpProxy pattern (shared event loop both legs, AUTO_READ=false, write-complete-gates-read) = the relay model.
- (version) JDK 25 LTS (GA 2025-09-16); pin a specific build (25.0.x) so 'latest 25' can't silently advance preview semantics. STABLE on 25: virtual threads (JEP 444 final), JEP 491 sync-no-pin (final 24->25), generational ZGC (the ONLY mode; -XX:+UseZGC, legacy -ZGenerational flag removed), ScopedValue (JEP 506 FINAL), jlink (~45-66MB).
- (version) Nimbus JOSE+JWT 10.9.1 (>=10.0.2 fixes CVE-2025-53864). jSMPP org.jsmpp:jsmpp:3.0.2 (Maven Central 2026-04; NOT 2.3.11 which GitHub misleadingly shows as Latest) -- interop/test counterpart only. Keycloak 26.x reference (26.7 Jul-2026). GraalVM for JDK 25 (Oracle 25.1.3 / CE 25.0.2) -- compatibility-stretch only.
- (decision) OQ-11 RESOLVED [pending confirm]: NO native-image build target in v1; keep MAINT-5 compatibility-stretch, revisit post-v1. Rationale: (a) always-on single-instance -> startup/footprint wins are noise; (b) ZGC UNAVAILABLE in native-image (SerialGC/G1 only) -> native build = strict regression on flagship GC; (c) Netty 4.2 reachability-metadata gaps (graalvm-reachability-metadata #655/#727) + TLS/JCA tracing-agent re-runs = maintenance burden + 2nd build matrix; (d) reflection-free codec/splice makes MAINT-5 nearly free to keep. Revisit trigger: a concrete cold-start-sensitive deployment (edge/serverless/memory-hard container).
- (decision) OQ-4 RESOLVED [pending confirm]: Mode C validates peer client-cert to operator-supplied trust anchor via JDK PKIX -- SslContextBuilder.trustManager(<operator trust store>) + .clientAuth(ClientAuth.REQUIRE). Rely on PKIX defaults for chain depth (maxPathLength=5, tightened by BasicConstraints.pathLenConstraint) -- do NOT override, no custom PKIXBuilderParameters, NO custom chain-validation code. Hard rules: trust store MUST NOT fall back to JDK cacerts (fail-fast if absent/empty -- else any publicly-trusted cert authenticates as a peer); REQUIRE never WANT (fail-closed if no cert / cert not reaching anchor); minimal single-purpose trust store (operator issuing-CA roots only) prevents cross-cert/bridge chasing; endpoint-identification is a no-op on client certs by JSSE design (correct for baked identity certs). Revocation OUT (OQ-5; rotation=re-deploy).
- (decision) OIDC token-flow RESOLVED [pending confirm]: ROPC (Keycloak Direct Access Grants) as v1 PRIMARY password-validation grant -- the ONLY standard grant validating a raw username+password (no browser, no pre-existing token; Auth-Code/Client-Creds/Token-Exchange/JWT-Bearer/Device-Code/Introspection all need a browser or an existing token). Flow: intercept bind (system_id+password in-memory, never persisted) -> ROPC POST /token with proxy confidential-client cred (mTLS RFC8705 or client_secret) -> 200=valid / 401=invalid (token-endpoint STATUS is the verdict) -> verify returned JWT locally via cached JWKS (defense-in-depth, not source of truth) -> DISCARD token, relay ORIGINAL SMPP bind to SMSC (sole credential authority). Re-validate EVERY bind (no verdict cache); cache JWKS only. Opaque-token fallback = RFC 7662 introspection. Fail-closed on unreachable/timeout/5xx/malformed/sig-mismatch; fail-fast at startup if provider lacks Direct Access Grants (non-default since Keycloak 26.2). Abstract behind a pluggable BindCredentialVerifier port so the ROPC adapter is swappable (hedge deprecation).
- (assumption) ACCEPTED RISK to own explicitly (SM-2 will probe): ROPC is DEPRECATED (RFC 9700 sec 4.1.2 'MUST NOT be used'; OAuth 2.1 removes it). Defensible because the deprecation rationale targets 3rd-party END-USER apps (raw-password exposure to untrusted clients, MFA/consent/WebAuthn incompatibility) -- NONE of which maps to a first-party, headless, operator-controlled M2M trust broker that already receives the password from the un-modifiable legacy wire + holds it in-memory per-bind only (no vault). Keycloak maintainer stianst (Dec-2025) endorsed this framing; Keycloak 26.7 still ships ROPC. Mitigated by BindCredentialVerifier port (swappable adapter). Must live in threat model + accepted-risk register.
- (change) CORRECTION to paradigm control plane (research-forced): StructuredTaskScope (JEP 505) is NOT stable on JDK 25 -- 5th PREVIEW, requires --enable-preview (all-or-nothing process flag forcing Netty/Nimbus/codec into preview semantics), major Joiner API revamp, not final until ~JDK 27. So the control plane MUST build on STABLE primitives only: virtual threads (JEP 444) + ScopedValue (JEP 506 final) + plain ExecutorService/CompletableFuture / try-with-resources ExecutorService (per JEP 444) for structured fan-out (concurrent OIDC, bind adjudication, graceful shutdown). StructuredTaskScope treated as opt-in/flag-gated behind a thin internal seam, NEVER load-bearing, never in the default build path. Paradigm name (Event-loop relay) unchanged; control-plane AD refined. [pending confirm]
- (change) CORRECTION to state-ownership invariant (research-forced): REMOVE 'bounded TTL OIDC verdict cache' from mutable runtime state. Cache JWKS ONLY (public keys, safe); re-validate EVERY bind via ROPC -- a cached 'valid' after the operator revokes/rotates the credential at the IdP is a security hole. Binds are infrequent session-setup events vs ~10K submit_sm/sec, so re-validation is cheap. REVISED mutable runtime state = (a) ephemeral per-bind connection-pair state, (b) monotonic metrics counters/gauges, (c) JWKS cache (public keys). Everything structural stays immutable post-startup.
- (constraint) NEW AD candidate -- egress TLS endpoint-identification: Netty 4.2 flips SslContextBuilder.endpointIdentificationAlgorithm default ON ('HTTPS', was null in 4.1) for client TLS. Hits the egress leg (Netty = TLS client to carrier SMSC, frequently a raw IP) -> must EXPLICITLY set endpointIdentificationAlgorithm (null for IP-based SMSC egress, or provision SMSC certs with IP SANs + keep verification on) or egress handshakes FAIL. sysprop escape hatch: io.netty.handler.ssl.defaultEndpointVerificationAlgorithm=NONE. Single most likely 4.2 bring-up breaker. (Applies to server-cert validation on the egress TLS client; orthogonal to OQ-4 client-cert side.)
- (constraint) NEW AD candidate -- SslHandler delegated-task offload: SSLEngine delegated tasks (CPU-heavy handshake crypto) run ON the EventExecutor (event loop) by DEFAULT; construct SslHandler with a bounded delegating Executor to avoid pinning event loops on TLS crypto during ingress accept-storm + egress connect burst. Safe with JDK SSLEngine (PRD default); test explicitly if tcnative/BoringSSL (issue #11850). Reinforces the existing invariant: NO blocking work on the event loop (OIDC/adjudication on VTs).
- (constraint) ByteBuf allocator posture (operational): de-risk 4.2 adaptive allocator (now default, 'designed for virtual threads') by starting io.netty.allocator.type=pooled, load-test BOTH (PERF-1 10K submit_sm/s + PERF-2 10K idle-pair soak) before picking; verify direct-memory/GC under ZGC. One shared PooledByteBufAllocator; size -XX:MaxDirectMemorySize; expose ByteBufAllocatorMetric. (addendum A4 + research.)
- (decision) TEST/CONFORMANCE TOOLCHAIN [pending confirm]: (1) AUTHOR a from-scratch SMPP 3.4 conformance/regression suite in the test module from SMPP v3.4 spec sec3/sec4 -- NO OSS suite exists to adopt (verified; FR-TRANSIT-3 cannot be satisfied by adopting one, only by authoring -- itself a portfolio contribution); optional JQF/jqwik structural fuzzing of the bind-PDU parser (only parsed surface, SEC-2). (2) Primary mock SMSC = purpose-built in-JVM mock on Companions' OWN codec (dogfoods codec; A-1 fixture + PERF-1..4 sink; emulates N concurrent binds/system_id + deliver_sm session affinity). (3) INDEPENDENT interop counterpart = jSMPP 3.0.2 (Apache-2.0) -- alternate ESME client vs Companions-egress + server-side mock (SMPPServerSimulator/StressServer examples); codec-bug backstop a self-mock cannot provide. NOT the production codec (stays from-scratch, MAINT-3). (4) A-1 carrier-side check = separate non-CI ops step vs real target carrier (mock validates the proxy splice half only; cannot validate the real carrier's affinity policy). (5) SM-3 benchmark honesty: scope 'first-of-kind' to 'no published SMPP PROXY/stateless-relay benchmark'; cite smpp-core/cloudhopper as library-level priors to preempt reviewer debunk.
- (event) 6-agent web fact-sweep COMPLETED (337K tokens, 107 tool-uses, ~12min, 0 errors; full per-agent results in workflow journal). 3 deferred Qs resolved (OQ-4, OQ-11, OIDC); 2 corrections to prior decisions (StructuredTaskScope preview -> stable control-plane primitives; no-verdict-cache -> JWKS-only); new ADs surfaced (egress endpoint-identification, SslHandler delegating executor); 1 inter-agent conflict to resolve (Netty 4.2 greenfield-lean vs 4.1 battle-tested). All grounded in 2026-07 verified sources. Full output: /tmp/claude-1000/.../tasks/wwf1u0205.output .
- (event) log-count-marker
- (decision) Netty line = 4.2.16.Final (USER CONFIRMED; resolves inter-agent conflict -- 4.2 greenfield-lean wins over 4.1 battle-tested). Via netty-bom; 4.2 IoHandler API (MultiThreadIoEventLoopGroup(NioIoHandler.newFactory())); NIO/Epoll only (NOT io_uring -- Java-25 shutdown regression); Netty 5 abandoned (never consider). Brings the load-bearing ADs already logged: egress endpointIdentificationAlgorithm must be explicit (4.2 default-on); SslHandler built with bounded delegating Executor; adaptive-vs-pooled allocator load-tested.
- (question) Config format OPEN: user answered 'springboot env vars' -- AMBIGUOUS, two readings. (A) Adopt Spring Boot platform: externalized config (application.yml + env-var relaxed binding) + DI + lifecycle + Actuator/Micrometer for /metrics; drive Netty directly (NOT WebFlux/Reactor). Diverges from PRD from-scratch/lean ethos; framework weight. (B) Env-var 12-factor config with Spring-Boot-style relaxed binding (COMPANIONS__ROLE, COMPANIONS__MODE, nested via __) + optional base file -> typed config record + fail-fast; NO Spring Boot framework; tiny hand-rolled Prometheus /metrics handler. My lean = (B) for the showcase/lean/Reactor-avoidance reasons; (A)'s Micrometer-for-/metrics is the main draw but achievable without Spring Boot. INVARIANT EITHER WAY (DEP-1): secrets (certs/keys/OIDC client secret) stay file-path-injected (env var holds the PATH to a mounted secret), never the secret value in an env var (leak via /proc/environ, docker inspect). Clarifying with user.
- (version) Spring Boot 4.1.x (current line; 4.0.0 GA 2025-11-20, 4.1.x is the mid-2026 line) on Spring Framework 7.0.8+, JDK 25 LTS -- web-verified: 'Java 25 + Spring Boot 4 = clean 2026 baseline'; first-class Java 25 support; min Java 17, compatible to JDK 26. Micrometer ships with Boot 4. Pin exact 4.1.x patch at build time.
- (decision) CONFIG APPROACH = Spring Boot platform (USER chose Reading A over my lean B). Spring Boot OWNS: externalized config (application.yml + env-var relaxed binding -> @ConfigurationProperties typed record + fail-fast via @Validated/JSR-380), DI (beans: RelayServer, BindAdjudicator, BindCredentialVerifier port + ROPC adapter, RoutingTable, MeterRegistry), lifecycle (SpringApplication + graceful shutdown), metrics (Micrometer). Mode/role/mode-B-acknowledgment + cert-missing + routing/SMSC-endpoint + provider-URL checks -> @ConfigurationProperties validation (FR-DEPLOY-3). Secrets stay file-path-injected (env var holds the PATH to a mounted secret, DEP-1) under Spring's config too.
- (constraint) SPRING BOOT HARD INVARIANTS: (1) NO WebFlux, NO Reactor -- drive Netty DIRECTLY (own ServerBootstrap/Bootstrap in a SmartLifecycle bean); Reactor would fight the event-loop relay + VT control plane. (2) Relay/adjudication threading is HAND-MANAGED virtual threads (ExecutorService per JEP 444) + ScopedValue; spring.threads.virtual.enabled is incidental (Spring's own managed executors only); Netty event loops stay platform threads. (3) No Spring web server on the SMPP path. Three threading models (Netty event loops / hand-managed VTs / Spring managed executors) kept coherent by this rule.
- (decision) GRACEFUL SHUTDOWN (REL-3) coordinates three layers: Spring lifecycle (SmartLifecycle/GracefulShutdownCallback) -> stop the Netty SMPP acceptor (no new binds) -> drain in-flight splices up to a configured timeout (the coupled channel set) -> VT control-plane drain -> exit. Spring's graceful shutdown timeout bounds the drain window.
- (question) /METRICS MECHANISM sub-decision (FR-OBS-2) [pending user]: (a) Actuator + Micrometer on a separate loopback management port (embedded server -- Tomcat by default -- serving ONLY /actuator/prometheus, all other endpoints disabled, loopback IPv4) = idiomatic Spring Boot but adds an embedded web server; OR (b) my lean = Micrometer metric model (no Actuator web stack) + a tiny loopback HTTP scrape handler on our OWN Netty (PrometheusMeterRegistry.scrape()) -- reuses Netty, NO Tomcat, NO WebFlux, leaner fit for the ethos. EITHER: loopback IPv4 only, system_id tags permitted (cardinality-controlled per FR-OBS-2), message content NEVER emitted, never on SMPP ports (OBS-1/2/3). NOTE: the 'active virtual threads' gauge (addendum A2) needs custom tracking -- Micrometer jvm_threads_* does NOT count virtual threads.
- (change) CROSS-ARTIFACT ITEM: Spring Boot adoption DIVERGES from the brief's stack positioning ('modern JVM stack: JDK 25, Netty, virtual threads' -- no Spring named). NOT a Decision-B violation ('from scratch' scoped to the SMPP layer only), but the brief/PRD stack framing should be reconciled to name Spring Boot as the config/DI/lifecycle/Micrometer substrate. Flag for finalize; offer to update brief/PRD wording. Same class of consistency fix as MT-only->payload-transparent and 'virtual-thread relay'.
- (decision) THREAT-MODEL INVARIANTS FIXED (user confirmed): (1) proxy tier holds NO trust root (no passwords/vault/CA/issuing keys; trust roots = OIDC provider + operator PKI + SMSC; proxy compromise = network position only); (2) fail-closed UNIVERSAL default for every auth-adjacent decision (enumerated indeterminate OIDC cases -> DENY; REQUIRE never WANT for mTLS; fail-fast on missing/empty trust store); (3) operator trust store NEVER falls back to JDK cacerts (else trust-root collapse); (4) payload transparency is NOT a content-level security control (no inspection/filtering/type-enforcement; adding non-bind parsing FORBIDDEN); (5) bind-family parser is the ONLY parsed attack surface (fuzz via JQF/jqwik; splice backpressures oversized frames); (6) no persistence of message bodies/passwords (passwords in-memory per-bind; content never in metrics/logs except TRACE off-by-default); (7) legacy<->proxy ingress leg UNAUTHENTICATED by the proxy (trusted network = sole gate; routes on system_id; A-3 isolation = deployer responsibility).
- (decision) ACCEPTED-RISK REGISTER OWNED (SM-2 probes these): INHERITED from PRD S11 (password-only legacy ingress; system_id spoofing on trusted net -> sole control A-3; authority-provider compromise = full impersonation A-2; Mode B plaintext over public internet; payload transparency = no content protection; long-lived baked Mode C certs rotation=re-deploy; provider outage blocks new binds; no local brute-force/rate-limit; no dashboard/management API; single-instance no HA) + NEW from research: ROPC deprecation (RFC 9700 'MUST NOT'; OAuth 2.1 removes) owned as accepted risk (deprecation targets 3rd-party end-user apps; doesn't map to first-party headless M2M broker receiving password off legacy wire in-memory per-bind; hedged by swappable BindCredentialVerifier port; Keycloak 26.7 still ships). Register lives in spine + walkthrough threat-model section.
- (event) ALL DEFERRED ITEMS CLOSED: design paradigm (event-loop relay); relay/splice core + state-ownership + module boundary (companions-codec / companions-proxy); config (Spring Boot platform, no WebFlux/Reactor) + /metrics (own Netty, Micrometer); OQ-4 (PKIX defaults, trust-store-never-cacerts); OQ-11 (no v1 native-image); OIDC flow (ROPC + JWKS + BindCredentialVerifier port); threat model + accepted-risk register. 44+ memlog entries. NEXT: distill ARCHITECTURE-SPINE.md from the memlog (Finalize step 1).
- (event) SPINE DISTILLED (draft): ARCHITECTURE-SPINE.md written from the memlog -- 24 ADs (AD-1..24) + Accepted-Risk Register + Consistency Conventions + web-verified Stack + 4 mermaid diagrams (paradigm data/control-plane, module deps, two-proxy topology+modes, deployment envelope) + source tree + Capability->Architecture Map + Deferred + Cross-artifact items. All deferred items resolved and captured as ADs (OQ-4=AD-13, OQ-11=AD-23, OIDC=AD-12, config=AD-16/17, threat model=AD-10/11/14/15/19+register). Launching the reviewer gate (lint + rubric + 2 configured reviewers + 3 reconcilers + security lens).
- (event) REVIEWER GATE COMPLETED (8 parallel lenses; post-loop log() had a 'serv' typo that threw after all agents finished -- no content lost, read 8 review files directly). VERDICTS: lint PASS (0); rubric pass-with-findings (6F: OIDC-transport-not-TLS, routing-cardinality doubly-deferred, default cipher allowlist, Docker base, PRIV-1 not bound, bind_resp mapping); webverify pass-with-findings (3F: 'Netty 5 abandoned' rationale WRONG -- it's alpha-only/main-active, no GA; GraalVM CE patch floor; IoHandler precision); adversarial FAIL (9F -- internal CONTRADICTION: paradigm diagram + AD-2 named two splice-flag flippers; + undefined BindCredentialVerifier port contract; role×mode config matrix undefined; intra-proxy ownership; executor assignment; per-bind state home; /metrics event-loop group; routing value schema; frame-size/memory budget); reconcile-prd pass-with-findings (F1 CONTRADICTION: AD-17 refuses Mode B but PRD S8 says warn-don't-refuse; F2 AD-10 overclaim; F4 VT-relay reconcile must extend to PERF-1/A1; F5 SEC-3 link encryption; F6 OPS-1 docs surface; + PRIV-1/REL-1 binds); reconcile-brief pass (Spring Boot correctly flagged); reconcile-brainstorm pass-with-findings (F1 unified-trust-root silently replaced by 3 roots -- user-confirmed but undisclosed; F2 proxy->provider TLS dropped); security pass-with-findings (15F: S1 AD-10 overclaim; S2 egress trust unowned -> cacerts collapse; S3 port is NOT a ROPC hedge; S4 frame-decoder is the exposed parser not in fuzz; S5 verdict-vs-JWT precedence; S6 two-proxy auth flow + AD-12/15 contradiction; S7 OIDC client cred not enumerated; S8 /metrics handler hardening; S9 system_id cardinality; + S10-S15 lows). APPLYING all critical/high/medium + cheap-low fixes inline.
- (decision) GATE FIX -- judgment calls made (surfaced to user, reversible): (1) ROUTING CARDINALITY = v1 INGRESS FRONTS EXACTLY ONE CARRIER EGRESS (1:1); routing table = system_id allow-list -> the single egress target; multi-carrier deferred post-v1 (simplicity + single-instance + statelessness + metrics cardinality). (2) MODE B = restored to PRD S8 posture: loud startup warning + explicit opt-in acknowledgment, STARTS (NOT refused); additionally Mode B is EGRESS-ONLY (ingress+ModeB rejected at fail-fast -- no ingress leg exists in Mode B topology). (3) UNIFIED-TRUST-ROOT -> 3 trust roots: DISCLOSED as brainstorm departure (user-confirmed memlog) + register 'mint certs' conditioned on OIDC-provider==operator-PKI-CA. (4) ROPC hard-dependency OWNED HONESTLY: BindCredentialVerifier port localizes rework to one adapter but does NOT eliminate the ROPC dependency (no standard replacement grant); register rewritten. (5) AD-10 = credential-free AT REST (qualified, honest); blast radius qualified. (6) EGRESS TLS trust = operator SMSC trust store, NEVER cacerts (new AD-26) -- closes the S2 trust-root-collapse-on-egress hole.
- (decision) GATE FIX -- spine structural changes: 7 NEW ADs (AD-25 bind->splice transition state machine [single flipper=RelayHandler, decoded bind_*_resp ROK trigger, post-couple unbind=opaque, teardown on TCP half-close]; AD-26 egress TLS trust anchoring [operator SMSC trust store, never cacerts, or explicit opt-in]; AD-27 intra-proxy ownership seams [bind-family command_id set in codec, SpliceObserver in observability]; AD-28 bounded delegating executors hand-managed/platform-thread/shared + JWKS refresh on hand-rolled VT; AD-29 routing cardinality 1:1 + value schema + Mode C cert per-instance; AD-30 max frame size + per-channel inbound budget + direct-memory derivation; AD-31 OPS-1 docs-as-operator-surface). ~14 ADs TIGHTENED (AD-2 transition+REL-1; AD-3 unbind-scope+5.x; AD-4 executor rejection; AD-8 ConnectionRegistry+JWKS shape+PRIV-1; AD-10 reword+enumeration; AD-11 enumerate+precedence+kid-miss+unknown-sysid; AD-12 TLS-link+port-contract+zeroize+two-proxy-flow+ROPC-honesty; AD-13 broaden-to-all-peer-paths+FR-AUTH-3+trust-store-enum; AD-15 local-check reword; AD-17 2x3 matrix+Mode-B-warn+egress-only+trust-enum; AD-19 dedicated-event-loop+handler-hardening+cardinality-mechanism; AD-20 egress-anchoring+SMSC-default-off; AD-22 deny-in-flight-adjudications; AD-24 fuzz-frame-decoder). Register rewrites (AD-10 blast radius, provider-compromise conditional, ROPC honesty). Cross-artifact additions (Netty-5 rationale, unified-trust-root disclosure, PERF-1/A1 reconcile, PRD-A2 /metrics opt-in, PRD-S11 ROPC). PRIV-1+REL-1 to binds. Spine 24->31 ADs.
- (event) VERIFY (2-lens re-review) COMPLETED: adversarial ALL 9 PRIOR (F-1..F-9) CLOSED + 5 new interaction-gap findings [NF-1 JWKS shutdown-ordering conflict AD-22 vs AD-28 HIGH; NF-2 SpliceObserver trigger events unpinned -> cardinality leak via the seam MEDIUM; NF-3 adjudication executor undefined (AD-12 refs a pool AD-28 never defines) MEDIUM; NF-4 bind_*_resp forwarder unnamed -> double bind_resp MEDIUM; NF-5 cacerts opt-in scope ambiguous LOW]; security ALL 15 PRIOR (S1..S15) CLOSED + 4 new [N1 Mode A two-proxy ingress-cannot-authenticate-egress -> plaintext relay to any peer MEDIUM; N2 introspection fallback not in AD-11 fail-closed enum + cache-forbidden + JWT-recommend MEDIUM; N3 AD-10 omits proxy TLS end-entity private keys LOW; N4 AD-12 DAG fail-fast overclaim (not startup-detectable) LOW]. APPLYING all 9 (AD-22 stop-JWKS-refresh step; AD-28(4) adjudication executor pin; AD-27 trigger-event pin; AD-25 bind_resp forwarder; AD-26 cacerts opt-in any-mode; AD-12 Mode-A-two-proxy ACL both-directions + register; AD-11 introspection clause; AD-12 introspection no-cache+JWT-rec; AD-10 enumeration(4) TLS keys; AD-12 DAG fail-fast honesty).
- (event) REVIEWER GATE PASSED (2 rounds). Round 1 (8 lenses): 33 findings (adversarial FAIL on the flipper contradiction + undefined seams; security 15F incl AD-10 overclaim + egress trust + ROPC-hedge theater; PRD-contradiction on Mode B; brainstorm unified-trust-root undisclosed; webverify Netty-5 rationale wrong) -- ALL closed by the 24->31 AD revision. Round 2 verify (adversarial + security): all 33 prior CLOSED; 9 new interaction-gap findings (NF-1..5, N1..4) -- ALL closed via 11 targeted edits. Spine = 31 ADs, lint 0 findings, mermaid valid. Gate verdict PASS. Next: build walkthrough HTML+SVG deck (audience: author + tech-fluent reviewers; deep on trust model + relay internals), then finalize.
- (change) Walkthrough artifact format switched per user request: HTML+SVG -> MARKDOWN + MERMAID (walkthrough.md). Mermaid sequenceDiagram used for the bind->splice lifecycle and inline OIDC (clearer than static SVG); flowcharts for trust roots / topology / paradigm / module seam; tables for stack + perf targets. walkthrough.html removed. Spine frontmatter companions updated to [walkthrough.md].
- (event) SPINE FINALIZED. Deliverables in workspace: ARCHITECTURE-SPINE.md (status: final, 31 ADs), walkthrough.md (mermaid+md deck, audience=author+tech-fluent), .memlog.md (54-entry decision trail), reviews/ (8-lens gate + 2-lens verify, all findings closed). Gate PASSED (2 rounds). Open reversible default pending user confirm: v1 ingress routing = 1:1 (AD-29). Cross-artifact items flagged for brief/PRD reconcile: Spring Boot stack naming; 'virtual-thread relay' wording (extend to PERF-1 condition + addendum A1 harness); unified-trust-root -> 3 roots disclosure; /metrics loopback-only (supersedes PRD A2 opt-in); ROPC hard-dependency framing. Next recommended: bmad-spec (adopt spine as PRD companion, stable AD ids) -> bmad-create-epics-and-stories.
- (decision) Routing cardinality CONFIRMED by user: v1 ingress = 1:1 (one carrier egress). No longer a provisional default -- locked. Multi-carrier routing remains deferred post-v1 (AD-29). ALL architecture open items now closed; spine fully settled (status: final, 31 ADs).
- (event) USER REVISION PASS (2026-07-20) -- 5 changes to the finalized spine + walkthrough:
- (decision) RENAME proxy1/proxy2 -> FORWARD PROXY / REVERSE PROXY (user). Semantically apt: the forward proxy fronts the legacy CLIENT (proxies outbound, ingress role); the reverse proxy fronts the SMSC SERVER (proxies inbound, egress role). Applied: AD-10, AD-17 matrix (ingress->reverse proxy; forward-proxy/SMSC trust), AD-26 (peer = forward proxy / SMSC), spine Structural Seed topology diagram, walkthrough §2 + §3 topology. Role CONFIG values (ingress|egress) unchanged -- only the proxy1/proxy2 shorthand labels are renamed.
- (change) REVERSAL -- control plane ADOPTS StructuredTaskScope (JEP 505) + ScopedValue (JEP 506) per user instruction, reversing the prior research-forced 'stable-primitives-only / STS-opt-in-behind-seam' stance (memlog earlier). AD-5 rewritten (header now 'Control plane on StructuredTaskScope + ScopedValue'); Accepted-Risk Register entry added; design-paradigm prose + frontmatter paradigm updated; walkthrough §4 + header updated. COMPOSITION: STS structures the intra-adjudication fan-out (fork ROPC + JWKS-verify, join, collapse to Verdict); the bounded hand-managed VT entry pool (AD-28(4)) STILL gates how many adjudications run concurrently + fail-closed-on-saturation (AD-11) -- STS does not replace that bounding. ScopedValue = per-bind RequestContext carrier (system_id, ChannelId, deadlines), never ThreadLocal. [USER CONFIRMED 2026-07-20: 'JDK 25 + --enable-preview' intent confirmed -- the STS adoption + process-wide preview is a deliberate, accepted decision, not an oversight. No JDK-pin bump; no revert.]
- (version) STRUCTURED CONCURRENCY status web-verified 2026-07-20: ScopedValue FINAL in JDK 25 (JEP 506; only change vs preview = ScopedValue.orElse no longer accepts null). StructuredTaskScope PREVIEW on JDK 25 (JEP 505, 5TH preview -- major 'joiner' API revamp) AND JDK 26 (JEP 525, 6TH preview, GA 2026-03-17, polishing timeouts/joiners); no firm final date (~JDK 27+). --enable-preview is all-or-nothing PROCESS flag -> forces Netty/Nimbus/codec under preview semantics. Sources: openjdk.org/jeps/505, /506, /525.
- (fix) TOPOLOGY BUG (user-caught): walkthrough §3 forward->reverse edge WRONGLY listed Mode B among A/C ('A: one-way TLS · B: plaintext · C: mTLS'); corrected to 'A: one-way TLS · C: mTLS' ONLY. Mode B is the SEPARATE legacy->reverse dashed path (no forward proxy in that topology). Spine Structural Seed diagram was already correct (A/C only). Clarifying prose added to walkthrough §3. NOTE: the rename (proxy1/proxy2 -> forward/reverse) was applied to BOTH topology diagrams in the same pass.
- (addition) BIND FRAMING DEFINED in AD-7 (user asked 'what exactly is bind framing in companions-codec'). Four-part definition: (1) PDU length-framing via SmppFrameDecoder -- read 4-octet command_length, accumulate exactly one PDU, emit one framed ByteBuf/PDU (the unit the relay forwards, AD-2); generic to every PDU on both legs; stays active during splice; enforces max command_length 65536, rejects <16, overflow-guards pre-allocation (AD-30). (2) Bind-family parsing via SmppCodec -- parses ONLY bind_transmitter/_receiver/_transceiver + their _resp into typed objects exposing header command_id/command_status/sequence_number + system_id/password/system_type/interface_version (what lets BindInterceptor route on system_id, hand password to BindAdjudicator AD-12, and let RelayHandler detect command_status==ROK = the AD-25 flip trigger). (3) SmppCommandIds.BIND_FAMILY = single source of truth for parsed-vs-opaque command_ids (AD-27). (4) OUT OF SCOPE: every non-bind PDU (submit_sm, deliver_sm, unbind...) is an opaque framed ByteBuf, never typed (AD-3). NET: parsed attack surface = exactly TWO decoders (generic framer + bind-family parser), both structurally fuzzed (AD-24).
- (fix) MERMAID PARSE ERROR (user-caught): walkthrough §5 sequenceDiagram Note had a U+2192 arrow glyph ('flips the flag ... -> COUPLE') that the sequenceDiagram lexer mis-parses as arrow syntax (error: line 20 'Expecting (), SOLID_OPEN_ARROW'). Replaced '-> COUPLE' with 'to COUPLE'. NOTE for future: arrow glyphs (->, <-, <->) are fine inside FLOWGRAPH quoted node labels and in prose, but BREAK unquoted sequenceDiagram Note/message text -- keep sequenceDiagram text ASCII (no -> / <- glyphs).
- (event) BRIEF/PRD RECONCILIATION PASS (2026-07-20) -- all 5 cross-artifact items folded into brief.md + brief addendum + prd.md + prd addendum (edits carry '(architecture AD-..)' / 'supersedes ...' markers): (1) 'virtual-thread relay' -> 'Netty event-loop relay (VT own the control plane)' in PRD tagline + PERF-1 + addendum A1 harness. (2) Spring Boot named as config/DI/lifecycle/Micrometer substrate (brief stack line + new brief-addendum bullet + new PRD-addendum A3 bullet under Decision B = SMPP-layer-only). (3) unified-trust-root -> THREE trust roots (brief-addendum topology + PRD-addendum A5 + PRD S11 authority-provider entry: cert-minting conditioned on provider==operator PKI CA). (4) PRD-addendum A2 '/metrics opt-in to bind elsewhere' SUPERSEDED -> loopback IPv4 only in v1 (AD-19); FR-OBS-2 tightened 'by default' -> 'in v1'. (5) ROPC hard-dependency ADDED to PRD S11 (port localizes-but-doesn't-eliminate). PLUS: native-image 'escape hatch' -> 'no v1 build, stretch' (brief + brief addendum; AD-23); OQ-4 + OQ-11 marked DECIDED in PRD S12 (PKIX defaults / no v1 native build); STS adoption reflected in brief/PRD stack framing (JDK 25 --enable-preview, StructuredTaskScope+ScopedValue) + COMP-2; proxy1/proxy2 -> forward/reverse proxy in brief addendum + PRD addendum A5.
- (decision) ROLE CONFIG VALUES CHANGED (user): the literal role enum ingress|egress -> forward|reverse (not just the proxy1/proxy2 shorthand). Applied: spine AD-12 two-proxy flow, AD-17 (enum `forward`|`reverse` + role x mode matrix rows + 'Mode B is reverse-only'), AD-26, AD-29, register Mode-A entry, topology diagram+prose; walkthrough (S3 diagram labels, S3 prose, S5 participant 'forward proxy', S10, S11); brief ('enterprise/forward' + 'carrier/reverse'); PRD (S2, S5, FR-DEPLOY-2, S8). DIRECTIONAL terms KEPT (leg/data-flow, not role): 'ingress leg'/'egress leg', 'ingress gate', 'Ingress routing', 'ingress/egress channel', 'ingress/egress TLS', 'egress target/endpoint', 'egress side', 'slow/pooled egress', 'harden ingress', 'ingress factor'. GLOSS: ROLE = forward|reverse (the instance's job); LEG = ingress|egress (data-flow direction within an instance). The forward proxy sits at ingress; its legacy-facing leg is the ingress leg.
- (decision) JAVA PACKAGE rename (user): base package prefix io -> smpp, i.e. io.companions.* -> smpp.companions.*. Applied: AD-12 port contract (smpp.companions.proxy.security.BindCredentialVerifier), Consistency Conventions/Naming (smpp.companions.codec.* / smpp.companions.proxy.*), Structural Seed source tree (src/main/java/smpp/companions/{codec,proxy}/). Sub-packages unchanged (relay/security/config/observability/bootstrap). CONFIG KEYS stay companions.* (separate Spring relaxed-binding namespace, not the java package) -- left as-is unless user wants those aligned too.
- (decision) JAVA PACKAGE rename (user): smpp.companions.* -> smpp.companion.* (plural -> singular base segment). Supersedes the prior io -> smpp.companions rename. Applied: AD-12 port contract (smpp.companion.proxy.security.BindCredentialVerifier), Naming (smpp.companion.codec.* / smpp.companion.proxy.*), source tree (src/main/java/smpp/companion/{codec,proxy}/). NET package base = smpp.companion. NOTE: the project/family NAME stays 'Companions' (plural); Gradle module names stay companions-codec / companions-proxy; config keys stay companions.* -- only the java package base went singular. Flag if user wants those aligned.
- (decision) MODULE NAMES + CONFIG KEYS aligned to singular (user): Gradle modules companions-codec / companions-proxy -> companion-codec / companion-proxy; Spring config keys companions.* -> companion.*. Applied across spine (Design Paradigm + AD-7 + AD-24 + AD-27 + Consistency Conventions/Modules+Naming + Structural Seed source tree + deployment mermaid + capability map + deferred) and walkthrough (S7 module mermaid). LEFT AS-IS (NOT module/config identifiers): the project/family NAME 'Companions' (plural, prose); the repo root dir 'companions/' (the Gradle ROOT project -- the doc defines 'the two modules' as the -codec/-proxy submodules, not the root); the doc frontmatter 'companions:' key (lists companion DOCS like walkthrough.md, not a Spring config key); the project slug 'smpp-companions'. NET: all code/Gradle/config identifiers now singular (smpp.companion.* packages, companion-codec/companion-proxy modules, companion.* config keys); the product/family name stays plural Companions.
- (decision) GRADLE MODULE names shortened (user): companion-codec -> codec, companion-proxy -> proxy (dropped the companion- prefix; bare module names). Applied via replace_all across spine + walkthrough (Design Paradigm, AD-7, AD-24, AD-27, Consistency Conventions/Modules, Structural Seed source-tree dirs codec/ + proxy/, deployment + module mermaid node labels, capability map, deferred). NET module names = codec + proxy. Java packages stay smpp.companion.{codec,proxy}.* (final package segment now == module name -- clean alignment); config keys stay companion.*; repo root dir stays companions/ (plural project dir, not a module). Module-naming history: io.companions -> smpp.companions -> smpp.companion -> companion-codec/proxy -> codec/proxy.
- (fix) MERMAID walkthrough S5 sequenceDiagram STILL would not render (user) even after the earlier U+2192 ->-glyph fix. ROOT CAUSE: the Mermaid sequenceDiagram grammar is strict about non-ASCII / special chars in UNQUOTED positions (participant aliases, message text, Note text, alt conditions) -- unlike flowgraph node labels, which are QUOTED ("...") and tolerate anything. Removed from S5: <br/> in participant aliases + Note text, the · (U+00B7) middot, the ; semicolon (a Mermaid statement TERMINATOR -- the likely persistent culprit, it cut the Note mid-sentence), the — em-dash in the alt condition, and the '/' separator in 'Allow / Deny' and 'indeterminate / invalid'. KEPT parens + commas (safe). Aliases now 'BindInterceptor (forward proxy, event loop)' / 'BindAdjudicator (virtual thread)'. STANDING RULE for these docs: sequenceDiagram text = ASCII only, no <br/> in aliases, no ·/—/;/arrow-glyphs anywhere in sequence text; flowgraph (graph/flowchart) labels may stay rich because they are quoted.
- (event) UPDATE run (2026-07-23): resolve test-design blockers Q1-Q7. User-locked, adversarially verified (4-lens workflow on Q1). Adds AD-32/33/34, amends AD-27/3/25/19/30/24, updates register+deferred+conventions; test-design flipped to accepted; epics synced.
- (decision) AD-32 Pre-couple non-bind PDU policy (resolves Q1, RELAY-002). Fail-closed, ZERO config knobs, SMPP 3.4-conformant. Pre-splice-flip either leg: unbind/unbind_resp = AD-3 handshake (unbind_resp ROK + close), carved out; enquire_link = enquire_link_resp ROK + KEEP (keepalive, both legs); any OTHER well-formed non-bind PDU = synthesize MATCHING _resp (command_id=offender|0x80000000, command_status=ESME_RINVBNDSTS 0x00000004, sequence_number=offender_seq, spec-conformant body via command_id->response-body-requirement lookup; offender body opaque), writeAndFlush+CLOSE (flush before FIN), tear down in-flight bind (cancel ROPC at HTTP layer, zeroize password, remove ConnectionRegistry entry); egress generic_nack from SMSC pre-bind_resp = bind-failure propagation R32, NOT nack-back; generic_nack RESERVED (SMPP 3.4 sec 4.3) for unresolvable frames (unknown command_id -> generic_nack RINVCMDID 0x03 seq=0 + close). Binds relay wire behavior, fail-closed, REL-1. Prevents malformed error PDUs, pre-couple buffer state (AD-9 preserved), bind-in-flight wire oracle.
- (decision) Q1 error-code FIX: ESME_RINVBNDSTS = 0x00000004 (NOT the invented ESME_RINVBINDSTAT 0x1D). Verified smpp.org sec 5.1.3 + 5 refs. The pre-update draft mis-cited it; corrected before the spine write.
- (decision) Q1 user-locked trade-offs: D1 matching-_resp carries a SPEC-CONFORMANT BODY (command_id->response-body-requirement lookup, e.g. submit_sm_resp.message_id empty C-octet) -- eliminates the header-only gray area by construction, offender body stays opaque; D3 NO eager-pdu/buffer knob -- pipelining ESMEs (submit_sm before bind_resp) and clients with bind-timeout < PERF-3 cold/DENY are CLOSED (hard constraint -> register + OPS-1 + A-1 carrier check); D2 NO close-on-violation knob (always-on above-spec policy); D4 IdP rate-limiting is operator-scope.
- (decision) AD-33 bind_resp denial wire-collapse (resolves Q7). ALLOW -> ESME_ROK; ALL bind denials -> ONE generic bind-failure code, scoped to bind_*_resp answering bind_* ONLY (exact code deferred to owning story). Rich OIDC outcome -> JSON-lines logs + bounded Verdict-type counters ONLY, never on wire. Prevents system_id enumeration / IdP-availability oracle. Q1 non-bind violations use the matching _resp + RINVBNDSTS, NOT a bind_resp -- Q7 collapse does not propagate to non-bind responses.
- (decision) AD-34 TLS cipher/protocol allowlist default (resolves Q3, SEC-1, resolves Deferred exact-cipher item). Protocols [TLSv1.3, TLSv1.2]; TLS-1.2 set = TLS_ECDHE_ECDSA/RSA_WITH_AES_{128,256}_GCM_SHA{256,384} (no CBC, no static RSA, no RC4/3DES/null/anon) + optional ChaCha20-Poly1305. Operator-tunable under companion.tls.*; startup intersect-with-provider, empty -> fail-fast (AD-17).
- (decision) AD-30 amendment (resolves Q5): codec max command_length (65536), the MaxDirectMemorySize formula input, and the config default reference ONE named constant (ArchUnit/Gradle static assertion catches conservative drift a runtime soak cannot) + a startup ByteBufAllocatorMetric self-check (live MaxDirectMemorySize >= computed budget, fail-fast). Closes the JVM-flag gap ArchUnit cannot reach.
- (decision) AD-27 amendment: add SpliceObserver.onConnectionClosed(Direction, CloseReason); closed enum CloseReason{PEER_HALF_CLOSE, PEER_RST, EGRESS_CONNECT_FAILED, OVERSIZED_FRAME, PRE_COUPLE_NON_BIND_PDU, SHUTDOWN_DRAIN, DECODE_ERROR}; called by RelayHandler, codec never invokes it. Replaces latent RELAY-010 teardown assumption; corrects Epic 4 SpliceObserver-unchanged -> one method added. Counter relay_connections_closed_total{direction,reason}.
- (decision) AD-3 corollary + AD-25 note + AD-19/OBS-009 generalization (all from Q1). AD-3: unbind pre-couple = clean handshake (carved out of non-bind=violation); enquire_link pre-couple = keepalive (not session op). AD-25: close-on-violation = above-spec fail-closed policy (always on, no knob), not SMPP-mandated; implicitly fails in-flight bind w/o bind_resp (AD-11 ok). AD-19: NO system_id/command_id/ChannelId label on ANY pre-flip counter; command_id ONLY in TRACE (ByteBuf.getInt(4), no codec helper).
- (decision) Q2/Q4/Q6 RESOLVED at test-design altitude (AD-24 gains notes): Q2 adopt jcstress scoped to cross-thread state (ConnectionRegistry teardown, AtomicReference JwkSet swap, CompletableFuture x ScopedValue handoff), nightly, PAIRED with delay-injection event-loop-jitter soak (visibility vs event-ordering races); earns P0 R5. Q4 two-real-instance E2E (E2E-001) weekly tier (forward+reverse over in-JVM mock SMSC+IdP, Mode C via deploy-time contract; validates DEP-1). Q6 CI-gate positive controls (known-bad fixture + assert-it-fires per gate).
- (change) Accepted-Risk Register UPDATE: sharpen no-rate-limit -> Q1 bind-in-flight abandons an in-flight ROPC the IdP still completes (IdP amplification, unbounded); AD-28(4)/AD-4 protect the PROXY not the IdP; operator IdP rate-limiting REQUIRED (operator-scope, D4). ADD: pipelining ESMEs / short-bind-timeout clients closed (Q1, no buffer mode, D3).
- (change) Deferred + Conventions UPDATE: bind_resp->OIDC mapping carries AD-33 collapse invariant (exact code still owning-story); exact-cipher default pinned AD-34 (operator-tunable). Consistency Conventions Errors line references AD-33 collapse + AD-32 pre-couple policy.
- (event) Spine re-distill + cross-artifact sync DONE: AD-32/33/34 added, AD-27/3/25/19/30/24 amended, register+deferred+conventions+capability-map updated, frontmatter updated 2026-07-23. test-design-architecture.md -> accepted; RELAY-002 rewritten + carve-out siblings; handoff Q1-Q7 resolved; epics.md AD summary+register synced. Reviewer gate next.
- (event) REVIEWER GATE (rubric + webverify + adversarial) on the UPDATE: all PASS_WITH_FINDINGS, no critical/blockers; spine stays status:final. 3 review files written to reviews/review-update-{rubric,webverify,adversarial}.md. Applied every closing action (UF-1..UF-7 + the 3 webverify precision findings + rubric refinements).
- (fix) GATE FIX (webverify, spec correction): error _resp is HEADER-ONLY. SMPP 3.4 Table 4-11 -- a _resp body is NOT returned when command_status is non-zero, so the conformant shape for AD-32 case 3 is command_length=16, no body. Reverts D1 (spec-conformant body) -- header-only IS the conformant shape (the prior gray area was a misread). Eliminates the command_id->response-body-requirement lookup (kills UF-1 owner ambiguity + rubric lookup-owner finding). RELAY-002 assertion updated to header-only.
- (fix) GATE FIX AD-32 (UF-1/UF-2/UF-7 + webverify): case 3 = REQUEST-direction non-bind only, header-only _resp + RACE-FREE teardown (sync remove ConnectionRegistry entry + mark tearing-down BEFORE writeAndFlush; cancel ROPC via AD-12 handle + zeroize on same event loop; then writeAndFlush+CLOSE). bind-in-flight defined == entry exists && !coupled. New case 3a: a *_resp (resp-to-a-resp malformed) or alert_notification (no _resp) pre-couple -> close, NO synthetic response. Case 5: unknown command_id -> generic_nack(RINVCMDID 0x3, seq=offender where parseable else 0); invalid length -> AD-30 drop+close as above-spec policy (desync makes generic_nack unreliable).
- (fix) GATE FIX AD-25/AD-27 (UF-2/UF-4): AD-25 flip re-checks the ConnectionRegistry entry before coupling (no-op if absent or tearing-down -- does not couple on a closing connection). AD-27 CloseReason enum made EXHAUSTIVE over the spine close paths (+UNDERSIZED_FRAME, UNKNOWN_COMMAND_ID, CLEAN_UNBIND_HANDSHAKE, GENERIC_NACK_PRE_BIND, BIND_REJECTED, BIND_FAILED_NON_ROK, INGRESS_TLS_HANDSHAKE_FAILED, OTHER catch-all); onConnectionClosed is exactly-once per channel (CAS-guarded, owned by channelInactive, fires for every channelInactive).
- (fix) GATE FIX AD-33/AD-34/AD-19/AD-3/AD-12 (UF-3/UF-5/UF-6 + rubric): AD-33 collapse set made EXHAUSTIVE -- verifier Deny* + ingress routing-miss + ALL egress-side bind failures pre-bind_resp (generic_nack/non-ROK bind_resp/connect-fail/RST/unbind/half-close); AD-32 case 3 is NOT a bind denial (no bind_resp); residual routing-miss-vs-IdP timing side-channel accepted under trusted-net (AD-15). AD-34 intersection per-SSLContext (ingress+egress+IdP) + TLS-1.3 AEAD set pinned. AD-19 read-vs-emit split (reading command_id for logic always ok; emission TRACE-gated) + counter spans both windows never-labeled. AD-3: header inspection (command_id/seq) permitted; only body parsing forbidden. AD-12: port gains a cancelHttp() handle so AD-32 can abort the in-flight ROPC at the HTTP layer (IdP-amplification mitigation otherwise unreachable).
- (event) UPDATE COMPLETE 2026-07-23: spine AD-1..AD-34 status:final (gate-fixed); epics synced; test-design accepted (RELAY-002 + 002a/b/c, header-only). Open: Epic 2 must falsify AD-32 carve-outs vs real/conformance SMSC; AD-33 exact bind-denial code owned by a single Epic-3 story; ROPC cancel-handle wiring in the AD-12 adapter. Next: sprint planning -> create story (E1/S1 scaffold).
- (fix) RE-VERIFY (focused adversarial) on the gate-fixed ADs: UF-1/2/3 CONFIRMED CLOSED. Found 1 new MEDIUM (N1) the cancelHttp() fix accentuated + 3 LOW (N2/N3/N4). Applied all 4: N1 -- AD-25 verdict-callback re-check extended to BOTH Allow/flip AND Deny (a cancelled-adjudication Deny no longer emits a bind_resp on a tearing-down channel, upholding AD-32 case-3a); + case-3 shuts the adjudication STS (AD-5) as defense-in-depth. N2 -- AD-8 reconciled (case-3 synchronous removal is a case-specific override; channelInvalid removal is a no-op). N3 -- AD-12 pins VerdictRequest.cancelHttp() as the single cancel-handle mechanism (ScopedValue carries context only). N4 -- AD-27 CloseReason adds EGRESS_TLS_HANDSHAKE_FAILED.
- (event) UPDATE FINALIZED 2026-07-23 (2 rounds: gate + re-verify). Spine status:final AD-1..AD-34 (lint 0 findings); 7 test-design blockers Q1-Q7 resolved; UF-1..UF-7 + N1..N4 closed. No further verification pass -- residual low-level detail rides into the owning stories at create-story time. Next: bmad-sprint-planning -> bmad-create-story (E1/S1 greenfield scaffold; Story 3.1 ROPC probe can run parallel to E1).
- (decision) AD-32 case 3 REVERSED to BARE-CLOSE (user): a pre-couple non-bind violation now just CLOSES with NO synthetic _resp (direction-agnostic -- absorbs former case 3a; *_resp / alert_notification pre-couple close too). Above-spec fail-closed (conformant would be matching _resp + RINVBNDSTS; we drop the connection). Simpler: removes response synthesis, flush-before-close PDU ordering, request-vs-response-direction handling, the header-only/body question. Carve-outs unchanged (unbind->unbind_resp; enquire_link->enquire_link_resp+keep). Teardown ordering + bind-in-flight def + AD-25 verdict-callback re-check unchanged. AD-19 drops "response synthesis". Propagated: epics AD-32 row, test-design Q1 line + Epic-2 items, handoff Q1 row, RELAY-002 (asserts NO PDU emitted, bare close). Accepted trade-off: narrows FR-TRANSIT-3/SM-3 conformance narrative slightly (conformant for legitimate in-state traffic; violations dropped) for simplicity + fail-closed purity; no security delta.
- (fix) Bare-close adversarial re-check: functionally CLEAN (no new leak/race/teardown/ambiguity; time-to-close still the only side-channel; onConnectionClosed unaffected). Fixed 5 stale synth-_resp cross-refs it surfaced: AD-33 x2 (case-3 now closes+no _resp; non-bind contract = bare-close), Consistency Conventions/Errors row, AD-24(1) (conformance suite asserts the above-spec bare-close, not the RINVBNDSTS _resp -- scoped to case 3; carve-outs stay conformant), AD-8 (writeAndFlush->ctx.close() + channelInvalid->channelInactive typo), AD-25 (case-3a->case 3 invariant). qa.md clean (its _resp refs are forwarded-PDU / codec-boundary, unrelated). Bare-close switch complete + consistent across spine/epics/test-design.
- (decision) AD-32 case 1+2 REVERSED to fail-closed (user): the unbind-handshake and enquire_link-keepalive carve-outs are DROPPED -- unbind and enquire_link now close under the uniform everything-else rule (above-spec, no response). AD-32 collapses to: bind-family -> AD-3/AD-25 (ONLY cooperative pre-couple handling); egress generic_nack -> bind-fail propagation; EVERYTHING else -> close no response (incl unbind, enquire_link, submit_sm, *_resp, alert_notification, unknown command_id). Also drops generic_nack-for-unknown-command_id (folds into close). Simplification: no carve-out branches, no unbind_resp/enquire_link_resp synthesis, no keep-vs-close decision. Accepted interop constraint (register + A-1): carrier ESME enquire_link keepalive interval AND bind-timeout must exceed PERF-3 bind latency (PERF-3 widens the window so keepalives that dont fire vs a fast SMSC can fire here). Removed RELAY-002a (unbind handshake) + RELAY-002b (enquire_link keepalive); RELAY-002c remains. Propagated: AD-3 corollary, register, Consistency Conventions, epics AD-32 row, test-design Q1 + Epic-2 items, handoff.
- (decision) AD-32 case 4 + AD-33 REFINED (user): the SMSCs generic_nack (or non-ROK bind_*_resp) pre-bind_resp is now FORWARDED VERBATIM to the legacy (SMSC is the sole credential authority; its actual response is ground truth, passes through unchanged; no splice flip; tear down both legs). Was: intercepted + rewritten via AD-33 collapse. AD-33 collapse re-scoped to the proxys OWN pre-SMSC denials ONLY (verifier Deny*, routing-miss) + SMSC transport-death (connect-fail/RST/unbind/half-close -- no SMSC PDU to forward). SMSC-originated response PDUs are NOT collapsed. Accepted residual (in AD-33): a prober can distinguish reached-the-SMSC from proxy-rejected -- mild credential-validity oracle, within the existing no-rate-limit accepted risk. RELAY-002c rewritten to assert verbatim forwarding (byte-capture). Propagated: epics AD-32 row, test-design Q1 line, handoff.
- (decision) AD-27 SpliceObserver shape changed 5->4 methods (user, 2026-08-11): onByteTransfer(Direction, long) REMOVED -- byte-volume telemetry is NOT carried across the seam; PDU count now comes from onFramedPdu(Direction) fires alone (one fire per framed PDU). Noop + Capturing impls trimmed (ByteTransfer record, byteTransfers queue, totalBytes accumulator dropped); SpliceObserverShapeTest .hasSize(5)->4 and the onByteTransferSignature case deleted; OBS-010 PRIV-1 observer-level assertion rewrote to onFramedPdu-only (the /metrics sentinel-scrape-absence stays the load-bearing privacy gate). Propagated: spine AD-27:224, epics.md:225+388, test-coverage-scenarios OBS-010, story 2.2 AC5/Task-2, implementation-readiness-report finding #2 annotated SUPERSEDED. Epic 4 unchanged-interface argument survives (the seed is still the full shape across E2->E4, now 4 methods).
- (amendment) AD-30 + AD-21 ceiling-read clause, Story 2.2 T5 code review (2026-08-12). The AD-30 startup self-check's live direct-memory ceiling is read via `java.lang.management.ManagementFactory` (explicit `-XX:MaxDirectMemorySize`) with a `Runtime.maxMemory()` fallback (the `-Xmx` default) — NOT via `jdk.internal.misc.VM.maxDirectMemory()` (rejected: would require a production bootJar `--add-exports=java.base/jdk.internal.misc` that crashes `java -jar` with `IllegalAccessError` before the budget comparison runs) and NOT via `ByteBufAllocatorMetric` (exposes only current *usage*, not the ceiling). Spine AD-30 + AD-21 amended in place to drop the "read/exposed via ByteBufAllocatorMetric" wording for the ceiling read; operator metric *exposure* stays an Epic 4 `/metrics` concern (the metric is usage, separate from the ceiling). The computed budget is unchanged: `MemoryBudget.compute` = `max_frame × max_inbound_depth × concurrent_pairs × safety_factor`, referencing `SmppFrame.MAX_COMMAND_LENGTH` (RELAY-026 single-source). Propagated: spine AD-30:239 + AD-21:194; story 2.2 Review Findings (patch C). No security/correctness delta to the invariant's intent — the check still fail-fasts (AD-17) when budget > live ceiling.
- (amendment) AD-30 self-check made UNCONDITIONAL + budget-check policy, Story 2.2 T5b (2026-08-15, operator decision). (1) The live direct-memory startup self-check now runs for EVERY role×mode cell — T5 had scoped it to reverse.mode-b (the slice that story wired, with a load-bearing note to widen in Epic 3); reversed because every cell relays (forward relays ESME->reverse) and the AD-17 constructor guarantees exactly one cell per deployment, so per-cell scoping bought nothing permanent — Epic 3 no longer needs to widen the guard (resolves the deferred-work "no mechanical guard forces the Epic-3 widening" entry: a forward.mode-a full-boot test now bites any reintroduced scoping). (2) New key companion.memory.budget-check: fail | warn (NOT a budget-formula input). fail = the default, including an absent/null key (fail-closed — @Nullable component, null behaves as FAIL at the single use site; deliberately NOT @DefaultValue, which could change whole-record-null-when-node-absent semantics that the omitted-memory-block refusal test pins). warn = loud starred System.err accepted-risk banner (the CompanionModeBWarning / SEC-052 Mode B pattern) + start. No value skips the check itself — it always computes and compares; only over-budget severity is tunable. An invalid enum token fails the bind (fail-closed, matrix-tested). Propagated: spine AD-30 rule text + amendment note; story 2.2 AC8/Task-5/Dev-Notes/file-ledger; deferred-work entry resolved; application.yml documents the key. Tests: 4-test startup-check suite (happy-no-banner, mode-b default refusal, forward.mode-a unconditionality refusal, warn boots+banner) + invalid-enum matrix refusal; minimal test budget moved from reverseB() into TestCompanionConfigs.common() and added to the two forward-A yml-backed full boots (BootstrapLifecycleTest, CompanionTlsBindingTest). Mutations proven RED: M1 neutered warn arm -> warn test RED; M2 reintroduced mode-b guard -> forward-A test RED.
- (amendment) AD-12/AD-17/AD-26 ADJUDICATION MOVED TO THE REVERSE ROLE (user-directed, 2026-08-18, during Story 3.2 T1). The ratified two-proxy flow had the FORWARD performing OIDC verification; the user's corrected trust model inverts it: the REVERSE is the sole enforcement point — it ROPC-adjudicates every bind's password-grant against the IdP BEFORE forwarding anything to the SMSC (the reverse protects the SMSC; the SMSC stays sole credential authority), and the FORWARD is a trusted-side relay: trusted legacy leg (no TLS/mTLS/IdP there — AD-15's sole-gate stands), no OIDC material, relays the original bind across the internet leg. Internet leg unchanged mechanically (reverse authenticates to forward as TLS client; Mode C = forward validates reverse's client cert; Mode A ACL-isolation accepted risk — now harvesting UNVERIFIED credentials, the ROPC screen no longer screens what the forward relays). Reverse→SMSC is PLAINTEXT in every mode (trusted SMSC-leg network; the modes apply to the internet leg only) — reverse A/C trust-store/client-cert semantics re-point from the SMSC leg to the internet leg (AD-26 peer = the forward's internet-leg server cert). Mode B = NO forward proxy (no forward×B cell, unchanged): legacy client connects DIRECTLY to the reverse over the plaintext internet leg, opt-in ack; the reverse still adjudicates ("plaintext + ROPC") — a STRENGTHENING vs the pre-amendment model where Mode B had no OIDC anywhere. Rationale (user): single enforcement point adjacent to what it protects; per-carrier IdP falls out naturally (each carrier's reverse fronts its own Keycloak); the forward becomes secret-free. Consequences: Story 3.2 AC1 wiring inverts (reverse.* → ROPC adapter, forward.* → AlwaysAllow — recorded in the story's Dev Agent Record); the oidc config node moves from forward.mode-a/c to ALL reverse cells (Oidc record itself unchanged, AD-12 port unchanged/AC8-immutable — the adapter is location-agnostic); T1 re-targeted same day. Propagated: spine AD-12 two-proxy-flow + AD-17 matrix (both rows) + AD-26 peer wording; epics.md AD-17:188 + AD-12:197; Story 3.2 file. NOT yet swept (follow-up at owning-story time): walkthrough.md narrative ("the proxy intercepts..." now = the reverse), test-design/handoff forward-role references if any.
- (amendment) RFC 8705 IdP mTLS CLIENT-AUTH OPTION REMOVED + AD-12-amendment RESIDUAL SWEEP COMPLETED (user-directed, 2026-08-19, post-review, Story 3.2). (1) REMOVAL: the oidc config surface drops the client-mtls-keystore arm entirely — provider client auth is client_secret ONLY (client-secret-path now @NotNull-required, sole arm; ClientMtlsKeystore record deleted; validator file-existence check unconditional; yml template + matrix tests swept −2/+1; :proxy:test 226/0/0 XML, :proxy:check GREEN). The port/adapter contract is untouched — the 2-1 test-tier slice (RopcSlice*, fixture client smpp-client-mtls) stays green as the historical 4-path ratification; Epic 3's production live suite becomes 3-path (JWT / introspection / DENY). Supersedes (story AC text left as-written per story-file rules): Story 3.2 AC7 "client auth = exactly one of client_secret or mTLS keystore", AC8's client-mtls-keystore bullet, AC9's live path 3. Propagated: spine AD-10:128 (holdings list), AD-12:138 (provider-link rule — "client_secret only"), AD-12:142 (ratification bullet: path-3 historical marker), AD-34:263 ("IdP client context"), Accepted-Risk Register :294 (dated update); epics.md AD-12 provider-link :211. (2) RESIDUAL SWEEP (discharges code-review decision D1, 2026-08-19 — the 2026-08-18 AD-12 amendment had propagated only first sentences): all remaining SMSC-trust/SMSC-leg-TLS wording re-pointed to the internet-leg peer — spine AD-13:147 ("Mode A validation of the forward's internet-leg server cert… no SMSC-cert validation path"), AD-20 (retitled "Egress TLS endpoint-identification"; rule re-targeted to the reverse→forward leg, IdP link = JDK HttpClient always-on verification, "operator-choice SMSC-leg TLS" posture retired with the leg), AD-26:218-219 (Prevents + remainder: internet-leg trust store as sole gate, public-PKI internet-leg-peer opt-in; dated amendment marker); epics.md AD-13:199, AD-20:204, AD-26:205-206, AD-20 summary-table row :267. Post-sweep grep: zero stale SMSC-validation/SMSC-trust/IdP-mTLS sites remain (only intended amendment markers + the legacy-leg "no TLS/mTLS/IdP" wording). Rationale (user): single client-auth path, simpler fail-closed surface; mTLS remains ONLY the forward↔reverse internet-leg Mode C option (verified against config: no other mTLS-bearing keys exist).
- (amendment) AD-11/AD-12 TOKEN-ENDPOINT VERDICT MAPPING REFINED — DenyInvalid reserved for positive invalid-credential signals (Story 3.2 T3, 2026-08-19; discharges deferred-work §2.1 item 1, the "429/404/403 → DenyInvalid misclassification"). The production ROPC adapter's normative table: DenyInvalid ONLY for (a) a bare 401 from the token endpoint — RFC 6749 §5.2 makes a bare 401 the provider's positive auth-layer rejection EVEN with an unparseable body — and (b) a 400 whose parsed OAuth error ∈ {invalid_grant, invalid_client} (2-1 fixture finding #6: bad USER credentials arrive as 400 invalid_grant, bad client as 401; the rule is to parse the 400 body as JSON and key on error SEMANTICS, never the status alone). Everything else is DenyIndeterminate: 200-with-non-JWT/HTML/empty body, 200-with-malformed-JWT, 3xx, any other 4xx (incl. 403/404/429 and 400s with other/absent error codes — rate-limit, configuration and authorization errors are not credential verdicts), 5xx, timeout, network error, cancellation, admission saturation, scope shutdown. DENY still wins on disagreement: a 200 verdict overridden by failed local JWT defense-in-depth → DenyIndeterminate (unverifiable is NOT invalid, matching the ratified slice). Supersedes the shorthand in the spine's AD-12 ratification bullet ("4xx → DenyInvalid") and the Verdict.DenyInvalid javadoc example ("e.g. an IdP 4xx") — the AC8-immutable port types stay untouched (the javadoc example is stale by design; wire impact NONE: AD-33 collapses both Deny* permits identically, the distinction surfaces only in logs/metrics labels, Epic 4). Pinned by RopcBindCredentialVerifierTest (mutation-guarded: reverting to the crude 4xx shorthand goes RED on the other-400 row). Recorded when the adapter core (T3) landed; T4 (JWT defense-in-depth + kid-miss) and T5 (RFC 7662 introspection rows) extend the same table.
- (amendment) AD-12/AD-17/AD-20/AD-26 + REGISTER SWEPT TO THE **[B] TOPOLOGY** (user-ratified 2026-08-21 — "Yes / no constraint → [B]"; swept 2026-08-25, Story 3.3 T1). THE FLIP: dial direction was never load-bearing (no carrier inbound constraint) — the FORWARD now DIALS the REVERSE **per SMPP session** (1:1:1 — one client session ↔ one internet-leg connection ↔ one SMSC session; pairing keyed per channel, never per system_id; N sessions per system_id all route to the same single target), and the REVERSE holds the internet-leg TLS listener. REJECTED at the same decision point: [A1] warm-lane pool and [A2] multiplexed standing connection (complexity / SMPP sequence-space demux impossibility). Consequences swept into the spine: (1) AD-12 two-proxy flow — the FORWARD is the TLS client; Mode C = the reverse validates the forward's per-instance client cert (clientAuth REQUIRE); Mode A = the REVERSE cannot authenticate the forward → the reverse's listener must be ACL-isolated to operator forward-proxy hosts or Mode C used. (2) AD-17 required-config matrix re-shaped (pre-release break, no shims): forward+A = trust store (dial anchor) + routing, NO server cert, no SMSC; forward+C = + per-instance client cert; reverse+A = internet-leg server cert+key (no trust store — one-way presents, never validates); reverse+C = server cert+key + trust store (REQUIRE); reverse+B unchanged. (3) AD-20 — the Netty TLS-egress leg is forward→reverse; endpointIdentificationAlgorithm null for IP-literal REVERSE targets. (4) AD-26 — the forward's TLS client anchors the reverse's server cert (never cacerts). (5) REGISTER Mode A entry RETITLED to its [B] residual: the "password harvest point" framing RETIRES (the reverse ROPC-adjudicates everything submitted — a fake forward harvests nothing); the residual is a SUBMISSION ORACLE (credential guesses can be driven through the reverse's ROPC screen + SMSC — the no-rate-limit family, Mode B-posture-equivalent). (6) AD-29 stays correct AS-BUILT (routing entry = the forward's dial target) and gains the tls.contexts completion (id → client-cert override; routing entry tls-context-id selects; absent → instance default). Propagated: spine AD-12:140 / AD-17 matrix / AD-20 / AD-26 + register; epics.md AD-12/17/20/26 bullets + summary rows + register. Story 3.3 implements the runtime this sweep authorizes (per-cell Netty SslContext factories, forward interceptor routing gate + per-session TLS dial, reverse TLS acceptor, F13 bind.host/max-connections).
- (amendment) AD-29 OVERRIDE MAP RE-SCOPED + F13 CAP KNOB RETIRED (user-directed, 2026-08-26, Story 3.3 checkpoint review — mid-review rework on the uncommitted changeset). (1) RE-SCOPE: the AD-29 per-target client-cert override map moves from `companion.tls.contexts` to `companion.forward.tls-contexts.<id>.*` (rides the Forward role record; the `companion.tls` node keeps POLICY only — protocols + cipher suites, consumed by both roles' AD-34 intersections). Rationale: `tls-context-id` is consumed exclusively by forward routing entries (RoutingEntry exists only on ForwardModeA/C; the factory's resolveClientCert runs only on the forward branch) — top-level placement read as if it applied to reverse TLS too (dead config on reverse cells; now structurally unreachable there); the flat name mirrors the entry-level `tls-context-id`. SEC-098 (dangling tls-context-id → refuse) unchanged in substance; validator sourcing + the factory tripwire re-keyed. (2) RETIREMENT: `companion.bind.max-connections` is DELETED — the F13 accepted-connection cap now reads `companion.memory.concurrent-pairs` directly (RelayServerLifecycle → ConnectionCapHandler). The old cap≤budget validator guard became an identity and is gone with its matrix tests (every accepted connection can become a budgeted pair — one number, no drift surface; equality was already the legal boundary). Rationale (user): two knobs for one number = drift surface; the budget input IS the physically-consistent cap. Test blast radius: contexts tests re-keyed; F13 knob tests retired (retirement note in CompanionConfigMatrixTest); fixtures now express the cap via the Memory record — mode-b fixtures carry DEFAULT_CONCURRENT_PAIRS=64 (the old DEFAULT_MAX_CONNECTIONS headroom re-expressed; without it the N-concurrent-binds smoke tests hit the pairs=1 cap — found RED, fixed GREEN). Gates: `clean build :buildSrc:test` GREEN; :proxy:test 347 tests / 0 failed / 0 skipped (XML). Propagated: spine AD-29:234 (dated marker); epics.md AD-29:189 (dated marker). NOT yet swept (deliberately deferred to the story-text pass at review wrap): Story 3.3 file (Tasks T2/T6 text, AC5, I/O-matrix rows, Design Notes, Completion Notes, File List, mutation-ledger rows for the retired guards), deferred-work.md:511, epic-3-context.md; a fresh RED-on-neuter pass on the re-keyed SEC-098 guard; checkpoint-state trail re-verification.
- (record) STORY 3.3 REVIEW WRAP + STORY-TEXT PASS (2026-08-27). The 2026-08-26 NOT-yet-swept list is now fully retired: (1) adversarial code review (4 layers: blind-hunter, edge-case-hunter, verification-gap, acceptance-auditor) over the `9618938`→worktree diff — 39 raw → 21 verified findings, 4 dismissed; owner resolved D1 (Mode A loud startup WARN restored as `CompanionModeAWarning`, mirroring the Mode B bean) and D2 (new SEC id **SEC-100** = TLS policy/material startup refusal, registered in test-coverage-scenarios.md §4.16; the AD-34 intersection + ctor-wrap refusals now carry it); all 15 patches applied. (2) Story contract text swept to the re-keyed/retired surface (AC5, T2/T6, I/O-matrix, Design Notes, File List → `companion.forward.tls-contexts` + `memory.concurrent-pairs` as-built); epic-3-context.md re-swept to [B]; deferred-work.md:511 key fixed; sprint-status stale comment removed. (3) Fresh RED-on-neuter pass on the re-keyed SEC-098 surface: M14 (validator arm → `sec098_unknownTlsContextIdRefuses` RED) + M15 (factory orphan eager-load arm → `tlsContextOverrideResolution` orphan arms RED); retired M4/M10 struck, M3 superseded. (4) Checkpoint review (paused 2026-08-26 at Detail Pass) resumed and APPROVED 2026-08-27; trail anchors re-verified against the moved tree. Gates: `clean build :buildSrc:test` GREEN; `:proxy:test` **352 tests / 0 failed / 0 skipped** (XML; = the 2026-08-26 gate's 347 + 5 review tests); FIXME/@Disabled added-lines scan clean (one pre-documented 2.2 T7 owner FIXME). Story 3.3 → done.
- (amendment) AD-12/AD-5/AD-8/AD-10/AD-11/AD-22/AD-28/AD-33/AD-36 + REGISTER + CONVENTIONS + MAPS — OIDC TOKEN-PATH SIMPLIFICATION: RFC 7662 ARM + LOCAL JWT VERIFICATION REMOVED (user-directed, 2026-08-27, Story 3.4 T1+T2). T1 (JWT-only adjudication): the opaque-token introspection fallback — its wire arm (`introspect`/`mapIntrospection`/`introspectRequest`), its discovery `introspection_endpoint` requirement + metadata component, and its client-auth path — is deleted; a 200 body whose token is not a three-segment JWS denies `DenyIndeterminate` + WARN naming the JWT-only policy and the operator remediation (D6, runtime arm — token shape is observable only at bind time). T2 (TLS-as-sole-trust-anchor): local JWT signature/claim verification — `verifyJwt` (sig/kid/typ/iss/aud/exp/nbf), `JwksCache` (class), its AD-28(2) refresh VT, the `oidc.jwks-cache-ttl` knob, and the `jwks_uri` discovery requirement — is deleted; `Allow` derives from the token endpoint's HTTPS-authenticated response alone, the only surviving token check being the STRUCTURAL three-segment gate (D7 lean: library-free split on `.`; content past the count is never parsed — unverified-claim checks are theater once the endpoint verdict decided). Rationale (owner): the proxy is the issued token's ONLY consumer (never forwarded, cached, nor shown); re-proving the provider's honesty locally guards a threat that already owns the keys' own transport; the introspection arm served opaque-token fleets this single-pinned-Keycloak (≥26.7.0, JWT-issuing) deployment is not. Simpler + fail-closed wins. Nimbus JOSE+JWT survives ONLY as the discovery-JSON parser (`JSONObjectUtils`; no other JSON lib on the classpath) — AC7 allowlist entry stands for that use. Stale-key retirement is LOUD: a config still carrying `jwks-cache-ttl` refuses startup under `ignoreUnknownFields=false` with the key named (pinned). Story 3.4 T2 REMOVES an executor (the JWKS refresh VT) and adds none (AD-28). Propagated (spine): AD-12 rule + ratification bullet (paths (2)/(4)-kid-miss production-retired, AC8 record stands), AD-5 (single forked arm), AD-8 clause (c) RETIRED (mutable state back to two buckets), AD-10 holdings, AD-11 OIDC enumeration re-worded, AD-22 drain steps 4–5 RETIRED (stop body = deny-in-flight → release client + secret), AD-28 arm (2) RETIRED, AD-33 deny-cause list, AD-36 Nimbus role, Accepted-Risk Register (3.1-verdict dated update; outage row re-worded — splices survive because adjudication is bind-time only), Consistency Conventions state row, module/package maps, deployment graph (ROPC-only edge), deferred-tuning row. Propagated (epics.md): AD-12 bullets + summary row + register rows + tuning row + the 4-path production-scope note at the Epic-3 risk gate. Propagated (TEA test-coverage-scenarios.md): §4.2 + §4.3 RETIRED section banners + per-row statuses — SEC-011/012/013..019/093/029/OBS-018 retired, SEC-005 expectation INVERTED (three-segment garbage now Allows; pinned), wording notes on SEC-001/002/028/047/094/OBS-016. Deliberately UNCHANGED (dispositions): the `RopcSlice*` family + `KeycloakFixture.INTROSPECTION_ENDPOINT` keep all four AC8 paths green as the historical ratification (the 2026-08-19 mTLS-removal precedent — product-arm removals leave the slice); epics' AD-summary mirrors for AD-5/8/10/11/22/28 + the module-map line stay the frozen planning snapshot (same precedent — 8705 left them; the spine markers govern) — flagged for the next full re-distill. Gates: `clean build :buildSrc:test` GREEN; `:proxy:test` **324 tests / 0 failed / 0 skipped** (XML, 48 suites; 340 post-T1 − 16: JwksCacheTest −7 rows with the class, verifier suite −9 retired verify rows +2 D7 pins, config matrix −2 net for the ttl rows vs +1 stale-key row); `grep 7662|introspect|jwks|Jwks proxy/src/main` empty; FIXME/@Disabled added-lines 0; MUTATED residue 0.
- (decision) STORY 3.4 T3 UMBRELLA — RELAY STATE-MANAGER SHAPE RATIFIED (user, 2026-08-28, owner checkpoint; governs T4–T9, drift requires a NEW owner checkpoint). (1) VARIANT: SINGLE manager — one relay-internal state manager holding the transition policy OVER `ConnectionRegistry`'s storage (no second state copy, AD-32) for BOTH role arms. Rationale: the pair lifecycle (`registered/adjudicating → egress-attached/connecting → awaiting-bind_resp → coupled → tearing-down`) is role-AGNOSTIC — the forward/reverse split lives entirely in the interceptor's control plane (role resolution at its ctor, the forward-only routing gate which never creates a pair, the dial-target/initializer assembly); per-role managers would duplicate an identical state machine (every invariant proof and every T8 mutation biter doubles; two copies drift silently). Per D2 this is ONE fork with the interceptor role-split: the interceptor stays a single class with its role branch, and the manager mirrors that. The lifecycle-divergence scenario that would ever justify per-role (forward pooling / standing lanes / multiplexing) was already examined and REJECTED at the 2026-08-21 [B] decision ([A1]/[A2]). (2) MECHANICS: SEALED DECISION RETURNED TO THE CALLER — manager transition methods return sealed outcomes (`beginTeardown` → `Won(entry) | Lost`); the manager performs the absorbed hygiene (`cancelHttp()` + zeroize) BEFORE returning `Won`, so the remove+mark → cancel+wipe half of the AD-32 ordering is single-sited and structurally un-gettable-wrong; callers execute only their leg-specific tails (closes, the AD-33 deny write, the reason stash). No manager→handler references exist (the circular-dependency rejection). The `channelInactive` teardown and the exactly-once `onConnectionClosed` CAS stay HANDLER-side (AD-27/AC) — which is why the executor-facing-port alternative was rejected: it could never own the inactive-path teardown, so the ordering would end up split manager/handler regardless while hollowing the handlers. HARD CONSTRAINT: the AD-25 couple/flip transition is NOT exposed on the manager surface — a manager-routable flip would let ANY caller flip, the opposite of AC6's structural-by-TYPE requirement. Manager surface = register / attachEgress / beginTeardown / lookups; the flip stays ENTRY state, its single-caller call path made structural-by-type in T5. Thread-bounding unchanged: the manager is event-loop-confined policy like the handlers; the verdict continuation hop is its ONE off-loop entry; the VT-side adjudication kickoff stays in the interceptor's control-plane half (AD-28 — no executor added). (3) ABSORPTION SCOPE — ALL THREE, with a dated AD-8 amendment landing at T6 time (the entry's "exactly the four things AD-8 names and NOTHING else" wording gains the new fields): (a) `pendingVerdict`/`pendingPassword` → the `ConnectionEntry` (4→6 fields, volatile, ingress-loop-written as today; the adjudicating state becomes REAL entry state so RELAY-004's in-flight predicate = the entry, fully; the `RelayHandler`→`BindInterceptor` pipeline-lookup delegation at `RelayHandler:168` DIES — the split handlers call the manager directly; zeroize stays caller-owned, the caller being the manager; the in-flight assert moves to the registration path). (b) `EgressLeg.answered` → the entry as the `awaiting-bind_resp → answered` transition (the candidate state set becomes fully entry-resident — zero shadow bits outside the entry; EgressLeg takes the entry ref at its construction site; its unanswered-death collapse reads entry state). (c) `CLOSE_REASON`/`CLOSE_FIRED` attribute keys → hoisted from RelayHandler-private to a shared relay-package home (D5 resolved — forced by the direction split anyway: both per-leg classes need them; mechanics only, the reason SEMANTICS stay Epic-4). State-derivation rule (AD-32) restated for the new shape: every explicit state DERIVES from the registry entry on each read — never a shadow boolean. Propagated: Story 3.4 Design Notes (dated T3 resolution addendum, same date); executed by T4–T9; the spine AD-8 amendment marker lands with T6's code.
- (amendment) AD-25 + AD-2 + AD-1/8/27/32 + EPICS/TEA — COUPLE/RELAY VOCABULARY UNIFICATION + `SpliceObserver` → `RelayObserver` (user-ratified D1 full-unification resolution, 2026-08-28; executed as Story 3.4 T4). THE SWEEP: the pair-state six-family vocabulary (`couple`/`pair`/`leg`/`session`/`spliced`/`flip`) is unified on COUPLE for state and RELAY for the data-plane action — code identifiers: `ConnectionEntry.spliced()` → `coupled()`, `flipSpliced()` → `couple()` (the T3 state set's own names; the couple stays ENTRY state, single caller), the `spliced` field/locals/params likewise; `RelayHandler.splice()` → `relayFramedPdu()`; `SpliceObserver` → `RelayObserver` (type rename behind an otherwise-unchanged seam — the L105 5→4-method precedent; `NoopSpliceObserver` → `NoopRelayObserver`, `CapturingSpliceObserver` → `CapturingRelayObserver`, `SpliceObserverShapeTest` → `RelayObserverShapeTest`; the 4 method names unchanged — already couple-neutral); wire bytes, teardown ordering, pinned triggers, and every fail-closed arm UNCHANGED (rename-only round). "forward" was DISQUALIFIED as the data-plane verb (collides with the proxy ROLE word + `openEgressAndForward`). Propagated (spine): AD-25 title "Bind→splice transition state machine" → "Bind→couple…" + rule swept ("splice flag flipped" → "couple flag set", single-flipper → single couple unit; dated AMENDED marker); AD-2 title "…framed-ByteBuf splice" → "…framed-PDU relay" + rule swept (dated AMENDED marker); AD-27 (SpliceObserver → RelayObserver + "the splice metric" → "the relay metric" + "opaque-spliced" → "opaque-relayed"; dated AMENDED marker); AD-1 ("steady-state byte splice" → "steady-state byte relay"), AD-8 ("flip-flag" → "couple flag"), AD-32 ("splice flip" → "couple"), AD-3/5/7/9/19/22/23 wording, Design-Paradigm preamble + mermaid edge label, register outage row ("ongoing splices" → "ongoing relayed sessions") + preview-API row, module-map observability line, Deferred "DLR-splice byte mechanics" → "DLR relay mechanics" — each with a dated wording note. Propagated (epics.md): operative rows FR-TRANSIT-1 / SEC-2 / REL-1 (dated notes; the PRD's original "spliced" reads through the mapping), Language/Runtime row, module-map row (the SpliceObserver token only — the rest of the row stays the frozen planning snapshot per the T2 disposition), the AD SUMMARY TABLE (rows AD-1/2/9/22/25/27/32 swept with one dated note under the table intro — NOTE: the AD-22 row's stale JWKS steps, a T2 sweep miss, were closed here WITH attribution to the T2 spine amendment), FR-coverage line, the Epic-2 goal + NFR parenthetical + packages-owned line, the Epic-4 packages-owned line, the register outage row, the Deferred DLR row. Propagated (TEA test-coverage-scenarios.md): ONE dated vocabulary-note banner at the doc head carrying the full mapping (live row text deliberately NOT rewritten per the D1 resolution — rows read through the banner). Deliberately UNCHANGED (the D1 survivor policy): epics' `[AD-N]` mirror bullets (frozen planning snapshot — the 8705/T2 precedent; spine governs; flagged for the next full re-distill); historical records never retro-edited (done-story files incl. docs/a-1-carrier-test-plan.md, `reviews/`, the deferred-work ledger, dated reports incl. the readiness report and the TEA working docs, the PRD/brief/brainstorm); incidental English "flip" that is NOT pair-state vocabulary (Netty writability flips in `RelayChannelOptions`, the "pure flag flip" lifecycle wording in `AdjudicationLifecycle`/`VerifierWiringConfig`/`AdjudicationLifecycleTest`, the Story-3.3 "[B] pin flip" narration + lifecycle running-flag flips in `RelayServerLifecycleTest`, the verdict-row "flip" comment in `RopcBindCredentialVerifierTest`, the register's dated "dial-direction flip" [B] marker) — all inventoried in the Story 3.4 T4 Completion Notes. Gates: `clean build :buildSrc:test` GREEN; `:proxy:test` 324 tests / 0 failed / 0 skipped (XML; rename-only round — zero rows added or retired, suite-count unchanged); `grep splice|flip` over `relay/` + `observability/` + the amended contract sites returns only the inventoried survivors; `grep 7662|introspect|jwks|Jwks proxy/src/main` still empty; FIXME/@Disabled added-lines 0; MUTATED residue 0.
- (amendment) AD-25 + AD-7/AD-27/DESIGN-PARADIGM + EPICS/TEA — RELAYHANDLER DIRECTION-SPLIT, COUPLE STRUCTURAL-BY-TYPE (owner-ratified D3 resolution, 2026-08-28; executed as Story 3.4 T5). THE SPLIT: the single `RelayHandler` (345 lines, two direction-dictated state machines in one class — direction conditionals at the EGRESS+nack conjunct, the INGRESS delegation, and `peerOf`) is replaced by TWO per-leg final classes over ONE shared abstract base — `RelayIngressHandler` / `RelayEgressHandler` (mirroring `RelayIngressInitializer`/`RelayEgressInitializer`; the Relay-family prefix keeps pipeline listings self-describing) extending `CoupledRelayHandler` (extends `ChannelInboundHandlerAdapter`; "AbstractRelayHandler" rejected for near-colliding with the dying name across grep/blame; composition rejected for the per-callback delegation hop + re-passing direction into the component). SHAPE: the base owns the shared `channelRead` prelude (FINAL — the AD-25 race-free re-check + the `coupled()` → `relayFramedPdu` branch), the post-couple relay + writability re-arm, `teardownPair`/`channelInactive`/`exceptionCaught`, the exactly-once close telemetry, `classify`/`stash`/`releaseFrame`, `handlerAdded`, and the `CLOSE_REASON`/`CLOSE_FIRED` attribute keys (D5 RESOLVED — the hoist home is the base class; runtime-only attrs, the key namespace moves from the dead class to the base, no wire effect; reason SEMANTICS stay Epic-4); each subclass implements the ONE pre-couple hook `readPreCouple(ctx, entry, msg)` (pre-couple is the bind-handshake plane — zero new virtual dispatch per relayed PDU, AC8). `direction` is a base FIELD set via the protected ctor (NOT an abstract method, per AC8); `peerOf` stays in the base with its field-compare ternary — the ONE residual direction conditional, T9-sanctioned. `GENERIC_NACK` + the `getInt(4)` command_id read live in the EGRESS class only (the ingress path never reads the id); the ingress class has NO bind arm at all — a stray decoded `bind_resp` there (unreachable today: `BindInterceptor.channelRead0` releases client-sent bind-responses upstream) fails CLOSED post-split: release + stash `PRE_COUPLE_NON_BIND_PDU` + bare-close via the `teardownForPreCoupleViolation` delegation (which SURVIVES T5 unchanged per the T6 boundary — T6's manager replaces it; the six teardown sites stay six until T6). STRUCTURAL-BY-TYPE (AC6): the couple — `entry.couple()`, the N1 site — is expressible in exactly one class; pinned by a NEW ArchUnit call-site suite `RelayCoupleSiteArchitectureTest` (the `ObservabilityLayerRulesTest`/`SecurityTlsSurfaceArchitectureTest` idiom: `DoNotIncludeTests` so `ConnectionRegistryTest`'s direct CAS pins are the sanctioned test-source exception; `class.getName()` on both sides so renames refactor the rule with its subject; a FORBID rule — no main class but `RelayEgressHandler` may call `ConnectionEntry.couple()` — plus a POSITIVE control so the forbid can never go vacuously green on a rename). The two `new RelayHandler(...)` wiring sites re-pointed to `new RelayIngressHandler(registry, observer)` / `new RelayEgressHandler(registry, observer)` — direction now carried by TYPE, the `Direction` ctor arg and both initializer `Direction` imports die. `RelayHandler` and its FILE die. Propagated (spine): AD-25 dated AMENDED marker (rule re-pointed at BOTH naming sites — "exactly one unit" + the read-only-observer clause — single-unit → structural-by-type) + the same marker sweeps the other five type-naming sites: the Design-Paradigm data-plane bullet, the mermaid leg labels + the couple edge label, AD-7's couple-trigger sentence (dated wording note), AD-27's "holds an injected reference" clause (re-pointed to the per-leg handlers over the base), the module-map `relay/` line. Propagated (epics.md): the module-map `relay/` parenthetical + the AD-25 summary-table row re-pointed with dated notes (the `[AD-N]` mirror bullets stay frozen per the 8705/T2/T4 precedent). Propagated (TEA): one dated split note appended to the T4 vocabulary banner (rows naming `RelayHandler` as the couple unit read as the egress class post-split; live row text stays). Test re-pointing (every delta enumerated in the Story 3.4 T5 Completion Notes): `RelayHandlerTest` (12 rows) SPLIT along the injection-leg seam into `RelayEgressHandlerTest` (6 rows — the couple unit, non-ROK, late-bind_resp re-check, RELAY-003, RELAY-002c nack, CODEC-021) + `RelayIngressHandlerTest` (6 rows + ONE new AC6 pin: a stray decoded bind_resp on the ingress leg fails closed, never couples — driven directly, the interceptor-less leg, since the real interceptor consumes bind-responses upstream) with the shared pair fixture extracted into `CoupledPairHarness` (the `LatchedBindCredentialVerifier`/`RelayTestFixtures` precedent); `RelayPipelineInitializersTest` re-pointed (`.class` lookups, prefix searches, order assertions, DisplayNames, javadoc pipelines — T4 had deliberately deferred these to T5); prose sweeps in `BindInterceptor` (incl. the interceptor-pass-through comment + the exceptionCaught propagation comment), `ConnectionEntry` (couple-owner javadoc), `ConnectionRegistry`, `RelayChannelOptions`, both initializers, `RelayTestFixtures`, `CapturingRelayObserver`, `RelayA1SmokeTest`, `BindInterceptorTest`, `BindInterceptorForwardRoleTest`. Behavior-identical: wire bytes, teardown ordering, pinned triggers, fail-closed arms unchanged; relay behavior rides the same final prelude + per-leg hooks. Gates: `clean build :buildSrc:test` GREEN; `:proxy:test` **327 tests / 0 failed / 0 skipped** (XML; = post-T4 324 − 12 retired RelayHandlerTest rows + 13 split rows + 2 ArchUnit rules); `grep -rn RelayHandler proxy/src` → only the new-type names (`RelayIngressHandler`/`RelayEgressHandler`/`CoupledRelayHandler` substrings); `grep 7662|introspect|jwks|Jwks proxy/src/main` still empty; FIXME/@Disabled added-lines 0; MUTATED residue 0.
