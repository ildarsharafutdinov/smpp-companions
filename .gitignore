# bmad-loop orchestrator — run state, cache, local policy
.bmad-loop/runs/
.bmad-loop/cache/
.bmad-loop/policy.toml

# BMad personal settings (intended to be gitignored; shared config is committed)
_bmad/config.user.yaml
_bmad/config.user.toml
_bmad/custom/*.user.toml

# Installed BMad skill library — regenerable tooling (reinstall via the bmad installer / bmad-loop init).
# .claude/settings.json (hooks) stays committed.
.claude/skills/

# Gradle / Java build outputs
.gradle/
build/
**/build/
out/
*.class
**/.jqwik-database

# Keep the Gradle wrapper committed (regenerable but intentionally tracked)
!gradle/wrapper/gradle-wrapper.jar

# IDE / OS
.idea/
*.iml
*.iws
.vscode/
.project
.classpath
.settings/
.DS_Store

# Story 6.3 sandbox — AD-18: the OIDC client secret rides a gitignored FILE consumed by path;
# the operator-created sandbox/secrets/ never enters the repo (a debug replica of the deploy
# contract, never a place where a secret value is "just sandbox data").
sandbox/secrets/
